# Lessons ledger — the compounding layer

`created 2026-07-06` · append-only · owner: kb-distiller agent (`.claude/agents/kb-distiller.md`)

**Why this file exists.** CLAUDE.md's rule is *invariants over memory* — but between "learned
it the hard way" and "it's a CI assertion" there is a gap where knowledge evaporates between
stateless cloud runs. This ledger is that gap made visible. Every hard-won lesson gets a row
the moment it's learned; its **enforcement** column tracks how far along the gradient it has
moved (`UNENFORCED` → `protocol` → `test` → `invariant`). `UNENFORCED` rows are the
kb-distiller's standing work queue. Some lessons are genuinely not assertable (venue
behavior, methodology judgment) — those record *why* and stay `protocol`/`ledger-only`;
that's an honest terminal state.

Rules: never edit or delete a row; supersede with a new entry citing the old ID. Every
lesson cites the finding/log entry that produced it (no claim without provenance).

---

| id | date | lesson | source | enforcement |
|---|---|---|---|---|
| L1 | 2026-06-18 | A synthetic/model price is NEVER a fill price. The pt1 trade lost 9.6% partly by treating synthetic prices as fillable. Every persisted number carries a source tag; untagged defaults to `synthetic`. | CLAUDE.md prime directive; `findings/2026-06-18-codebase-money-map.md` | **invariant** — Hard Rules #3/#4 in `scripts/invariants.py`; `core/source_tag.py` default; `tests/test_substrate_primitives.py` |
| L2 | 2026-06-18 | `pstdev` over ensemble members without `member_count >= 4` produces garbage spread estimates. | arb-bot post-mortem, inherited | **invariant** — Hard Rule #2; `core/stats.safe_pstdev` |
| L3 | 2026-06-18 | `ncep_gefs025` is byte-identical to `gfs_seamless` — including it double-counts a model and silently narrows ensembles. | arb-bot post-mortem, inherited | **invariant** — Hard Rule #1 in `scripts/invariants.py` |
| L4 | 2026-07-03 | Cloud sessions cannot `git push origin main` (permission boundary, not a race — proven by two clean rebases that still fell back). Assuming otherwise caused Q1 to be built twice from scratch. | LOOP-QUEUE run log 2026-07-03 reconciliation | **protocol** — LOOP-QUEUE steps 0 (claim-check) + 6 (PR + self-merge-if-green) |
| L5 | 2026-07-04 | Kalshi's maker fee is 0.0175, taker 0.07 — a 4x difference. A first S13 draft charged every maker fill the taker rate; at mid-range prices the maker fee alone is ~1¢/contract, enough to consume a 1¢ edge entirely. Always take fees from `core.pricing.fee_per_contract`, never hand-roll. | `findings/2026-07-04-sports-maker-s13-verdict.md` | **test** — fee math pinned in `tests/test_substrate_primitives.py` / `tests/test_s13_maker_fillsim.py`; UNENFORCED as a static "no hand-rolled fee constants" invariant |
| L6 | 2026-07-04 | Bootstrap the independent unit, never the row: outcomes within one game are correlated draws — resampling by outcome fakes n. Same for strikes within a release, hours within a session. | `findings/2026-07-04-sports-clv-s7-verdict.md` (block-bootstrap by GAME) | **protocol** — encoded in probe precedents + `.claude/agents/edge-prober.md`; not statically assertable (unit choice is per-design) |
| L7 | 2026-07-04 | Never hardcode a bracket/strike width: BTC hourly brackets are $50-spaced (fine bands $100), ETH $10/$20. A fixed-$100 half-band check silently mis-scored every ETH hour. Derive spacing from the ladder itself. | `findings/2026-07-04-crypto-basis-s8-verdict.md` (latent bug fixed) | **test** — `tests/test_s8_basis_probe.py`; UNENFORCED generally |
| L8 | 2026-07-03 | A "paired" reference price lagging the event by collector cadence is a confound, not a basis: S8's spot lagged settlement by mean 29 min, fully explaining the observed gaps. Align timestamps to lag 0 (historical minute candles) before measuring any basis. | `findings/2026-07-03-crypto-basis-s8-q5.md` → fixed in `findings/2026-07-04-crypto-basis-s8-verdict.md` | **protocol** — probe discipline (verifier attack #5); not statically assertable |
| L9 | 2026-07-03 | Before joining two datasets, verify their date windows actually overlap: S7a's ESPN pull (Jun 15–21) had zero overlap with the Kalshi tape (Jun 26–Jul 02) and would have joined nothing — or worse, joined wrongly. | `findings/2026-07-03-sports-history-s7b.md` | **protocol** — verifier attack #5; join code reports matched/no_match honestly (`tests/test_sports_history.py`) |
| L10 | 2026-07-04 | Kalshi's open-market universe is 10,000+ markets; an unbounded `/markets` pull grew RSS past 3GB. Cap pulls and carry an honest `markets_truncated` flag — never silently claim full coverage. | LOOP-QUEUE Q6 entry, 2026-07-04 run log | **test** — `tests/test_anomaly_sweep.py` (cap + flag); pattern documented in `.claude/agents/collector-engineer.md` |
| L11 | 2026-07-04 | Kalshi purges settled markets ~60 days after close (NFL fully purged by discovery time). Un-collected history is lost forever — collection starts the day a strategy is conceived, not the day it's tested. | `findings/2026-07-03-sports-history-s7a.md` | **protocol** — shaped Q10/S12 (collect-now-test-later); not assertable |
| L12 | 2026-07-04 | Fine-band ladders near Kalshi's 1¢ minimum ask produce enormous *apparent* overround (KXBTC bracket_sum 7.78 across 188 members) that is NOT an arb — the fee floor and unfillable wings absorb it. Any arb claim must clear `core.pricing.true_arb_edge`'s real fee-floor math. | LOOP-QUEUE Q2/Q5/Q6 entries; `findings/2026-07-03-crypto-basis-s8-q5.md` (overround decomposition) | **test** — `tests/test_anomaly_sweep.py` + pricing tests (KXBTC ladder correctly not flagged) |
| L13 | 2026-07-05 | A membership-diff over accumulated tape has a startup artifact: everything "appears" at the first capture. Exclude the first capture window before calling a membership change an event. | `findings/2026-07-05-polymarket-leadlag-s9-first-cut.md`, `scripts/s9_shock_eventstudy.py` | **test** — `tests/test_s9_shock_eventstudy.py` |
| L14 | 2026-07-05 | The sandbox's local `main` ref can silently lag `origin/main` by days — every diff against it lies. Always `git fetch origin main` and diff against the remote tip before trusting any line-set comparison. | LOOP-QUEUE run log 2026-07-05T00:14Z (step-0b sweep caught its own bug) | **protocol** — LOOP-QUEUE step 0b wording |
| L15 | 2026-07-06 | Kalshi lists no hourly BTC/ETH group during the 20 UTC hour — a recurring venue-side hole (12/12 incomplete crypto passes 07-03→07-05 are exactly the 20:2x/20:5x captures, both collectors). Expect 2 incomplete passes/day; a probe over hourly tape must treat that hour as structurally missing, not as collector failure. | `findings/2026-07-06-tape-audit.md` | **ledger-only** — venue behavior; revisit if the pattern shifts |
| L16 | 2026-07-06 | Match cross-venue/cross-series markets by their own title/rules text, never by ticker arithmetic: KXFEDDECISION uses "26" as a stand-in for ">25bps", and ticker-suffix matching would mispair it. | `findings/2026-07-06-fed-decision-macro-pairs-q12-first-cut.md` | **test** — `tests/test_polymarket_pairs.py` (structural confirmation paths) |
| L17 | 2026-07-06 | The hourly collector's push to main fails intermittently and strands tape on `tape/hourly-*` branches — tape that never reaches main on its own. Every research run must union-append the line-set diff (30-min freshness rule) or the canonical tape silently under-counts. This run swept 1,158 stranded lines. | LOOP-QUEUE step 0b; `findings/2026-07-06-tape-audit.md` | **protocol** — LOOP-QUEUE step 0b; UNENFORCED as an automated check (candidate: an invariants warning when stranded branches exist) |
| L18 | 2026-07-06 | Kalshi fee-schedule rates live solely in `core/pricing.py` as `TAKER_FEE_RATE` (0.07) / `MAKER_FEE_RATE` (0.0175) / `SP500_NDX_FEE_RATE` (0.035); no module may hand-roll a fee coefficient. Supersedes L5's `UNENFORCED` static-check clause. Honest scope: name-bound fee/rate/coeff constants and positional literal args into `fee_per_contract()` are statically caught; a from-scratch re-implementation of the fee formula that reads an imported rate is NOT statically catchable and stays **protocol**. | this run (escalate L5/L7/L17); supersedes L5; `findings/2026-07-04-sports-maker-s13-verdict.md` | **invariant** — `no_handrolled_fee_rate` in `scripts/invariants.py` (pattern A name-bound + pattern B positional fee-call), sanctioned to `core/pricing.py`; `tests/test_invariants.py::test_fee_rate_rule_fires`, `::test_fee_rate_rule_exempt_in_sanctioned_pricing_site`, `::test_fee_rate_rule_skips_comment_lines`, `::test_fee_rate_rule_silent_on_non_schedule_uses` |
| L19 | 2026-07-06 | L7 (never hardcode a bracket/strike width) is genuinely NOT statically assertable: a numeric width literal is indistinguishable from any other constant at regex level, and the one live site (`scripts/s8_basis_probe.py` `BAND_WIDTH_DOLLARS_BY_SYMBOL = {"BTC": 100.0, "ETH": 20.0}`) is a deliberate documented schedule map — a name-anchored ban would either false-positive on it or have to sanction it, and the next hand-rolled width would just carry the sanctioned-looking name. Residual hazard this review found: `s8_basis_probe.py` lines 162/284 use `.get(symbol, 100.0)` — a silent $100 fallback that repeats the exact ETH-class bug for any NEW symbol (e.g. SOL). S8 is dead and the script historical so it is NOT edited; any future crypto probe (Q7/S10) MUST derive spacing from the ladder itself or fail loudly on an unknown symbol. Records L7's terminal state. | this run (escalate L5/L7/L17); records L7; `findings/2026-07-04-crypto-basis-s8-verdict.md` | **protocol** — terminal (not statically assertable); L7's `tests/test_s8_basis_probe.py` still pins the fixed bug; future-probe discipline addressed to Q7/S10 |
| L20 | 2026-07-06 | `scripts/invariants.py --full` now surfaces locally-known stranded `tape/hourly-*` refs as a **non-gating** stderr warning: offline-safe (any git failure yields no refs and can never poison the gate), reports LOCAL refs as of the last fetch only, and never flips the exit code — 35 such refs print on today's tree and the gate still exits 0. Supersedes L17's `UNENFORCED`-automated-check clause. LOOP-QUEUE step 0b (fetch + union-append line-set sweep) remains the binding reconciliation. | this run (escalate L5/L7/L17); supersedes L17; `findings/2026-07-06-tape-audit.md` | **protocol + non-gating invariants warning** — `_git_tape_refs` / `stranded_tape_warning` wired into `scripts/invariants.py` `main()` (--full/default only, never --pre-edit-hook or --db); `tests/test_invariants.py::test_stranded_tape_warning_none_when_empty`, `::test_stranded_tape_warning_message_content`, `::test_git_tape_refs_returns_list_without_raising`, `::test_stranded_tape_warning_never_gates_exit_code`; LOOP-QUEUE step 0b binding |
| L21 | 2026-07-07 | When a new sub-pass needs "the set of tickers an upstream sub-pass already discovered this pass" (e.g. S6's depth collector needs sports_pairs/crypto_hourly's tickers), don't re-run discovery and don't thread a new return field through every sibling collector's `run()` (would change every existing return shape and break their tests) — read the tickers straight back out of the freshly-written tape, filtered by this pass's own `capture_id`. Zero extra API calls, no shape change to existing collectors. | `collection/hourly_pass.py::_gather_depth_tickers`/`_tickers_from_tape`, LOOP-QUEUE Q16 (2026-07-07) | **protocol** — a reusable wiring pattern, not statically assertable; demonstrated in `tests/test_hourly_pass.py`'s Q16 tests |
| L22 | 2026-07-07 | `core/source_tag.py`'s `VALID_SOURCE_TAGS` (`real_ask`/`midpoint`/`synthetic`/`broker_truth`, per CLAUDE.md) has no slot for a resting bid's own genuine fillable price — a live order book has two real sides, not one. `collection/orderbook_depth.py` tags the ask ladder `real_ask` and the bid ladder `real_bid`, a value outside the canonical enum; harmless today (JSONL tape, not a DB row the invariant scans) but would trip `scripts/invariants.py`'s source-tag check the moment a `real_bid`-tagged value lands in a DB column. | `kb/lessons/00-lessons.md` L22 (this run); `findings` — see LOOP-QUEUE Q16 (2026-07-07) | **UNENFORCED** — kb-distiller to decide: add `real_bid` to `VALID_SOURCE_TAGS` + `scripts/invariants.py`, or document bid-side liveness as a separate tag namespace, before any bid-tagged value reaches a DB table |
| L23 | 2026-07-07 | An empty `yes_bids`/`no_bids` ladder on one side of a Kalshi order book is normal for a far-from-strike wing market (real one-sided book), not a capture failure — a depth collector must record it as valid data (`depth=0` on that side, ticker still counted captured), never as a DROP, or genuine market shape will spuriously tank `completeness_ok` on every wing. | `collection/orderbook_depth.py`, `tests/test_orderbook_depth.py::test_run_missing_book_still_captures_empty_ladders`, confirmed against a live Kalshi pass (LOOP-QUEUE Q16, 2026-07-07) | **test** — pinned in `tests/test_orderbook_depth.py`; UNENFORCED as a general "empty ≠ drop" invariant beyond this one collector |
| L24 | 2026-07-07 | L22 asked whether to add `real_bid` to `VALID_SOURCE_TAGS` or keep it a separate tape-only namespace. Decision: keep it separate — `VALID_SOURCE_TAGS` mirrors CLAUDE.md's own literal four-tag trust-taxonomy contract, and widening a project-contract enum is outside a single research-loop milestone's authority (the same class of decision as S9's sub-hourly-automation call and the PreToolUse-hook registration, both left for Ryan). The "harmless today" half of L22's claim is no longer just inspection — a new regression test proves the existing DB-side enum check already rejects `real_bid` (or any non-canonical tag) the moment one reaches a `price_source_tag` column, so no live gap exists to close. `core/source_tag.py`'s docstring now cross-references this decision so the next reader doesn't reopen it as a bug. | this run; supersedes L22; `core/source_tag.py` docstring; `tests/test_invariants.py::test_db_real_bid_tag_is_caught_as_invalid_enum` | **test** — the DB-boundary safety net is now regression-tested by name; the taxonomy-widening question itself is **protocol** (terminal, Ryan's call, not a loop decision) |
| L25 | 2026-07-10 | A queue day-count check (e.g. Q7's "≥7 distinct days of `tape/crypto_hourly/`") must verify a `dt=<date>` entry is the expected **file** shape, not just that a same-named path exists. The 2026-07-08 main-rewind caused the collectors to briefly run a different (post-reset) code version that wrote `tape/crypto_hourly/dt=2026-07-10/` and `tape/sports_pairs/dt=2026-07-10/` as **directories** of raw per-market API blobs, not the canonical `dt=2026-07-10.jsonl` line format — a naive `ls tape/<family>/ \| grep dt=` day-count would have miscounted Q7 as newly eligible on unreadable data. 23 hourly passes (2026-07-10T00:26Z–19:24Z) affected; self-corrected the moment collectors picked up the post-PR#35-reconciliation code (confirmed via the first pass after merge, stranded branch `tape/hourly-20260710T1955Z`). | `findings/2026-07-10-tape-format-regression-crypto-sports.md` | **UNENFORCED** — candidate: an invariants/tape-audit check that a `dt=<date>` path is a regular file, not a directory, per known tape family |
| L26 | 2026-07-11 | Kalshi's 1¢ minimum YES tick mechanically mirrors into a **\$1.00 NO ask** on any genuinely-far / floor-pinned bracket: `yes_ask` at the 1¢ floor ⇒ `yes_bid = 0` ⇒ `no_ask = 1 − yes_bid = \$1.00`. A **taker** "fade the rich tail" trade therefore has NO fillable positive-EV price on that side — profit is capped at exactly \$0 before fees (and `fee_per_contract(\$1.00) = 0`). In S10's tape only 4/18,992 (0.02%) far observations had any `no_ask < \$1.00` room, 3 of the 4 from a single hour. Any real tail-fade edge must live on the **maker** side (rest a NO offer / sell the rich YES *at* the elevated ask), which is S6/S11 territory (maker fee 4× cheaper) and needs L2 depth + a fill-sim — not falsified by a taker cut. Generalizes L12's fine-band floor-artifact caution to the NO / fade direction. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md` (S10 structural DEAD, verifier-CONFIRMED) | **ledger-only** — venue microstructure fact (the mirror is arithmetic: `no_ask = 1 − yes_bid`); cross-references L12's `tests/test_anomaly_sweep.py` floor-artifact test. The "tail-fade is structurally a maker trade" corollary is future-probe discipline (addressed to S6/S11), not statically assertable. |
| L27 | 2026-07-11 | `core.pricing.fee_per_contract(1.00) == 0` is **correct, not a bug** (a \$1.00 fill has no fee), but a block-bootstrap dominated by ~\$1.00-priced legs can yield a 95% CI whose lower bound is a floating-point **+0.000000** — technically "not negative" yet economically meaningless. S10: mean **+\$0.000008**, 95% CI **[+\$0.000000, +\$0.000024]** — three orders of magnitude below the 1¢ minimum tick (~0.001¢), i.e. unfillable rounding residue of a few floored trades. A sign-only CI check lets a mechanically-floored population masquerade as "alive"; every CI-based verdict must ALSO clear an **economic-significance / magnitude gate** (compare the estimate and CI bounds against the 1¢ tick), not just strictly-positive-on-sign. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md` | **UNENFORCED** — candidate: a magnitude-vs-tick assertion in a shared bootstrap-verdict helper (no such shared helper exists yet — each probe rolls its own CI); kb-only this milestone (scope limited to `kb/`). Not statically assertable at the invariants layer (it is a per-verdict methodology gate), so likely terminal as **protocol** once a probe-precedent encodes it. |
| L28 | 2026-07-11 | Before running a decay/CI pipeline to satisfy a gate like "clear the artifact floor," first verify the floor is even **observable** in the data: check what fraction of the candidate population is ALREADY floor-pinned at the **earliest** observation. S10's far brackets sat at the 1¢ YES floor ~40 min before close (mean early→late Δ`yes_ask` **+0.00014** across 18,992 obs) — there was no decay window beneath the floor to measure, a fact the probe surfaced only after building the whole EARLY/LATE-capture pipeline. A cheap early floor-pinned-fraction precheck would have surfaced this before the full build. Same discipline family as L6 (bootstrap the right unit) and L13 (exclude the startup artifact): descriptive sanity checks precede the verdict machinery. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md`, `scripts/s10_reachability_probe.py` | **UNENFORCED** — candidate: a reusable first-observation floor-pinned-fraction precheck as probe hygiene; not statically assertable as one invariant (per-probe population choice, like L6), so likely terminal as **protocol** encoded in probe precedents / `.claude/agents/edge-prober.md`. kb-only this milestone. |
| L29 | 2026-07-11 | L25's candidate (an invariants/tape-audit check that a `dt=<date>` path is a regular file, not a directory) is now built: `scripts/invariants.py`'s `_tape_dir_shape_issues()`/`tape_dir_shape_warning()`, wired into `--full`'s stderr output alongside the L20 stranded-tape advisory (same non-gating pattern — offline-safe, never flips the exit code). Live-validated against the real committed tree: it correctly flags the 4 stray directories the 2026-07-08 regression left behind (`crypto_hourly/dt=2026-07-10`, `sports_pairs/dt=2026-07-02`, `sports_pairs/dt=2026-07-09`, `sports_pairs/dt=2026-07-10`) that L25 first documented — those directories were never cleaned up after the regression self-corrected, only the newly-written data went back to the canonical format. Cleaning up the stray directories themselves (or reprocessing their raw blobs) is separate follow-up work, out of scope for this milestone. | this run; supersedes L25; `scripts/invariants.py`; `tests/test_invariants.py::test_tape_dir_shape_warning_none_when_empty`, `::test_tape_dir_shape_warning_message_content`, `::test_tape_dir_shape_issues_finds_directories`, `::test_tape_dir_shape_issues_clean_tree_is_empty`, `::test_tape_dir_shape_issues_missing_tape_root_is_empty`, `::test_tape_dir_shape_warning_never_gates_exit_code` | **invariant (non-gating advisory)** — fires automatically on every `invariants.py --full` run; supersedes L25's `UNENFORCED` |
| L30 | 2026-07-11 | Kalshi's maker fee is a **flat $0.01/contract at every interior price**, not price-scaled: `fee_per_contract(P, MAKER_FEE_RATE) = ceil(0.0175·P·(1−P)·100)/100 = 0.01` for all `0<P<1`, because `max P(1−P) = 0.25` ⇒ `0.0175·0.25·100 = 0.4375` always ceils to 1. Consequence: a maker must net **more than a full cent** of half-spread-minus-adverse-selection to break even, so the modal 1–2¢-spread Kalshi book (0.5–1¢ half-spread) is structurally unprofitable to quote *before* adverse selection is even counted — this is the structural killer of S6 (spread-capture) and the same fee-floor that killed S13 (the maker fee ate the assumed 1¢ bid-under-fair margin). Sharpens L5/L18's "4× cheaper than taker" framing into a hard 1¢ floor. | `findings/2026-07-11-mm-spread-s6-firstcut.md` (S6 first-cut DEAD, verifier-CONFIRMED) | **test** — pinned by `tests/test_s6_maker_firstcut.py::test_maker_fee_is_flat_one_cent_at_every_interior_price` (value-sweep over `p∈[0.01,0.99]`, only distinct value 0.01). Best home is that value-sweep test, not a static invariant: flatness is a numeric property of `core.pricing.fee_per_contract`, and `scripts/invariants.py` scans source *text* — it cannot evaluate the fee function, so a static assert here would be dishonest theater. Extends L5/L18. |
| L31 | 2026-07-11 | A **wide (>30¢) bid-ask spread on a far / one-sided Kalshi bracket is a nominal, not maker-capturable, spread** — it is wide *because* there is no two-sided interest (e.g. a 0.03 bid against a 0.89 ask). A naive maker-spread P&L that books the half-spread as income lets these unfillable wings dominate the mean: S6's >30¢-wing population showed **+$0.339/contract** (99.9% "profitable") vs **−$0.002** on realistic ≤10¢ two-sided books, and the naive "ALL two-sided" mean (+$0.06928) was *entirely* this wing artifact. Any maker-P&L calc must cap the spread / exclude the wing before a verdict — declaring the wing "alive" repeats the L12 (arb-direction) and L26 (taker fade / $1.00-NO-ask direction) floor-artifact errors, now generalized to the **spread-capture** direction. | `findings/2026-07-11-mm-spread-s6-firstcut.md` | **ledger-only** — venue microstructure fact (a far bracket quotes a wide spread precisely because one side of its book is empty). The actionable corollary (exclude/cap wing spreads before a maker-edge verdict) is a per-probe methodology gate, not statically assertable (the fillable-population cut is per-design, same class as L6's bootstrap-unit choice); cross-references L12's `tests/test_anomaly_sweep.py` floor-artifact test. kb-only this milestone. |
| L32 | 2026-07-11 | For a maker-spread proxy built over **hourly BBO snapshots**, a *frozen* consecutive pair (bid AND ask unchanged between two captures) is a **no-fill**, not free spread income — booking its nominal half-spread as riskless profit (`dmid=0` ⇒ `net = half_spread − fee`) is the naive error that inflates a phantom maker edge. Report the frozen fraction as an L28-style observability precheck (S6: 25,618/36,738 = **69.7%** of ≤90-min consecutive two-sided pairs frozen) and bracket every verdict with BOTH a frozen-inclusive cut (maximally generous — counts unrealized spread as income) AND a movement-conditioned cut (the only population where a fill plausibly occurred and adverse selection is real). S6's DEAD is robust precisely because both ends came out strictly negative (≤10¢ frozen-inclusive CI [−$0.00297,−$0.00094]; movement-conditioned CI [−$0.02271,−$0.01759]) — the kill survives either fill assumption. | `findings/2026-07-11-mm-spread-s6-firstcut.md`, `scripts/s6_maker_firstcut.py` | **UNENFORCED** — a per-probe methodology gate (frozen-fraction precheck + dual-cut bracketing), same family as L28 (verify the artifact floor is observable first) and L6 (bootstrap the right unit); not statically assertable as one invariant because the population and fill assumption are per-design. Likely terminal as **protocol** once encoded in probe precedents / `.claude/agents/edge-prober.md`. kb-only this milestone (scope limited to `kb/`). |
| L33 | 2026-07-11 | L27's (magnitude-vs-tick CI gate) and L28's (floor-pinned-fraction precheck) own candidate notes said each was "likely terminal as protocol... once a probe-precedent encodes it" rather than a statically-assertable invariant — but every probe to date (`s6_maker_firstcut.py`, `s10_reachability_probe.py`, `s7c_sports_clv_bootstrap.py`, ...) still hand-rolls its own block-bootstrap loop from scratch, so the fix has to be re-remembered per script instead of reused. Encoding the protocol as an importable helper (not a memory) is what turns "likely terminal as protocol" into something a future probe actually gets for free. | this run; generalizes L27/L28; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13/Q14/Q15 BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29 | **protocol, now importable** — `core/bootstrap.py`: `block_bootstrap` (generic by-unit block resample; L6-compliant — it takes an already-grouped-by-unit mapping and never guesses the key itself), `clears_tick_magnitude` (L27's gate as a function), `floor_pinned_fraction` (L28's precheck as a function). 17 new tests in `tests/test_bootstrap.py` (offline, synthetic). Does not retrofit already-verdicted probes — those verdicts stand as-is; this is for the next probe that needs a bootstrap (S11/S14/S16/S18, whenever their own data blockers clear). |
| L34 | 2026-07-11 | L33 built the importable bootstrap-helper module but left the actual "probe-precedent encodes it" half of L27/L28's own candidate wording undone — the module existed, but nothing yet told a future probe to reach for it instead of hand-rolling again, so the next probe was still one memory-lapse away from repeating L27/L28. Closed that gap in the one place every probe is required to read before writing code: `.claude/agents/edge-prober.md`'s house-style section now names `core.bootstrap.block_bootstrap`/`clears_tick_magnitude`/`floor_pinned_fraction` explicitly and folds the tick-magnitude gate into the three-outcome verdict rule itself (a CI>0 that fails the magnitude gate is DEAD, not merely "worth flagging"). Docs-only — no probe re-run, no verdict re-opened. | this run; supersedes the "encode it" half of L27/L28 and closes L33's remaining gap; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 8 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue | **protocol, encoded** — `.claude/agents/edge-prober.md` house style; L27/L28 stay individually UNENFORCED as ledger rows (append-only — not rewritten) but their candidate is now live in the one file every future probe reads first; fully resolved once an actual probe (not just the charter) uses it. |
| L35 | 2026-07-12 | L34 closed L27/L28's "probe-precedent encodes it" gap but left L32 (frozen-pair no-fill precheck + dual-cut bracketing) as the one still-open UNENFORCED methodology candidate from the S6 lineage — `core/bootstrap.py` had `block_bootstrap`/`clears_tick_magnitude`/`floor_pinned_fraction` importable, but nothing importable for L32's own pattern, so a future maker/spread-style probe would still hand-roll the frozen-vs-movement split from scratch (exactly the class of duplication L33 was built to stop). | this run; generalizes L32; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 9 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29 and L33→L34 | **protocol, now importable** — `core/bootstrap.py` gains `bracket_by_movement(frozen_flags, values)`: takes the caller's already-computed per-observation frozen flags (L6-style — it never inspects raw book fields itself, "frozen" is a per-probe judgment call) and returns both the frozen-inclusive and movement-conditioned value lists plus the frozen fraction. 6 new tests in `tests/test_bootstrap.py` (empty input, all/none/partial frozen, length-mismatch raises). `.claude/agents/edge-prober.md` house style updated to name it alongside the L27/L28 helpers, for any probe built over repeated same-entity snapshots (BBO, order-book depth, ladder captures) rather than one-shot trade outcomes. Does not retrofit S6's already-verdicted probe — that verdict stands as-is. |
| L36 | 2026-07-12 | L7 ("never hardcode a bracket/strike width... derive spacing from the ladder itself") stayed `UNENFORCED` for over a week: `scripts/s8_basis_probe.py`'s actual fix only swapped a single fixed-$100 half-band check for a per-symbol dict (`BAND_WIDTH_DOLLARS_BY_SYMBOL = {"BTC": 100.0, "ETH": 20.0}`) — still a hardcoded guess two symbols wide, not a value read off the ladder's own strikes, so a third symbol or a mid-series spacing change would silently mis-score exactly like the original bug. No importable helper existed for the thing L7's own wording asked for. | this run; generalizes L7; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 9 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35 | **protocol, now importable** — `core/pricing.py` gains `infer_strike_spacing(strikes)`: dedupes and sorts the ladder's own strikes, returns the MEDIAN consecutive gap (robust to one missing/duplicated member), `None` below 2 distinct strikes. 5 new tests in `tests/test_substrate_primitives.py` (BTC-like $100 ladder, ETH-like $20 ladder, one-gap-doubled robustness, order/duplicate insensitivity, <2-strike None cases). `.claude/agents/edge-prober.md` house style updated to name it. Does not retrofit S8's already-verdicted probe (DEAD, Q5) — that verdict stands as-is; this is for the next probe/collector that needs a ladder's spacing. |
| L37 | 2026-07-12 | `scripts/invariants.py`'s `inv_no_yes_ask_arithmetic` (Hard Rule #3) scans raw text, including comments and docstrings, for `(yes\|no)_ask` immediately adjacent to an arithmetic operator. A test docstring's plain-English prose "`kalshi.yes_ask / polymarket.best_ask`" (describing two fields, not dividing them) tripped it — a false positive, not a real Hard Rule #3 violation. When naming a sanctioned real-ask field next to another field in a comment or docstring, use a word ("and"/"vs") instead of `/`, `+`, `-`, `*`, or `%` as the separator. | this run (Q12/S17 lead-lag probe build); `tests/test_s17_leadlag_probe.py` docstring (fixed before commit) | **ledger-only** — a prose-writing convention, not a code bug; the scanner is deliberately text-based (per L18's own note that some Hard Rule #3 shapes aren't statically catchable) and re-tightening the regex to parse comments/docstrings differently risks missing real violations, not worth the complexity for one avoidable prose collision. |
| L38 | 2026-07-12 | The step-0b stranded-tape sweep's raw total-lines-swept number is NOT a health metric on its own: full chronological series 2,076→223→1,936→873→872→1,708→2,632 is noisy/non-monotone, not climbing. `orderbook_depth` alone runs a flat ~1,100-1,280 lines/hour (not growing) but is ~3-4x every other family's per-hour volume combined, so whether a sweep window happens to catch 0/1/2 orderbook_depth passes swings the total by ±1,200-2,400 lines regardless of any real trend. A weekly retro (PR #46) read a local 4-sweep upswing as a possible regression before checking the fuller series. | `findings/2026-07-12-stranded-tape-sweep-growth-diagnosis.md` (tape-auditor diagnosis) | **ledger-only** — if sweep-size monitoring is ever automated, track lines-swept PER FAMILY, not the aggregate total, so orderbook_depth's inherent chunkiness doesn't mask a genuine drift in a smaller family; no code change warranted today (verdict: not a real problem) |
| L39 | 2026-07-13 | When a bracket-ladder P&L is a **small net of two large legs** (Σ collected premium ≈ the \$1 payout — S14's mean +\$0.0925 sits on top of a ~\$0.51–\$0.97 winner-\$1 loss), a **candlestick/volume fill proxy** (`high ≥ ask AND volume > 0`) that credits the **income leg without queue position is biased UPWARD**: a bar only proves the price *printed*, not that a resting offer ahead of the whole book would have filled. The loss leg is NOT so biased (heavy volume sweeps the offer — S14 winner filled 96.7%), so the bias is **asymmetric** and inflates the net. A per-leg **volume gate** (require the income leg to carry volume ≥ X) is **necessary-but-INSUFFICIENT** — it caps thin-print optimism but cannot model the **adverse-selection correlation** between *which* income strikes fill and *which* strike wins (which the queue-blind proxy assumes independent). Before claiming fillability, **decompose the edge as a fraction of the thinnest income legs**: S14's \$0.072 of \$0.093 (**78%**) came from sub-100-contract-volume legs, and the "complete fill" gate term was exactly \$0 (0.0% complete-fill) — the fat nominal overround never underwrites; the edge is entirely near-money partial pass-through. Family L27 (magnitude gate) / L30 (flat 1¢ fee annihilates 30.9% of the nominal overround) / L31 (nominal ≠ capturable) / L32 (frozen-vs-movement dual-cut). | `findings/2026-07-13-ladder-underwriting-s14-firstcut.md` (S14 first cut, verifier CONFIRMED-WITH-CAVEAT) | **UNENFORCED** — a per-probe verdict-methodology gate (the income-leg decomposition + queue-blind-proxy caution), same class as L6/L27/L28/L32: the population cut and fill assumption are per-design, so it is **not statically assertable** at the invariants layer. Not pinnable by a unit test either (it is a judgment about how to read a proxy, not a numeric property of a function). Terminal as **protocol** once a probe-precedent/edge-prober house-style encodes "decompose a small-net-of-two-legs edge by income-leg volume before calling it fillable" — an importable helper (à la L33/L35's bootstrap functions) would live in `core/`, outside this distiller's lane; flagged for a future run with core-write scope. The queue-aware L2 fill-sim named as S14's remaining binding gate is the real fix. |
| L40 | 2026-07-13 | `validation.v3_market`'s Kalshi `get_*` retries on HTTP **status codes** (429/5xx) but NOT on transport-level `ConnectionError` — a long candlestick sweep (S14 fetched 6,524 per-ticker summaries) that hits a mid-stream connection drop will raise rather than retry. The S14 script wrapped its own `ConnectionError` retry (self-contained, resumable via the cached `tape/s14_ladder_fillsim/dt=*.jsonl`), so no data was lost; recording so the next large-sweep script does the same rather than re-discovering it. | `findings/2026-07-13-ladder-underwriting-s14-firstcut.md`; `scripts/s14_ladder_fillsim.py` (self-contained retry) | **ledger-only** — an operational note about a client's retry surface (venue/library behavior), not a repo bug; the fix belongs in each sweep script's own fetch loop (out of this distiller's collection/core lane), and changing `validation.v3_market`'s retry policy is core-lane work. No code change warranted here. |
| L41 | 2026-07-13 | **Degenerate bootstrap: when every resample cluster resolves the same direction (zero losing clusters), a one-sided cluster-bootstrap p is MECHANICALLY 0 and carries no evidentiary weight** — no resample can produce a pooled mean ≤ 0 if no unit is ≤ 0, so p=0.0 regardless of the population's true edge. S20's sole BH-FDR survivor (`afkpnlucl`, mean e +6.6¢, boot CI90 [+1.3¢,+9.3¢], n=174) drew all 174 fills from **8 market clusters, every one of which resolved to 0** (the wallet sold longshots that all lost) — the "significance" was a construction artifact, and the catastrophic leg (a sold longshot that *wins*, −89¢ at this median) was structurally absent because the eval set conditions on markets already RESOLVED during the sample, and in a long-horizon winner ladder the early resolutions are disproportionately longshot eliminations. Root-cause family = **resolution-conditioning bias** (conditioning long-horizon skill evaluation on already-resolved markets truncates the tail that hasn't resolved yet — the wallet's still-alive book, Spain/England/France, was exactly the unpriced tail). Any positive-edge bootstrap claim requires **≥1 losing (opposite-sign-mean) cluster** in the resample unit AND a minimum cluster count (8 is below any comfortable floor — cf. S19's 2-event-hour filled population, L-family: a positive magnitude-gate-clearing CI over a below-floor unit count is a resampling artifact, not an edge). | `findings/2026-07-13-polymarket-wallet-forensics-s20-dossier.md` (Result 2, verifier finding; §Peer review); `findings/2026-07-13-polymarket-wallet-forensics-s20-prereg.md` (C1 cluster-bootstrap spec) | **helper + test (2026-07-13, same-day core-write pass, Ryan-approved local session)** — `core.bootstrap.bootstrap_verdict_admissible(unit_values, *, min_units=10)`: inadmissible unless ≥1 unit's mean sits strictly opposite the pooled mean's sign AND ≥`min_units` non-empty units; returns a reasons dict ({below_min_units, no_opposing_unit, empty}) so verdict reports record WHY, sibling to `clears_tick_magnitude` (L27) in the verdict rule — a CI failing EITHER gate is not-a-verdict by construction. 8 pinned tests in `tests/test_bootstrap.py` (incl. the exact S20 8/8-winning-clusters shape and S19's 2-unit shape). Like L27/L33 this is opt-in-by-import, not a static invariant (same runtime-vs-text reason); probe charters (L34) + Q24's binding gate make it protocol-mandatory for new probes. Until then the resolution-conditioning half is a per-probe methodology gate (population choice is per-design, L6-class) and stays **protocol** once encoded — the S20 dossier + Q24's probe spec ("explicit negative-skew accounting: the sold-longshot-wins leg modeled, not conditioned away; the ≥1-losing-cluster floor enforced") already carry it forward. |
| L42 | 2026-07-13 | **Trace every headline example number in a dossier to its exact source row (wallet / rank / PnL / category) before publication** — S20's first draft attributed a −27.8¢/n=3,248 per-trade-edge figure to the #1 leaderboard wallet (`0x3a8aa345…`, a 5-min crypto grinder) when it actually belonged to a **rank-47 sports wallet** (`0x8c80d213…`): wrong wallet, rank, PnL, and category. Caught ONLY by an independent verifier recomputing from raw fills, not by internal review. This is the same failure family as the founding pt1 synthetic-price mistake (L1) — a number detached from its provenance. The qualitative conclusion survived the correction, but the specific claim was false as first written; a dossier's numbers are load-bearing evidence and each must cite the row it came from. | `findings/2026-07-13-polymarket-wallet-forensics-s20-dossier.md` (Result 1 correction note + §Peer review: verifier REFUTED-and-corrected, zero-fill count 1→6 and the −27.8¢ mis-attribution) | **protocol** — a methodology/verification-hygiene discipline, not statically assertable (no scanner can know which wallet a prose figure "should" cite). The binding control that actually caught it is the **two-agent verdict rule** (independent `verifier` full-recomputation-from-source before any registry flip or published number), codified in LOOP-QUEUE.md protocol v3 / `.claude/agents/verifier.md`; this lesson records that the control fired and worked. Terminal as protocol — the honest enforcement is the redundant independent recompute, already in the loop. |
| L43 | 2026-07-13 | **L9 (verify two datasets' date windows overlap before joining) recurs at the COLLECTOR level, and there it is permanently fatal, not just a probe-time caution.** Q24's binding probe needs `tape/sports_clv/` (fair-anchored longshots) joined to `tape/orderbook_depth/` (the `no_bids` resting queue) — but the two collectors ran over **disjoint game windows** (`sports_clv` fair anchors cover kickoffs ≤07-03; sports `orderbook_depth` began 07-07), so every fair-anchored game had already SETTLED before any depth was captured → **0/81 joinable (0.00%)**, zero event- AND outcome-ticker overlap. Because the calendar date is embedded in the Kalshi ticker string, the non-overlap is STRUCTURAL — no probe-time join-window relaxation can manufacture a match (the verifier reproduced 0 by bypassing the probe's join code). The generalization beyond L9: when a future probe will need two tape families JOINED, the fix is not a careful probe-time check (L9) but **collector alignment** — the two capture passes must fire concurrently over the same *upcoming* (not-yet-settled) games, or the join is permanently empty regardless of how carefully the probe is written. Q24 is re-testable only on a fresh collection where `sports_clv` and `orderbook_depth` run concurrently over the same future window (e.g. a re-collected WC-final). | `findings/2026-07-13-q24-sports-longshot-maker-fillsim-verdict.md` (S21 DEAD by data-adequacy, verifier-CONFIRMED); generalizes L9 | **protocol** — a collector-design/alignment discipline (which two families a future probe will need joined, and that they must be captured concurrently over the same upcoming games), addressed to `collection/` design and to any milestone that specs a two-family join. Not statically assertable: a scanner cannot know which two tape families a future probe will want to join, nor that their capture windows must overlap (population/dataset choice is per-design, same class as L6's bootstrap-unit choice). L9's own probe-time join code still honestly reports matched/no_match (`tests/test_sports_history.py`); this records the forward-collection half L9 didn't cover. |
| L44 | 2026-07-13 | `tape/sports_history_s7/worldcup2026.jsonl` is a viable **OFFLINE sports executed-volume / touch source** for future sports fill-sims — it carries per-outcome `candles[].volume_fp` (executed contract volume, `real_ask`) and `yes_ask.high_dollars` (the bar's ask touch), the sports analogue of the crypto-only cached-candlestick source S14's ladder fill-sim used. In Q24 it supplied the ADEQUATE settlement + executed-volume side (81/81 fair-longshots settled, 8/81=9.88% YES; `broker_truth` settlement) while `orderbook_depth`'s queue was the missing leg — i.e. it is a real, reusable offline tape source, WC-only (Kalshi purges settled markets ~60 days post-close per L11, so this file is a fixed World-Cup-2026 snapshot, not a growing feed). Useful for any future sports fill-sim's settlement/executed-volume leg, but it does NOT carry the resting-queue depth a queue-aware fill-sim (L39) needs — that only lives in `orderbook_depth`. | `findings/2026-07-13-q24-sports-longshot-maker-fillsim-verdict.md`; `scripts/q24_sports_longshot_maker_fillsim.py` | **ledger-only** — a tape-source availability note (which offline file supplies sports settlement + executed volume, and its WC-only scope), not statically assertable and not a code bug; recorded so the next sports fill-sim reaches for it instead of re-discovering it. Complements L40 (self-contained candle-sweep retry) and L11 (60-day purge — why it is a fixed snapshot). |
| L45 | 2026-07-13 | Kalshi's crypto hourly-market ticker hour token (`KXBTC-26JUL0621-...`) is **America/New_York (ET), not UTC** — empirically confirmed against live tape: token hour 21 (`KXBTC-26JUL0621`) was captured 00:57:50 UTC on 07-07 (= 20:57 EDT on 07-06), closing 21:00 EDT = 01:00 UTC 07-07; `collection/crypto_hourly.py`'s own docstring already says "HH in ET". Q25's own milestone spec worked example ("...1221 → 21:00 UTC") was off by exactly the ET offset (4h in summer) — a close/ttc parser that reads the token as UTC mis-times every crypto market. Confirm a ticker grammar's stated timezone against the tape empirically before trusting a spec's worked example. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `scripts/q25_depth_tape_anatomy.py` | **UNENFORCED** — candidate: a shared ticker-grammar parsing helper (crypto-hour-is-ET) in `core/` or `collection/`, so future scripts import the corrected semantics instead of re-deriving them; no such shared close-time parser exists yet (each probe currently rolls its own, same class as L33's pre-bootstrap-helper state). |
| L46 | 2026-07-13 | Kalshi sports-market tickers with an `HHMM` component (`KXAFLGAME-26JUL160530SKSGEE-GEE`) encode a **league-local time whose zone is NOT independently verifiable from the depth tape** — settled markets linger in the `orderbook_depth` feed, so last-capture time is not a reliable close-time proxy to check the token against (unlike crypto, where a live book cross-check was possible for L45). Q25 treated sports HHMM as UTC per its own spec's contract but flagged the caveat explicitly; a wrong assumption only shifts near-close bucket boundaries by a few hours in an hourly-cadence descriptive scan, not structurally, but any consumer wanting < few-hour precision on a sports close time needs an independent source (e.g. matched odds-api kickoff time, L18) to confirm the zone. | `findings/2026-07-13-depth-tape-anatomy-q25.md` | **ledger-only** — a data-provenance limitation of the sports ticker grammar (no reliable independent verification path exists in currently-collected tape), not a code bug and not resolvable by a static assertion; record so a future probe needing sub-bucket sports close-time precision knows to source it from `odds_leg`/kickoff data instead of the ticker token. |
| L47 | 2026-07-13 | `collection/orderbook_depth.py`'s persisted book-side sizes (`yes_bids`/`no_bids` price+size ladders) are stored as **floats and can be fractional** (Q25 observed a real KXWCGAME median best-level size of 91,316.82 contracts) — a consumer assuming integer contract counts (e.g. `int(size)` truncation, or an equality check against a whole-number queue position) will silently corrupt real queue-depth reads. Report/compare sizes as floats throughout; do not coerce to int without an explicit, justified rounding rule. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `findings/depth_anatomy.json` | **UNENFORCED** — candidate: a `collection/orderbook_depth.py` docstring/schema note stating sizes are floats (not currently documented at the source), so a future consumer reads the field's real type before assuming int; not yet a code change, this milestone only surfaced the fact via a real observed value. |
| L48 | 2026-07-13 | A **resting-order turnover proxy** computed from consecutive same-ticker, same-best-price snapshot pairs (`max(0, size_prev−size_now)/size_prev`) is a strictly **looser, order-of-magnitude observable than a queue-aware fill rate** — it is snapshot-sampled (intra-hour round-trips invisible, undercounted) and a best-price move excludes the pair entirely (also undercounted, differently). Q25 used it only to orient cells against the S19 0.45%/S14 2.5% fill-rate anchors and explicitly could ONLY rule a cell OUT (dead-thin, e.g. KXBIG3GAME's 0.48% sitting on the S19-dead line) — never rule one IN as fillable; the highest-turnover cells (WNBA 11.06%, KBO 8.35%) are candidate-generation signal for a *future* queue-aware fill-sim, not evidence a fill-sim would succeed. Generalizes L39's "necessary-but-insufficient proxy" caution from candlestick fill-proxies to turnover-style observability proxies. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `scripts/q25_depth_tape_anatomy.py` | **protocol** — a per-probe interpretation discipline (a cheap observability proxy orients but never substitutes for a queue-aware fill-sim), same class as L39; not statically assertable (the proxy's role in any given probe is a per-design judgment call). Any future probe citing Q25's turnover numbers as fill-plausibility evidence must carry this "rules OUT, never IN" caveat explicitly. |
| L49 | 2026-07-14 | L45's own candidate wording ("a shared ticker-grammar parsing helper... no such shared close-time parser exists yet") stayed unbuilt for a run cycle: Q25's `scripts/q25_depth_tape_anatomy.py` still hand-rolled the ET-localize-then-convert-to-UTC logic for the crypto hour token inline (`parse_ticker_close`'s crypto branch, lines ~174-183) rather than importing it from anywhere — exactly the re-derive-per-script duplication L33/L34/L35/L36 closed for the bootstrap, magnitude-gate, floor-precheck, frozen-vs-movement, and strike-spacing helpers. No importable close-time parser existed for the thing L45's own wording asked for. Idle research-loop run (no eligible TODO/IN-PROGRESS queue item this firing — all of Q0-Q25 DONE/DEAD except Q19's per-event legs, still time-gated ahead of the Jul-14 CPI burst window) drew this from the lessons ledger's standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36. | this run; escalates L45; queue drained (Q0-Q25 all DONE/DEAD except Q19 per-event, time-gated) | **protocol, now importable** — `core/timeutil.py` gains `parse_crypto_hour_token_close_utc(token)`: parses a crypto-hourly ticker's bare date+hour middle segment (e.g. `'26JUL0621'`), localizes to `America/New_York` (DST-correct via `zoneinfo`, not a hardcoded EDT/EST offset), returns the tz-aware UTC close (or `None` on a grammar mismatch / out-of-range hour). 10 new tests in `tests/test_timeutil.py`, including the exact L45 empirical example and a winter (EST) case to prove it isn't summer-offset-hardcoded. `.claude/agents/edge-prober.md` house style updated to name it, alongside the L33/L35/L36 helpers. Does not retrofit Q25's already-verdicted script (that scan's numbers stand as-is, discovery-class, no registry flip) — this is for the next probe that needs a crypto-hourly close time. |
| L50 | 2026-07-14 | The fix for L43 (a two-family join permanently empty because the two collectors ran over disjoint windows) is to source the join's SECOND leg from a same-venue outcome/settlement endpoint retrievable ex-post over the PRIMARY tape's OWN window (within Kalshi's ~60-day purge, L11), rather than from a separate pre-computed anchor collected on a different, earlier schedule. S21 died because `sports_clv` fair anchors (kickoffs ≤07-03) were disjoint from `orderbook_depth` (≥07-07) → 0/81 joinable; S22/S23 (Q21 2026-07-14) instead pull realized settlement from Kalshi's free settled-markets endpoint (`fetch_kalshi_settled`) over the SAME depth-window games, so the join is non-empty by construction. At idea stage, this is the test that distinguishes a registrable candidate from an S21-class permanently-empty join: before registering any two-family-join probe, confirm the second leg is retrievable ex-post over the first leg's own window. | Q21 2026-07-14 idea-gen round (verifier-surfaced); generalizes L43/L9, cites L11 | **protocol** — a collector/probe-design discipline, not statically assertable (which two families a future probe joins and whether the second leg is ex-post retrievable is per-design, L6-class); addressed to `collection/` design and any milestone specing a two-family join. |
| L51 | 2026-07-14 | A "does signal X beat the mid" calibration precheck run on a DISAGREEMENT subset (both directional, X's call ≠ mid's call) produces two hit rates that are mechanically COMPLEMENTARY on a two-way market (`hit_X ≡ NOT hit_mid`, sum to exactly 1.0) — they are not two independent measurements. Q26/S22's disagreement-subset hit rate (imbalance 27.9% vs mid 72.1%) looked suspicious ("far below 50%, is the sign flipped and this a hidden contrarian edge?") until the verifier confirmed the arithmetic identity: X can only "beat the mid" on this subset if the mid is <50% accurate exactly where they disagree — a bar a liquid, calibrated market essentially never fails, and sign-flipping X on this subset just reproduces betting the mid (zero independent edge either direction). Any future "feature beats the market price" probe on a 2-way market should report the disagreement-subset result as "mid accuracy where they disagree = X%", not as two independent hit rates, to avoid the illusion of an extra data point. | `findings/2026-07-14-ofi-depth-imbalance-s22-verdict.md` (Q26/S22 verdict, verifier-surfaced) | **UNENFORCED** — a statistical-interpretation discipline for calibration-precheck-style probes, not statically assertable (whether a given probe's "disagreement subset" is a strict two-way partition is per-design); candidate: `.claude/agents/edge-prober.md` house-style note for any future calibration-precheck milestone. |
| L52 | 2026-07-14 | Kalshi sports settlement results are not always binary: Q26's live pull of `fetch_kalshi_settled` over 458 settled markets in 7 sports series returned 8 with `result:"scalar"`, not `result ∈ {"yes","no"}`. A binary-outcome probe must filter explicitly on the result field's actual value, never assume "settled" implies "yes/no" — an unfiltered join would silently inject non-binary rows into a yes/no hit-rate or P&L calculation. | `findings/2026-07-14-ofi-depth-imbalance-s22-verdict.md` (Q26/S22); `scripts/q26_ofi_depth_imbalance_probe.py` | **test** — pinned in `tests/test_q26_ofi_depth_imbalance_probe.py`'s settlement-cache handling; UNENFORCED as a general static invariant (no shared settlement-join helper yet exists to anchor a "scalar-filter required" rule to — candidate for a future `collection/sports_history.py`-adjacent shared join helper, similar in spirit to L50). |
| L53 | 2026-07-14 | A queue-aware fill-sim's fill-rate KILL gate (the S19 0.45% floor, Q27 gate G3) does NOT bind when the resting window is long: over a median ~37 hourly pre-close snapshots a cumulative-departures model clears almost any queue (S23 fill rate **95.83%**, 23/24), so a high fill rate is NOT evidence of a real edge — the strategy must still die or live on the EDGE (win-rate vs fill_price+fee), which is exactly where S23 died (DEAD-by-fee, not by fill or adequacy). Complements L39/L48's caution that a generous fill proxy orients but never substitutes for the edge test. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23, verifier-CONFIRMED) | **protocol** — a per-probe interpretation discipline (a passing fill-rate gate is necessary-but-insufficient; the edge test still binds), same class as L39/L48; not statically assertable (the fill window and departures model are per-design, L6-class). |
| L54 | 2026-07-14 | Favorite-longshot bias does NOT manifest as a fillable maker-BID edge on Kalshi two-sided sports: buying the favorite YES (normalized ask/bracket_sum ≥0.65) at the resting bid, favorites were marginally RICH — win-rate **0.6957** < breakeven fill_price+fee **0.7361** (mean fill $0.7261 `real_bid` + $0.01 flat maker fee) — the OPPOSITE of the textbook bias's prediction. With S13 (bid side vs devig, null), S21 (ask side, data-adequacy dead) and now S23 (bid side vs realized settlement, DEAD-by-fee), the entire favorite-longshot / S7-family maker lens is closed DEAD on Kalshi sports at real fills. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23, verifier-CONFIRMED) | **ledger-only** — an empirical venue finding (the bias is attenuated/absent as a fillable maker edge on this venue's two-sided sports books); not statically assertable and not a code bug. Cross-references L30's fee-floor family and `kb/quant-finance/favorite-longshot-bias.md`. |
| L55 | 2026-07-14 | An entry-time favorite filter (normalized ask/bracket_sum ≥0.65 chosen strictly from pre-close snapshots, with NO settlement lookahead) is small by construction — S23 yielded only **24 rested markets from 207 joinable games** — because a no-lookahead pre-close favorite population is inherently thin. This is the honest cost of an anti-lookahead design (contrast a lookahead filter that would inflate n by selecting on the outcome); a thin-but-honest population that still clears the ≥10-game floor is the correct trade. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23) | **protocol** — a probe-design discipline (accept a thin honest population over a fat lookahead-contaminated one, provided the ≥10-unit floor is met), same anti-leak family as L6's unit choice and the S5/S7 anti-lookahead precedents; not statically assertable. |
| L56 | 2026-07-14 | L37 (the Hard-Rule-#3 static scanner false-positives on prose/comments literally writing "yes_ask/bracket_sum" with a slash) RECURRED in `scripts/q27_favorite_underpricing_fillsim.py` (lines 6/78/171) and was again fixed by rewording the separator to "over"/"and". L37 is a prose-convention protocol, not a code bug; recording the recurrence so the next probe author writes "ask over bracket_sum" in comments/docstrings from the start. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23); `scripts/q27_favorite_underpricing_fillsim.py` (reworded pre-commit) | **ledger-only** — records the L37 recurrence (supersedes nothing); the scanner is deliberately text-based (L18/L37) and re-tightening its regex to parse comments risks missing real Hard-Rule-#3 violations. The honest fix is the prose convention, addressed to future probe authors. |
| L57 | 2026-07-14 | A burst-resolution cross-venue lead-lag correlation can be DOMINATED BY A SINGLE SHOCK TICK: on the June-CPI burst tape, Kalshi<->Polymarket rho(polymarket-leads) on the two CPI-sensitive July fed buckets was 0.902/0.777, but removing the single 12:30:13Z release-instant capture collapsed it to noise (0.196/0.037) AND the residual sign is unstable (flips toward Kalshi on leave-one-out) — the "lead" was that one snapshot expressed as a Pearson rho over ~100 near-flat steps, not a persistent relationship. Any burst-window lead-lag figure MUST be reported WITH the release-tick-removed recompute before any directional claim. Two corollaries confirmed this run: (i) the WIDTH x DURATION cross cleanly discriminates a real shock dislocation from a stale-quote nominal basis and generalizes L31 from single-venue wings to CROSS-VENUE dislocations — the two largest gaps ($0.079/$0.06 net of both fees) were single-capture release-instant transients while every durable gap was small ($0.01-0.04), the S6/L31 signature; (ii) `tape/polymarket_macro_pairs/` is SIZE-BLIND (only best_ask/best_bid, no depth) and `captured_at` is a single pass-level timestamp shared by all records in a capture, so it can neither prove venue simultaneity nor claim fillability — every cross-venue dislocation over it is a price observation, never a fill. | `findings/2026-07-14-s17-burst-cpi-q19.md` (Q19 CPI-burst per-event, verifier CONFIRMED-numbers / REFUTED-both-tradeable-claims) | **protocol** — a per-probe burst-window methodology gate (report leave-one-out for any single-shock lead-lag; the width x duration discriminator; the size-blind / single-pass-timestamp fillability caveat), same class as L27/L28/L32/L48 (the shock-tick and population choices are per-design, not statically assertable); cross-references L8 (lagged confound), L13 (startup artifact), L31 (nominal != capturable, now cross-venue), L39/L48 (a proxy orients but never substitutes for a size/queue-aware fill test). Addressed to the next S17 burst leg (WC Jul 14/15/19, FOMC Jul 29) and `.claude/agents/edge-prober.md` house style. |
