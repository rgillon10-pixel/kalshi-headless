# Lessons ledger — the compounding layer

`created 2026-07-06` · append-only · owner: kb-distiller agent (`.claude/agents/kb-distiller.md`)

**Why this file exists.** CLAUDE.md's rule is *invariants over memory* — but between "learned
it the hard way" and "it's a CI assertion" there is a gap where knowledge evaporates between
stateless cloud runs. This ledger is that gap made visible. Every hard-won lesson gets a row
the moment it's learned; its **enforcement** column tracks how far along the gradient it has
moved (`UNENFORCED` → `protocol` → `test` → `invariant`). `UNENFORCED` rows are the
kb-distiller's standing work queue. Some lessons are genuinely not assertable (venue
behavior, methodology judgment) — those record *why* and stay `protocol`/`ledger-only`;
that's an honest terminal state.

Rules: never edit or delete a row; supersede with a new entry citing the old ID. Every
lesson cites the finding/log entry that produced it (no claim without provenance).

---

| id | date | lesson | source | enforcement |
|---|---|---|---|---|
| L1 | 2026-06-18 | A synthetic/model price is NEVER a fill price. The pt1 trade lost 9.6% partly by treating synthetic prices as fillable. Every persisted number carries a source tag; untagged defaults to `synthetic`. | CLAUDE.md prime directive; `findings/2026-06-18-codebase-money-map.md` | **invariant** — Hard Rules #3/#4 in `scripts/invariants.py`; `core/source_tag.py` default; `tests/test_substrate_primitives.py` |
| L2 | 2026-06-18 | `pstdev` over ensemble members without `member_count >= 4` produces garbage spread estimates. | arb-bot post-mortem, inherited | **invariant** — Hard Rule #2; `core/stats.safe_pstdev` |
| L3 | 2026-06-18 | `ncep_gefs025` is byte-identical to `gfs_seamless` — including it double-counts a model and silently narrows ensembles. | arb-bot post-mortem, inherited | **invariant** — Hard Rule #1 in `scripts/invariants.py` |
| L4 | 2026-07-03 | Cloud sessions cannot `git push origin main` (permission boundary, not a race — proven by two clean rebases that still fell back). Assuming otherwise caused Q1 to be built twice from scratch. | LOOP-QUEUE run log 2026-07-03 reconciliation | **protocol** — LOOP-QUEUE steps 0 (claim-check) + 6 (PR + self-merge-if-green) |
| L5 | 2026-07-04 | Kalshi's maker fee is 0.0175, taker 0.07 — a 4x difference. A first S13 draft charged every maker fill the taker rate; at mid-range prices the maker fee alone is ~1¢/contract, enough to consume a 1¢ edge entirely. Always take fees from `core.pricing.fee_per_contract`, never hand-roll. | `findings/2026-07-04-sports-maker-s13-verdict.md` | **test** — fee math pinned in `tests/test_substrate_primitives.py` / `tests/test_s13_maker_fillsim.py`; UNENFORCED as a static "no hand-rolled fee constants" invariant |
| L6 | 2026-07-04 | Bootstrap the independent unit, never the row: outcomes within one game are correlated draws — resampling by outcome fakes n. Same for strikes within a release, hours within a session. | `findings/2026-07-04-sports-clv-s7-verdict.md` (block-bootstrap by GAME) | **protocol** — encoded in probe precedents + `.claude/agents/edge-prober.md`; not statically assertable (unit choice is per-design) |
| L7 | 2026-07-04 | Never hardcode a bracket/strike width: BTC hourly brackets are $50-spaced (fine bands $100), ETH $10/$20. A fixed-$100 half-band check silently mis-scored every ETH hour. Derive spacing from the ladder itself. | `findings/2026-07-04-crypto-basis-s8-verdict.md` (latent bug fixed) | **test** — `tests/test_s8_basis_probe.py`; UNENFORCED generally |
| L8 | 2026-07-03 | A "paired" reference price lagging the event by collector cadence is a confound, not a basis: S8's spot lagged settlement by mean 29 min, fully explaining the observed gaps. Align timestamps to lag 0 (historical minute candles) before measuring any basis. | `findings/2026-07-03-crypto-basis-s8-q5.md` → fixed in `findings/2026-07-04-crypto-basis-s8-verdict.md` | **protocol** — probe discipline (verifier attack #5); not statically assertable |
| L9 | 2026-07-03 | Before joining two datasets, verify their date windows actually overlap: S7a's ESPN pull (Jun 15–21) had zero overlap with the Kalshi tape (Jun 26–Jul 02) and would have joined nothing — or worse, joined wrongly. | `findings/2026-07-03-sports-history-s7b.md` | **protocol** — verifier attack #5; join code reports matched/no_match honestly (`tests/test_sports_history.py`) |
| L10 | 2026-07-04 | Kalshi's open-market universe is 10,000+ markets; an unbounded `/markets` pull grew RSS past 3GB. Cap pulls and carry an honest `markets_truncated` flag — never silently claim full coverage. | LOOP-QUEUE Q6 entry, 2026-07-04 run log | **test** — `tests/test_anomaly_sweep.py` (cap + flag); pattern documented in `.claude/agents/collector-engineer.md` |
| L11 | 2026-07-04 | Kalshi purges settled markets ~60 days after close (NFL fully purged by discovery time). Un-collected history is lost forever — collection starts the day a strategy is conceived, not the day it's tested. | `findings/2026-07-03-sports-history-s7a.md` | **protocol** — shaped Q10/S12 (collect-now-test-later); not assertable |
| L12 | 2026-07-04 | Fine-band ladders near Kalshi's 1¢ minimum ask produce enormous *apparent* overround (KXBTC bracket_sum 7.78 across 188 members) that is NOT an arb — the fee floor and unfillable wings absorb it. Any arb claim must clear `core.pricing.true_arb_edge`'s real fee-floor math. | LOOP-QUEUE Q2/Q5/Q6 entries; `findings/2026-07-03-crypto-basis-s8-q5.md` (overround decomposition) | **test** — `tests/test_anomaly_sweep.py` + pricing tests (KXBTC ladder correctly not flagged) |
| L13 | 2026-07-05 | A membership-diff over accumulated tape has a startup artifact: everything "appears" at the first capture. Exclude the first capture window before calling a membership change an event. | `findings/2026-07-05-polymarket-leadlag-s9-first-cut.md`, `scripts/s9_shock_eventstudy.py` | **test** — `tests/test_s9_shock_eventstudy.py` |
| L14 | 2026-07-05 | The sandbox's local `main` ref can silently lag `origin/main` by days — every diff against it lies. Always `git fetch origin main` and diff against the remote tip before trusting any line-set comparison. | LOOP-QUEUE run log 2026-07-05T00:14Z (step-0b sweep caught its own bug) | **protocol** — LOOP-QUEUE step 0b wording |
| L15 | 2026-07-06 | Kalshi lists no hourly BTC/ETH group during the 20 UTC hour — a recurring venue-side hole (12/12 incomplete crypto passes 07-03→07-05 are exactly the 20:2x/20:5x captures, both collectors). Expect 2 incomplete passes/day; a probe over hourly tape must treat that hour as structurally missing, not as collector failure. | `findings/2026-07-06-tape-audit.md` | **ledger-only** — venue behavior; revisit if the pattern shifts |
| L16 | 2026-07-06 | Match cross-venue/cross-series markets by their own title/rules text, never by ticker arithmetic: KXFEDDECISION uses "26" as a stand-in for ">25bps", and ticker-suffix matching would mispair it. | `findings/2026-07-06-fed-decision-macro-pairs-q12-first-cut.md` | **test** — `tests/test_polymarket_pairs.py` (structural confirmation paths) |
| L17 | 2026-07-06 | The hourly collector's push to main fails intermittently and strands tape on `tape/hourly-*` branches — tape that never reaches main on its own. Every research run must union-append the line-set diff (30-min freshness rule) or the canonical tape silently under-counts. This run swept 1,158 stranded lines. | LOOP-QUEUE step 0b; `findings/2026-07-06-tape-audit.md` | **protocol** — LOOP-QUEUE step 0b; UNENFORCED as an automated check (candidate: an invariants warning when stranded branches exist) |
| L18 | 2026-07-06 | Kalshi fee-schedule rates live solely in `core/pricing.py` as `TAKER_FEE_RATE` (0.07) / `MAKER_FEE_RATE` (0.0175) / `SP500_NDX_FEE_RATE` (0.035); no module may hand-roll a fee coefficient. Supersedes L5's `UNENFORCED` static-check clause. Honest scope: name-bound fee/rate/coeff constants and positional literal args into `fee_per_contract()` are statically caught; a from-scratch re-implementation of the fee formula that reads an imported rate is NOT statically catchable and stays **protocol**. | this run (escalate L5/L7/L17); supersedes L5; `findings/2026-07-04-sports-maker-s13-verdict.md` | **invariant** — `no_handrolled_fee_rate` in `scripts/invariants.py` (pattern A name-bound + pattern B positional fee-call), sanctioned to `core/pricing.py`; `tests/test_invariants.py::test_fee_rate_rule_fires`, `::test_fee_rate_rule_exempt_in_sanctioned_pricing_site`, `::test_fee_rate_rule_skips_comment_lines`, `::test_fee_rate_rule_silent_on_non_schedule_uses` |
| L19 | 2026-07-06 | L7 (never hardcode a bracket/strike width) is genuinely NOT statically assertable: a numeric width literal is indistinguishable from any other constant at regex level, and the one live site (`scripts/s8_basis_probe.py` `BAND_WIDTH_DOLLARS_BY_SYMBOL = {"BTC": 100.0, "ETH": 20.0}`) is a deliberate documented schedule map — a name-anchored ban would either false-positive on it or have to sanction it, and the next hand-rolled width would just carry the sanctioned-looking name. Residual hazard this review found: `s8_basis_probe.py` lines 162/284 use `.get(symbol, 100.0)` — a silent $100 fallback that repeats the exact ETH-class bug for any NEW symbol (e.g. SOL). S8 is dead and the script historical so it is NOT edited; any future crypto probe (Q7/S10) MUST derive spacing from the ladder itself or fail loudly on an unknown symbol. Records L7's terminal state. | this run (escalate L5/L7/L17); records L7; `findings/2026-07-04-crypto-basis-s8-verdict.md` | **protocol** — terminal (not statically assertable); L7's `tests/test_s8_basis_probe.py` still pins the fixed bug; future-probe discipline addressed to Q7/S10 |
| L20 | 2026-07-06 | `scripts/invariants.py --full` now surfaces locally-known stranded `tape/hourly-*` refs as a **non-gating** stderr warning: offline-safe (any git failure yields no refs and can never poison the gate), reports LOCAL refs as of the last fetch only, and never flips the exit code — 35 such refs print on today's tree and the gate still exits 0. Supersedes L17's `UNENFORCED`-automated-check clause. LOOP-QUEUE step 0b (fetch + union-append line-set sweep) remains the binding reconciliation. | this run (escalate L5/L7/L17); supersedes L17; `findings/2026-07-06-tape-audit.md` | **protocol + non-gating invariants warning** — `_git_tape_refs` / `stranded_tape_warning` wired into `scripts/invariants.py` `main()` (--full/default only, never --pre-edit-hook or --db); `tests/test_invariants.py::test_stranded_tape_warning_none_when_empty`, `::test_stranded_tape_warning_message_content`, `::test_git_tape_refs_returns_list_without_raising`, `::test_stranded_tape_warning_never_gates_exit_code`; LOOP-QUEUE step 0b binding |
| L21 | 2026-07-07 | When a new sub-pass needs "the set of tickers an upstream sub-pass already discovered this pass" (e.g. S6's depth collector needs sports_pairs/crypto_hourly's tickers), don't re-run discovery and don't thread a new return field through every sibling collector's `run()` (would change every existing return shape and break their tests) — read the tickers straight back out of the freshly-written tape, filtered by this pass's own `capture_id`. Zero extra API calls, no shape change to existing collectors. | `collection/hourly_pass.py::_gather_depth_tickers`/`_tickers_from_tape`, LOOP-QUEUE Q16 (2026-07-07) | **protocol** — a reusable wiring pattern, not statically assertable; demonstrated in `tests/test_hourly_pass.py`'s Q16 tests |
| L22 | 2026-07-07 | `core/source_tag.py`'s `VALID_SOURCE_TAGS` (`real_ask`/`midpoint`/`synthetic`/`broker_truth`, per CLAUDE.md) has no slot for a resting bid's own genuine fillable price — a live order book has two real sides, not one. `collection/orderbook_depth.py` tags the ask ladder `real_ask` and the bid ladder `real_bid`, a value outside the canonical enum; harmless today (JSONL tape, not a DB row the invariant scans) but would trip `scripts/invariants.py`'s source-tag check the moment a `real_bid`-tagged value lands in a DB column. | `kb/lessons/00-lessons.md` L22 (this run); `findings` — see LOOP-QUEUE Q16 (2026-07-07) | **UNENFORCED** — kb-distiller to decide: add `real_bid` to `VALID_SOURCE_TAGS` + `scripts/invariants.py`, or document bid-side liveness as a separate tag namespace, before any bid-tagged value reaches a DB table |
| L23 | 2026-07-07 | An empty `yes_bids`/`no_bids` ladder on one side of a Kalshi order book is normal for a far-from-strike wing market (real one-sided book), not a capture failure — a depth collector must record it as valid data (`depth=0` on that side, ticker still counted captured), never as a DROP, or genuine market shape will spuriously tank `completeness_ok` on every wing. | `collection/orderbook_depth.py`, `tests/test_orderbook_depth.py::test_run_missing_book_still_captures_empty_ladders`, confirmed against a live Kalshi pass (LOOP-QUEUE Q16, 2026-07-07) | **test** — pinned in `tests/test_orderbook_depth.py`; UNENFORCED as a general "empty ≠ drop" invariant beyond this one collector |
| L24 | 2026-07-07 | L22 asked whether to add `real_bid` to `VALID_SOURCE_TAGS` or keep it a separate tape-only namespace. Decision: keep it separate — `VALID_SOURCE_TAGS` mirrors CLAUDE.md's own literal four-tag trust-taxonomy contract, and widening a project-contract enum is outside a single research-loop milestone's authority (the same class of decision as S9's sub-hourly-automation call and the PreToolUse-hook registration, both left for Ryan). The "harmless today" half of L22's claim is no longer just inspection — a new regression test proves the existing DB-side enum check already rejects `real_bid` (or any non-canonical tag) the moment one reaches a `price_source_tag` column, so no live gap exists to close. `core/source_tag.py`'s docstring now cross-references this decision so the next reader doesn't reopen it as a bug. | this run; supersedes L22; `core/source_tag.py` docstring; `tests/test_invariants.py::test_db_real_bid_tag_is_caught_as_invalid_enum` | **test** — the DB-boundary safety net is now regression-tested by name; the taxonomy-widening question itself is **protocol** (terminal, Ryan's call, not a loop decision) |
| L25 | 2026-07-10 | A queue day-count check (e.g. Q7's "≥7 distinct days of `tape/crypto_hourly/`") must verify a `dt=<date>` entry is the expected **file** shape, not just that a same-named path exists. The 2026-07-08 main-rewind caused the collectors to briefly run a different (post-reset) code version that wrote `tape/crypto_hourly/dt=2026-07-10/` and `tape/sports_pairs/dt=2026-07-10/` as **directories** of raw per-market API blobs, not the canonical `dt=2026-07-10.jsonl` line format — a naive `ls tape/<family>/ \| grep dt=` day-count would have miscounted Q7 as newly eligible on unreadable data. 23 hourly passes (2026-07-10T00:26Z–19:24Z) affected; self-corrected the moment collectors picked up the post-PR#35-reconciliation code (confirmed via the first pass after merge, stranded branch `tape/hourly-20260710T1955Z`). | `findings/2026-07-10-tape-format-regression-crypto-sports.md` | **test / non-gating advisory (BUILT — superseded by L29, 2026-07-11; stale `UNENFORCED` marker corrected 2026-07-21 by the kb-distiller, lesson text unchanged)** — implemented as `scripts/invariants.py`'s `_tape_dir_shape_issues()`/`tape_dir_shape_warning()` (~L433-466), the non-gating `--full` advisory that flags any `tape/<family>/dt=<date>` path that is a directory instead of the canonical `.jsonl`; pinned by `tests/test_invariants.py::test_tape_dir_shape_issues_finds_directories` (+siblings). Original candidate (preserved): an invariants/tape-audit check that a `dt=<date>` path is a regular file, not a directory, per known tape family. |
| L26 | 2026-07-11 | Kalshi's 1¢ minimum YES tick mechanically mirrors into a **\$1.00 NO ask** on any genuinely-far / floor-pinned bracket: `yes_ask` at the 1¢ floor ⇒ `yes_bid = 0` ⇒ `no_ask = 1 − yes_bid = \$1.00`. A **taker** "fade the rich tail" trade therefore has NO fillable positive-EV price on that side — profit is capped at exactly \$0 before fees (and `fee_per_contract(\$1.00) = 0`). In S10's tape only 4/18,992 (0.02%) far observations had any `no_ask < \$1.00` room, 3 of the 4 from a single hour. Any real tail-fade edge must live on the **maker** side (rest a NO offer / sell the rich YES *at* the elevated ask), which is S6/S11 territory (maker fee 4× cheaper) and needs L2 depth + a fill-sim — not falsified by a taker cut. Generalizes L12's fine-band floor-artifact caution to the NO / fade direction. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md` (S10 structural DEAD, verifier-CONFIRMED) | **ledger-only** — venue microstructure fact (the mirror is arithmetic: `no_ask = 1 − yes_bid`); cross-references L12's `tests/test_anomaly_sweep.py` floor-artifact test. The "tail-fade is structurally a maker trade" corollary is future-probe discipline (addressed to S6/S11), not statically assertable. |
| L27 | 2026-07-11 | `core.pricing.fee_per_contract(1.00) == 0` is **correct, not a bug** (a \$1.00 fill has no fee), but a block-bootstrap dominated by ~\$1.00-priced legs can yield a 95% CI whose lower bound is a floating-point **+0.000000** — technically "not negative" yet economically meaningless. S10: mean **+\$0.000008**, 95% CI **[+\$0.000000, +\$0.000024]** — three orders of magnitude below the 1¢ minimum tick (~0.001¢), i.e. unfillable rounding residue of a few floored trades. A sign-only CI check lets a mechanically-floored population masquerade as "alive"; every CI-based verdict must ALSO clear an **economic-significance / magnitude gate** (compare the estimate and CI bounds against the 1¢ tick), not just strictly-positive-on-sign. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md` | **UNENFORCED** — candidate: a magnitude-vs-tick assertion in a shared bootstrap-verdict helper (no such shared helper exists yet — each probe rolls its own CI); kb-only this milestone (scope limited to `kb/`). Not statically assertable at the invariants layer (it is a per-verdict methodology gate), so likely terminal as **protocol** once a probe-precedent encodes it. |
| L28 | 2026-07-11 | Before running a decay/CI pipeline to satisfy a gate like "clear the artifact floor," first verify the floor is even **observable** in the data: check what fraction of the candidate population is ALREADY floor-pinned at the **earliest** observation. S10's far brackets sat at the 1¢ YES floor ~40 min before close (mean early→late Δ`yes_ask` **+0.00014** across 18,992 obs) — there was no decay window beneath the floor to measure, a fact the probe surfaced only after building the whole EARLY/LATE-capture pipeline. A cheap early floor-pinned-fraction precheck would have surfaced this before the full build. Same discipline family as L6 (bootstrap the right unit) and L13 (exclude the startup artifact): descriptive sanity checks precede the verdict machinery. | `findings/2026-07-11-crypto-reachability-s10-firstcut.md`, `scripts/s10_reachability_probe.py` | **UNENFORCED** — candidate: a reusable first-observation floor-pinned-fraction precheck as probe hygiene; not statically assertable as one invariant (per-probe population choice, like L6), so likely terminal as **protocol** encoded in probe precedents / `.claude/agents/edge-prober.md`. kb-only this milestone. |
| L29 | 2026-07-11 | L25's candidate (an invariants/tape-audit check that a `dt=<date>` path is a regular file, not a directory) is now built: `scripts/invariants.py`'s `_tape_dir_shape_issues()`/`tape_dir_shape_warning()`, wired into `--full`'s stderr output alongside the L20 stranded-tape advisory (same non-gating pattern — offline-safe, never flips the exit code). Live-validated against the real committed tree: it correctly flags the 4 stray directories the 2026-07-08 regression left behind (`crypto_hourly/dt=2026-07-10`, `sports_pairs/dt=2026-07-02`, `sports_pairs/dt=2026-07-09`, `sports_pairs/dt=2026-07-10`) that L25 first documented — those directories were never cleaned up after the regression self-corrected, only the newly-written data went back to the canonical format. Cleaning up the stray directories themselves (or reprocessing their raw blobs) is separate follow-up work, out of scope for this milestone. | this run; supersedes L25; `scripts/invariants.py`; `tests/test_invariants.py::test_tape_dir_shape_warning_none_when_empty`, `::test_tape_dir_shape_warning_message_content`, `::test_tape_dir_shape_issues_finds_directories`, `::test_tape_dir_shape_issues_clean_tree_is_empty`, `::test_tape_dir_shape_issues_missing_tape_root_is_empty`, `::test_tape_dir_shape_warning_never_gates_exit_code` | **invariant (non-gating advisory)** — fires automatically on every `invariants.py --full` run; supersedes L25's `UNENFORCED` |
| L30 | 2026-07-11 | Kalshi's maker fee is a **flat $0.01/contract at every interior price**, not price-scaled: `fee_per_contract(P, MAKER_FEE_RATE) = ceil(0.0175·P·(1−P)·100)/100 = 0.01` for all `0<P<1`, because `max P(1−P) = 0.25` ⇒ `0.0175·0.25·100 = 0.4375` always ceils to 1. Consequence: a maker must net **more than a full cent** of half-spread-minus-adverse-selection to break even, so the modal 1–2¢-spread Kalshi book (0.5–1¢ half-spread) is structurally unprofitable to quote *before* adverse selection is even counted — this is the structural killer of S6 (spread-capture) and the same fee-floor that killed S13 (the maker fee ate the assumed 1¢ bid-under-fair margin). Sharpens L5/L18's "4× cheaper than taker" framing into a hard 1¢ floor. | `findings/2026-07-11-mm-spread-s6-firstcut.md` (S6 first-cut DEAD, verifier-CONFIRMED) | **test** — pinned by `tests/test_s6_maker_firstcut.py::test_maker_fee_is_flat_one_cent_at_every_interior_price` (value-sweep over `p∈[0.01,0.99]`, only distinct value 0.01). Best home is that value-sweep test, not a static invariant: flatness is a numeric property of `core.pricing.fee_per_contract`, and `scripts/invariants.py` scans source *text* — it cannot evaluate the fee function, so a static assert here would be dishonest theater. Extends L5/L18. |
| L31 | 2026-07-11 | A **wide (>30¢) bid-ask spread on a far / one-sided Kalshi bracket is a nominal, not maker-capturable, spread** — it is wide *because* there is no two-sided interest (e.g. a 0.03 bid against a 0.89 ask). A naive maker-spread P&L that books the half-spread as income lets these unfillable wings dominate the mean: S6's >30¢-wing population showed **+$0.339/contract** (99.9% "profitable") vs **−$0.002** on realistic ≤10¢ two-sided books, and the naive "ALL two-sided" mean (+$0.06928) was *entirely* this wing artifact. Any maker-P&L calc must cap the spread / exclude the wing before a verdict — declaring the wing "alive" repeats the L12 (arb-direction) and L26 (taker fade / $1.00-NO-ask direction) floor-artifact errors, now generalized to the **spread-capture** direction. | `findings/2026-07-11-mm-spread-s6-firstcut.md` | **ledger-only** — venue microstructure fact (a far bracket quotes a wide spread precisely because one side of its book is empty). The actionable corollary (exclude/cap wing spreads before a maker-edge verdict) is a per-probe methodology gate, not statically assertable (the fillable-population cut is per-design, same class as L6's bootstrap-unit choice); cross-references L12's `tests/test_anomaly_sweep.py` floor-artifact test. kb-only this milestone. |
| L32 | 2026-07-11 | For a maker-spread proxy built over **hourly BBO snapshots**, a *frozen* consecutive pair (bid AND ask unchanged between two captures) is a **no-fill**, not free spread income — booking its nominal half-spread as riskless profit (`dmid=0` ⇒ `net = half_spread − fee`) is the naive error that inflates a phantom maker edge. Report the frozen fraction as an L28-style observability precheck (S6: 25,618/36,738 = **69.7%** of ≤90-min consecutive two-sided pairs frozen) and bracket every verdict with BOTH a frozen-inclusive cut (maximally generous — counts unrealized spread as income) AND a movement-conditioned cut (the only population where a fill plausibly occurred and adverse selection is real). S6's DEAD is robust precisely because both ends came out strictly negative (≤10¢ frozen-inclusive CI [−$0.00297,−$0.00094]; movement-conditioned CI [−$0.02271,−$0.01759]) — the kill survives either fill assumption. | `findings/2026-07-11-mm-spread-s6-firstcut.md`, `scripts/s6_maker_firstcut.py` | **UNENFORCED** — a per-probe methodology gate (frozen-fraction precheck + dual-cut bracketing), same family as L28 (verify the artifact floor is observable first) and L6 (bootstrap the right unit); not statically assertable as one invariant because the population and fill assumption are per-design. Likely terminal as **protocol** once encoded in probe precedents / `.claude/agents/edge-prober.md`. kb-only this milestone (scope limited to `kb/`). |
| L33 | 2026-07-11 | L27's (magnitude-vs-tick CI gate) and L28's (floor-pinned-fraction precheck) own candidate notes said each was "likely terminal as protocol... once a probe-precedent encodes it" rather than a statically-assertable invariant — but every probe to date (`s6_maker_firstcut.py`, `s10_reachability_probe.py`, `s7c_sports_clv_bootstrap.py`, ...) still hand-rolls its own block-bootstrap loop from scratch, so the fix has to be re-remembered per script instead of reused. Encoding the protocol as an importable helper (not a memory) is what turns "likely terminal as protocol" into something a future probe actually gets for free. | this run; generalizes L27/L28; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13/Q14/Q15 BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29 | **protocol, now importable** — `core/bootstrap.py`: `block_bootstrap` (generic by-unit block resample; L6-compliant — it takes an already-grouped-by-unit mapping and never guesses the key itself), `clears_tick_magnitude` (L27's gate as a function), `floor_pinned_fraction` (L28's precheck as a function). 17 new tests in `tests/test_bootstrap.py` (offline, synthetic). Does not retrofit already-verdicted probes — those verdicts stand as-is; this is for the next probe that needs a bootstrap (S11/S14/S16/S18, whenever their own data blockers clear). |
| L34 | 2026-07-11 | L33 built the importable bootstrap-helper module but left the actual "probe-precedent encodes it" half of L27/L28's own candidate wording undone — the module existed, but nothing yet told a future probe to reach for it instead of hand-rolling again, so the next probe was still one memory-lapse away from repeating L27/L28. Closed that gap in the one place every probe is required to read before writing code: `.claude/agents/edge-prober.md`'s house-style section now names `core.bootstrap.block_bootstrap`/`clears_tick_magnitude`/`floor_pinned_fraction` explicitly and folds the tick-magnitude gate into the three-outcome verdict rule itself (a CI>0 that fails the magnitude gate is DEAD, not merely "worth flagging"). Docs-only — no probe re-run, no verdict re-opened. | this run; supersedes the "encode it" half of L27/L28 and closes L33's remaining gap; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 8 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue | **protocol, encoded** — `.claude/agents/edge-prober.md` house style; L27/L28 stay individually UNENFORCED as ledger rows (append-only — not rewritten) but their candidate is now live in the one file every future probe reads first; fully resolved once an actual probe (not just the charter) uses it. |
| L35 | 2026-07-12 | L34 closed L27/L28's "probe-precedent encodes it" gap but left L32 (frozen-pair no-fill precheck + dual-cut bracketing) as the one still-open UNENFORCED methodology candidate from the S6 lineage — `core/bootstrap.py` had `block_bootstrap`/`clears_tick_magnitude`/`floor_pinned_fraction` importable, but nothing importable for L32's own pattern, so a future maker/spread-style probe would still hand-roll the frozen-vs-movement split from scratch (exactly the class of duplication L33 was built to stop). | this run; generalizes L32; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 9 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29 and L33→L34 | **protocol, now importable** — `core/bootstrap.py` gains `bracket_by_movement(frozen_flags, values)`: takes the caller's already-computed per-observation frozen flags (L6-style — it never inspects raw book fields itself, "frozen" is a per-probe judgment call) and returns both the frozen-inclusive and movement-conditioned value lists plus the frozen fraction. 6 new tests in `tests/test_bootstrap.py` (empty input, all/none/partial frozen, length-mismatch raises). `.claude/agents/edge-prober.md` house style updated to name it alongside the L27/L28 helpers, for any probe built over repeated same-entity snapshots (BBO, order-book depth, ladder captures) rather than one-shot trade outcomes. Does not retrofit S6's already-verdicted probe — that verdict stands as-is. |
| L36 | 2026-07-12 | L7 ("never hardcode a bracket/strike width... derive spacing from the ladder itself") stayed `UNENFORCED` for over a week: `scripts/s8_basis_probe.py`'s actual fix only swapped a single fixed-$100 half-band check for a per-symbol dict (`BAND_WIDTH_DOLLARS_BY_SYMBOL = {"BTC": 100.0, "ETH": 20.0}`) — still a hardcoded guess two symbols wide, not a value read off the ladder's own strikes, so a third symbol or a mid-series spacing change would silently mis-score exactly like the original bug. No importable helper existed for the thing L7's own wording asked for. | this run; generalizes L7; queue drained (Q1 claimed by open PR #4, Q7/Q16 DONE, Q13 BLOCKED — 9 of ≥10 valid `tape/sports_pairs/` days, eligible ~07-13, Q14/Q15 data-adequacy BLOCKED) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35 | **protocol, now importable** — `core/pricing.py` gains `infer_strike_spacing(strikes)`: dedupes and sorts the ladder's own strikes, returns the MEDIAN consecutive gap (robust to one missing/duplicated member), `None` below 2 distinct strikes. 5 new tests in `tests/test_substrate_primitives.py` (BTC-like $100 ladder, ETH-like $20 ladder, one-gap-doubled robustness, order/duplicate insensitivity, <2-strike None cases). `.claude/agents/edge-prober.md` house style updated to name it. Does not retrofit S8's already-verdicted probe (DEAD, Q5) — that verdict stands as-is; this is for the next probe/collector that needs a ladder's spacing. |
| L37 | 2026-07-12 | `scripts/invariants.py`'s `inv_no_yes_ask_arithmetic` (Hard Rule #3) scans raw text, including comments and docstrings, for `(yes\|no)_ask` immediately adjacent to an arithmetic operator. A test docstring's plain-English prose "`kalshi.yes_ask / polymarket.best_ask`" (describing two fields, not dividing them) tripped it — a false positive, not a real Hard Rule #3 violation. When naming a sanctioned real-ask field next to another field in a comment or docstring, use a word ("and"/"vs") instead of `/`, `+`, `-`, `*`, or `%` as the separator. | this run (Q12/S17 lead-lag probe build); `tests/test_s17_leadlag_probe.py` docstring (fixed before commit) | **ledger-only** — a prose-writing convention, not a code bug; the scanner is deliberately text-based (per L18's own note that some Hard Rule #3 shapes aren't statically catchable) and re-tightening the regex to parse comments/docstrings differently risks missing real violations, not worth the complexity for one avoidable prose collision. |
| L38 | 2026-07-12 | The step-0b stranded-tape sweep's raw total-lines-swept number is NOT a health metric on its own: full chronological series 2,076→223→1,936→873→872→1,708→2,632 is noisy/non-monotone, not climbing. `orderbook_depth` alone runs a flat ~1,100-1,280 lines/hour (not growing) but is ~3-4x every other family's per-hour volume combined, so whether a sweep window happens to catch 0/1/2 orderbook_depth passes swings the total by ±1,200-2,400 lines regardless of any real trend. A weekly retro (PR #46) read a local 4-sweep upswing as a possible regression before checking the fuller series. | `findings/2026-07-12-stranded-tape-sweep-growth-diagnosis.md` (tape-auditor diagnosis) | **ledger-only** — if sweep-size monitoring is ever automated, track lines-swept PER FAMILY, not the aggregate total, so orderbook_depth's inherent chunkiness doesn't mask a genuine drift in a smaller family; no code change warranted today (verdict: not a real problem) |
| L39 | 2026-07-13 | When a bracket-ladder P&L is a **small net of two large legs** (Σ collected premium ≈ the \$1 payout — S14's mean +\$0.0925 sits on top of a ~\$0.51–\$0.97 winner-\$1 loss), a **candlestick/volume fill proxy** (`high ≥ ask AND volume > 0`) that credits the **income leg without queue position is biased UPWARD**: a bar only proves the price *printed*, not that a resting offer ahead of the whole book would have filled. The loss leg is NOT so biased (heavy volume sweeps the offer — S14 winner filled 96.7%), so the bias is **asymmetric** and inflates the net. A per-leg **volume gate** (require the income leg to carry volume ≥ X) is **necessary-but-INSUFFICIENT** — it caps thin-print optimism but cannot model the **adverse-selection correlation** between *which* income strikes fill and *which* strike wins (which the queue-blind proxy assumes independent). Before claiming fillability, **decompose the edge as a fraction of the thinnest income legs**: S14's \$0.072 of \$0.093 (**78%**) came from sub-100-contract-volume legs, and the "complete fill" gate term was exactly \$0 (0.0% complete-fill) — the fat nominal overround never underwrites; the edge is entirely near-money partial pass-through. Family L27 (magnitude gate) / L30 (flat 1¢ fee annihilates 30.9% of the nominal overround) / L31 (nominal ≠ capturable) / L32 (frozen-vs-movement dual-cut). | `findings/2026-07-13-ladder-underwriting-s14-firstcut.md` (S14 first cut, verifier CONFIRMED-WITH-CAVEAT) | **UNENFORCED** — a per-probe verdict-methodology gate (the income-leg decomposition + queue-blind-proxy caution), same class as L6/L27/L28/L32: the population cut and fill assumption are per-design, so it is **not statically assertable** at the invariants layer. Not pinnable by a unit test either (it is a judgment about how to read a proxy, not a numeric property of a function). Terminal as **protocol** once a probe-precedent/edge-prober house-style encodes "decompose a small-net-of-two-legs edge by income-leg volume before calling it fillable" — an importable helper (à la L33/L35's bootstrap functions) would live in `core/`, outside this distiller's lane; flagged for a future run with core-write scope. The queue-aware L2 fill-sim named as S14's remaining binding gate is the real fix. |
| L40 | 2026-07-13 | `validation.v3_market`'s Kalshi `get_*` retries on HTTP **status codes** (429/5xx) but NOT on transport-level `ConnectionError` — a long candlestick sweep (S14 fetched 6,524 per-ticker summaries) that hits a mid-stream connection drop will raise rather than retry. The S14 script wrapped its own `ConnectionError` retry (self-contained, resumable via the cached `tape/s14_ladder_fillsim/dt=*.jsonl`), so no data was lost; recording so the next large-sweep script does the same rather than re-discovering it. | `findings/2026-07-13-ladder-underwriting-s14-firstcut.md`; `scripts/s14_ladder_fillsim.py` (self-contained retry) | **ledger-only** — an operational note about a client's retry surface (venue/library behavior), not a repo bug; the fix belongs in each sweep script's own fetch loop (out of this distiller's collection/core lane), and changing `validation.v3_market`'s retry policy is core-lane work. No code change warranted here. |
| L41 | 2026-07-13 | **Degenerate bootstrap: when every resample cluster resolves the same direction (zero losing clusters), a one-sided cluster-bootstrap p is MECHANICALLY 0 and carries no evidentiary weight** — no resample can produce a pooled mean ≤ 0 if no unit is ≤ 0, so p=0.0 regardless of the population's true edge. S20's sole BH-FDR survivor (`afkpnlucl`, mean e +6.6¢, boot CI90 [+1.3¢,+9.3¢], n=174) drew all 174 fills from **8 market clusters, every one of which resolved to 0** (the wallet sold longshots that all lost) — the "significance" was a construction artifact, and the catastrophic leg (a sold longshot that *wins*, −89¢ at this median) was structurally absent because the eval set conditions on markets already RESOLVED during the sample, and in a long-horizon winner ladder the early resolutions are disproportionately longshot eliminations. Root-cause family = **resolution-conditioning bias** (conditioning long-horizon skill evaluation on already-resolved markets truncates the tail that hasn't resolved yet — the wallet's still-alive book, Spain/England/France, was exactly the unpriced tail). Any positive-edge bootstrap claim requires **≥1 losing (opposite-sign-mean) cluster** in the resample unit AND a minimum cluster count (8 is below any comfortable floor — cf. S19's 2-event-hour filled population, L-family: a positive magnitude-gate-clearing CI over a below-floor unit count is a resampling artifact, not an edge). | `findings/2026-07-13-polymarket-wallet-forensics-s20-dossier.md` (Result 2, verifier finding; §Peer review); `findings/2026-07-13-polymarket-wallet-forensics-s20-prereg.md` (C1 cluster-bootstrap spec) | **helper + test (2026-07-13, same-day core-write pass, Ryan-approved local session)** — `core.bootstrap.bootstrap_verdict_admissible(unit_values, *, min_units=10)`: inadmissible unless ≥1 unit's mean sits strictly opposite the pooled mean's sign AND ≥`min_units` non-empty units; returns a reasons dict ({below_min_units, no_opposing_unit, empty}) so verdict reports record WHY, sibling to `clears_tick_magnitude` (L27) in the verdict rule — a CI failing EITHER gate is not-a-verdict by construction. 8 pinned tests in `tests/test_bootstrap.py` (incl. the exact S20 8/8-winning-clusters shape and S19's 2-unit shape). Like L27/L33 this is opt-in-by-import, not a static invariant (same runtime-vs-text reason); probe charters (L34) + Q24's binding gate make it protocol-mandatory for new probes. Until then the resolution-conditioning half is a per-probe methodology gate (population choice is per-design, L6-class) and stays **protocol** once encoded — the S20 dossier + Q24's probe spec ("explicit negative-skew accounting: the sold-longshot-wins leg modeled, not conditioned away; the ≥1-losing-cluster floor enforced") already carry it forward. |
| L42 | 2026-07-13 | **Trace every headline example number in a dossier to its exact source row (wallet / rank / PnL / category) before publication** — S20's first draft attributed a −27.8¢/n=3,248 per-trade-edge figure to the #1 leaderboard wallet (`0x3a8aa345…`, a 5-min crypto grinder) when it actually belonged to a **rank-47 sports wallet** (`0x8c80d213…`): wrong wallet, rank, PnL, and category. Caught ONLY by an independent verifier recomputing from raw fills, not by internal review. This is the same failure family as the founding pt1 synthetic-price mistake (L1) — a number detached from its provenance. The qualitative conclusion survived the correction, but the specific claim was false as first written; a dossier's numbers are load-bearing evidence and each must cite the row it came from. | `findings/2026-07-13-polymarket-wallet-forensics-s20-dossier.md` (Result 1 correction note + §Peer review: verifier REFUTED-and-corrected, zero-fill count 1→6 and the −27.8¢ mis-attribution) | **protocol** — a methodology/verification-hygiene discipline, not statically assertable (no scanner can know which wallet a prose figure "should" cite). The binding control that actually caught it is the **two-agent verdict rule** (independent `verifier` full-recomputation-from-source before any registry flip or published number), codified in LOOP-QUEUE.md protocol v3 / `.claude/agents/verifier.md`; this lesson records that the control fired and worked. Terminal as protocol — the honest enforcement is the redundant independent recompute, already in the loop. |
| L43 | 2026-07-13 | **L9 (verify two datasets' date windows overlap before joining) recurs at the COLLECTOR level, and there it is permanently fatal, not just a probe-time caution.** Q24's binding probe needs `tape/sports_clv/` (fair-anchored longshots) joined to `tape/orderbook_depth/` (the `no_bids` resting queue) — but the two collectors ran over **disjoint game windows** (`sports_clv` fair anchors cover kickoffs ≤07-03; sports `orderbook_depth` began 07-07), so every fair-anchored game had already SETTLED before any depth was captured → **0/81 joinable (0.00%)**, zero event- AND outcome-ticker overlap. Because the calendar date is embedded in the Kalshi ticker string, the non-overlap is STRUCTURAL — no probe-time join-window relaxation can manufacture a match (the verifier reproduced 0 by bypassing the probe's join code). The generalization beyond L9: when a future probe will need two tape families JOINED, the fix is not a careful probe-time check (L9) but **collector alignment** — the two capture passes must fire concurrently over the same *upcoming* (not-yet-settled) games, or the join is permanently empty regardless of how carefully the probe is written. Q24 is re-testable only on a fresh collection where `sports_clv` and `orderbook_depth` run concurrently over the same future window (e.g. a re-collected WC-final). | `findings/2026-07-13-q24-sports-longshot-maker-fillsim-verdict.md` (S21 DEAD by data-adequacy, verifier-CONFIRMED); generalizes L9 | **protocol** — a collector-design/alignment discipline (which two families a future probe will need joined, and that they must be captured concurrently over the same upcoming games), addressed to `collection/` design and to any milestone that specs a two-family join. Not statically assertable: a scanner cannot know which two tape families a future probe will want to join, nor that their capture windows must overlap (population/dataset choice is per-design, same class as L6's bootstrap-unit choice). L9's own probe-time join code still honestly reports matched/no_match (`tests/test_sports_history.py`); this records the forward-collection half L9 didn't cover. |
| L44 | 2026-07-13 | `tape/sports_history_s7/worldcup2026.jsonl` is a viable **OFFLINE sports executed-volume / touch source** for future sports fill-sims — it carries per-outcome `candles[].volume_fp` (executed contract volume, `real_ask`) and `yes_ask.high_dollars` (the bar's ask touch), the sports analogue of the crypto-only cached-candlestick source S14's ladder fill-sim used. In Q24 it supplied the ADEQUATE settlement + executed-volume side (81/81 fair-longshots settled, 8/81=9.88% YES; `broker_truth` settlement) while `orderbook_depth`'s queue was the missing leg — i.e. it is a real, reusable offline tape source, WC-only (Kalshi purges settled markets ~60 days post-close per L11, so this file is a fixed World-Cup-2026 snapshot, not a growing feed). Useful for any future sports fill-sim's settlement/executed-volume leg, but it does NOT carry the resting-queue depth a queue-aware fill-sim (L39) needs — that only lives in `orderbook_depth`. | `findings/2026-07-13-q24-sports-longshot-maker-fillsim-verdict.md`; `scripts/q24_sports_longshot_maker_fillsim.py` | **ledger-only** — a tape-source availability note (which offline file supplies sports settlement + executed volume, and its WC-only scope), not statically assertable and not a code bug; recorded so the next sports fill-sim reaches for it instead of re-discovering it. Complements L40 (self-contained candle-sweep retry) and L11 (60-day purge — why it is a fixed snapshot). |
| L45 | 2026-07-13 | Kalshi's crypto hourly-market ticker hour token (`KXBTC-26JUL0621-...`) is **America/New_York (ET), not UTC** — empirically confirmed against live tape: token hour 21 (`KXBTC-26JUL0621`) was captured 00:57:50 UTC on 07-07 (= 20:57 EDT on 07-06), closing 21:00 EDT = 01:00 UTC 07-07; `collection/crypto_hourly.py`'s own docstring already says "HH in ET". Q25's own milestone spec worked example ("...1221 → 21:00 UTC") was off by exactly the ET offset (4h in summer) — a close/ttc parser that reads the token as UTC mis-times every crypto market. Confirm a ticker grammar's stated timezone against the tape empirically before trusting a spec's worked example. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `scripts/q25_depth_tape_anatomy.py` | **UNENFORCED** — candidate: a shared ticker-grammar parsing helper (crypto-hour-is-ET) in `core/` or `collection/`, so future scripts import the corrected semantics instead of re-deriving them; no such shared close-time parser exists yet (each probe currently rolls its own, same class as L33's pre-bootstrap-helper state). |
| L46 | 2026-07-13 | Kalshi sports-market tickers with an `HHMM` component (`KXAFLGAME-26JUL160530SKSGEE-GEE`) encode a **league-local time whose zone is NOT independently verifiable from the depth tape** — settled markets linger in the `orderbook_depth` feed, so last-capture time is not a reliable close-time proxy to check the token against (unlike crypto, where a live book cross-check was possible for L45). Q25 treated sports HHMM as UTC per its own spec's contract but flagged the caveat explicitly; a wrong assumption only shifts near-close bucket boundaries by a few hours in an hourly-cadence descriptive scan, not structurally, but any consumer wanting < few-hour precision on a sports close time needs an independent source (e.g. matched odds-api kickoff time, L18) to confirm the zone. | `findings/2026-07-13-depth-tape-anatomy-q25.md` | **ledger-only** — a data-provenance limitation of the sports ticker grammar (no reliable independent verification path exists in currently-collected tape), not a code bug and not resolvable by a static assertion; record so a future probe needing sub-bucket sports close-time precision knows to source it from `odds_leg`/kickoff data instead of the ticker token. |
| L47 | 2026-07-13 | `collection/orderbook_depth.py`'s persisted book-side sizes (`yes_bids`/`no_bids` price+size ladders) are stored as **floats and can be fractional** (Q25 observed a real KXWCGAME median best-level size of 91,316.82 contracts) — a consumer assuming integer contract counts (e.g. `int(size)` truncation, or an equality check against a whole-number queue position) will silently corrupt real queue-depth reads. Report/compare sizes as floats throughout; do not coerce to int without an explicit, justified rounding rule. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `findings/depth_anatomy.json` | **helper + test + advisory-invariant (2026-07-25 idle-run enforcement pass; supersedes the earlier UNENFORCED marker per L152's own-row-update rule, L25/L123/L152 precedent)** — three layers, all built this pass: (1) **helper** `core/depth.py::whole_contracts_available(size)` is now THE single sanctioned int coercion of a ladder size — it states the rounding rule explicitly (a fill lifts WHOLE contracts, so the liftable quantity at a level is the FLOOR of its resting size; flooring is the conservative direction, it can only under-state depth, never manufacture it; a 0<size<1 level therefore contributes ZERO), cites L47, is numerically identical to a bare `int(size)` for every finite size ≥ 0, and returns 0 defensively on None/non-numeric/NaN/±inf/negative. `execution/fill_models.py::_taker_depth` (the paper-P&L path, which carried a bare `int(size)` — see L154) now calls it. (2) **schema notes** — this row's literal named candidate is now in `collection/orderbook_depth.py`'s module docstring (SCHEMA — LADDER SIZES ARE FLOATS, with the real-tape census and a cross-reference to `collection/normalize.py`, which already carried the note at the shared normalizer). (3) **tests** — `tests/test_ladder_size_float_contract.py` (10 tests) pins the whole chain: a HARD acceptance test over REAL committed `tape/orderbook_depth/` proving fractional and 0<size<1 levels genuinely exist (the fact is pinned by DATA, not a fixture), `normalize_snapshot` preserving fractional sizes end to end, `_taker_depth` flooring / never reporting a fractional qty / continuing the ladder walk past a sub-1 dust level, and a real-tape REPLAY proving the swap changed no fill; plus 8 helper tests in `tests/test_depth.py`. (4) **non-gating advisory with a TESTED — not intended — shape set** `scripts/invariants.py::_ladder_size_coercion_issues` / `ladder_size_coercion_warning`, exempting `LADDER_SIZE_COERCION_EXEMPT = ("core/depth.py",)` (a WHOLE-FILE exemption, commented as such: a future bare `int(size)` elsewhere in `core/depth.py` is invisible). It is a LEXICAL PROXY, so its coverage is stated as the shapes a constructed-negative corpus asserts FIRE, per L155 — an earlier draft of this row claimed it "flags any NEW `int()`/`round()`/`floor()`/`ceil()` on a size-ish name", which a 15-shape adversarial probe (verifier pass, 2026-07-25) REFUTED in 10 cases; the matcher was then widened for 3 of them and the rest recorded as deliberate holes. **Fires on (regression-tested, `tests/test_invariants.py::test_ladder_size_coercion_fires_on_tested_shape_set`):** `int`/`round`/`floor`/`ceil`/`trunc` (incl. `math.` forms) applied to a size-ish name (`*size`/`*sizes`/`*_sz`/`queue_ahead*`/`*_depth`, bare, attribute, or single-subscript), to a ladder-level pair subscript (`level[1]`/`lvl[1]`/`bid[1]`), to a MULTI-subscript ladder level (`no_bids[0][1]`, `ladder[i][1]` — widened 2026-07-25 because `analysis/observatory/features.py:160-161` already writes exactly that shape for `touch_queue`, making it the likeliest reintroduction path), or to one NESTED cast (`int(float(size))`). **Deliberate, tested BLIND SPOTS (`::test_ladder_size_coercion_known_blind_spots_are_misses`):** bare `int(depth)` (NOT widened on purpose — `depth` is a legitimate already-integer level COUNT field in the record schema, so the rule would fire on correct code), `int(row[1])`/`int(pair[1])`/`int(entry[1])` (ordinary CSV/dict-iteration names), renamed intermediates (`n = size; int(n)` — needs dataflow), size-ish PREFIXes (`int(size_remaining)`) and paraphrases (`int(resting_qty_at_level)`), multi-LINE calls (the scan is line-by-line), and non-call coercions (`size // 1`, `'%d' % size`, `f"{size:d}"`). Closing any of these needs an AST/dataflow pass, not a wider regex; an honest documented hole beats a false-positive-prone guard. Also noted in code: the backtick-prose skip is a ONE-CHARACTER lookbehind, so only the single-backtick form is handled (latent precision fragility, 0 hits today). 0 issues on the clean tree AFTER widening, 36 collected cases across 11 test functions in `tests/test_invariants.py`. Deliberately ADVISORY, not gating: it cannot know whether an arbitrary `size` name is an order-book ladder size, so it must not be able to block a run — same class as the L138/L152 advisories. **Still open:** (a) the second half of this row's text (an equality check against a whole-number queue position, e.g. `size == 5`) is NOT flagged — no such site exists in the tree today and no high-precision lexical rule for it was found; (b) the coercion-shape recall holes enumerated above — a 0-issue report from this advisory is evidence of PRECISION only, never of recall (L155). |
| L48 | 2026-07-13 | A **resting-order turnover proxy** computed from consecutive same-ticker, same-best-price snapshot pairs (`max(0, size_prev−size_now)/size_prev`) is a strictly **looser, order-of-magnitude observable than a queue-aware fill rate** — it is snapshot-sampled (intra-hour round-trips invisible, undercounted) and a best-price move excludes the pair entirely (also undercounted, differently). Q25 used it only to orient cells against the S19 0.45%/S14 2.5% fill-rate anchors and explicitly could ONLY rule a cell OUT (dead-thin, e.g. KXBIG3GAME's 0.48% sitting on the S19-dead line) — never rule one IN as fillable; the highest-turnover cells (WNBA 11.06%, KBO 8.35%) are candidate-generation signal for a *future* queue-aware fill-sim, not evidence a fill-sim would succeed. Generalizes L39's "necessary-but-insufficient proxy" caution from candlestick fill-proxies to turnover-style observability proxies. | `findings/2026-07-13-depth-tape-anatomy-q25.md`; `scripts/q25_depth_tape_anatomy.py` | **protocol** — a per-probe interpretation discipline (a cheap observability proxy orients but never substitutes for a queue-aware fill-sim), same class as L39; not statically assertable (the proxy's role in any given probe is a per-design judgment call). Any future probe citing Q25's turnover numbers as fill-plausibility evidence must carry this "rules OUT, never IN" caveat explicitly. |
| L49 | 2026-07-14 | L45's own candidate wording ("a shared ticker-grammar parsing helper... no such shared close-time parser exists yet") stayed unbuilt for a run cycle: Q25's `scripts/q25_depth_tape_anatomy.py` still hand-rolled the ET-localize-then-convert-to-UTC logic for the crypto hour token inline (`parse_ticker_close`'s crypto branch, lines ~174-183) rather than importing it from anywhere — exactly the re-derive-per-script duplication L33/L34/L35/L36 closed for the bootstrap, magnitude-gate, floor-precheck, frozen-vs-movement, and strike-spacing helpers. No importable close-time parser existed for the thing L45's own wording asked for. Idle research-loop run (no eligible TODO/IN-PROGRESS queue item this firing — all of Q0-Q25 DONE/DEAD except Q19's per-event legs, still time-gated ahead of the Jul-14 CPI burst window) drew this from the lessons ledger's standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36. | this run; escalates L45; queue drained (Q0-Q25 all DONE/DEAD except Q19 per-event, time-gated) | **protocol, now importable** — `core/timeutil.py` gains `parse_crypto_hour_token_close_utc(token)`: parses a crypto-hourly ticker's bare date+hour middle segment (e.g. `'26JUL0621'`), localizes to `America/New_York` (DST-correct via `zoneinfo`, not a hardcoded EDT/EST offset), returns the tz-aware UTC close (or `None` on a grammar mismatch / out-of-range hour). 10 new tests in `tests/test_timeutil.py`, including the exact L45 empirical example and a winter (EST) case to prove it isn't summer-offset-hardcoded. `.claude/agents/edge-prober.md` house style updated to name it, alongside the L33/L35/L36 helpers. Does not retrofit Q25's already-verdicted script (that scan's numbers stand as-is, discovery-class, no registry flip) — this is for the next probe that needs a crypto-hourly close time. |
| L50 | 2026-07-14 | The fix for L43 (a two-family join permanently empty because the two collectors ran over disjoint windows) is to source the join's SECOND leg from a same-venue outcome/settlement endpoint retrievable ex-post over the PRIMARY tape's OWN window (within Kalshi's ~60-day purge, L11), rather than from a separate pre-computed anchor collected on a different, earlier schedule. S21 died because `sports_clv` fair anchors (kickoffs ≤07-03) were disjoint from `orderbook_depth` (≥07-07) → 0/81 joinable; S22/S23 (Q21 2026-07-14) instead pull realized settlement from Kalshi's free settled-markets endpoint (`fetch_kalshi_settled`) over the SAME depth-window games, so the join is non-empty by construction. At idea stage, this is the test that distinguishes a registrable candidate from an S21-class permanently-empty join: before registering any two-family-join probe, confirm the second leg is retrievable ex-post over the first leg's own window. | Q21 2026-07-14 idea-gen round (verifier-surfaced); generalizes L43/L9, cites L11 | **protocol** — a collector/probe-design discipline, not statically assertable (which two families a future probe joins and whether the second leg is ex-post retrievable is per-design, L6-class); addressed to `collection/` design and any milestone specing a two-family join. |
| L51 | 2026-07-14 | A "does signal X beat the mid" calibration precheck run on a DISAGREEMENT subset (both directional, X's call ≠ mid's call) produces two hit rates that are mechanically COMPLEMENTARY on a two-way market (`hit_X ≡ NOT hit_mid`, sum to exactly 1.0) — they are not two independent measurements. Q26/S22's disagreement-subset hit rate (imbalance 27.9% vs mid 72.1%) looked suspicious ("far below 50%, is the sign flipped and this a hidden contrarian edge?") until the verifier confirmed the arithmetic identity: X can only "beat the mid" on this subset if the mid is <50% accurate exactly where they disagree — a bar a liquid, calibrated market essentially never fails, and sign-flipping X on this subset just reproduces betting the mid (zero independent edge either direction). Any future "feature beats the market price" probe on a 2-way market should report the disagreement-subset result as "mid accuracy where they disagree = X%", not as two independent hit rates, to avoid the illusion of an extra data point. | `findings/2026-07-14-ofi-depth-imbalance-s22-verdict.md` (Q26/S22 verdict, verifier-surfaced) | **UNENFORCED** — a statistical-interpretation discipline for calibration-precheck-style probes, not statically assertable (whether a given probe's "disagreement subset" is a strict two-way partition is per-design); candidate: `.claude/agents/edge-prober.md` house-style note for any future calibration-precheck milestone. |
| L52 | 2026-07-14 | Kalshi sports settlement results are not always binary: Q26's live pull of `fetch_kalshi_settled` over 458 settled markets in 7 sports series returned 8 with `result:"scalar"`, not `result ∈ {"yes","no"}`. A binary-outcome probe must filter explicitly on the result field's actual value, never assume "settled" implies "yes/no" — an unfiltered join would silently inject non-binary rows into a yes/no hit-rate or P&L calculation. | `findings/2026-07-14-ofi-depth-imbalance-s22-verdict.md` (Q26/S22); `scripts/q26_ofi_depth_imbalance_probe.py` | **helper + test + non-gating advisory (2026-07-25 idle-run enforcement pass; supersedes the earlier UNENFORCED marker per the L152 own-row-update rule, L25/L47/L123/L152 precedent)** — three layers, all built this pass. **(1) READ-side helper** `core/settlement.py` — the shared classifier this row asked for: `BINARY_RESULTS`/`VALID_BINARY_RESULTS`, `normalize_result`, `is_binary_result`, `binary_outcome` (returns `None`, never a fabricated `0`, for a non-binary value), `require_binary_result`, `filter_binary_settlements(rows, *, result_key="result")` / `filter_binary_results_map(results)` returning a frozen `BinaryFilterReport` (`total`/`kept`/`dropped`/`dropped_by_result`/`kept_fraction`/`summary()`, keyed on the RAW un-normalized value for diagnostic fidelity). Classification is a strict ALLOW-list (`normalize_result(x) in VALID_BINARY_RESULTS`), so an unknown future result string is non-binary by default — this row's bug with a new label in it cannot recur; `KNOWN_NON_BINARY_RESULTS = {"scalar"}` is documentation only and is never consulted by the classifier. `"void"` was deliberately NOT invented (`grep -rn '"void"' kb/ collection/ scripts/ tape/` → 0 hits, 2026-07-25). **(2) tests** `tests/test_settlement_binary_filter.py` — **61 passed** (`python -m pytest -q tests/test_settlement_binary_filter.py`), including `test_regression_l52_unfiltered_hit_rate_differs_from_filtered` (an unfiltered yes/no hit-rate numerically differs from the filtered one) and the HARD acceptance test `test_acceptance_1_l52_real_settlement_ledger_tape_is_all_binary` over the real committed `tape/settlement_ledger/*.jsonl`. **(3) non-gating advisory** `scripts/invariants.py::_handrolled_binary_result_sites` / `handrolled_binary_result_warning`, wired into `main()`'s whole-tree (`--full`/default) branch ONLY — never `--pre-edit-hook`, never `--db` — inside an `except BaseException` wrapper, stderr-only, never appended to the gating `failures` list; `HANDROLLED_BINARY_RESULT_EXEMPT = ("core/settlement.py",)`. Detection: an `==`/`!=` against a `'yes'`/`'no'` string literal (either operand order) sharing ONE line with a settlement token (`result`/`results`/`settle*`/`outcome`); a file is exempted wholesale if it contains a `"scalar"` literal, an explicit `in`/`not in` over a 2-element yes/no collection, or any `core.settlement` reference; docstring lines excluded via an `ast` pass, comment lines and `tests/` skipped. Pinned by **64 tests** in `tests/test_settlement_result_advisory.py` (`python -m pytest -q tests/test_settlement_result_advisory.py`) — 8 constructed positives, 17 constructed negatives, 4 regression-pinned blind-spot MISSES, robustness cases incl. an unparseable file, 7 `test_acceptance_*` real-tree cases, 7 exit-code/non-gating/offline pins. **Live measured (real tree, 2026-07-25):** the advisory reports **6 sites** — `scripts/s14_ladder_fillsim.py:119`, `scripts/s19_wing_fade_fillsim.py:142`, `scripts/seed5_funding_prior_probe.py:180`, `scripts/weather_rehab_s5.py:508`/`:610`/`:616` (the first three all the MECE-ladder shape `winners = [k for k, v in results.items() if v == "yes"]`) — and `python scripts/invariants.py --full` still exits **0**. Ten sibling probes (`longshot_fade_probe.py`, `q24_*`, `q26_*`, `q27_*`, `q28_*`, `q29_*`, `q30_*`, `q37_*`, `s10_reachability_probe.py`, `sports_clv_s7.py`) are correctly suppressed as guarded; `execution/fill_models.py`'s `side == "yes"` (an ORDER SIDE, not a settlement result) is correctly NOT matched, pinned by `test_acceptance_order_side_comparisons_are_not_reported`. The 6 reported scripts were deliberately left UNEDITED — they are frozen, verdicted historical probes and changing them would alter recorded results; that is exactly why this layer is advisory, not gating. **Reconciliation with L106 (not overturned):** L106 (2026-07-19) recorded L52 as "honestly terminal (test-pinned at L92), SKIPPED, recorded so the next idle run does not re-attempt it." That verdict was scoped to a **GATING** scanner, and it remains CORRECT — a gating check would false-gate ~9 frozen legacy scripts and an allowlist of them all would be the L30/L19 dishonest-theater anti-pattern. Nothing built here contradicts it: what shipped is the NON-gating advisory form (the L109/L118/L126/L144/L150/L152/L156/L157/L160/L161 pattern), which needs no allowlist and cannot false-gate — the 6 legacy sites are reported honestly to stderr and the exit code is untouched. **Nuance on L92 (row not edited):** L92 claimed `collection/settlement_ledger.py` closed this row's shared-helper gap. It closed the **WRITE path** — its scalar filter is inline inside `run()` (~L249-257) and `migrate_caches()` (~L381-385) against its own PRIVATE local `BINARY_RESULTS = ("yes","no")` (~L86), so a downstream probe joining settlement data cannot import a classifier from it. `core/settlement.py` is the **READ path** helper that did not exist. Consequence worth stating plainly: `tape/settlement_ledger/` is 100% binary (**10,605/10,605**, 2 files, `{no: 8235, yes: 2370}` — command in `findings/2026-07-25-l52-settlement-binary-result-enforcement.md`) precisely BECAUSE the collector pre-filters scalars upstream; a reader who samples that tape and concludes "Kalshi settlement is always binary" is generalizing a COLLECTOR ARTIFACT to the API, where the observed rate is 8/458 (~1.7%). The acceptance test asserts `kept_fraction == 1.0` as "the pre-filter still holds", NOT "the world is clean". **Known blind spots (both stated in the warning text, both regression-pinned as misses):** (a) the settlement token on an EARLIER line than the comparison — `scripts/probe_ladder_coherence.py:140` (`if res == "yes":`) is a genuine unguarded settlement read this line-scoped rule does NOT report; (b) file-level guard granularity — one guard anywhere in a file exempts every other hand-rolled comparison in it. A 6-site count is PRECISION evidence, not RECALL (L155). |
| L53 | 2026-07-14 | A queue-aware fill-sim's fill-rate KILL gate (the S19 0.45% floor, Q27 gate G3) does NOT bind when the resting window is long: over a median ~37 hourly pre-close snapshots a cumulative-departures model clears almost any queue (S23 fill rate **95.83%**, 23/24), so a high fill rate is NOT evidence of a real edge — the strategy must still die or live on the EDGE (win-rate vs fill_price+fee), which is exactly where S23 died (DEAD-by-fee, not by fill or adequacy). Complements L39/L48's caution that a generous fill proxy orients but never substitutes for the edge test. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23, verifier-CONFIRMED) | **protocol** — a per-probe interpretation discipline (a passing fill-rate gate is necessary-but-insufficient; the edge test still binds), same class as L39/L48; not statically assertable (the fill window and departures model are per-design, L6-class). |
| L54 | 2026-07-14 | Favorite-longshot bias does NOT manifest as a fillable maker-BID edge on Kalshi two-sided sports: buying the favorite YES (normalized ask/bracket_sum ≥0.65) at the resting bid, favorites were marginally RICH — win-rate **0.6957** < breakeven fill_price+fee **0.7361** (mean fill $0.7261 `real_bid` + $0.01 flat maker fee) — the OPPOSITE of the textbook bias's prediction. With S13 (bid side vs devig, null), S21 (ask side, data-adequacy dead) and now S23 (bid side vs realized settlement, DEAD-by-fee), the entire favorite-longshot / S7-family maker lens is closed DEAD on Kalshi sports at real fills. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23, verifier-CONFIRMED) | **ledger-only** — an empirical venue finding (the bias is attenuated/absent as a fillable maker edge on this venue's two-sided sports books); not statically assertable and not a code bug. Cross-references L30's fee-floor family and `kb/quant-finance/favorite-longshot-bias.md`. |
| L55 | 2026-07-14 | An entry-time favorite filter (normalized ask/bracket_sum ≥0.65 chosen strictly from pre-close snapshots, with NO settlement lookahead) is small by construction — S23 yielded only **24 rested markets from 207 joinable games** — because a no-lookahead pre-close favorite population is inherently thin. This is the honest cost of an anti-lookahead design (contrast a lookahead filter that would inflate n by selecting on the outcome); a thin-but-honest population that still clears the ≥10-game floor is the correct trade. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23) | **protocol** — a probe-design discipline (accept a thin honest population over a fat lookahead-contaminated one, provided the ≥10-unit floor is met), same anti-leak family as L6's unit choice and the S5/S7 anti-lookahead precedents; not statically assertable. |
| L56 | 2026-07-14 | L37 (the Hard-Rule-#3 static scanner false-positives on prose/comments literally writing "yes_ask/bracket_sum" with a slash) RECURRED in `scripts/q27_favorite_underpricing_fillsim.py` (lines 6/78/171) and was again fixed by rewording the separator to "over"/"and". L37 is a prose-convention protocol, not a code bug; recording the recurrence so the next probe author writes "ask over bracket_sum" in comments/docstrings from the start. | `findings/2026-07-14-favorite-underpricing-s23-verdict.md` (Q27/S23); `scripts/q27_favorite_underpricing_fillsim.py` (reworded pre-commit) | **ledger-only** — records the L37 recurrence (supersedes nothing); the scanner is deliberately text-based (L18/L37) and re-tightening its regex to parse comments risks missing real Hard-Rule-#3 violations. The honest fix is the prose convention, addressed to future probe authors. |
| L57 | 2026-07-14 | A burst-resolution cross-venue lead-lag correlation can be DOMINATED BY A SINGLE SHOCK TICK: on the June-CPI burst tape, Kalshi<->Polymarket rho(polymarket-leads) on the two CPI-sensitive July fed buckets was 0.902/0.777, but removing the single 12:30:13Z release-instant capture collapsed it to noise (0.196/0.037) AND the residual sign is unstable (flips toward Kalshi on leave-one-out) — the "lead" was that one snapshot expressed as a Pearson rho over ~100 near-flat steps, not a persistent relationship. Any burst-window lead-lag figure MUST be reported WITH the release-tick-removed recompute before any directional claim. Two corollaries confirmed this run: (i) the WIDTH x DURATION cross cleanly discriminates a real shock dislocation from a stale-quote nominal basis and generalizes L31 from single-venue wings to CROSS-VENUE dislocations — the two largest gaps ($0.079/$0.06 net of both fees) were single-capture release-instant transients while every durable gap was small ($0.01-0.04), the S6/L31 signature; (ii) `tape/polymarket_macro_pairs/` is SIZE-BLIND (only best_ask/best_bid, no depth) and `captured_at` is a single pass-level timestamp shared by all records in a capture, so it can neither prove venue simultaneity nor claim fillability — every cross-venue dislocation over it is a price observation, never a fill. | `findings/2026-07-14-s17-burst-cpi-q19.md` (Q19 CPI-burst per-event, verifier CONFIRMED-numbers / REFUTED-both-tradeable-claims) | **protocol** — a per-probe burst-window methodology gate (report leave-one-out for any single-shock lead-lag; the width x duration discriminator; the size-blind / single-pass-timestamp fillability caveat), same class as L27/L28/L32/L48 (the shock-tick and population choices are per-design, not statically assertable); cross-references L8 (lagged confound), L13 (startup artifact), L31 (nominal != capturable, now cross-venue), L39/L48 (a proxy orients but never substitutes for a size/queue-aware fill test). Addressed to the next S17 burst leg (WC Jul 14/15/19, FOMC Jul 29) and `.claude/agents/edge-prober.md` house style. |
| L58 | 2026-07-14 | **A behavioral signal can be directionally real yet un-tradeable by an order of magnitude — distinguish "the effect exists" from "the effect is fillable."** S24's near-close mid genuinely mean-reverts after a jump (~0.7¢ net, De Bondt-Thaler confirmed as a `synthetic` mid-price observation) but a taker round-trip must pay 2× the 0.07 fee + 2× the half-spread (~6-7¢ on a ~3.7¢-overround two-sided book), turning the confirmed signal into a strictly-negative `real_ask`/`real_bid` CI (mean −$0.029, 95% CI [−0.052,−0.006], n=123 games/739 trades). Same family as L31/L57 (nominal ≠ capturable) and L39/L48 (a signal orients, the fill/cost test binds) — this is the round-trip-cost instance of that pattern. | `findings/2026-07-14-nearclose-fade-s24-verdict.md` (Q28/S24, verifier-CONFIRMED) | **ledger-only** — a per-probe interpretive discipline (report the raw signal AND the fully-costed fill test as two separate facts, never let the former stand in for the latter), not statically assertable; already the working pattern in `.claude/agents/edge-prober.md`'s house style — this is the S24 instance recorded for provenance. |
| L59 | 2026-07-14 | **Reversal *frequency* and reversal *magnitude* can disagree in sign of implication — a momentum/reversal precheck must report BOTH, not pick one.** S24's raw continuation frequency was 0.454 (a slight MAJORITY of jumps kept going, which alone would read as momentum), yet the sign-conditioned mean next-step pointed the opposite way (post-jump-up mean −$0.0061, post-jump-down mean +$0.0087) — a minority of large reversals carried the mean. Classifying on frequency alone would have mislabeled this DEAD-by-momentum and skipped the real (round-trip-cost) kill entirely. | `findings/2026-07-14-nearclose-fade-s24-verdict.md` (Q28/S24 Gate 2 direction precheck, verifier-CONFIRMED) | **UNENFORCED** — a per-probe verdict-methodology gate for any future momentum/reversal precheck (report reversal fraction AND sign-conditioned mean as two separate numbers before classifying); not statically assertable (population/classification choice is per-design, L6-class) and not yet encoded as an importable helper — candidate for a future `core/`-write pass alongside L27/L28/L32's dual-cut helpers. |
| L60 | 2026-07-14 | **The S6/L32 frozen-vs-movement maker dual-cut does not apply to a taker round-trip — it has a different boundary.** L32 exists because a maker's resting order can go UNFILLED when the book is frozen (no fill, no loss); but a TAKER round-trip fills both legs by crossing the spread regardless of whether the book was "frozen" at entry, so a frozen exit is a real (losing) fill, not a no-fill — bucketing taker trades by frozen-vs-movement would misapply L32's rationale. The correct place for a frozen guard on a taker jump-detection probe is at DETECTION (a zero-Δmid pair is never counted as a jump event in the first place), not at the fill-outcome layer. Records the boundary of where L32 applies (maker resting fills) vs where it doesn't (taker round-trips). | `findings/2026-07-14-nearclose-fade-s24-verdict.md` (Q28/S24 design note, verifier-CONFIRMED) | **ledger-only** — a scope clarification for L32, not a new mechanism; no code change (S24's probe already applies the frozen guard correctly at detection, per its own design section). Recorded so a future taker-round-trip probe doesn't reach for L32's dual-cut by reflex. |
| L61 | 2026-07-15 | **A scheduled-macro Kalshi ladder settles AT the print, so its resolving-month event has no post-release live quotes at all — the tradeable window ends *before* the outcome becomes public.** On the June-CPI burst tape (`tape/econ_prints/dt=2026-07-14.jsonl`) the resolving events `KXCPI-26JUN`/`KXCPICORE-26JUN`/`KXCPIYOY-26JUN` have `close_time` 12:25:00Z/12:29:00Z — *ahead* of the 12:30Z release — and vanish from the tape at 12:28:59Z; there are **zero** post-12:30Z records of the resolving ladder. The 100 "post-print" records in the 12:30–12:49Z window are all FORWARD months (`-26JUL`/`-26AUG`/…), a *different object* whose outcome the June print does not determine, and they carry ~0.88 median yes-spreads. Any "post-print staleness pickoff" (buy the now-certain bucket) or "print-overshoot fade" thesis on econ tape is therefore structurally DOA — Kalshi closes the market early precisely to deny the stale-order pickoff. Verify the resolving event's `close_time` against the release instant before proposing any post-print econ trade. | Q21 idea-gen round 2026-07-15, verifier KILL of S25 & S27 (`findings/2026-07-15-q21-idea-gen-round.md`) | **ledger-only** — a market-microstructure fact (close-before-release) + a pre-probe design check (compare resolving `close_time` to the release timestamp); per-design, not statically assertable. Kills the S10-family (`findings/2026-07-11-crypto-reachability-s10-firstcut.md`) for econ prints too: even the last pre-close near-certain-NO buckets show the 1¢-YES→$1.00-NO mirror (no fillable fade). |
| L62 | 2026-07-15 | **A cross-venue ask-to-ask "gap" is not an edge when one venue's own bid-ask spread exceeds the gap.** Scoring the S26 entry condition `\|kalshi_yes_ask − poly_best_ask\| > overround_share + fee` on `tape/polymarket_macro_pairs/dt=2026-07-14.jsonl` cleared for 19.8% of records — but on these thin far-dated KXFEDDECISION buckets the Kalshi yes bid-ask spread is median 9¢ (mean 11¢, up to 30¢) and the Polymarket anchor sits **INSIDE** Kalshi's own bid-ask in **62.6%** of the entry-met records. So the "divergence" is Kalshi's half-spread against Poly's tight top-of-book ask — Kalshi's true mid already agrees with Poly, and there is nothing to converge (the size-blind top-of-book illusion, one layer worse than L39). Any Kalshi↔Polymarket divergence claim must first check whether the anchor falls inside the Kalshi bid-ask, and must name a realizable exit (round-trip the Kalshi spread vs hold-to-settlement) before calling a gap an edge. | Q21 idea-gen round 2026-07-15, verifier KILL of S26 (`findings/2026-07-15-q21-idea-gen-round.md`) | **ledger-only** — generalizes L31 (stale-nominal quote) / L39 (size-blind proxy) to the cross-venue case; a per-probe methodology gate (compute the anchor-inside-spread fraction first), same class as L6's bootstrap-unit choice. |
| L63 | 2026-07-15 | **A cross-venue laggard trade is market-neutral only if BOTH legs are held; a single-venue "take the laggard toward the anchor" is a directional bet, not an arbitrage.** S26 tried to escape S17's cross-venue rail-risk caveat by executing only the Kalshi leg (Polymarket as a free anchor, never held) — but dropping the hedge leg removes the *hedge*, not the *risk*: "convergence-hold" on a single macro bucket to its FOMC/CPI resolution is unbounded directional event risk (the S2/S16 family), and it re-inherits the exact "Kalshi is the laggard" premise that S17-burst already REFUTED as sign-unstable (residual lead flips toward Kalshi on leave-one-out). Escaping S9's data-adequacy death and S17's rail caveat only re-lands the idea on the directional-macro-bet objection. Before proposing any "single-venue convergence" trade, state whether "convergence" is defined anywhere but at settlement — if not, it is a directional position. | Q21 idea-gen round 2026-07-15, verifier KILL of S26 (`findings/2026-07-15-q21-idea-gen-round.md`) | **ledger-only** — a proposal-stage discipline (name the non-settlement convergence event or admit it is directional); cross-references S2/S16 (directional macro), S9/S17 (refuted laggard premise). |
| L64 | 2026-07-15 | **A "post-close" definition anchored on a ticker's HHMM token, not the settlement `close_time` (broker_truth), silently mislabels the majority of a population as post-close when it is actually pre-close.** Q25's `post_close` sports-depth bucket (n=2,478, ticker-HHMM-as-UTC) turned out to be 2,860/2,864 (99.86%) still pre-close under the real settlement `close_time` — median understatement +7.07h, max +24.33h, concretizing L46's ~13h league-local tz uncertainty into a measured number. The genuinely-post-close population was only 4 captures / 2 games. Any liquidity or fill-plausibility claim keyed off a ticker-embedded time token must be re-verified against `broker_truth` settlement timestamps before being trusted as a population definition. | Q29/S28 probe, verifier-CONFIRMED (`findings/2026-07-15-q29-settlement-lag-s28-verdict.md`) | **UNENFORCED** — retroactively reframes Q25's headline `post_close` liquidity numbers as describing the in-game live book, not a post-close resting-quote window; candidate for a `core/`-level close-time helper so future probes don't reach for the ticker token by reflex. |
| L65 | 2026-07-15 | **Kalshi empties and settles a sports book AT close — there is no post-close resting-quote window to pick off.** Across the entire committed `tape/orderbook_depth/` history the maximum observed gap between a capture and its market's real settlement close is 0.024h (~1.4 min), and the 4 genuinely-post-close captures found (L64) all have a fully empty book (both BBO sides `None`, empty ladders) — confirmed by hand against the raw tape (e.g. `KXNPBGAME-26JUL120500YAKHAN-HAN`: full two-sided ladder at 11:24:58Z, fully empty at 11:55:18Z, ~30min after close). The sports-analogue of L61's "econ ladder closes before the print": Kalshi structurally denies the stale-quote pickoff by pulling the book at settlement, not by keeping asks near $1. | Q29/S28 probe, verifier-CONFIRMED | **UNENFORCED** — a market-structure fact, not statically assertable; kills any future "settlement-lag"/"post-close staleness" proposal on sports tape at the idea stage (S28 family). |
| L66 | 2026-07-15 | **"Buy the ex-post known winner" constructions are near-L41-degenerate by design — record the boundary precisely, don't overstate it.** A trade defined as "lift the winner-side ask after the outcome is already public" has `edge = 1 − ask − fee`, so a losing cluster requires `ask` to already be priced within the fee of $1 (or a settlement correction/void after entry) — both are real but rare, so while not perfectly degenerate, in practice this trade shape structurally starves the block-bootstrap of the opposing-sign clusters L41 requires. State this precisely as "a genuine but thin losing tail" rather than "provably degenerate," since an adversarial verifier can construct a synthetic counterexample (as done here) even when the real tape never realizes it. | Q29/S28 probe, verifier-CONFIRMED (minor overstatement caught, did not change the verdict) | **UNENFORCED** — a precision note for future kill-condition writeups in the "trade the known outcome" family (S28, and any S29-adjacent settlement-timing idea); no code change, the underlying DEAD verdict is unaffected. |
| L67 | 2026-07-15 | **Median total-ladder depth on an illiquid two-sided book is not evidence of a capturable spread — decompose the resting size by price band before believing a wide two-sided spread is tradeable.** A maker-spread candidate (S30, foreign-sports wide-spread capture) rested on "the wide KBO spread is backed by thousands of two-sided contracts (4,601 yes / 10,556 no)." That figure is the *total ladder summed across all price levels*; the top-of-book at the 27¢ spread was **10 yes / 26 no** contracts, and **98.83%** of the resting yes-ladder size sat at price ≤ 0.10 (deep-OTM lottery bids, e.g. 4,000 contracts at 6¢). Capture happens at the top of book, so the fillable number is the 10-contract one, not the 4,600 total — the wide spread is an **L31 wing with a two-sided tail**, not a competition gap. Always split ladder depth into "size within ~5¢ of top" vs "lottery tail" before quoting a two-sided-liquidity claim. Generalizes L31/L39 to the two-sided-depth-illusion case; sibling of L62's anchor-inside-spread check. | Q21 idea-gen S30 verifier-KILL (`findings/2026-07-15-q21-ideagen-edge-hunter.md`) | **test** — `core/depth.py`'s `capturable_depth`/`total_ladder_depth`/`lottery_tail_fraction` (size within N cents of BBO vs the whole-ladder total, plus the tail-fraction split) give a fill-sim the right number by default; pinned in `tests/test_depth.py` (11 cases incl. an L67-shaped KBO regression). Not yet a blocking static invariant — no probe currently violates the pattern to retrofit, and a lexical scanner for "raw ladder sum" risks false positives (e.g. `orderbook_depth`'s own `depth` field is a legitimate whole-ladder count); revisit if a future probe reaches for a hand-rolled ladder sum where `capturable_depth` belongs. |
| L68 | 2026-07-15 | **A maker-spread-capture idea evaluated over `tape/orderbook_depth/` alone is toxicity-untestable by construction — kill it at the idea stage rather than register it as "untestable."** `orderbook_depth` carries resting-depth snapshots only (`best_*_bid/ask`, `yes_bids`, `no_bids`) — **no trade / volume / last-price fields** — and the only sports executed-volume tape anywhere is `tape/sports_history_s7/worldcup2026.jsonl` (WC-only, L44). A maker's survival hinges on whether fills are adversely selected (does resting size get lifted disproportionately on the side that then loses?); with no trade prints that correlation cannot be measured, so the L41 adverse-modeled block-bootstrap CI is unconstructible. A wide spread on a thin book is equally consistent with "absent competition" (good) and "adverse-selection compensation" (fatal), and the depth tape cannot adjudicate. Such a candidate has no path to the registration bar and is a clean idea-stage KILL — do not register it as data-collecting/untestable (that would burn a research-loop probe). | Q21 idea-gen S30 verifier-KILL (`findings/2026-07-15-q21-ideagen-edge-hunter.md`) | **UNENFORCED** — a maker-spread candidate must name its trade/toxicity data leg at proposal time; absent one over `orderbook_depth`, reject before registration. |
| L69 | 2026-07-15 | **A queue-aware maker fill-sim that rests at the EARLIEST pre-close snapshot systematically enters on thin, often one-sided early books — the fillable-entry restriction (two-sided book / near-close) must be the PRIMARY population, never a robustness footnote applied after a positive headline.** Q30's spec population (earliest-entry, median 65.6h pre-close, p90 entry spread 86¢) cleared every binding gate (+9.03¢, CI [+0.021,+0.163]) on nickel bids against 87-94¢ asks days before kickoff — nominal lottery-ticket placeholders the generous fill-sim (L48: a cancel ahead counts as advancing us) still marks FILLED. Both honest fillable-entry cuts (two-sided book ≤10¢, near-close ttc≤24h) failed to reproduce the edge; near-close went negative. This is the pt1 "synthetic price treated as fillable" mistake one abstraction up — a nominal ONE-SIDED quote, not a synthetic midpoint. Sibling of L67's two-sided-depth-illusion (that one attacked a static depth CLAIM; this one attacks a fill-SIM's default entry rule). | Q30/S29 probe, verifier-REFUTED the ALIVE framing (numbers CONFIRMED, interpretation overturned) | **UNENFORCED** — candidate: any earliest-pre-close queue-aware fill-sim template (Q27/Q30-style) should compute the fillable-entry-restricted population as its PRIMARY verdict by default, with the unrestricted population as a labeled diagnostic only. |
| L70 | 2026-07-15 | **Draw-aversion IS directionally present on Kalshi 3-way soccer, but is not a fillable maker edge — record the outcome-type bias as real-but-unfillable, distinct from L54's absent/reversed favorite-longshot closure.** Empirical record (157 games, 19 pooled `-TIE` series): draws settled 28.0% of the time against resting draw-YES bids priced (nominally) at ~18¢ — a genuine ~9¢ gap in the direction the literature predicts. But under the only defensible fillable-entry populations (two-sided book, near-close) the edge collapses to a zero-straddling CI or goes negative (L69). Distinguishes "bias absent/reversed" (S23/L54) from "bias present but only visible in unfillable prices" (S29) — both are DEAD verdicts but for different reasons, and a future literature-driven candidate should check which failure mode it's walking into. | Q30/S29 probe verdict, verifier-CONFIRMED | **ledger-only** — an empirical result for the registry, not a generalizable process fix. |
| L71 | 2026-07-15 | **The gate-4 "power floor" for a hold-to-settlement Bernoulli P&L is `sqrt(p(1-p)/n)` — a one-sigma standard error, NOT a 1.96-scaled 95% half-width.** Q30's milestone spec stated "~$0.09 at n≈24, ~$0.044 at n≈100," which matches `sqrt(0.1875/n)` (p=0.25) exactly, not `1.96*sqrt(p(1-p)/n)`. Pinning this so the next settlement-Bernoulli probe (any "buy the outcome, held to settlement" trade shape) doesn't reach for the wrong CI scaling by reflex when computing its own power-floor commentary. | Q30/S29 probe (`power_floor_halfwidth` in `scripts/q30_draw_aversion_maker_probe.py`), verifier-CONFIRMED | **test** — `tests/test_q30_draw_aversion_maker_probe.py` pins `power_floor_halfwidth(24)≈0.0884`, `power_floor_halfwidth(100)≈0.0433` at p=0.25. |
| L72 | 2026-07-15 | Supersedes L59's enforcement column (the lesson content is unchanged: reversal *frequency* and reversal *magnitude* can disagree in sign of implication, so a momentum/reversal precheck must report both, never classify on frequency alone). Idle-run policy order (a): built `core/reversal.py` (`reverses`, `direction_precheck` — mirrors `scripts/q28_s24_nearclose_fade_probe.py`'s `gate2_direction` exactly: `is_momentum` requires BOTH `reversal_fraction <= 0.5` AND both sign-conditioned means agreeing with the jump direction) so a future momentum/reversal precheck imports the two-number check by default instead of re-deriving a frequency-only classifier per script, the `core/`-write pass L59 flagged as future work. | this run; `core/reversal.py`; supersedes L59's enforcement | **test** — `tests/test_reversal.py` (11 cases, incl. an L59-shaped regression: reversal_fraction=0.25 — comfortably reads as momentum by frequency alone — but a minority of large reversals flips both sign-conditioned means, so `is_momentum` correctly comes out False). No blocking static invariant: which probes need this check is a per-design population choice (L6-class), not a lexical pattern to ban. |
| L73 | 2026-07-15 | Supersedes L39's enforcement column (the lesson content is unchanged: a bracket-ladder P&L that nets a small edge against a large loss leg is vulnerable to a queue-blind fill proxy crediting the income leg too easily — decompose the edge as a fraction of the thinnest-volume income legs before calling it fillable). Idle-run policy order (a): built `core/income_legs.py` (`income_leg_thin_fraction`, `income_leg_edge_at_gate` — the S14-shaped "78% of the edge came from sub-100-contract legs" decomposition, and its complementary volume-gated haircut sum, given per-leg `(income, volume)` pairs) so a future small-net-of-two-legs probe imports the volume-gate decomposition by default instead of re-deriving it per script, the `core/`-write pass L39 flagged as future work. Does not retrofit S14's already-verdicted first-cut probe (PROXY-POSITIVE, not proven — Q13's own remaining binding gate is the queue-aware L2 fill-sim, unchanged by this) — this is for the next probe that needs the decomposition. | this run; `core/income_legs.py`; supersedes L39's enforcement | **test** — `tests/test_income_legs.py` (12 cases, incl. an S14-shaped regression: two legs at $0.072/47-contracts and $0.021/1047-contracts reproduce the reported 78% thin-leg fraction at a 100-contract threshold; boundary, empty-legs, and zero/negative-total-income cases all resolve to `None`/0.0 rather than raising or dividing by zero). No blocking static invariant: which probes need this decomposition is a per-design population choice (L6-class), not a lexical pattern to ban. |
| L74 | 2026-07-15 | A daily-cadence tape family gated to a single `ts.hour == N` window (no retry, no backfill) is exposed to a full-day blackout from any one bad hour, and a multi-day incident costs it MORE days than an always-hourly family in the same window. Concretely: the 2026-07-08 main-branch reset (`kb/00-LOG.md` reconciliation entry) cost `sports_pairs`/`crypto_hourly`/`polymarket_macro_pairs` (hourly cadence, 23 other chances/day) exactly one missing day (2026-07-09), but cost `econ_prints`/`polymarket_cpi_pairs`/`anomalies` (all gated to the SAME single `ts.hour == 9` slot in `collection/hourly_pass.py`) two consecutive missing days (2026-07-09 AND 2026-07-10) — a 48h CPI/GDP/payrolls/Fed-nowcast/anomaly-sweep blackout, confirmed by direct `.jsonl`-file + directory listing (not just a glob, to rule out an L25-style masked directory). Data quality when the pass DOES run is otherwise clean (4,774/4,774 nested `completeness_ok=true`, 0 settlement-value drift across 8 repeated-capture event tickers). Idle-run policy order (c) deep-dive; no fix built — `scripts/invariants.py` has no missing-calendar-day check for a daily family (only a shape check, per L25), and none was requested; flagging the exposure ahead of the Jul-29 FOMC window, whose burst leg is separate infra and unaffected. | `findings/2026-07-15-econ-daily-cadence-gap-dataquality.md` (this run) | **non-gating advisory + test (BUILT; stale `UNENFORCED` marker corrected 2026-07-24 by the kb-distiller, lesson text unchanged — precedent: the same correction made for L25 on 2026-07-21)** — the candidate named here WAS built: `scripts/invariants.py::_daily_family_gap_issues()` / `daily_family_gap_warning()` over `DAILY_CADENCE_FAMILIES`, the `--full` advisory that lists every missing calendar day between a daily-cadence family's earliest and latest committed `dt=<date>.jsonl` (it currently reports 12 real missing days). Pinned by `tests/test_invariants.py::test_daily_family_gap_issues_finds_gap`, `::test_daily_family_gap_issues_clean_run_is_empty`, `::test_daily_family_gap_issues_treats_dir_shaped_dt_entry_as_missing`, `::test_daily_family_gap_warning_never_gates_exit_code`, and the real-tape HARD acceptance `::test_acceptance_l126_weather_actuals_real_gap_detected`. The OTHER candidate in this row (widening the single-UTC-hour capture window itself) remains UNBUILT — it changes live collector firing gates; the registration half of that risk is separately guarded by L144's meta-guard. |
| L75 | 2026-07-16 | Supersedes L74's enforcement column (the lesson content is unchanged: a daily-cadence family gated to a single UTC hour with no retry/backfill is exposed to a full-day blackout from one bad hour). Idle-run policy order (a): built `scripts/invariants.py`'s `DAILY_CADENCE_FAMILIES`/`_daily_family_gap_issues()`/`daily_family_gap_warning()` (same non-gating stderr-advisory pattern as L25/L29's `tape_dir_shape_warning` — offline-safe, per-family exception-swallowed, never flips the exit code), wired into `--full` alongside the L17/L25 advisories. Live-validated against the real committed tree: correctly flags all 6 days L74 first documented (`anomalies`/`econ_prints`/`polymarket_cpi_pairs` × `dt=2026-07-09`/`dt=2026-07-10`), 0 false positives on the always-hourly families. Backfilling the 2 lost days themselves is out of scope (data no longer exists to capture) — this is detection only, so the NEXT incident is caught same-day instead of found retroactively during an idle-run deep-dive. | this run; `scripts/invariants.py`; `tests/test_invariants.py::test_daily_family_gap_warning_none_when_empty`, `::test_daily_family_gap_warning_message_content`, `::test_daily_family_gap_issues_finds_gap`, `::test_daily_family_gap_issues_clean_run_is_empty`, `::test_daily_family_gap_issues_single_file_is_empty`, `::test_daily_family_gap_issues_missing_family_dir_is_empty`, `::test_daily_family_gap_issues_missing_tape_root_is_empty`, `::test_daily_family_gap_issues_treats_dir_shaped_dt_entry_as_missing`, `::test_daily_family_gap_warning_never_gates_exit_code`; supersedes L74's enforcement | **invariant (non-gating advisory)** — fires automatically on every `invariants.py --full` run; supersedes L74's `UNENFORCED` |
| L76 | 2026-07-15 | A run's "executable duration" gate keyed on **snapshot count** (`MIN_SNAPS>=2`) is not a duration gate — a sub-second repricing burst writes >=2 consecutive snapshots in ~0s. Every one of W-D's 17 executable-by-snapshot-count ladder runs persisted **<=1.0s wall-clock**. A real duration gate must use **wall-clock seconds** (the probe already carries `dt_s`/`total_seconds` per snapshot), never the snapshot count — otherwise a burst masquerades as a durable, fillable opportunity. | `findings/2026-07-15-weather-revival-dossier.md` (W-D, S33); `reports/ladder_coherence_summary.json`; `scripts/probe_ladder_coherence.py` | **UNENFORCED** — the probe surfaces BOTH (flags executable on `MIN_SNAPS` but reports per-run wall-clock seconds so the <=1s truth is visible), and `tests/test_probe_ladder_coherence.py::test_runs_single_deep_snapshot_fails_duration_gate`/`::test_runs_collapse_consecutive_and_flag_executable` pin the snapshot-count run-collapse logic — but nothing yet REJECTS a run on wall-clock-seconds below a floor. A per-probe methodology gate (the population/gate choice is per-design, L6-class), not statically assertable; candidate: a shared "min wall-clock duration" run-collapse helper for the next structural-arb probe. |
| L77 | 2026-07-15 | **Forward-filling each market's last-seen snapshot into a JOINT multi-leg ladder state manufactures phantom cross-sectional arbs.** During sub-second repricing bursts the losing legs collapse to the 1c floor while the winner leg's ask is stale one beat, so the observed `sum(ask) < $1` was never SIMULTANEOUSLY true — verified on `KXHIGHPHIL-26APR22-B66.5`, where the joint state used a forward-filled 0.38 while that leg's own contemporaneous ticker read 0.52. This is the **intra-ladder instance of L8** (a cadence-lagged reference price is a confound, not a basis). Any joint-state cross-market incoherence must survive an INDEPENDENT re-capture of the binding leg before it counts as real. | `findings/2026-07-15-weather-revival-dossier.md` (W-D, S33); `scripts/probe_ladder_coherence.py`; generalizes L8 | **protocol** — a per-probe methodology gate generalizing L8 to the forward-fill/joint-state case; not statically assertable (which leg is "binding" and the re-capture design are per-design, L6-class). Addressed to the next multi-leg joint-state probe and `.claude/agents/edge-prober.md` house style. |
| L78 | 2026-07-15 | **Bootstrapping a population PRE-FILTERED to net>0 (every unit already profitable) mechanically fails `bootstrap_verdict_admissible` — 0 opposing-sign units (L41).** W-D's `class_a_buy_yes` and `class_b_buy_no` both returned `admissible=false` / `no_opposing_unit` for exactly this reason (the runs were selected FOR being net-positive). For a **structural arb** the binding test is not a P&L bootstrap at all — it is the **depth x duration joint distribution** (are there >=N contracts resting for >=T wall-clock seconds?). W-D died there: **0 opportunities >=10 contracts AND >=60s**, and 0 at >=5/>=60s or >=20/>=10s. | `findings/2026-07-15-weather-revival-dossier.md` (W-D, S33); `reports/ladder_coherence_summary.json`; extends L41 | **protocol (helper-backed)** — the admissibility half is enforced by `core.bootstrap.bootstrap_verdict_admissible` (L41, pinned in `tests/test_bootstrap.py`) and the probe's reports correctly record `admissible=false`; the "structural arb ⇒ score the depth x duration structure, not a P&L bootstrap" half is a per-probe methodology gate, not statically assertable. |
| L79 | 2026-07-15 | The recovered replica `arb-bot-v2/data/tape_replica/orderbook_archive_recovered.db`'s Kalshi `ticker` snapshot rows (~5.85M) carry the **full BBO + sizes in DOLLAR units inside `raw_json`** — a top-of-book probe can read price/size directly, and delta/orderbook reconstruction is unnecessary (needed only for full L2 depth). A reusable offline top-of-book source for the weather-revival probes, sibling to L44's `worldcup2026.jsonl` executed-volume file. | `findings/2026-07-15-weather-revival-dossier.md` (W-D data provenance, S33) | **ledger-only** — a tape-source availability note (which recovered-replica field carries top-of-book), not a code bug and not statically assertable; recorded so Q36/Q37 reach for `raw_json` BBO instead of reconstructing depth. |
| L80 | 2026-07-15 | **Weather daily-high ladders empty-collapse their losing legs to the 1c YES floor well before close** as the day's outcome locks in — the weather analogue of L65 (Kalshi empties/settles a sports book AT close) and L26 (the 1c-YES->$1.00-NO mirror leaves no fillable fade). This is the same mechanism the W-D probe surfaced (losing legs sit at the fee floor while only the winner leg carries a live ask), and it forecloses a "buy the collapsing loser cheap / fade the stale winner" TAKER angle on weather ladders before it is proposed. | `findings/2026-07-15-weather-revival-dossier.md` (W-D, S33) | **ledger-only** — a venue microstructure fact (the fee-floor collapse is arithmetic), cross-references L26/L61/L65; the taker-vs-maker corollary (a real weather tail edge, if any, lives on the maker side) is future-probe discipline for Q37, not statically assertable. |
| L81 | 2026-07-15 | **Wiring a new OWN-DISCOVERY sub-pass into `collection/hourly_pass.py` silently makes every existing test that reaches the real `run()`/`main()` hit the network** — the new pass enumerates a live catalog before any injected stub covers it, so a previously-offline suite goes live. Each such call site needs a stub injected when a new self-discovering collector joins a shared multi-pass entrypoint. | `findings/2026-07-15-weather-revival-dossier.md` (collection/weather_books.py wiring, 2026-07-15); `tests/test_hourly_pass.py` | **protocol** — a test-hygiene discipline surfaced by the weather_books wiring (the `hourly_pass` tests now stub the weather sub-pass); not statically assertable (a scanner cannot know which call site reaches a live catalog). Addressed to `.claude/agents/collector-engineer.md` house style and any future `hourly_pass` sub-pass. |
| L82 | 2026-07-15 | **Kalshi's weather ticker taxonomy has drifted to >=3 coexisting prefixes per city** (`KXHIGH*` / `KXHIGHT*` / legacy) plus the new hourly `KXTEMP*` family — a frozen `config/cities.yaml` seed list is a FLOOR, never the truth. Day-1 `weather_books` discovery found 67 daily + 8 hourly series, **27 daily NOT in the config seed**. A weather collector must category-sweep every pass and log the **sweep-only delta** (discovered-but-unseeded), never trust the seed as complete. | `findings/2026-07-15-weather-revival-dossier.md`; `collection/weather_books.py` (2026-07-15) | **test** — `tests/test_weather_books.py::test_discovery_union_and_sweep_only` + `::test_discovery_hourly_seed_and_classification` pin the seed-union-sweep and the sweep-only delta; the taxonomy-drift fact itself is a venue observation (ledger). |
| L83 | 2026-07-15 | **Kalshi's live orderbook payload is `orderbook_fp` with prices as STRING DOLLARS (`"0.6100"`); the integer-cents `orderbook` is the LEGACY shape.** A weather/depth collector must handle BOTH — a venue rollback to the old shape against a fp-only parser would silently zero-capture every book (empty ladders read as no-depth, not as an error). | `findings/2026-07-15-weather-revival-dossier.md`; `collection/weather_books.py` (2026-07-15) | **test** — `tests/test_weather_books.py::test_legacy_integer_cents_orderbook_shape` + `::test_run_record_shape_and_derived_asks` pin both shapes and the derived-ask identity (derived ask = 1 - opposite_bid). |
| L84 | 2026-07-15 | **Per-(entity, day) dedup implemented by READING the day's already-written tape** (skip a series-day whose meta already exists) is concurrency-safe across concurrent writers — two collectors racing the same series-day both see the persisted meta and neither double-writes (proven live on the day-1 `weather_books` pass: 530/530 books, meta once per series-day). A reusable idempotency pattern for any append-only per-day tape family, sibling to L21 (read tickers back from freshly-written tape instead of threading a new return field). | `findings/2026-07-15-weather-revival-dossier.md`; `collection/weather_books.py` (2026-07-15) | **test** — `tests/test_weather_books.py::test_meta_written_once_per_series_day` pins the write-once-per-series-day behavior; the cross-writer concurrency-safety is an operational property (ledger). |
| L85 | 2026-07-16 | **The proxy→queue-aware purge is now COMPLETE — S13, S19, S21, S23, and now S14 (the repo's last non-DEAD candidate) all died once the candle-through fill proxy (L39) was replaced with a price-time-priority queue model.** S14's candle-proxy +\$0.0925/contract headline inverted to a queue-aware **−\$0.0453 CI [−0.0809,−0.0121]** — the "edge" was an L39 income-leg artifact (the proxy credited premium on legs that merely printed while the near-money winner still filled 93% and cost the full \$1). Every candle-proxy "edge" in this repo to date has been an L39 income-leg artifact. Treat a candle-through fill proxy as **presumptively VERDICT-INVALIDATING** (not merely flag-worthy) for any P&L that is a small net of two large legs (premium collected vs winner payout); such a result is not evidence until re-run through a queue model. | `scripts/s14_queue_fillsim.py`; `findings/2026-07-16-q34-s14-queue-fillsim-verdict.md` (Q34/S14); supersedes nothing, generalizes L39/L53 | **protocol** — a probe-design/verdict discipline addressed to future edge-probers and verifiers (a candle-through proxy is not verdict-grade for two-large-leg P&L); genuinely not statically assertable — a scanner cannot tell whether a probe's fill rule is candle-through vs queue-aware nor whether the net is small-vs-two-large-legs, and re-tightening the L39 text-scanner to guess would false-positive on legitimate offline candle-volume reads (which S14/S19 use correctly). Honest terminal state; enforced by the two-agent verdict rule, not a static check. |
| L86 | 2026-07-16 | **The winner/catastrophic-leg measurability asymmetry.** When a net-P&L contains a leg that is a large FIXED LOSS (here the winner's \$1 payout), the S19 discipline "unmeasurable → no-fill" is NOT symmetric: applied to the LOSS leg it fabricates a free win and biases the result POSITIVE. Correct handling = **DROP the whole unit on that leg's measurability** (measurability is exogenous to settlement), never count it with the loss zeroed; **AND verify the drop moves the result the conservative direction** — here, counting the 290 dropped event-hours as payout=0 moved −0.0453 → −0.0152 (still negative), confirming the drop is honest, not a thumb on the scale. | `scripts/s14_queue_fillsim.py`; verifier counterfactual; `findings/2026-07-16-q34-s14-queue-fillsim-verdict.md` (Q34/S14) | **UNENFORCED** — a methodology-judgment discipline for probes whose P&L carries a fixed catastrophic leg (drop-on-measurability + conservative-direction stress-check). Not statically assertable (a scanner cannot identify which leg is the catastrophic loss nor whether a drop biases positive), and not yet pinned by a shared test harness; recorded as the kb-distiller's standing queue item. The concrete verifier stress-check (payout=0 counterfactual) is the honest enforcement pattern to reuse, but it lives in each probe's verifier pass, not a reusable assert. |
| L87 | 2026-07-16 | **Kalshi crypto-hourly near-money brackets are overround-RICH — the net-buying-pressure / variance-risk-premium thesis is REVERSED on this venue.** Last pre-close capture (`real_ask`) joined to `previous_settlement` broker_truth realizes BELOW its ask *before* fees: BTC band win-rate 0.140 vs mean ask 0.167; ETH 0.101 vs 0.164; BTC near-money [0.03,0.97] 0.258 vs 0.277; ETH 0.421 vs 0.447. Near-money sum-of-yes-ask runs a median 1.120 for BTC (+12¢, worse than S1's +9.84¢ overround). So the literature thesis (retail lottery demand overprices the tails → fillable near-money left cheap; Bollen & Whaley 2004 / Coval & Shumway 2001, `kb/quant-finance/net-buying-pressure-implied-distribution.md`) does NOT hold here: the overround is spread ACROSS the near-money region, not concentrated in the wings, and the directional skew that does exist is one-sided (upside rich, downside fair) and lives inside the spread (maker-only, cross-refs L26/L31). **Any future held-to-settlement single-near-money-leg crypto candidate is presumptively S1-on-crypto and must clear a win-rate-above-ask-plus-fee calibration cut before registration.** | 2026-07-16 Q21 round, S35/S36 verifier kills; `findings/2026-07-16-q21-idea-gen-round.md` | **ledger-only** — an empirical venue fact plus a per-design pre-probe calibration check; not statically assertable (the win-rate-vs-ask calibration is a per-probe numeric gate, same class as L28, and the venue-shape fact is an observation like L12/L26/L31). Cross-refs L1/L12/L54/L58. |
| L88 | 2026-07-16 | **A Kalshi `crypto_hourly` "between" range-ladder has NO fillable ATM up/down directional instrument.** It lists ~186 identical fixed-width ($100) brackets plus two tail caps (`less`/`greater`); a single near-ATM bracket is a pin/volatility bet — "closes in [floor,cap)", non-monotonic in direction — that does NOT express P(up). To bet direction you must buy the STRIP of all brackets above spot, which inherits the ladder's ~189% bracket_sum overround (2.89) plus a per-leg 0.07 taker fee floor plus each leg's own half-spread. **Any directional-crypto proposal must name at proposal time whether it trades the multi-leg strip (and then price the overround + per-leg fee floor) or a single bracket (which is a pin bet, not a direction bet); absent a fillable directional instrument, kill at idea.** Sibling to L68 (name your trade/data leg at proposal time). | 2026-07-16 Q21 round, S37 verifier kill; `findings/2026-07-16-q21-idea-gen-round.md` | **ledger-only** — a structural product fact (the range-ladder simply lists no directional binary; the strip-overround is arithmetic). Not statically assertable — a scanner cannot know a product's instrument set; enforced as proposal-time discipline, cross-refs S10 graveyard / L26. |
| L89 | 2026-07-17 | A **clamp-vs-rounding discriminator (dead-band vs quantization) must test the GAP relative to the data's OWN granularity — never against an absolute threshold.** The two hypotheses for a zero-heavy series are: (a) a **dead-band clamp** — rates inside a ±band forced to *exactly* 0 while the surviving nonzeros stay **continuous/unquantized**; vs (b) a **symmetric-rounding/quantization artifact** — the "zeros" are a lattice bucket straddling zero and the nonzeros sit on that *same* tick grid. The decisive test is therefore (i) are the nonzeros continuous or lattice-quantized, and (ii) is there a **hard gap in the open interval just above zero**. Q42: the Kalshi perp finalized funding backfill (1,447 `broker_truth` prints, 2026-06-03→07-16, 13 contracts) has a pooled exact-zero fraction 0.762 with a hard gap in `(0, 1e-4)` — **1,102** exact zeros, **0** nonzeros in `(0, 1e-4)`, **186** in `[1e-4, 1.5e-4)` — and the smallest nonzero `|rate|` *varies* per contract (BTC 1.0004e-4 … SUI 1.0560e-4, a floor near ~1e-4, not one shared value). Because the nonzeros are continuous rather than on a 1e-4 lattice, the zeros are a **genuine ±1bp clamp**, not a rounding bucket — confirmed on 12/13 contracts (KXLINKPERP undecidable, 1 nonzero print). An absolute-threshold test ("call it a clamp if zeros dominate below some fixed ε") would misclassify EITHER a fine-tick series (real clamp hidden under a coarse ε) OR a coarse-tick series (rounding mistaken for a clamp). Do NOT headline an inferred single tick / ticks-from-zero figure — it is a sample-dependent proxy, not a physical venue tick. Same per-probe-methodology-gate family as L6 (bootstrap the right unit) / L27 (magnitude-vs-tick) / L28 (observability precheck). | `findings/2026-07-17-q42-funding-clamp-characterization.md` (Q42 part 1, verifier-CONFIRMED); `scripts/q42_funding_clamp_probe.py` | **test (this discriminator) + protocol (the general principle)** — the gap-based discriminator is pinned for the Q42 probe by `tests/test_q42_funding_clamp_probe.py::test_clamp_signature_clear_gap` and `::test_rounding_signature_one_tick_from_zero` (clear-gap vs one-tick-from-zero fixtures) plus `::test_infer_tick_smallest_gap`. The GENERAL methodology (test the gap relative to the data's own granularity, never an absolute threshold) is a per-probe verdict-methodology judgment — like L6/L27/L28 the population and granularity are per-design, so it is **not statically assertable** at the invariants layer (the scanner scans text, it cannot evaluate a series' lattice structure) and is terminal as **protocol** once encoded in probe precedents. NOTE — a second Q42 lesson candidate (route population/sample stdev through `core.stats.safe_pstdev`, which the probe does at `scripts/q42_funding_clamp_probe.py:172` with an `n>=4` guard) was NOT given its own row: it is already covered at the `invariant` end of the gradient by **Hard Rule #2 / L2** (`inv_no_bare_pstdev` + `inv_no_pstdev_import` in `scripts/invariants.py`, sanctioned to `core/stats.py`); the probe simply complied, so a new row would restate an enforced rule rather than move any lesson along the gradient. |
| L90 | 2026-07-17 | Kalshi's settled `/markets` objects carry numeric settlement fields under **suffixed** names, not the bare ones a naive reader would reach for: `settlement_value_dollars`, `volume_fp`, `open_interest_fp` (all wire-format strings, sometimes blank). A collector that reads `settlement_value`/`volume`/`open_interest` directly silently gets `None` for every row instead of erroring — a completeness-invalidating bug that looks like clean output. `collection/settlement_ledger.py` parses the `_dollars`/`_fp` variants explicitly and treats a blank/unparseable string as an honest `None`, never a fabricated `0`. | Q45 build (`collection/settlement_ledger.py:143-146`, `_to_float`); `findings/`-class discovery, no dedicated finding filed (collector build) | **test** — pinned by `tests/test_settlement_ledger.py`'s field-parsing fixtures; UNENFORCED as a general static invariant (no shared "read Kalshi's `_fp`/`_dollars` suffix convention" helper exists yet outside this module to anchor a scanner rule to). |
| L91 | 2026-07-17 | `/events?status=settled&with_nested_markets=true` returns **empty nested `markets` arrays platform-wide** on Kalshi's public API — the per-market settlement fields (`result`, `settlement_value_dollars`, `volume_fp`, `open_interest_fp`) live only on the `/markets` object, not the event. A systematic settled-market harvest that follows the events-endpoint route literally would need one extra `/markets?event_ticker=...` call per event (~1.7k calls/day at Q45's discovery volume) — exactly the unbounded-pull expansion L10 warns against. The bounded alternative is to paginate `/markets?status=settled` directly (the same endpoint the four legacy `qNN_settlement_cache` probes already called, just without a series filter) — one capped, cursor-paginated sweep returns every field needed in place. | Q45 build (`collection/settlement_ledger.py` module docstring, `fetch_settled_markets`); live probe against the real Kalshi API this run | **protocol** — an API-shape discovery (which endpoint actually carries the fields, not a code bug); not statically assertable (a scanner cannot know what a live endpoint returns). Addressed to any future collector that needs settled-market data: reach for `/markets?status=settled` directly, don't route through `/events`. |
| L92 | 2026-07-17 | L52 (2026-07-14) named the exact gap this closes: "no shared settlement-join helper yet exists to anchor a scalar-filter-required rule to." `collection/settlement_ledger.py` is now that shared home — every settled market it ingests (live pull AND the folded-in legacy `qNN_settlement_cache` migration) passes through one `result`-field filter (`run()`/`migrate_caches()`) that drops `result=="scalar"` rows explicitly and counts them (`n_scalar_dropped`), never lets them pass as a yes/no label. Live-confirmed this run: migrating the 4 legacy caches dropped 25 scalar-result markets that had been sitting uncounted in per-probe JSON blobs. Superseding note only — L52 itself is not edited (ledger is append-only); this row records that its named gap is closed and points future settled-market consumers at the new module instead of re-deriving the filter per-probe. | Q45 build (`collection/settlement_ledger.py`); supersedes/closes L52's enforcement gap, does not replace L52's own text | **protocol → candidate for `invariant`** — the filter itself is `test`-pinned in `tests/test_settlement_ledger.py`; escalating it to a static invariant (e.g. "no direct read of a settled market's `result` field outside `collection/settlement_ledger.py` without an explicit scalar check") is a reasonable follow-up for a future `scripts/invariants.py` pass but not done here (scope guard — this run is a collector build, not an invariants change). |
| L93 | 2026-07-17 | Supersedes L76's enforcement column (lesson content unchanged: a run-collapse executability gate keyed on snapshot COUNT is not a duration gate — a sub-second repricing burst can satisfy a `MIN_SNAPS`-style count floor while lasting <1s of real time). Idle-run policy order (a): built `core.bootstrap.collapse_duration_gated_runs(is_hit, seconds, depths, min_duration_seconds=..., min_depth=...)` — collapses consecutive per-snapshot hits into maximal runs, tracks BOTH snapshot count and summed wall-clock seconds, and gates `executable` on the duration (and depth, if given) floor, never on count alone. Same pattern as L33/L35/L36/L49's bootstrap-module helpers. Does not retrofit `scripts/probe_ladder_coherence.py`'s own already-run W-D dossier (that scan's numbers stand as-is, discovery-class, no registry flip) — this is for the next structural-arb probe that collapses snapshot runs. | this run; generalizes L76; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19/Q36/Q37/Q43 all time/day-count gated, matches the prior run's own 2026-07-17 finding) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49 | **protocol, now importable** — `core/bootstrap.py` gains `collapse_duration_gated_runs`; 9 new tests in `tests/test_bootstrap.py` (burst-fails-duration-gate, sustained-run-clears, depth-gate-alongside-duration, no-depths-skips-depth-gate, multiple-maximal-runs, trailing-run-included, no-hits-empty, 2 length-mismatch-raises cases). `.claude/agents/edge-prober.md` house style updated to name it for any future structural-arb run-collapse probe. |
| L94 | 2026-07-18 | Supersedes L59's enforcement column (lesson content unchanged: a momentum/reversal precheck must report reversal FREQUENCY and the sign-conditioned mean next-step as two independent numbers, never classify on frequency alone — S24's raw continuation frequency 0.454 alone reads as momentum, but the sign-conditioned mean pointed the opposite way). Idle-run policy order (a): the importable helper this lesson called for already existed (`core.reversal.direction_precheck`, `tests/test_reversal.py`, both already `test`-tier per L72) but was undiscoverable — not one `.claude/agents/edge-prober.md` house-style line named it, and nothing in the repo imported it outside its own test file, so a future probe had no signpost to reach for it instead of re-deriving a frequency-only classification. Added a house-style paragraph naming `core.reversal.direction_precheck` for any future momentum/reversal precheck, same pattern as L45/L49's and L76/L93's core-helper signposts. No code change to `core/reversal.py` itself — verified its existing 11 tests still green. | this run; generalizes L59 (already `test`-tier via L72); queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [2/7 days weather_books coverage], Q37 gated to ~Aug-05, Q43 gated to ~Jul-24 [1/7 days perp_tape coverage], Q21 idea-gen round completed 2026-07-16 and not yet re-run by the nightly edge-hunter) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93 | **protocol, encoded** — `.claude/agents/edge-prober.md` house style now names `core.reversal.direction_precheck` for any future momentum/reversal precheck. Not statically assertable (a scanner cannot know a probe script is doing frequency-only reversal classification vs. some unrelated use of a fraction); terminal as protocol/discoverability, same tier as L45's `core.timeutil` signpost. |
| L95 | 2026-07-18 | Supersedes L47's enforcement column (lesson content unchanged: `collection/orderbook_depth.py`'s persisted book-side sizes, `yes_bids`/`no_bids` price+size ladders, are floats and can be fractional — a real observed KXWCGAME best-level size was 91,316.82 contracts; a consumer that coerces a level's size to int silently corrupts queue-depth reads). Idle-run policy order (a): L47's own candidate wording asked for a docstring/schema note at the source, not yet written. Added one to `collection.normalize.normalize_snapshot` (the single shared function every book-side-size consumer already reuses per its own module docstring — `collection/orderbook_depth.py`'s "Reuse" section) stating the float/fractional fact directly at the field's origin, plus a `.claude/agents/edge-prober.md` house-style paragraph for any future probe reading `tape/orderbook_depth/`, same signpost pattern as L45/L49, L76/L93, L59/L94. No behavior change — `normalize_snapshot` already cast sizes via `float()`; this closes the discoverability gap the lesson actually named, not a latent bug. | this run; generalizes L47; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [2/7 days weather_books coverage], Q37 gated to ~Aug-05, Q43 gated to ~Jul-24 [1/7 days perp_tape coverage], Q21 idea-gen round completed 2026-07-16 and not yet re-run by the nightly edge-hunter) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94 | **protocol, encoded** — `collection.normalize.normalize_snapshot`'s docstring now states the float/fractional fact at the field's origin; `.claude/agents/edge-prober.md` house style now names the discipline for any future probe reading `tape/orderbook_depth/`. Not statically assertable (a scanner cannot know a given int-coercion in a probe script is operating on a book-side size vs. an unrelated integer field); terminal as protocol/discoverability, same tier as L45/L59's signposts. |
| L96 | 2026-07-18 | **Before proposing (or building) any `tape/universe_sweep/` strategy that differences a market across sweeps or joins its entry-BBO depth to settlement, verify the tape actually supports a per-market panel — schema presence ≠ signal presence, and a cross-section ≠ a time series.** Two failures found the same run (S39/S38 idea-stage kills): (a) `volume_24h` is persisted **0.0 on 100% of the 100,000 committed census rows** (the field exists, `volume`/`open_interest` are populated, but the specific signal is identically zero — likely a `volume_24h_fp` source-field-name bug in `collection/universe_sweep.py`), so a "volume-surge" trigger is uncomputable; (b) the 20k-call-cap (`PAGE_LIMIT=1000 × MAX_CALLS=20`) truncates the >80k-market cursor mid-stream, so each pass paginates a **disjoint slice — 0 ticker overlap across all 5 committed passes** — meaning `universe_sweep` is a bag of one-shot cross-sections, NOT a re-observation of the same markets. Any consecutive-sweep delta is structurally impossible, and per-market entry-depth→settlement joins get only the trickle of tickers that recur by luck. Check `volume_24h!=0` count AND cross-capture ticker-overlap > 0 on the real tape before the mechanism is even conceivable. Same "attack the data claim, don't trust the proposal" family as L9/L43/L50 (join reality) and the general L68 (name+verify your data leg at proposal time). | 2026-07-18 Q21 round, S38+S39 verifier kills; `findings/2026-07-18-q21-idea-gen-round.md` | **ledger-only + collector-bug candidate** — the disjoint-slice property is arithmetic (cap < universe) and the `volume_24h=0` is a concrete `collection/universe_sweep.py` field-mapping bug worth a fix in a future collector run (PR #107 already has Ryan on the universe_sweep cap/storage design calls). Not statically assertable as a strategy rule (a scanner cannot know a probe assumes a panel); enforced as proposal-time data-reality discipline. |
| L97 | 2026-07-18 | **A Kalshi incentive-program parameter (e.g. `discount_factor_bps=5000` from `/incentive_programs`) is `synthetic` for economic modeling until its mechanism is pinned by a real fee-paid-on-a-fill check or vendor confirmation — never treat a 4-digit bps field as a settled fee/rebate fact.** The Q37 probe (`scripts/weather_fee_schedule_probe.py`) found the *actual* weather fee schedule is standard (`fee_type=quadratic`, `fee_multiplier=1`, empty `/series`+`/events/fee_changes`); the "50%" lives ONLY inside a separate `incentive_programs` object whose payout mechanics (`discount_factor_bps` vs the separate `period_reward` centi-cent pool) `kb/kalshi-api/03-fees-and-breakeven.md` explicitly flags as undocumented beyond field names. A Liquidity Incentive Program is structurally a reward *pool*, so the parameter is at least as likely a reward weight as a maker-fee discount. Reading it as "5000 bps → 50% off my fill's fee" and sizing a strategy on it is the pt1 plausible-unattacked-number failure mode (CLAUDE.md prime directive #1). Any candidate whose viability rests on such a parameter must carry a `mechanic_unverified` block on graduation, not a `broker_truth` fee — and idea-stage proposals citing it as "confirmed-live" overstate the source. | 2026-07-18 Q21 round, S40 verifier kill; `kb/kalshi-api/03-fees-and-breakeven.md`, `findings/2026-07-18-q21-idea-gen-round.md` | **ledger-only** — a trust/provenance discipline (the field value is real; its economic *meaning* is unpinned). Not statically assertable (a scanner cannot know a number's real-world mechanic); enforced as proposal-time discipline, cross-refs the Hard-Rule-#1 real-ask/source-tag bar and the 2026-06-18 "LIP maker-rebate harvest" dead-end ledger. |
| L98 | 2026-07-18 | Supersedes L39's enforcement column (lesson content unchanged: a candlestick/volume fill proxy that credits a bracket-ladder's income leg on `high >= ask AND volume > 0` alone is biased upward for any P&L that is a small net of two large legs — S14's own +$0.0925 mean was 78% attributable to sub-100-contract-volume legs, so a fat nominal overround can be almost entirely thin near-money pass-through rather than a real underwritten edge). Idle-run policy order (a): L39's own candidate wording asked for a core/-write helper "à la L33/L35's bootstrap functions," flagged as outside a distiller's lane. Built `core.bootstrap.decompose_edge_by_leg_volume(leg_pnls, leg_volumes, thin_volume_threshold=...)` — reports what fraction of a pooled edge is carried by legs below a volume threshold, honest `None` (not a fabricated 0.0 or a crash) on a zero-total edge — plus a `.claude/agents/edge-prober.md` house-style paragraph for any future probe evaluating a small-net-of-two-large-legs bracket edge, same signpost pattern as L45/L49, L59/L94, L76/L93, L47/L95. No retrofit of `scripts/s14_queue_fillsim.py`'s already-run, already-DEAD Q34 verdict (S14 died on the queue-aware CI itself, not on this decomposition) — this is for the next candle-proxy-adjacent probe. | this run; generalizes L39; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [weather_books day-count], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape day-count], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95 | **protocol, now importable** — `core/bootstrap.py` gains `decompose_edge_by_leg_volume`; 7 new tests in `tests/test_bootstrap.py` (the S14-shape thin-legs-dominate case, all-thick/all-thin fractions, tunable threshold, zero-total-is-honest-None, empty-input, length-mismatch-raises). `.claude/agents/edge-prober.md` house style updated to name it for any future bracket-ladder edge that nets two large legs. |
| L99 | 2026-07-18 | Supersedes L86's enforcement column (lesson content unchanged: when a per-unit P&L carries a large FIXED-LOSS catastrophic leg and some units are dropped on that leg's measurability, never zero the dropped leg instead of dropping the unit — zeroing an unmeasurable LOSS fabricates a free win and biases the mean positive; drop the unit, then verify the drop moved the verdict the conservative direction). Idle-run policy order (a): L86's own candidate wording named "the concrete verifier stress-check (payout=0 counterfactual)... but it lives in each probe's verifier pass, not a reusable assert" as the gap. Built `core.bootstrap.catastrophic_leg_drop_stress_check(retained_pnls, n_dropped, generous_replacement_value=...)` — recomputes the mean crediting the dropped units with the caller's chosen generous counterfactual and reports `sign_preserved` (True iff the reported and stress means share a sign, None if either is undefined), reproducing S14's own -0.0453→-0.0152 check as a reusable function instead of one-off verifier arithmetic — plus a `.claude/agents/edge-prober.md` house-style paragraph for any future probe with a catastrophic-leg-measurability drop, same signpost pattern as L45/L49, L59/L94, L76/L93, L47/L95, L39/L98. No retrofit of `scripts/s14_queue_fillsim.py`'s already-run, already-DEAD Q34 verdict — this is for the next probe with a fixed-catastrophic-leg P&L. | this run; generalizes L86; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [weather_books 3/7 days], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape 2/7 days], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter, only 1 open PR #77 stale/already-landed) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98 | **protocol, now importable** — `core/bootstrap.py` gains `catastrophic_leg_drop_stress_check`; 8 new tests in `tests/test_bootstrap.py` (the S14-shape sign-preserved case, a sign-flip red-flag case, zero-dropped no-op, both-exact-zero preserves, zero-vs-nonzero does-not-preserve, empty-input honest-None, nonempty-retained-zero-dropped stays defined, negative-n_dropped raises). `.claude/agents/edge-prober.md` house style updated to name it for any future probe whose P&L carries a fixed catastrophic leg. |
| L100 | 2026-07-18 | Supersedes L90's enforcement column (lesson content unchanged: Kalshi's `/markets` objects carry settlement/BBO numeric fields under `_dollars`/`_fp`-suffixed string keys, not the bare names — a collector reading the bare key silently gets `None` for every row instead of erroring). Idle-run policy order (a): found the duplication L90's own wording anticipated had already happened in production code — `collection/settlement_ledger.py` and `collection/universe_sweep.py` each independently hand-rolled a byte-identical `_to_float` (same body, near-identical docstring) rather than sharing one. Extracted `core/kalshi_fields.py` (`parse_kalshi_numeric`) and re-pointed both collectors to import it under their existing internal `_to_float` name (`from core.kalshi_fields import parse_kalshi_numeric as _to_float`) — zero behavior change, their own test suites (including `test_settlement_ledger.py`'s direct `sl._to_float(...)` calls) pass unmodified. 5 new tests in `tests/test_kalshi_fields.py` pin the shared implementation directly. `.claude/agents/collector-engineer.md` house style now names it so the next new collector (or the next universe_sweep-class family) imports it instead of re-deriving a third copy. | this run; generalizes L90; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [weather_books 3/7 days], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape 2/7 days], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter, only 1 open PR #77 stale/already-landed) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99 | **protocol, now importable** — `core/kalshi_fields.py` gains `parse_kalshi_numeric`; 5 new tests in `tests/test_kalshi_fields.py` (valid strings, None/blank honest-None, unparseable-string-is-None, numeric passthrough, whitespace-stripping). `.claude/agents/collector-engineer.md` house style updated to name it for any future collector parsing Kalshi's `_dollars`/`_fp` fields. |
| L101 | 2026-07-18 | Supersedes L64's enforcement column (lesson content unchanged: a "post-close" definition anchored on a sports ticker's HHMM token read as UTC, not the settlement `close_time` [`broker_truth`], silently mislabels the majority of a population as post-close when it is actually pre-close — Q25's ticker-HHMM-as-UTC `post_close` bucket was 99.86% mislabeled, understated by up to +24.33h). Idle-run policy order (a): L64's own candidate wording asked for "a `core/`-level close-time helper so future probes don't reach for the ticker token by reflex." Extracted `scripts/q29_settlement_lag_probe.py`'s own `parse_sports_ticker_hhmm_as_utc`/`is_coarse_close_time` (byte-identical logic) into `core/timeutil.py` and re-pointed the script to import them (zero behavior change — its own `tests/test_q29_settlement_lag_probe.py` calls, which reach these as `q29.<name>`, pass unmodified). Added a new `core.timeutil.is_genuine_post_close(captured_at, close_dt, tz_uncertainty_hours=13.0, max_game_duration_hours=6.0)` that encodes the actual discipline (gate on `close_time` + a conservative tz-uncertainty-plus-game-duration margin, `None` on a coarse/date-only close) as one importable call, plus a `.claude/agents/edge-prober.md` house-style paragraph naming it for any future post-close/settlement-lag-adjacent probe, same signpost pattern as L45/L49, L59/L94, L76/L93, L47/L95, L39/L98, L86/L99, L90/L100. No retrofit of `scripts/q29_settlement_lag_probe.py`'s already-run, already-DEAD Q29 verdict — this is for the next probe in the settlement-timing family (S28-adjacent). | this run; generalizes L64; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final [tomorrow, burst tape not yet captured]/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [weather_books 3/7 days], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape 2/7 days], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter, only 1 open PR #77 stale/already-landed, 0 new stranded-tape branches since PR #115's sweep) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99, L90→L100 | **protocol, now importable** — `core/timeutil.py` gains `parse_sports_ticker_hhmm_as_utc`, `is_coarse_close_time`, `is_genuine_post_close`; 11 new tests in `tests/test_timeutil.py` (basic/bad-grammar/out-of-range ticker parses, 2359/midnight/None/real-intraday coarse-close cases, genuine-post-close true/false/coarse-None/custom-margin cases). `.claude/agents/edge-prober.md` house style updated to name `is_genuine_post_close` for any future post-close-adjacent probe. |
| L102 | 2026-07-18 | Closes a residual gap L36 left open (lesson content traces to L7: never hardcode a bracket/strike width, even per-symbol; derive spacing from the ladder itself). **Correction to this run's own initial read:** L7 was NOT still `UNENFORCED` — L36 (2026-07-12) already built `core.pricing.infer_strike_spacing(strikes)` and both consumers used it correctly. But the higher-level wrapper that turns a snapshot's `outcomes` list into a strike coordinate / ladder-wide spacing was never itself shared: `member_coord`/`ladder_spacing` were independently duplicated byte-for-byte (identical bodies, near-identical docstrings) across `scripts/s19_wing_fade_fillsim.py` and `scripts/s20_ladder_overround_anatomy.py` — the exact "third probe re-derives it" failure mode L7/L36 targeted, recurring one layer up from where the first fix landed (same shape as L90's `_to_float` duplication). Extracted both into `core/pricing.py` alongside `infer_strike_spacing` and re-pointed both scripts to import them — zero behavior change, both scripts' own existing tests (`tests/test_s20_ladder_overround_anatomy.py`'s direct `s20.member_coord`/`s20.ladder_spacing` calls) pass unmodified. 6 new tests in `tests/test_substrate_primitives.py` pin the shared implementation directly. Extended the `.claude/agents/edge-prober.md` house-style bullet L36 already added (naming `infer_strike_spacing`) to also name `member_coord`/`ladder_spacing`. | this run; extends L36 (L7's actual successor); found via a manual duplication grep after this run's own UNENFORCED-ledger table scan proved unreliable (inconsistent supersession phrasing across rows made an automated "already handled" check give a false negative on L7) — queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final [tomorrow, burst tape not yet captured]/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36(queue) gated to ~Jul-22 [weather_books 3/7 days], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape 2/7 days], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter, only 1 open PR #77 stale/already-landed, 0 new stranded-tape branches since PR #116's sweep) | **protocol, now importable** — `core/pricing.py` gains `member_coord`, `ladder_spacing`; 6 new tests in `tests/test_substrate_primitives.py` (between-midpoint, edge-boundary, none-when-no-strike, spacing-from-between-floors, spacing-none-below-two-strikes, spacing-ignores-non-between-members). `.claude/agents/edge-prober.md` house style extended to name both for any future bracket-ladder probe. New lesson candidate: the ledger's supersession phrasing is inconsistent enough ("Supersedes L<N>'s enforcement column" vs "generalizes L<N>" vs "L<N>'s own candidate wording... stayed unbuilt") that a future idle run doing this same lookup should grep for the L-number as a whole word across the WHOLE file (not just a fixed supersession phrase) before assuming a row is still open. |
| L103 | 2026-07-19 | Supersedes L51's enforcement column (lesson content unchanged): L51's own candidate ("an edge-prober.md house-style note for any future calibration-precheck milestone") stayed unbuilt; every future "feature beats the mid" probe would re-derive (and could re-misread, as Q26/S22 nearly did) the disagreement-subset complementarity from scratch. Encoded it as an importable guardrail so a future calibration-precheck probe gets the correct "mid accuracy where they disagree" framing for free instead of reporting two illusory independent hit rates. Built `core.bootstrap.disagreement_subset_calibration(hit_signal, hit_mid, tol=1e-9)` — reports `{"n", "mid_accuracy", "signal_accuracy", "is_strict_two_way", "violating_indices"}`, treating any row where `hit_signal[i] == hit_mid[i]` as a violation that PROVES the caller's "disagreement subset" was not a strict two-way partition (never raised on, only reported), plus an honest empty-input report (`n=0`, accuracies `None`, `is_strict_two_way=True`) matching the sibling helpers' empty-input discipline. Tests in `tests/test_bootstrap.py` (incl. the Q26/S22 27.9%/72.1% regression fixture) pin it, and `.claude/agents/edge-prober.md` house style now names it for any future calibration-precheck / "does feature X beat the mid" milestone on a two-way market. | this run; supersedes L51; core/bootstrap.py; tests/test_bootstrap.py | **protocol, now importable** — `core/bootstrap.py` gains `disagreement_subset_calibration`; tests in `tests/test_bootstrap.py` pin `n`/`mid_accuracy`/`signal_accuracy`/`is_strict_two_way`/`violating_indices`, the Q26/S22 27.9%/72.1% regression fixture, a violating-row (non-two-way) case, empty-input honest-`None`, and length-mismatch-raises. `.claude/agents/edge-prober.md` house style updated to name it for any future calibration-precheck probe. |
| L104 | 2026-07-19 | Supersedes L65's enforcement column (lesson content unchanged: Kalshi empties and settles a sports order book AT close — the maximum observed capture-to-settlement gap across all committed `tape/orderbook_depth/` is 0.024h [~1.4min], and every genuinely-post-close capture found has a fully empty book on both sides, so there is no post-close resting-quote window to pick off on ANY timing definition). Idle-run policy order (a): found via the whole-word-grep-across-the-whole-file discipline L102's own lesson candidate proposed (cross-checked every UNENFORCED row's number against every later row's text for an actual `supersedes`/`generalizes`/`escalates`/`closes` claim, not just a substring match on "UNENFORCED" — L18/L20/L23/L28/etc. false-positived on a naive scan because they merely QUOTE another row's UNENFORCED status). L65's own candidate wording ("kills any future settlement-lag/post-close-staleness proposal on sports tape at the idea stage, S28 family") stayed unbuilt — `.claude/agents/edge-prober.md` already named the L64/L101 close-time-parsing gate but nothing told a future prober to reject the whole premise before reaching for that helper. Added a house-style bullet stating the underlying market-structure fact directly (no per-probe helper needed — L65 is a fact about the venue, not a computation) so a future S28-family or "buy the ex-post known winner" (L66) proposal is rejected before a fill-sim gets built. No code change; this closes at the documentation/discoverability tier, same as L94/L95. | this run; supersedes L65; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 time-gated on Jul-19 WC final [tonight, burst tape not yet captured — kickoff 19:00Z]/Jul-29 FOMC, Q32/Q33/Q35-build blocked on Polymarket creds, Q36 gated to ~Jul-22 [weather_books 4/7 days], Q37 gated to ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated to ~Jul-23/24 [perp_tape 3/7 days], Q21 idea-gen round completed 2026-07-18 by the nightly edge-hunter, only 1 open PR #77 stale/already-landed) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99, L90→L100, L64→L101, L51→L103 | **protocol, encoded** — `.claude/agents/edge-prober.md` house style now states the L65 fact directly (post-close sports settlement-lag pickoffs are dead on ANY timing definition, not just the L64-style mis-timed one) for any future post-close/settlement-lag-adjacent probe proposal. Not statically assertable (a scanner cannot detect a proposal's underlying premise); terminal as protocol/discoverability, same tier as L94/L95's signposts. |
| L105 | 2026-07-19 | `tape/universe_sweep/` (schema `universe_sweep.v1`) is a TOP-OF-BOOK census WITHOUT strike-ladder fields — `strike_type`/`floor_strike`/`cap_strike`/`yes_ask_dollars` are all absent, so it CANNOT feed `scripts/anomaly_sweep.py::check_bracket_arb` (whose `_segment_bounds()` exhaustiveness proof requires them), and its sub-$1 Σ`yes_ask` groups are ~98% all-zero NO-OFFER artifacts, not crossed books: over `dt=2026-07-19` (20,000 rows, single `capture_id`), 1,565/2,441 multi-market groups have Σ`yes_ask` < $1.00 but 0/1,565 are fillable (all-legs `yes_ask_size ≥ 1 ∧ yes_ask > 0`), and 1,537/1,565 are all-zeros. Treating a `yes_ask=0.0` no-offer leg as a $0.00 buyable fill is the pt1 / prime-directive violation (a nominal price is never a fill). This restates the L96/S38 illiquidity floor (fillable population ~0.56–1.05%, `yes_ask_size==0` on ~96%) for the full-universe SIMULTANEOUS census — a crossable complete-set book never appears there. Kills any future "point the anomaly sweep / a within-event overround-underflow arb at `tape/universe_sweep/`" proposal at the idea stage (the killed S41 of the 2026-07-19 Q21 round). | 2026-07-19 kalshi-edge-hunter Q21 round (S41 verifier attack); `findings/2026-07-19-q21-idea-gen-round.md`; computed over `tape/universe_sweep/dt=2026-07-19.jsonl` vs `scripts/anomaly_sweep.py` required fields | **UNENFORCED** — recorded at the documentation tier in the finding + this row; a future research-loop idle run (idle-policy a) should add the `.claude/agents/edge-prober.md` house-style bullet naming this (the edge-hunter leaves `.claude/agents` for Ryan-review, so it did not add it here). Not statically assertable (a scanner cannot detect a proposal's target-tape assumption); generalizes L96 to the bracket-arb use case. |
| L106 | 2026-07-19 | Supersedes L68's enforcement column (lesson content unchanged: a maker-spread / spread-capture candidate whose ONLY data leg is `tape/orderbook_depth/` is toxicity-untestable by construction — the depth tape carries resting-depth snapshots only [`best_*_bid/ask`, `yes_bids`, `no_bids`], NO trade/volume/last-price fields, so whether a resting offer is adversely selected cannot be measured and the adverse-selection-modeled block-bootstrap CI that is the L41 admissibility bar is unconstructible; a wide spread is equally consistent with absent-competition [good] and adverse-selection-compensation [fatal] and the depth tape cannot adjudicate; kill it at the IDEA stage rather than register it as data-collecting/untestable, which would burn a research-loop probe — the only sports executed-volume tape anywhere is `tape/sports_history_s7/worldcup2026.jsonl`, WC-only per L44). Idle-run policy order (a), strict-lowest-first: (i) re-ran the L102/L104 whole-file whole-word grep discipline — every `UNENFORCED`-bearing row's L-number checked as a WHOLE WORD across the whole repo for an ACTUAL later `supersedes`/`generalizes`/`closes`/`escalates` claim (not a substring mention): L5/L7/L22/L25/L39/L45/L47/L51/L59/L64/L65/L74/L76/L86/L90 are all genuinely closed by later rows (L18/L36/L102, L24, L29, L73/L98, L49, L95, L103, L72/L94, L101, L104, L75, L93, L99, L100); (ii) L27/L28/L32 are helper-built (`core.bootstrap.clears_tick_magnitude`/`floor_pinned_fraction`/`bracket_by_movement`) AND named in `.claude/agents/edge-prober.md` — nothing left to build; (iii) L52/L92's remaining "escalate to a static invariant" thread was inspected and found a FALSE START — `result == "yes"/"no"` is read WITHOUT an adjacent scalar guard in 9 legacy/verdicted scripts (`weather_rehab_s5.py`, `sports_clv_s7.py`, `longshot_fade_probe.py`, `q24_sports_longshot_maker_fillsim.py`, `q26_ofi_depth_imbalance_probe.py`, `q27_favorite_underpricing_fillsim.py`, `q28_s24_nearclose_fade_probe.py`, `q29_settlement_lag_probe.py`, `q30_draw_aversion_maker_probe.py`), so a gating scanner would false-gate frozen historical code and an allowlist of all of them is the L30/L19 dishonest-theater anti-pattern — L52 is honestly terminal (test-pinned at L92), SKIPPED, recorded so the next idle run does not re-attempt it; (iv) that leaves L68 as the true lowest genuinely-open, cleanly-convertible row (its L88/L96 mentions are sibling citations, not supersessions; it is absent from the charter). Added a house-style idea-stage-kill bullet in the L65/L104 cluster naming the L68 kill and cross-referencing `core.depth.capturable_depth` (L67) for the sibling two-sided-depth-illusion check. No code change; closes at the documentation/discoverability tier, same as L104/L94/L95. | this run; supersedes L68; queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 WC-final burst tape not yet captured [kickoff tonight 19:00Z], Q36 gated ~Jul-22, Q37 gated ~Aug-05, Q42 part 3 BLOCKED(needs-auth), Q43 gated ~Jul-24, Q21 idea-gen round just run by tonight's edge-hunter PR #121, only open PR #77 stale) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99, L90→L100, L64→L101, L51→L103, L65→L104 | **protocol, encoded** — `.claude/agents/edge-prober.md` house style now names the L68 idea-stage kill (a maker-spread candidate over `tape/orderbook_depth/` alone is toxicity-untestable — reject before registration, do not burn a probe). Not statically assertable (a scanner cannot detect a proposal's maker-spread-over-depth-only premise); terminal as protocol/discoverability, same tier as L104/L94/L95. |
| L107 | 2026-07-19 | Supersedes L105's enforcement column (lesson content unchanged: `tape/universe_sweep/` [schema `universe_sweep.v1`] is a TOP-OF-BOOK census WITHOUT strike-ladder fields — `strike_type`/`floor_strike`/`cap_strike`/`yes_ask_dollars` are all ABSENT from the persisted record [confirmed at `collection/universe_sweep.py`, which persists only a top-of-book `yes_ask`], so it CANNOT feed `scripts/anomaly_sweep.py::check_bracket_arb`, whose `_segment_bounds()` exhaustiveness proof reads exactly those ladder fields; and its sub-$1 Σ`yes_ask` groups are ~98% all-zero NO-OFFER artifacts [1,537/1,565 all-zeros over dt=2026-07-19, 0/1,565 fillable], not crossable books — a `yes_ask=0.0` no-offer leg treated as a $0.00 buyable fill is the pt1/prime-directive violation). Idle-run policy order (a): L105's own candidate wording asked for a `.claude/agents/edge-prober.md` house-style bullet — the kalshi-edge-hunter that raised L105 (PR #121) leaves `.claude/agents` edits for the research loop, so the bullet was never added, and L106 (which closed L68 the same cycle) picked the strict-lowest-open row and did not reach L105. Added an idea-stage-kill bullet in the L65/L104/L106 cluster naming the L105 kill (any 'point the anomaly sweep / a within-event overround-underflow complete-set arb at `tape/universe_sweep/`' proposal is rejected before registration — the census lacks the ladder fields the check needs, its sub-$1 sums are absent-offer artifacts not arbs, and the 20k-cap-over-80k-universe [L96] splits straddling sets so exhaustiveness is unprovable in principle), cross-referencing L96/S38's illiquidity floor and L67's `core.depth.capturable_depth` for the sibling nominal-vs-fillable-depth distinction. Verifier CONFIRMED (three independent attacks): L105 genuinely open AND the lowest such (whole-file whole-word grep over every UNENFORCED row — L22→L24, L25→L29, L27/L28→L33+L34, L32→L35, L39→L73/L98, L45→L49, L47→L95, L51→L103, L52→terminal[L92], L59→L72/L94, L64→L101, L65→L104, L68→L106, L74→L75, L76→L93, L86→L99, L90→L100 all genuinely closed), and the schema-incompatibility fact true at source. No code change; closes at the documentation/discoverability tier, same as L104/L106/L94/L95. | this run; supersedes L105; `.claude/agents/edge-prober.md`; `findings/2026-07-19-q21-idea-gen-round.md` (S41 verifier kill that raised L105); verifier-CONFIRMED target selection + structural fact. Queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 WC-final burst tape not yet captured [kickoff tonight]; Q36 gated ~Jul-22; Q37 gated ~Aug-05; Q42 part 3 BLOCKED[needs-auth]; Q43 gated ~Jul-24; Q21 idea-gen just run by edge-hunter PR #121; Q32/Q33/Q35-build blocked on Polymarket creds; only open PR #77 stale) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99, L90→L100, L64→L101, L51→L103, L65→L104, L68→L106 | **protocol, encoded** — `.claude/agents/edge-prober.md` house style now names the L105 idea-stage kill (do not point the anomaly sweep / a within-event complete-set arb at `tape/universe_sweep/` — reject before registration). Not statically assertable (a scanner cannot detect a proposal's target-tape assumption); terminal as protocol/discoverability, same tier as L104/L106/L94/L95. |
| L108 | 2026-07-19 | Supersedes L23's enforcement column (lesson content unchanged: an empty `yes_bids`/`no_bids` ladder on one side of a Kalshi order book is normal for a far-from-strike/thin market, not a capture failure, and must record as `depth=0` valid data, never a DROP). Corrects a mischaracterization in **L104** (not L106 — verifier caught this): L104 named L23 alongside L18/L20/L28 as merely QUOTING another row's `UNENFORCED` status, but L23's own enforcement column carries its OWN genuine open candidate ("UNENFORCED as a general 'empty ≠ drop' invariant beyond this one collector"), not a quote of anyone else's (contrast L18, which literally reads "Supersedes L5's UNENFORCED static-check clause" — that IS a quote). **L106 and L107 each independently claimed to have found "the true lowest genuinely-open row" (L68, then L105) via an exhaustive whole-file whole-word scan — both missed L23, which is lower and was open the whole time; L106 never mentions L23 at all, so the miss wasn't even a mischaracterization like L104's, just an omission that L107 then inherited uncritically from L106's closure map.** Net: the ledger's "strict-lowest-first" discipline has now failed twice in a row (L106, L107) before this run's direct enumeration caught it — a future idle run should mechanically re-derive the open set from scratch rather than trust the previous row's stated closure map. Confirmed at source (verifier-CONFIRMED): `collection/orderbook_depth.py` and `collection/weather_books.py` both import the SAME `collection.normalize.normalize_snapshot` (a third module, `collection/capture_orderbooks.py`, also calls it) — an empty side there is just `[]`/`None` (never an exception), so the empty≠drop distinction is already a SHARED pure-function property, not per-collector logic that could silently regress. L23's candidate ("generalize beyond this one collector") is therefore already satisfied by the existing architecture; the remaining gap was purely discoverability — nothing told a future ladder-collector author to reuse `normalize_snapshot` rather than re-deriving the distinction. Added a `.claude/agents/collector-engineer.md` house-style bullet naming it. No code change (the shared function already existed and already had this property); closes at the documentation/discoverability tier, same as L104/L106/L107/L94/L95. | this run; supersedes L23; corrects L104's mischaracterization and flags L106/L107's lowest-open-row omission; verifier-CONFIRMED (four independent attacks — see this run's verifier report); `collection/normalize.py`; `collection/orderbook_depth.py`; `collection/weather_books.py`; `collection/capture_orderbooks.py`; `.claude/agents/collector-engineer.md`. Queue scan reconfirmed 0 eligible TODO/IN-PROGRESS items (Q19 WC-final burst tape not yet captured [kickoff tonight 19:00Z]; Q36 gated ~Jul-22; Q37 gated ~Aug-05; Q42 part 3 BLOCKED[needs-auth]; Q43 gated ~Jul-24; Q21 idea-gen just run by edge-hunter PR #121; Q32/Q33/Q35-build blocked on Polymarket creds; only open PR #77 stale) — drawn from the lessons ledger's own standing UNENFORCED queue, same pattern as L25→L29, L33→L34, L32→L35, L7→L36, L45→L49, L76→L93, L59→L94, L47→L95, L39→L98, L86→L99, L90→L100, L64→L101, L51→L103, L65→L104, L68→L106, L105→L107 | **protocol, encoded** — `.claude/agents/collector-engineer.md` house style now names `collection.normalize.normalize_snapshot` as the sanctioned shared ladder-normalization site any new depth-family collector should import rather than re-derive the empty≠drop distinction per-collector. Not statically assertable (a scanner cannot prove a future collector reused the helper vs. rewrote the logic); terminal as protocol/discoverability, same tier as L104/L106/L107. |
| L109 | 2026-07-19 | **PROVISIONAL / candidate.** L25's format-regression self-correction fixed *forward* collection but never garbage-collected the orphaned directory-shaped `dt=` days it documented — they are STILL RESIDENT in the committed tree ~9 days later: `tape/sports_pairs/dt=2026-07-02/` (early-format pass), `dt=2026-07-09/` (raw `capture-*/*.raw.json` blobs with NO canonical `dt=2026-07-09.jsonl` at all -> 07-09 is a PERMANENTLY MISSING day), `dt=2026-07-10/` (raw blobs coexisting with a TRUNCATED `dt=2026-07-10.jsonl` of only 9/48 captures / 1,968 lines). `orderbook_depth` also has 07-09 absent. A naive `ls tape/sports_pairs/ \| grep dt=` day-count therefore over-counts (07-02, 07-09) and mis-shapes (07-10). L25's assert (a `dt=` path should be *file*-shaped) does not flag an ORPHANED directory-shaped day for cleanup dispatch / GC. | `findings/2026-07-19-sports-pairs-join-adequacy-dataquality.md` (provenance: L25 format-regression debris; L9/L43 fair-depth timing gap; L64 settlement-emptying transition) | **non-gating advisory + test (BUILT; stale `UNENFORCED` marker corrected 2026-07-24 by the kb-distiller, lesson text unchanged — L25 precedent)** — the candidate named here WAS built: `scripts/invariants.py::_tape_dir_shape_orphan_classification()` / `tape_dir_shape_orphan_warning()`, which goes beyond L25's shape assert and CLASSIFIES each orphaned directory-shaped `dt=` day for GC dispatch (`superseded` = a canonical `.jsonl` for that date already exists, safe to delete; `unrecoverable` = no canonical file and collection has moved past that day, needs a human; a directory at/after the family's newest day is deliberately left unclassified). Pinned by `tests/test_invariants.py::test_orphan_classification_superseded_when_canonical_file_coexists`, `::test_orphan_classification_unrecoverable_when_no_canonical_file_and_collection_moved_on`, `::test_orphan_classification_unclassified_when_directory_is_the_newest_day`, `::test_orphan_warning_never_gates_exit_code`, and the real-tree HARD acceptance `::test_orphan_classification_matches_real_committed_tree` (which pins this row's exact three `sports_pairs` orphans). The DELETION itself is still a human/GC action, not automated — the advisory dispatches it, it does not perform it. |
| L110 | 2026-07-19 | Supersedes L109's enforcement column (lesson content unchanged: L25's shape-check flags a directory-shaped `dt=<date>` entry as wrong-shape but never says what to DO about one). Built `scripts/invariants.py::_tape_dir_shape_orphan_classification()`/`tape_dir_shape_orphan_warning()` (same non-gating stderr-advisory pattern as L25/L75) that classifies every directory-shaped `dt=<date>` entry for GC dispatch: **"superseded"** when a canonical `dt=<date>.jsonl` for the SAME date already coexists (pure post-fix debris, safe to delete) vs **"unrecoverable"** when no canonical file exists for that date AND the family has since captured a LATER canonical day (forward collection has moved on — this day can never self-heal via normal cadence, a human must decide backfill-or-accept-the-gap). A directory at/after the family's newest canonical day is deliberately left unclassified — collection may still be mid-write, so flagging it for GC would be premature. Live-validated against the real committed tree, ground-truthing the exact L109 finding: `crypto_hourly/dt=2026-07-10` and `sports_pairs/dt=2026-07-10` both classify `superseded` (a canonical `.jsonl` coexists for each); `sports_pairs/dt=2026-07-02` and `dt=2026-07-09` both classify `unrecoverable` (no canonical file, and `sports_pairs` has captured many later days) — 2 safe-to-delete, 2 permanently-lost, exactly as L109 described. Deliberately does NOT perform the deletion itself (a tape-data removal is a judgment call for Ryan/a future run, not unilateral cloud-run action) — this is detection/dispatch only, same posture as L75's gap-detector not backfilling the lost days it finds. | this run; `scripts/invariants.py`; `tests/test_invariants.py::test_orphan_classification_superseded_when_canonical_file_coexists`, `::test_orphan_classification_unrecoverable_when_no_canonical_file_and_collection_moved_on`, `::test_orphan_classification_unclassified_when_directory_is_the_newest_day`, `::test_orphan_classification_clean_tree_is_empty`, `::test_orphan_classification_missing_tape_root_is_empty`, `::test_orphan_warning_none_when_empty`, `::test_orphan_warning_message_content`, `::test_orphan_warning_never_gates_exit_code`, `::test_orphan_classification_matches_real_committed_tree`; supersedes L109's enforcement | **invariant (non-gating advisory)** — fires automatically on every `invariants.py --full` run; supersedes L109's `UNENFORCED`. **Judgment call flagged for Ryan, not acted on:** the 2 classified-`superseded` directories (`crypto_hourly/dt=2026-07-10`, `sports_pairs/dt=2026-07-10`) are now confirmed safe to delete but this run did not delete them. |
| L112 | 2026-07-20 | Supersedes L69's enforcement column (lesson content unchanged: a queue-aware maker fill-sim that can rest at the EARLIEST pre-close snapshot systematically enters on thin, often one-sided early books, so the fillable-entry-restricted population — two-sided book <=~10c and/or near-close ttc<=24h — must be the PRIMARY verdict by default, never a robustness footnote applied after a positive headline; S29/Q30's earliest-entry spec population cleared every gate at +9.03c purely on nickel bids against 87-94c days-out asks the generous fill-sim still marks FILLED, the pt1 synthetic-price-as-fillable mistake one abstraction up on a nominal ONE-SIDED quote). Idle-run policy order (a), full-file whole-word re-derivation of the UNENFORCED queue: after L23->L108 and L109->L110 the open set is exactly {L66, L69} (verifier-CONFIRMED, re-derived from scratch — no row below L66 open). Strict-lowest-first would pick L66, but an independent verifier judged L66 honestly-terminal-leaning — its "precision note for a trade-the-known-outcome kill writeup" governs a block-bootstrap surface that is never reached, because the S28/buy-the-ex-post-known-winner family is already an IDEA-STAGE kill (L65/L104, `.claude/agents/edge-prober.md`) so no fill-sim ever gets built (a foreclosed-surface docs bullet). L69 by contrast governs a LIVE recurring template (the Q27/Q30-style earliest-pre-close queue-aware maker fill-sim) and prevents a real future fill-price error, so per the two-agent rule the run converted L69 instead. L69's candidate ("make the fillable-entry-restricted population the PRIMARY verdict, unrestricted a labeled diagnostic") was unbuilt — nothing in `.claude/agents/edge-prober.md` named it. Added a house-style bullet stating it directly. No code change; closes at the documentation/discoverability tier, same as L94/L95/L104/L106/L107/L108. L66 left flagged terminal-leaning for a future run's formal disposition, not unilaterally closed here. | this run; supersedes L69; `.claude/agents/edge-prober.md`; verifier-CONFIRMED open set + L66->L69 target pivot | **protocol, encoded** — `.claude/agents/edge-prober.md` house style now names the L69 fillable-entry-restricted-population-as-PRIMARY discipline for any Q27/Q30-style earliest-pre-close queue-aware maker fill-sim. Not statically assertable (a scanner cannot know a probe's default population is the unrestricted earliest-entry set); terminal as protocol/discoverability, same tier as L104/L106/L107/L108. |
| L113 | 2026-07-20 | **`tape/econ_prints/` committed window contains exactly n=1 in-window macro RELEASE moment (June CPI, settlement ~2026-07-14T12:2Xz on `KXCPI/KXCPICORE/KXCPIYOY-26JUN` — the same simultaneous BLS print, not 3 independent events).** Every other econ series settles AFTER the tape ends (`KXGDP-26JUL30` close 07-30, `KXPAYROLLS-26JUL` close 08-07; all other CPI months are forward ladders 26AUG..26NOV). So any cross-venue *directional* econ-lag / lead-lag idea (S43) is data-adequacy-dead by n=1 — one autocorrelated intra-event digestion path is not a bootstrappable cross-event distribution (same S9 grave). Also `tape/polymarket_cpi_pairs/` Kalshi leg is 100% `synthetic` (1452/1452), so the real_ask lives only in the separate `econ_prints` ladder. Re-testable only when the econ tape spans ≥4 distinct release moments (monthly cadence → ~4 months out). | 2026-07-20 Q21 round, S43 verifier kill; `findings/2026-07-20-q21-idea-gen-round.md` | **ledger-only** — a data-adequacy count of the committed tape (parallels S9's cadence-adequacy grave), not statically assertable; kills any cross-venue directional econ-print candidate at the idea stage until the release count grows. |
| L114 | 2026-07-20 | **`universe_sweep.v1` within-market YES+NO box has min(yes_ask+no_ask) = 1.002 (never below $1.00 even pre-fee) — the "logical-complement coherence arb" (S44) is the S41 no-offer/longshot artifact re-skinned.** Over one census (`capture_id 20260718T003036Z`), the tightest guaranteed-exhaustive complement (YES vs NOT-YES) across the 237 both-asks-fillable markets never inverts; that gap IS the overround. Apparent sub-$1 cross-ticker sums are two *different* parlay conjunctions in one multigame event — mutually exclusive but NOT exhaustive (both can lose → buying both collects $0, not $1). Complements are moreover unidentifiable from `universe_sweep.v1` (no partition-linking field; 84% is `KXMVESPORTSMULTIGAMEEXTENDED` parlays), only 2.5% of rows are fillable (`yes_ask_size>=1`), and the L96 20,000-row cap means a full MEE partition (40+ legs/event) is never guaranteed captured. Generalizes L105. | 2026-07-20 Q21 round, S44 verifier kill; `findings/2026-07-20-q21-idea-gen-round.md` | **ledger-only** — an empirical census fact (box never <$1, complements unidentifiable) generalizing L105; kills any "complement/coherence arb over the universe census" proposal at the idea stage. |
| L115 | 2026-07-20 | **`tape/settlement_ledger/` (as committed, `dt=2026-07-17`, 5605 rows) has ZERO crypto rows — it is entirely sports/metals (`KXMVESPORTS…`/`KXMLBGAME`/`KXGOLDH`/`KXWTIH`…), so any "join `crypto_hourly` to `settlement_ledger`" is a zero-overlap join.** Crypto settlement truth lives ONLY inside `crypto_hourly.previous_settlement` (`broker_truth`). Separately, `crypto_hourly` outcome records carry NO size/queue/depth field (10 keys: cap/floor_strike, no/yes_ask, no/yes_bid, price_source_tag, strike_type, ticker, title), so no queue-aware crypto maker fill-sim is constructible (S19/S21 unprovable-queue + S10/S14 no-depth grave); and only 3.4% of crypto brackets are two-sided (modal spread 1–4¢ vs flat 1¢ maker fee = S6/S13/S23 fee swamp). Kills the single-series settlement-ledger-anchored crypto maker candidate (S45). | 2026-07-20 Q21 round, S45 verifier kill; `findings/2026-07-20-q21-idea-gen-round.md` | **ledger-only** — a tape-coverage + schema fact (settlement_ledger has no crypto; crypto_hourly has no depth field); kills any crypto maker fill-sim or crypto×settlement_ledger join at the idea stage. |
| L116 | 2026-07-20 | Formal disposition of L66 (supersedes its enforcement column; lesson content unchanged: a "buy the ex-post known winner" kill-writeup must state its losing-tail boundary as "genuine but thin," never "provably degenerate," since `edge = 1 − ask − fee` admits a real if rare losing cluster and an adversarial verifier can construct a synthetic counterexample even when the real tape never realizes one). Idle-run policy order (a): whole-file whole-word re-derivation of the UNENFORCED queue (per L108/L112's discipline) found the open set is now exactly `{L66}` — every other row is closed (L112 converted L69 and left L66 flagged "terminal-leaning" for this run's formal disposition). Confirmed terminal: the surface L66 governs (a block-bootstrap kill-writeup for the S28 "trade the known winner" family) is foreclosed at the IDEA stage by L65/L104 — `.claude/agents/edge-prober.md` already rejects any S28-family fill-sim before one is ever built, so no kill-writeup for this family is reachable under the current idea-stage kill. Rather than leave the discipline stranded (unreachable = undiscoverable), added a conditional clause to the SAME L65/L66 house-style bullet: if a future run ever formally reopens the S28 idea-stage kill and a kill-writeup gets built anyway, it must apply L66's precision language. This is a genuine "protocol, encoded" closure, not a no-op — the alternative (say nothing) would let the exact overstatement L66 caught recur silently the day S28 is ever reopened. | this run; supersedes L66; `.claude/agents/edge-prober.md`; re-derived open UNENFORCED set = {} after this row (verified via whole-word grep, same method as L108/L112) | **protocol, encoded** — `.claude/agents/edge-prober.md`'s existing L65/L66 house-style bullet now carries a conditional clause naming the L66 precision discipline for if/when the S28 idea-stage kill is ever reopened. Not statically assertable (a scanner cannot detect a future kill-writeup's premise); terminal as protocol/discoverability, same tier as L104/L106/L107/L108/L112. **The lessons ledger's UNENFORCED backlog is now empty as of this row** — a future idle run drawing on policy order (a) will find nothing to convert until a new lesson is filed as UNENFORCED. |
| L117 | 2026-07-20 | **When an hourly tape family's line count halves day-over-day IN LOCKSTEP with other unrelated families, at a CONSTANT lines-per-pass, the cause is the shared external scheduler dying, not a per-family collector bug — and minute-of-hour bucketing of `captured_at` attributes the drop to a SPECIFIC collector.** `perp_tape`/`crypto_hourly`/`orderbook_depth` all halved 07-17→07-20 (511→238→102→51, 64→28→14→6, 29435→15020→6655→3295) with per-pass size dead constant (perp 17.0/pass, crypto 2.0/pass) — ruling out per-family record loss. Bucketing every line's capture minute into the VPS cron's signature (`:2x`) vs the cloud routine's (`:5x`, per `ops/ROUTINES.md`) shows the VPS leg going non-zero→non-zero→**zero→zero** exactly on 07-19/07-20 while the cloud leg (already Q44's flagged ~60%-cadence laggard) persists as the sole survivor — pinpointing a dead VPS cron, not a code regression. This is a data-quality/ops diagnosis, not a repo bug: every collector module inspected (`crypto_hourly.py`, `perp_tape.py`) has a fixed symbol list and 100% `completeness_ok` on passes that DO fire. | `findings/2026-07-20-tape-cadence-decline-vps-collector-down.md`; extends L74/L75 and Q44's `scripts/tape_gap_monitor.py` aggregate under-capture finding (`kb/00-LOG.md` 2026-07-17) | **UNENFORCED** — candidate: extend `scripts/tape_gap_monitor.py` with a minute-of-hour bucketing pass so it reports WHICH of the two staggered collectors (VPS `:2x` vs cloud `:5x`) died, not only an aggregate family-level under-capture ratio. Not built this run (idle-run scope, option (c) is diagnosis not build) — row filed only. The actionable remediation itself (restarting the VPS cron / diagnosing the cloud routine) is Ryan/VPS-side and outside any cloud run's lane regardless of enforcement tier. |
| L118 | 2026-07-20 | Formal disposition of L117 (supersedes its enforcement column; lesson content unchanged). Idle-run policy order (a): re-derived the UNENFORCED queue and found the open set is now exactly `{L117}` (L116 had closed the backlog to empty; the immediately-prior firing filed L117 fresh). Built the proposed extension: `scripts/tape_gap_monitor.py` gains `collector_bucket()` (minute-of-hour → `vps`/`cloud`/`other`, ranges `20-29`/`50-59` per `ops/ROUTINES.md`'s `:23`/`:53` cron minutes, calibrated against real committed-tape minute histograms — never forces an ambiguous minute into either bucket) and a `collectors` breakdown attached to every `hourly-dual`-kind family's health record (`FamilyAggregate.collector_summary()`). When a family alerts and exactly one bucket is empty while the other still produces passes, `collector_diagnosis` names which collector looks dead (`vps_dead`/`cloud_dead`) and folds into `alert_reason`; both-zero and both-nonzero cases are left unattributed rather than guessed at (verified live: `weather_books`' real cloud-leg offset doesn't land in the `:5x` window, so it honestly reports `other`/unattributed instead of a false `vps_dead`). 12 new unit tests plus a 4th HARD acceptance test anchored to the real 2026-07-19 VPS outage (`now=2026-07-20T00:30Z`): `crypto_hourly`/`orderbook_depth`/`sports_pairs`/`polymarket_macro_pairs` all resolve to a clean `vps_dead` attribution against the actual committed tape, not a synthetic fixture. | this run; extends `scripts/tape_gap_monitor.py`; `tests/test_tape_gap_monitor.py` (12 new + 1 acceptance); re-derived open UNENFORCED set = {} after this row | **test** — `tests/test_tape_gap_monitor.py`'s new collector-bucketing suite, including a real-tape acceptance test; the monitor's own CLI/JSON output now carries `collectors`/`collector_diagnosis` on every hourly-dual family (non-gating — `tape_gap_monitor.py` is a standalone reliability script, not wired into `scripts/invariants.py`'s gate). **The lessons ledger's UNENFORCED backlog is now empty as of this row.** |
| L119 | 2026-07-20 | **A "book notional at touch" descriptor computed as `stored_price * size / 100` silently understates resting-book dollar depth ~100x when `stored_price` is already in dollars (e.g. 0.98), not cents.** Surfaced during this run's Q36 weather_books adequacy audit: a draft liquidity metric reported medians of $2.3-$19.7/market-hour; independently re-deriving with the correct `price_dollars * size` formula gave the true medians, **$215-$1,968** — a real KXTEMPNYCH book is not nearly as thin as the buggy figure implied. Caught by the second of two independent verifier passes on the same claim set; the buggy metric was omitted from the final finding rather than corrected in place, since it was never part of the load-bearing gate-adequacy conclusion (captures-per-market-hour, VPS-attribution, and settlement-join-n all reproduced exactly on both passes and did not depend on this metric). | 2026-07-20 Q36 data-adequacy audit; `findings/2026-07-20-q36-weather-books-data-adequacy.md` | **UNENFORCED** — candidate: any "book dollars"/notional-at-touch descriptor computed from Kalshi's `_dollars`-suffixed price fields (L90) should assert its result is not implausibly below the observed spread × typical size (a liquid hourly market's touch notional should rarely print under ~$50-100) as a cheap units sanity check; no shared helper computes this metric yet, so there is nothing to anchor a static assert to. |
| L121 | 2026-07-21 | Formal disposition of L119 (supersedes its enforcement column; lesson content unchanged: a "book notional at touch" descriptor computed as `stored_price * size / 100` understates resting-book dollar depth ~100x when `stored_price` is already in dollars via L90's `_dollars` convention, not cents). Idle-run policy order (a): re-derived the UNENFORCED queue by whole-word grep (per L108/L112/L116/L118's discipline) and found the open set is exactly `{L119, L120}`; converted the lower-numbered `L119` per the established convention (L120 stays open for a future run — no reachability/priority argument to jump it, unlike L116's explicit L66 skip). L119's own candidate said "no shared helper computes this metric yet, so there is nothing to anchor a static assert to" — so this run BUILT that helper: `core/pricing.py::book_notional_at_touch(price_dollars, size)` computes `price_dollars * size` (never `/100`) and carries a non-gating `UserWarning` units sanity check (fires when `0 < notional < LOW_TOUCH_NOTIONAL_WARN_DOLLARS` ($50), the fingerprint of a cents-vs-dollars bug on an already-dollars price; a zero/empty touch stays silent, and a caller can pass `warn_if_implausibly_low=False` for a genuinely thin market). It lives in `core/pricing.py` (the sanctioned price-arithmetic site, Hard Rule #3's only-arithmetic home) as a natural sibling of `fee_per_contract`/`true_arb_edge`, not a bolted-on module. No live hand-rolled `price*size/100` instance exists in committed code to repoint (L119 records the buggy draft metric was omitted from the final Q36 finding rather than wired in) — the deliverable is the importable helper so the NEXT script has something to call, exactly as L119 asked. Advisory (non-gating), not a raising assert, per the repo's default for anything short of a hard data-integrity violation (same posture as L100/L109/L110's `scripts/invariants.py` advisories) — a legitimately thin market can sit below $50, so a hard gate would false-positive. | this run; supersedes L119; `core/pricing.py`; `tests/test_pricing_book_notional.py` (8 tests); re-derived open UNENFORCED set = {L120} after this row (verified via whole-word grep, same method as L108/L112/L116/L118) | **test** — `tests/test_pricing_book_notional.py` pins the correct `price_dollars * size` computation, a regression that a reintroduced `/100` mistake is caught (off by exactly 100x), the L119 real-numbers case ($215 not $2-$19), and the sanity `UserWarning` firing/silent/disabled/boundary behavior. The `book_notional_at_touch` helper in `core/pricing.py` carries the units sanity warning inline. Not a `scripts/invariants.py` static gate: the mistake is a runtime units error inside a math helper, honestly caught by a pinned unit test + an in-function advisory, not by a file-text scan. The now-open UNENFORCED set is exactly `{L120}`. |
| L120 | 2026-07-20 | **`tape_gap_monitor.collector_diagnosis` (L118) reads a family as ambiguous/unattributed whenever BOTH the named `vps`(:20-29) and `cloud`(:50-59) buckets are zero — but a family whose only-ever-live collector lands in neither named bucket (`other`) will ALWAYS read that way once its actual collector dies, even though the tape itself proves the leg's identity (a consistent, narrow minute range distinct from both named crons).** Surfaced by this run's Q36 audit: `weather_books`' real cloud leg sits at minutes ~00-03 ("other" by L118's own design, verified correct there), so when its VPS leg (minutes 20-29) died on 07-19, the monitor's diagnosis reads `vps=0, cloud=0` → ambiguous, silently missing that "other" also dropped from ~5/day (a co-equal signal, since "other" persisted at its normal ~5-6/day rate throughout — this run's real catch was the VPS-bucket zeroing, not an "other" collapse, so L118's existing logic was not actually wrong here, but the family-level blind spot is real: a monitor consumer cannot currently tell "other is healthy" from "other is the last survivor of a bigger die-off" without doing the per-family minute-histogram read this audit did by hand). | 2026-07-20 Q36 data-adequacy audit; extends L117/L118; `findings/2026-07-20-q36-weather-books-data-adequacy.md` | **test (BUILT — superseded by L122, 2026-07-21, PR #142; stale `UNENFORCED` marker corrected 2026-07-21 by the kb-distiller, lesson text unchanged)** — implemented as `scripts/tape_gap_monitor.py`'s `EXPECTED_COLLECTOR_BUCKETS` map + `diagnose_collector()` (~L225-303), pinned by `tests/test_tape_gap_monitor.py`'s expected-bucket-map suite incl. the real-tape weather_books 2026-07-19 VPS-drop acceptance test (non-gating standalone reliability script). Original candidate (preserved): a per-family expected-collector-bucket map (e.g. `weather_books: {primary: vps, secondary: other}`) so `collector_diagnosis` can name a dead PRIMARY leg even when the surviving leg is "other", instead of only ever naming `vps_dead`/`cloud_dead`. Not built this run (idle-run policy-c scope is diagnosis, not a `tape_gap_monitor.py` code change — L117's own precedent) — row filed only. |
| L122 | 2026-07-21 | Formal disposition of L120 (supersedes its enforcement column; lesson content unchanged: `tape_gap_monitor.collector_diagnosis` (L118) reads a family as ambiguous whenever BOTH named `vps`(:20-29)/`cloud`(:50-59) buckets are zero, permanently blinding it to a family like `weather_books` whose second live collector lands in `other`). Idle-run policy order (a): re-derived the UNENFORCED queue by whole-word grep (`\*\*UNENFORCED\*\*`, per L108/L112/L116/L118/L121's discipline, minus superseded rows) and found the open set is exactly `{L120}` — L121 having just converted L119. Built the proposed fix: `scripts/tape_gap_monitor.py` gains `EXPECTED_COLLECTOR_BUCKETS`, a per-family `{primary, secondary}` bucket map **calibrated against the REAL committed-tape minute histograms** (`tape/<family>/dt=2026-07-18..20`, read not guessed): `weather_books` = `{primary: vps, secondary: other}` (07-18 vps=4098 lines / other=2410; 07-19 & 07-20 vps=0 while other persists 2940/3278 — the VPS primary died, `other` is the survivor); `crypto_hourly` read as the dual-cron anchor (primary :23 vps, secondary :54-55 cloud) and deliberately left OUT of the map because its secondary is the already-named `cloud` bucket, so L118's logic already attributes it correctly (same for orderbook_depth/sports_pairs/polymarket_*). A new `diagnose_collector(family, collectors)` helper routes a MAPPED family through the primary/secondary buckets (names `{dead}_dead: 0 passes in window, {alive} collector still producing` when exactly one expected bucket is zero) and an UNMAPPED family through L118's EXACT vps/cloud logic (zero regression). Both-expected-zero and both-expected-nonzero stay unattributed — L118's "never guess when ambiguous" discipline preserved. Now weather_books' real 07-19 VPS death reads `vps_dead` via its `other` survivor instead of ambiguous, WITHOUT any false positive on a healthy family. 7 new/updated unit tests (mapped dead-primary, mapped dead-secondary, mapped both-healthy, mapped both-expected-zero, unmapped other-only-stays-None no-regression, the `diagnose_collector` helper directly) plus a 5th HARD acceptance test anchored to the real 2026-07-19 weather_books VPS drop (`now=2026-07-20T00:30Z`: vps=0, cloud=0, other>0 → `vps_dead: 0 passes in window, other collector still producing`, and crypto_hourly's unmapped attribution unchanged). | this run; supersedes L120; extends `scripts/tape_gap_monitor.py`; `tests/test_tape_gap_monitor.py` (net +6 tests); re-derived open UNENFORCED set = {} after this row (verified via whole-word grep, same method as L108/L112/L116/L118/L121) | **test** — `tests/test_tape_gap_monitor.py`'s new expected-bucket-map suite, including a real-tape acceptance test; the monitor's `collector_diagnosis`/`alert_reason` now name a dead PRIMARY leg for a mapped family (`weather_books`) even when the survivor is bucketed `other` (non-gating — `tape_gap_monitor.py` is a standalone reliability script, not wired into `scripts/invariants.py`'s gate). **The lessons ledger's UNENFORCED backlog is now empty as of this row.** |
| L123 | 2026-07-21 | **UNENFORCED** — A once-per-UTC-day collector leg gated on exact-hour equality (`if ts.hour == N`) silently FREEZES whenever the scheduler never fires at hour `N` — there is no catch-up, and the failure is invisible (no error, just a tape family that stops growing). Concretely: `collection/hourly_pass.py` gates `settlement_ledger` at `SETTLEMENT_LEDGER_UTC_HOUR=10` and `forecast_collector` at `FORECAST_COLLECTOR_UTC_HOUR=11`, but the live `kalshi-collector` routine runs `cron: 53 */3 * * *` (every 3h → UTC {0,3,6,9,12,15,18,21}) and so NEVER runs at hour 10 or 11; with the VPS `:23` collector dead since 07-19 (L117), `tape/settlement_ledger/` has been frozen at its `dt=2026-07-17` build day (1 day / 5605 rows) and Q36's `n_settled_events>=10` gate sits permanently at 1. `weather_actuals` (hour 12, in the every-3h set) survives, and `universe_sweep` (gated on the SET `{0,6,12,18}`, not a single hour) is robust — the multi-hour gate is exactly what saves it. Root cause is a routine-cadence × gate-hour mismatch compounded by no catch-up: the single-hour legs assume an HOURLY collector (still the `ops/ROUTINES.md` desired state) while the live cadence is every-3h. | `findings/2026-07-21-settlement-ledger-frozen-hour10-deadzone.md`; verifier-CONFIRMED; `collection/hourly_pass.py` (L134/L513 gate); live `kalshi-collector` cron `53 */3 * * *` | **candidate (a) BUILT — advisory + test; candidate (b) still UNBUILT (stale `UNENFORCED` marker corrected 2026-07-24 by the kb-distiller, lesson text unchanged — L25 precedent; note the row's own TEXT also opens with `UNENFORCED` and is left untouched as written history)** — (a) the DETECTION half was built by L124: `settlement_ledger` is registered in `scripts/tape_gap_monitor.py::FAMILY_CONFIG` so its freeze raises a runtime STALE alert instead of stalling silently, pinned by `tests/test_tape_gap_monitor.py::test_acceptance_6_l123_settlement_ledger_frozen_since_build_day`; and the STRUCTURAL half was built by L144: `scripts/invariants.py::_unregistered_single_hour_leg_issues()` / `unregistered_single_hour_leg_warning()` (with `SINGLE_HOUR_LEG_FAMILIES` / `SINGLE_HOUR_LEG_EXEMPT` + `DAILY_CADENCE_FAMILIES`) fails the moment a single-hour committed leg in `hourly_pass.py` is not registered for cadence monitoring, pinned by `tests/test_invariants.py::test_unregistered_single_hour_leg_real_tree_is_clean` (HARD acceptance), `::test_unregistered_single_hour_leg_fires_on_unregistered_known_family`, `::test_unregistered_single_hour_leg_fires_on_unrecognized_new_leg`. (b) Widening the exact-hour gates themselves remains UNBUILT and Ryan/VPS-side (the primary fix is restoring collector cadence); see also L144, which supersedes this row's structural half. |
| L124 | 2026-07-21 | Formal disposition of L123 (supersedes its enforcement column; lesson content unchanged: a once-per-UTC-day exact-hour collector gate silently freezes, invisibly, whenever the live cron never lands on that hour). Idle-run policy order (a): re-derived the open UNENFORCED set (whole-word grep, per L108/L112/L116/L118/L121/L122's discipline) and found it is exactly `{L123}`. Built candidate (a) — the monitor-side half, deliberately the smaller/safer of L123's two proposed fixes (candidate (b), widening a live gate, is explicitly out of scope: "changing live-leg firing gates unattended exceeds the additive-collector self-merge precedent"). Root cause: `scripts/tape_gap_monitor.py`'s `FAMILY_CONFIG` never carried an entry for `settlement_ledger` at all, so its STALE detector was a structural no-op for that family (`interval_h=None` skips the age check entirely; `evaluate_family` falls through to a bare `"ok"` for any family with tape but no config) — the freeze was invisible not because the detector logic was wrong, but because the family was never registered. Added `"settlement_ledger": {"interval_h": 24.0, "passes_per_day": 1, "kind": "daily"}` (same shape as the already-tracked `weather_actuals`/`econ_prints`), which turns the STALE detector on for this family with zero other code changes. `forecast_collector`'s parallel freeze (also named in L123, `FORECAST_COLLECTOR_UTC_HOUR=11` — also outside the every-3h cron set) is explicitly NOT fixed here: it writes to gitignored `data/forecast_tape/`, outside `tape/` entirely, so it is structurally invisible to a monitor that only reads committed tape (per this script's own documented scope) — flagged, not silently left out. One new HARD acceptance test anchored to the real committed tape (`tests/test_tape_gap_monitor.py::test_acceptance_6_l123_settlement_ledger_frozen_since_build_day`, mirrors the acceptance-4/5 pattern): `now=2026-07-21T06:00Z` over the real `tape/settlement_ledger/dt=2026-07-17.jsonl` (last real `captured_at` 2026-07-17T12:23:02Z, age ~89.6h) resolves `alert=True`, `"stale"` in `alert_reason`, `age_hours > 48.0` — the monitor now actually catches the freeze this run's own predecessor (the edge-hunter) found by hand. | this run; supersedes L123; `scripts/tape_gap_monitor.py` `FAMILY_CONFIG`; `tests/test_tape_gap_monitor.py` (+1 acceptance test); re-derived open UNENFORCED set = {} after this row | **test** — `tests/test_tape_gap_monitor.py::test_acceptance_6_...` pins the real-tape freeze as a detected alert (non-gating — `tape_gap_monitor.py` is a standalone reliability script, not wired into `scripts/invariants.py`'s gate, same posture as L118/L121/L122). **The lessons ledger's UNENFORCED backlog is empty again as of this row.** `forecast_collector`'s analogous freeze remains a known, documented, monitor-blind gap (out of `tape/` scope) — not this row's claim to have fixed. |
| L125 | 2026-07-21 | `tape/universe_sweep/` is **~97% auto-generated multi-leg no-offer artifacts** — over the full committed 5-day history (`dt=2026-07-17`..`21`, 300,000 lines / 0 malformed / 100% `real_ask`), **96.97%** of the census is a non-fillable dead tail, ~96.9% of it just two `KXMVE*` series (`KXMVESPORTSMULTIGAMEEXTENDED` 82.21% of the whole census, `KXMVECROSSCATEGORY` 14.68%; every other series <0.02%). Only **3.03%** of lines are FILLABLE (`yes_ask>0 AND yes_ask_size>=1`), 2.89% LIQUID (size>=10), 10.84% carry any activity (`open_interest`/`volume`); `volume_24h` is effectively always-zero (~5/300k rows — the L96 schema defect, confirmed across the full history). A cross-sectional consumer MUST filter to the fillable/active ~3–11% before treating any line as a real quote (a `yes_ask=0.0` no-offer leg is never a $0.00 fill — L1/L105), and per-pass fillable% is unstable (07-18 0.62%–8.09%) because each capped 20-call pass reaches a different arbitrary slice of the >80k cursor (L96 disjoint-slice property), so a single pass is not a reliable liquidity estimate. Storage lens (answers Q46 Ryan design-call (b)): an activity-tier capture filter keeps **10.74% of bytes** (~89% cut, ~71 MB/day → ~7.6 MB/day), a fillable-tier filter 2.89% (~97% cut). Generalizes L105 (single day 07-19, anomaly-sweep use case) to the full history + the whole-census fillable question + the storage-decision lens; restates the L96 illiquidity floor. NOT a strategy claim — the opposite: a crossable/fillable book never appears in the breadth census. | `findings/2026-07-21-universe-sweep-liquidity-census.md`; `scripts/universe_sweep_liquidity_census.py`; `findings/universe_sweep_liquidity_census.json`; idle-run policy (c), verifier CONFIRMED (independent re-count, does not import the census script, reproduced all 6 headline numbers exactly); generalizes L105, cites L96/L1 | **documentation / house-style** — a census fact about a tape family's composition, not statically assertable as a gating invariant (a scanner cannot know a downstream consumer treats an unfiltered `universe_sweep` line as a quote; the honest enforcement is the finding + this row + the reproduce command `python scripts/universe_sweep_liquidity_census.py`). L105's own candidate (a `.claude/agents/edge-prober.md` house-style bullet naming the target-tape assumption) stays open and outside this distiller's write lane (`.claude/agents/` is Ryan-review); recorded here, not converted. Same documentation/discoverability tier as L104/L105's signposts. |
| L126 | 2026-07-21 | **A once-per-UTC-day exact-hour collector gate (L74/L123's failure shape) silently starved `weather_actuals` too — a real 2-day hole (2026-07-19, 2026-07-20) sat in committed tape, invisible to the one tool built to catch exactly this.** `collection/hourly_pass.py` gates `weather_actuals` at `WEATHER_ACTUALS_UTC_HOUR=12`, but the live cloud collector's post-VPS-death (L117) effective cron phase is `≡1 mod 3` (`{01,04,07,10,13,16,19,22}` UTC, reconstructed from the always-on `perp_tape` leg) — it never lands on hour 12, and the VPS `:23` collector that could is dead since 07-19. Confirmed directly against committed tape: `tape/weather_actuals/` has files for `dt=2026-07-16/17/18` (VPS era) and `dt=2026-07-21` (a lucky adjacent-run catch, capture 13:06:33Z, 2 min after a 13:04 cloud pass) but NOTHING for `dt=2026-07-19`/`dt=2026-07-20`. The gap was invisible because `weather_actuals` was simply never added to `scripts/invariants.py::DAILY_CADENCE_FAMILIES` — the exact tool L74/L75 built for this failure class — even though `settlement_ledger`'s twin freeze was already root-caused via L123/L124 in an earlier run today. `forecast_collector` (hour 11, same cron-phase exclusion) remains a documented, monitor-blind, OUT-OF-SCOPE gap (writes to gitignored `data/forecast_tape/`, never reaches `tape/` at all — no fix possible from a tool that only reads committed tape). | this run (idle-run policy c → immediate policy-a-style fix, same session); tape-auditor subagent live cron-phase reconstruction over `tape/perp_tape/`; extends L74/L75/L123/L124; `tape/weather_actuals/` (real 2-day hole) | **test** — `scripts/invariants.py::DAILY_CADENCE_FAMILIES` now includes `weather_actuals` (was `anomalies`, `econ_prints`, `polymarket_cpi_pairs` only); `tests/test_invariants.py::test_daily_cadence_families_includes_weather_actuals` (membership) + `::test_acceptance_l126_weather_actuals_real_gap_detected` (HARD acceptance test anchored to the real committed tape, asserts both `weather_actuals/dt=2026-07-19` and `weather_actuals/dt=2026-07-20` are now flagged). Live-verified: `python scripts/invariants.py --full`'s daily-cadence warning count went from 7 to 9 missing days and now names `weather_actuals` in its family list. Non-gating advisory, same posture as L75/L124 — `forecast_collector`'s parallel freeze stays a known, out-of-scope, unfixed gap (not this row's claim). |
| L127 | 2026-07-21 | `scripts/tape_gap_monitor.py` had `perp_tape` misfiled as `"one-shot-backfill"` (`interval_h=None`) since its 2026-07-16 build, even though `collection/hourly_pass.py` (lines ~56-61, 383-385) runs one `collection.perp_tape` pass EVERY `hourly_pass()` invocation, unconditionally — the exact same cadence as the already-tracked `hourly-dual` families (`sports_pairs`/`crypto_hourly`/`orderbook_depth`/`weather_books`/`polymarket_pairs`/`polymarket_macro_pairs`). Because a `None`-interval family never runs the UNDER-CAPTURE ratio check, `perp_tape`'s real post-L117-VPS-death collapse (30→14→6→7→5 captures/day, 07-17→07-21, ~29% of nominal 48/day) was structurally invisible to the one tool built to catch exactly this — the same L123/L126 failure shape (a growing family miscategorized so its own degradation detector can't fire), this time via wrong `kind` rather than a missing entry. Separately, `perp_tape`'s captures land at minute-of-hour ~00-04 (verified against committed tape) — neither the vps (:20-29) nor cloud (:50-59) bucket, the same "other" signature L120 found for `weather_books`' secondary leg — so an `EXPECTED_COLLECTOR_BUCKETS` mapping was needed too, else the real vps-dead state would read ambiguous (vps=0 & cloud=0) even after reclassification. A second, NOT-fixed defect surfaced in the same audit: `tape/hyperliquid_funding/` — `perp_tape`'s only cross-venue join partner (`scripts/q42_crossvenue_funding_join.py`) — is frozen at a single manual 2026-07-17 backfill (108h+ stale, +1 day/day drift) with no collector ever wired to refresh it; the join's `EXCLUDE`-partial-window logic means every Kalshi funding window after 07-17 silently has no HL counterpart, with no error and no age-alert (a join-critical one-shot leg going stale is, by design, never paged — Q44's "one-shot = age-tracked-only" policy doesn't distinguish "harmless backfill" from "silently strangling a live join"). | tape-auditor subagent deep-dive (idle-run policy c), 2026-07-21; `scripts/tape_gap_monitor.py` `FAMILY_CONFIG`/`EXPECTED_COLLECTOR_BUCKETS`; `scripts/q42_crossvenue_funding_join.py`; `collection/hyperliquid_funding.py` (exists, never scheduled); extends L120/L123/L124/L126 | **test** (perp_tape half) — `"perp_tape"` moved to `{"interval_h": 1.0, "passes_per_day": 48, "kind": "hourly-dual"}` + added to `EXPECTED_COLLECTOR_BUCKETS` as `{"primary": "vps", "secondary": "other"}`; `tests/test_tape_gap_monitor.py::test_acceptance_7_l127_perp_tape_reclassified_hourly_dual` (HARD, anchored to real committed tape: `now=2026-07-21T18:00Z` resolves `alert=True`, under-capture ratio 0.146 < 0.8, `collector_diagnosis="vps_dead: 0 passes in window, other collector still producing"`); `test_one_shot_family_never_alerts` repointed to `hyperliquid_funding` (its remaining valid exemplar) since `perp_tape` no longer belongs to that class. **UNENFORCED** (hyperliquid_funding join-staleness half) — candidate enforcement: (a) wire `collection.hyperliquid_funding` into `hourly_pass.py` (or a daily pass) with an incremental `startTime` = last archived `funding_time`, so it tracks `perp_tape`'s forward cadence instead of freezing at the backfill; (b) a join-partner staleness check (in `tape_gap_monitor.py` or a new script) that age-alerts a one-shot family specifically when a live family depends on it for a join, rather than the current blanket "one-shot = never alerted." NOT built this run — (a) is a real collector-build milestone (Q38-scale scope, not a one-line registration) and (b) is a monitor design decision; both are bigger than one idle-run milestone. |
| L128 | 2026-07-21 | Formal disposition of L127's `hyperliquid_funding` monitor half (supersedes its enforcement column for candidate (b); lesson content unchanged: a join-critical one-shot/backfill tape family going silently stale strangles a LIVE cross-venue join with no age-alert, because Q44's "one-shot = age-tracked-only" policy never distinguished "harmless backfill" from "silently truncating a live join"). Idle-run policy order (a): after the last run filed L127 fresh, the open UNENFORCED set was exactly L127's two candidates — (a) wire `collection.hyperliquid_funding` into a scheduled incremental pass so it tracks `perp_tape`'s forward cadence, and (b) a join-partner staleness detector that age-alerts a one-shot family specifically when a LIVE join depends on it. Built the DETECTION half, candidate (b), deliberately the smaller/safer of the two (candidate (a) is a real collector-build milestone, Q38-scale / Q42-adjacent, not a lesson-to-test conversion — it stays OPEN and out of this distiller's lane). Added `scripts/tape_gap_monitor.py::JOIN_CRITICAL_ONE_SHOT = {"hyperliquid_funding": {"max_age_h": 48.0, "consumer": "scripts/q42_crossvenue_funding_join.py"}}` plus a JOIN-STALENESS branch in `evaluate_family()` that appends a `join_stale` reason (flowing through the existing `would_alert` path) when a JOIN_CRITICAL_ONE_SHOT family's age exceeds its `max_age_h`. The 48h threshold is documented at source: the join finalizes funding windows every 8h, so >48h means ~6 windows silently dropped — mirrors the daily-family 2×24h STALE posture. All other families' STALE/UNDER-CAPTURE/collector-diagnosis logic untouched (zero regression). `test_one_shot_family_never_alerts` was repointed to a non-join-critical unconfigured family so "a family with no cadence config never pages on age" is still covered (`hyperliquid_funding` is no longer a valid exemplar of that class, now that it IS join-critical). After this row the UNENFORCED backlog is empty again — the detection half is enforced; the repair half (candidate (a)) is a flagged collector follow-up, not an UNENFORCED-lesson-conversion item. | this run; supersedes L127's candidate (b) enforcement; `scripts/tape_gap_monitor.py` (`JOIN_CRITICAL_ONE_SHOT` + JOIN-STALENESS detector in `evaluate_family()`); `tests/test_tape_gap_monitor.py` (+`test_hyperliquid_funding_is_join_critical`, `test_join_critical_one_shot_alerts_on_join_staleness`, `test_acceptance_8_l127_hyperliquid_funding_join_stale`); re-derived open UNENFORCED set = {} after this row | **test** — `tests/test_tape_gap_monitor.py::test_acceptance_8_l127_hyperliquid_funding_join_stale` (HARD, anchored to real committed tape: `now=2026-07-21T18:00Z` over the real `tape/hyperliquid_funding/` newest `captured_at` 2026-07-17T06:20:03Z → age 107.67h > 48h → `join_stale` in `alert_reason`), plus `test_hyperliquid_funding_is_join_critical` (config membership) and `test_join_critical_one_shot_alerts_on_join_staleness` (unit-level fire). Non-gating — `tape_gap_monitor.py` is a standalone reliability script, not wired into `scripts/invariants.py`'s gate (same posture as L118/L121/L122/L124/L127-perp_tape). Candidate (a) — wiring `collection.hyperliquid_funding` into a scheduled forward-refresh pass (the actual data FIX) — stays an OPEN, out-of-scope collector-build follow-up, NOT an UNENFORCED-lesson-conversion item; flagged for Ryan / a future collector-build run. **The lessons ledger's UNENFORCED backlog is empty again as of this row.** |
| L146 | 2026-07-22 | **A vendor's "public market data needs no key" documentation is not load-bearing — probe the live API before designing a public-mode collector.** Polymarket US's docs promise keyless market-data reads; a live signed probe (Ryan-interactive session, real `POLYMARKET_US_*` creds on the VPS) FALSIFIED this — every `/v1/` data endpoint 401s unauthenticated, only `/v1/health` is open, and the block is app-level (returned by the gateway to a signed-but-wrong-scope request), NOT a geo-block. The collector was therefore built gated on `POLYMARKET_US_API_KEY` with a `blocked_key` no-op. The flip side is a *feature*, not a limitation: a credential-gated leg with a proven no-public-mode is a structural cloud-safety property — `blocked_key` (zero network, zero files) is the CORRECT steady state on every non-credentialed box, mirroring CLAUDE.md's "the gate is structural (missing credentials), not behavioral." Q33 → UNBLOCKED. | Ryan-interactive session 2026-07-21/22; `findings/2026-07-21-polymarket-us-public-api-probe.md`; `collection/polymarket_us_live.py` | **ledger-only (venue behavior — a live probe, not a static assert) + test** — the `blocked_key` no-network no-op is pinned by `tests/test_polymarket_us_live.py::test_run_blocked_key_makes_no_network_call`; the "docs lie, probe first" methodology is not statically assertable (same terminal class as L15's venue-behavior rows). |
| L145 | 2026-07-22 | **Authenticated ≠ order-capable: the 2026-07-12 execution-lane rule ("Authenticated/order endpoints may exist ONLY in `execution/kalshi_client.py`") targets ORDER verbs and the structural trade-gate — read-only market-data auth (a signed WS handshake, signed GETs to book/market-data paths, with no order verb and no private/fill channel subscription) legitimately lives in `collection/`.** Kalshi's `orderbook_delta` WS channel requires an RSA-signed upgrade even for PUBLIC book data, so `collection/ws_depth.py` must sign — it does so read-only (`GET`+WS-path, query stripped), never subscribing to fills/orders/positions. **LIVE COLLISION found & verified this session:** the existing invariant `inv_order_endpoints_confined` (`scripts/invariants.py`) encodes the STRICTER letter — its regex catches any `KALSHI-ACCESS-(KEY\|SIGNATURE\|TIMESTAMP)` header name — and FIRES on `collection/ws_depth.py` (confirmed by calling the rule directly on the file; `polymarket_us_live.py` is clean, it uses `X-PM-*` headers). ws_depth.py is currently UNTRACKED so `main`'s gate is green today, but it will go RED the moment the file is committed/merged. The invariant's premise ("collectors are read-only public REST by construction") is factually outdated for the WS tier. | Ryan-interactive session (Ryan opened the WS `orderbook_delta` build gate, GOAL.md amended); `collection/ws_depth.py`; `scripts/invariants.py::inv_order_endpoints_confined`; extends the 2026-07-12 Stop-rules amendment | **UNENFORCED — UNRESOLVED COLLISION, flagged to parent/Ryan (NOT touched this docs-only pass).** Resolution is a policy call: EITHER sanction `collection/ws_depth.py` in the invariant's exemption tuple (mirroring the existing `scripts/kalshi_sign.py` exemption) + a matching `tests/test_invariants.py` case asserting ws_depth is exempt AND that a real order-verb inside it would still fire — the honest read, since Ryan opened the WS build gate knowing it signs — OR relocate the signer to `execution/kalshi_client.py` and have `collection/` import it. MUST be settled before ws_depth.py merges to `main` or the gate breaks. Never relax the invariant silently. |
| L132 | 2026-07-22 | **A sequence gap in a streaming feed is DATA, not a drop — generalizes L23 (empty ladder ≠ drop) to the streaming tier.** `collection/ws_depth.py` tracks one `last_seq` per market; when a delta skips a `seq` it emits an explicit `ws_depth.gap.v1` line (expected vs got) and forces a resync (reconnect → fresh snapshot) so the seq chain re-anchors — a gap NEVER passes silently as a clean chain, and the collector never trades/rebuilds off an unverified post-gap book. A snapshot re-anchors the chain (no false gap); a missing `seq` is not a false gap. | Ryan-interactive session 2026-07-21/22; `collection/ws_depth.py` (`SeqTracker`, `process_message`); extends L23/L11 | **test** — `tests/test_ws_depth.py::test_seq_gap_emits_gap_line_and_forces_resync`, `::test_snapshot_reanchors_after_gap`, `::test_missing_seq_is_not_a_false_gap`, `::test_run_driven_gap_triggers_resync_reconnect`. UNENFORCED as a general streaming-tier invariant beyond this one collector (same terminal shape as L23). |
| L133 | 2026-07-22 | **Streaming/delta tape families are orders of magnitude larger than hourly REST, so they need gzip + UTC-midnight rotation from day one.** `collection/ws_depth.py` writes `dt=YYYY-MM-DD.jsonl.gz` by default (with `WS_DEPTH_COMPRESS=none` for readable bring-up) and rotates files at UTC midnight — a convention any future high-rate feed collector should inherit rather than discover under disk pressure. | Ryan-interactive session 2026-07-21/22; `collection/ws_depth.py` (writer); `ops/ws-depth-bringup.md` | **test** — `tests/test_ws_depth.py::test_writer_gzip_round_trip`, `::test_writer_gzip_append_after_reopen_is_valid`, `::test_writer_plain_appends_and_rotates_at_utc_midnight`. Convention-level; UNENFORCED as a cross-family invariant. |
| L134 | 2026-07-22 | **Monitor classification is structure-dependent, not cadence-dependent — and this CLOSES L127's candidate (a)** (the repair half L128 left OPEN as out-of-scope collector-build). `collection.hyperliquid_funding.run_incremental` is now wired into `collection/hourly_pass.py` to run EVERY pass (incremental `startTime` = last archived print), and `scripts/tape_gap_monitor.py` reclassifies the family from `one-shot-backfill` → `{"interval_h": 1.0, "passes_per_day": None, "kind": "hourly"}` (STALE-only) and REMOVES it from `JOIN_CRITICAL_ONE_SHOT` (the mechanism is retained but its map is now empty/dormant for a future genuine one-shot join). Structural insight: a **data-driven single-writer leg** — one write only when a NEW upstream funding print exists — must NOT be typed `hourly-dual` even though two staggered collectors may run it, because the under-capture-ratio / vps-vs-cloud attribution would false-alarm (writes are gated by upstream data availability, not a fixed 48/day cadence); STALE-only is correct. Refines L120/L127. Live smoke this session appended 116 BTC + 116 ETH `broker_truth` prints; the q42 cross-venue join now reads 1179 HL hours, 130/130 windows joined, 0 partial-excluded. | Ryan-interactive session 2026-07-21/22; supersedes the "candidate (a) OPEN" clause of L127/L128 (lesson content unchanged, that half now BUILT); `collection/hourly_pass.py`, `collection/hyperliquid_funding.py`, `scripts/tape_gap_monitor.py`; `scripts/q42_crossvenue_funding_join.py` | **test** — `tests/test_hourly_pass.py::test_hyperliquid_funding_runs_every_pass_and_does_not_alter_counts` (+`::test_hyperliquid_funding_no_new_prints_is_not_a_failure`, `::test_hyperliquid_funding_incomplete_marks_overall_incomplete`, `::test_hyperliquid_funding_raising_marks_incomplete_not_crash`); `tests/test_tape_gap_monitor.py::test_hyperliquid_funding_forward_refreshed_not_join_critical`, `::test_hyperliquid_funding_stale_alerts_within_cadence`, `::test_acceptance_8_l127_hyperliquid_funding_forward_refreshed_catches_freeze_via_stale`. |
| L135 | 2026-07-22 | **An incremental-append collector needs a per-observation dedup identity distinct from the pass-level `capture_id` — here `(coin, time_ms)` — so re-runs and two staggered collectors are idempotent and never double-write a funding print.** `collection/hyperliquid_funding.run_incremental` reads the last committed `time_ms` per coin and only appends strictly-newer prints; paginated fetches dedup on the same key. Without this, the every-pass wiring from L134 would have re-written overlapping history on each invocation. | Ryan-interactive session 2026-07-21/22; `collection/hyperliquid_funding.py`; sibling to L134 | **test** — `tests/test_hyperliquid_funding.py::test_incremental_appends_only_new_prints`, `::test_incremental_idempotent_across_two_collectors`, `::test_committed_time_ms_reads_prior_backfill`, `::test_fetch_paginates_and_dedups_full_series`. |
| L136 | 2026-07-22 | **Python 3.9's `datetime.fromisoformat` REJECTS single-digit fractional-second ISO timestamps (e.g. `...04.7Z`) — a latent parse hole in any tape reader that trusts the stdlib parser.** Live symptom: `tests/test_s17_leadlag_probe.py` fails on clean `main` under Python 3.9 for exactly this reason (confirmed pre-existing — the full suite this session was 1467 passed, 1 failed, and this one failure reproduces on untouched `main`; it was NOT introduced by this session's work). | Ryan-interactive session 2026-07-21/22 full-suite run; `tests/test_s17_leadlag_probe.py` (live symptom on clean main under 3.9) | **invariant (GATING, 2026-07-24) + test — for the NO-NEW-SITES half; the MIGRATION half stays open (L138/L141 scope)** — the shared tolerant parser exists (`core.timeutil.parse_iso_utc`, L138) and `scripts/invariants.py::inv_no_raw_datetime_fromisoformat` now RATCHETS it: any `datetime.fromisoformat(` call site outside the sanctioned `core/timeutil.py` that is not on the `LEGACY_RAW_FROMISOFORMAT_SITES` allowlist — or any allowlisted file whose count exceeds its pin — flips the `--full` exit code and blocks the PreToolUse hook (pins may only shrink). Pinned by `tests/test_invariants.py::test_acceptance_raw_iso_ratchet_real_tree_is_clean` (HARD acceptance over the real tree) + `::test_raw_iso_ratchet_fires_on_new_unlisted_file` / `::test_raw_iso_ratchet_fires_when_legacy_file_exceeds_pin` / `::test_raw_iso_ratchet_does_not_fire_on_date_fromisoformat` / `::test_raw_iso_allowlist_hygiene` (+5 siblings). STILL OPEN: the 33 files / 39 legacy call sites are frozen, NOT migrated — L150 records that migration is behaviour-sensitive per site (tz-aware vs naive), so it remains L138/L141 scope. Runtime is still unpinned (`requires-python = ">=3.9"` vs CI's 3.11) — see L151 for why that gap makes a static ratchet, not a test, the only possible enforcement. |
| L138 | 2026-07-22 | **Closes L136's candidate.** `core.timeutil` already had a private tolerant ISO-8601 parser (`_parse_iso`, built for `parse_kalshi_ts`'s epoch-vs-ISO branch) that fixes exactly L136's Python-3.9 fractional-second gap — it just had no public entry point for a caller parsing a standalone ISO string directly, so `scripts/s17_leadlag_probe.py` (L136's live-symptom file) still called `datetime.fromisoformat` raw in two places (`parse_capture_time`, `parse_window_bound`). Added `core.timeutil.parse_iso_utc(s)` as the sanctioned public wrapper and repointed both call sites at it. This sandbox runs Python 3.11 (where `fromisoformat` already tolerates short fractions), so the fix could not be proven against the literal 3.9 failure here — verified instead by construction: `parse_iso_utc`'s fraction-padding regex is version-independent (it normalizes the string BEFORE calling `fromisoformat`, so 3.9 and 3.11 see byte-identical input), and new tests pin the exact `L136` malformed-input shape (`...04.7Z`, single fractional digit + trailing Z) end-to-end through both `parse_iso_utc` and `s17_leadlag_probe.parse_capture_time`/`parse_window_bound`. The other ~30 files still calling `datetime.fromisoformat` directly (`collection/`, other `scripts/`) were left untouched — out of scope for one milestone; converting them is a separate future pass, optionally backed by a static invariant forbidding raw `fromisoformat` outside `core/timeutil.py`. | `findings/2026-07-22-l136-tolerant-iso-parser.md`; `core/timeutil.py::parse_iso_utc`; `scripts/s17_leadlag_probe.py` | **test** — `tests/test_timeutil.py::test_parse_iso_utc_*` (7 cases: single-digit fraction, Z+short-fraction, no-fraction, full-microseconds, naive-assumed-utc, non-UTC-offset, bad-string-raises) + `::test_parse_kalshi_ts_iso_path_tolerates_short_fraction`; `tests/test_s17_leadlag_probe.py::test_parse_capture_time_z_suffix_with_short_fraction`, `::test_parse_window_bound_short_fraction`. Remaining UNENFORCED residue: the other ~30 raw-`fromisoformat` call sites outside `core/timeutil.py` are not yet migrated or statically forbidden — a future idle run's candidate. |
| L129 | 2026-07-22 | L117's VPS `:23` collector outage (dead since 2026-07-19, tracked through L118/L126/L127) **recovered 2026-07-21T~22:41Z**, immediately after `PR #151`'s self-healing hourly-runner wrapper merged (`22:37:40Z`) — the wrapper's fix (abort a stale `rebase-merge`/`rebase-apply` state older than 2h before pulling, and `git rebase --abort` on a failed pre-pass pull) worked on first deploy. Confirmed three independent ways: (1) two fresh `vps-collector`-authored commits after the merge (`069df6b` 22:48:18Z, `261133e` 23:31:05Z); (2) per-line `captured_at` timestamps inside those diffs are genuinely fresh (`23:23-23:28Z`), not relabeled backfill; (3) `scripts/tape_gap_monitor.py`'s independent `collectors.vps` breakdown (reads only committed tape, no knowledge of commit messages) shows exactly one fresh VPS-bucketed (`:20-29`) pass in its 24h window at `23:23:54Z`. A one-off catch-up pass landed off-cadence at `22:41-22:48Z` (minute 41-48, neither the vps nor cloud bucket) before the collector re-settled into its normal `:23` phase — the expected shape of "wedged for 3 days, unwedged, resumed." This does NOT itself clear the existing `under_capture` alerts (the 24h-window denominator still carries the outage) nor unblock `settlement_ledger`/`hyperliquid_funding` (separate, hour-gated / never-scheduled problems) — a future run should expect those to self-clear over the next ~24-36h of VPS passes rather than re-diagnosing a fresh outage. | `findings/2026-07-22-vps-collector-recovered-post-pr151.md`; idle-run policy (c); extends/closes-out L117/L118/L126/L127's outage thread; independently re-derived against `scripts/tape_gap_monitor.py` live output + raw tape diffs, not just commit messages | **documentation / house-style** — an ops-recovery confirmation, not a gating invariant (there is nothing to statically assert about a collector coming back up; the honest enforcement is the finding + the reproduce command in it). No code change this row. |
| L130 | 2026-07-22 | A temporal-derivative (rate-of-change) reformulation of a depth feature that is ALREADY integrated into the displayed mid does NOT escape the disagreement-subset complementarity trap that killed S22. Q21 idea-gen proposed S46 (touch-queue growth with the mid held fixed as a settlement predictor, claiming the time-derivative was distinct from S22's static imbalance level). Verifier reproduced the L50 ex-post settlement join over `tape/orderbook_depth/` (197 games ≥ the 10-game floor): on the disagreement subset the growth-side hit-rate is 0.15–0.20 vs the mid's 0.80–0.85 across four charitable constructions, with exact arithmetic complementarity (`signal_hit ≡ NOT mid_hit`) — identical to S22. Tightening the growth threshold LOWERS the disagreement hit (0.154), a spoof/noise tell, not a stronger signal. The only diagnostic that matters for any "buy the depth signal, not the mid" proposal is whether the signal beats 0.5 ON THE DISAGREEMENT SUBSET; the level (S22) and its derivative (S46) both fail it. | `findings/2026-07-22-q21-idea-gen-round.md` (S46 verdict); nearest cousin S22 `findings/2026-07-14-ofi-depth-imbalance-s22-verdict.md`; re-runnable via the `scripts/q26_ofi_depth_imbalance_probe.py` join logic over `tape/orderbook_depth/` + `tape/q26_settlement_cache/settlement.json` (both committed) | **documentation / house-style (idea-stage kill rule)** — pre-kills any depth-signal-vs-mid proposal (level OR derivative) at the idea stage before a build; gate it on the disagreement-subset hit-rate test. Same tier as L65/L104 (a fact about the venue/data, not a computation to assert). No code change this row. |
| L131 | 2026-07-22 | `two_sided_share` as computed by the Observatory (`analysis/observatory/features.py`: size-blind, counts a market "two-sided" whenever `0<yes_bid<yes_ask<1` with no size threshold) is NOT a proxy for maker-capturable two-sidedness — on `tape/orderbook_depth/` it is monotonically ANTI-correlated with capturable edge. Q21 idea-gen proposed S47 (select the persistently well-two-sided, wide-enough series as the untested S11 selective-maker lane). Verifier (10,427 two-sided touches): where the book is deep and two-sided the spread is ≤2¢ so the capturable half ≤ the flat 1¢ maker fee (`MAKER_FEE_RATE=0.0175`, S6/S13 death); where the spread clears the fee (≥3¢) the touch queue collapses to ~10 contracts, an L31 nominal-not-capturable wing. The deep-AND-fee-clearing intersection is empty. Compounding kill: `orderbook_depth` has no `volume`/`trade`/`last_price` field, so a maker fill is unobservable at hourly cadence — any fill-sim would synthesize the fill (`synthetic`-as-fill-price, prime-directive-forbidden, the pt1 failure mode). Any future maker screen must gate on touch SIZE at the fee-clearing spread, and cannot claim a fill without a trade-print tape. | `findings/2026-07-22-q21-idea-gen-round.md` (S47 verdict); `analysis/observatory/features.py`, `core/pricing.py`; grave cousins S6/S13/S19/S21/S23 | **documentation / house-style (idea-stage kill rule)** — pre-kills any "select the two-sided books and rest a maker order" proposal keyed on a share-of-markets metric; requires a size-at-fee-clearing-spread gate + a trade-print tape before a fill-sim is built. No code change this row. |
| L137 | 2026-07-22 | **A cross-venue join whose one leg is sourced from a one-shot `backfill`-mode capture that has SINCE been superseded by ongoing `recent`-mode captures must read BOTH modes, or the join silently freezes at the backfill horizon even after the OTHER leg is healed — the consumer-side mirror of L127's collection-side HL freeze.** `scripts/q42_crossvenue_funding_join.py::collect_kalshi_prints` read `mode="backfill"` only, so after L127's HL freeze was healed by PR #153 (L134's incremental refresh) the join STILL returned **130 windows/asset** (through ~07-17) while perp_tape's Kalshi finalized prints already reached 07-22T04:00 in raw committed tape (346 raw KXBTCPERP prints across 1 `backfill` + 73 `recent` captures — only the 1 backfill read). Reading both modes deduped on `(ticker, funding_time)` extends the join **130→146 windows/asset** (span 06-03T20:00→07-22T04:00, 0 partial-excluded) and un-freezes it forward. **Corrects L134's own PR-#153 smoke** ("130/130 windows joined, 0 partial-excluded"), which read the frozen count as healthy. Descriptive re-characterization only — NOT a P&L verdict, no registry change (Q42 part-2 posture); part 3 still BLOCKED(needs-auth). | `findings/2026-07-22-q42-crossvenue-join-recent-mode-unfreeze.md`; `scripts/q42_crossvenue_funding_join.py`; mirrors L127, corrects L134's smoke | **test** — `tests/test_q42_crossvenue_funding_join.py::test_collect_kalshi_both_modes_included_and_cross_mode_dedup` pins the both-mode read + cross-mode dedup AND that `mode="backfill"` alone still freezes at the one window; the CLI `--modes` default and `analyze`'s default now read both. Not statically assertable (a scanner cannot know a probe's join is sourced from a superseded one-shot backfill leg); enforced by the pinned regression + the fixed default. |
| L139 | 2026-07-23 | **`tape/anomalies/` (written by `scripts/anomaly_sweep.py`, run only when `ts.hour == ANOMALY_SWEEP_UTC_HOUR` in `collection/hourly_pass.py` — the same single-exact-UTC-hour gate shape as `settlement_ledger` before L123 and `weather_actuals` before L126) had no entry in `scripts/tape_gap_monitor.py::FAMILY_CONFIG`.** Because `build_report`'s default family list is `list(FAMILY_CONFIG.keys())`, an unregistered family isn't merely mis-scored, it is never evaluated at all — the exact same structural blind spot that let L123/L126's freezes go unnoticed for days, but caught here BEFORE a freeze happened rather than after: real committed tape shows `anomalies` healthy through `dt=2026-07-22` (last real capture 2026-07-22T10:05:33Z), not currently frozen. | idle-run policy (c), 2026-07-23 research loop; `findings/2026-07-23-anomalies-tape-gap-monitor-blind-spot.md`; extends L123/L126's failure-shape class | **test** — registered `"anomalies": {"interval_h": 24.0, "passes_per_day": 1, "kind": "daily-econ-slot"}` in `scripts/tape_gap_monitor.py::FAMILY_CONFIG`; `tests/test_tape_gap_monitor.py::test_acceptance_9_l139_anomalies_was_a_monitoring_blind_spot` (proves visibility + no false alarm on the real healthy state) + `::test_acceptance_10_l139_anomalies_would_be_caught_if_it_ever_froze` (HARD, anchored past the real last capture + 48h, proves the detector would actually page). Non-gating — `tape_gap_monitor.py` is a standalone reliability script, same posture as L118/L121/L122/L124/L126/L127/L128. |
| L140 | 2026-07-23 | **A HARD acceptance test that reads the REAL committed tape must derive its `now` from the family's OWN newest capture at test time — never a hardcoded calendar date — or a routine fresh capture on a healthy, growing family silently flips it red (a born-broken time-bomb that red-lines the gate).** L139's own tests (`test_acceptance_9`/`_10` in `tests/test_tape_gap_monitor.py`, merged into `main` earlier the same day) hardcoded `now` against the assumption the newest `anomalies` capture stayed frozen at 2026-07-22T10:05:33Z; a live collector pass wrote `tape/anomalies/dt=2026-07-23.jsonl` mid-run (capture advanced 09:18→09:30Z within one run), pushing age under the 48h STALE threshold so `alert` correctly went `False` and `_10`'s `assert alert is True` failed. Distinct from L14/L25 (stale-ref / directory-shape): the tape here is correct and fresh — the TEST's time-anchor is the defect. Fix: probe the true newest via `build_report(_REAL_TAPE, far-future)["<family>"]["last_captured_at"]` and set `now = newest + threshold+eps` (stale case) / `newest + small` (fresh case). Generalizes to every real-tape-anchored acceptance test on a still-growing family. | this run (idle-run policy a/c, 2026-07-23 research loop); `tests/test_tape_gap_monitor.py` (no separate findings file — tests-only repair); the broken tests were L139's own additions, merged same-day | **test** — `tests/test_tape_gap_monitor.py::test_acceptance_9_l139_*` / `::test_acceptance_10_l139_*` now anchor `now` to the real tape's own newest `anomalies` capture (robust to fresh daily captures); independently re-derived that the old anchor reproduces the failure and the new one preserves both guarantees (pages when stale, quiet when fresh) and stays robust after an even-fresher line is added. Non-gating (standalone reliability script), same posture as L118/L124/L126/L139. |
| L142 | 2026-07-23 | **An unresolved git merge-conflict marker landed inside committed tape and stayed there, silently corrupting the append-only audit trail, until a data-quality audit found it by hand.** `tape/econ_prints/dt=2026-07-18.jsonl` and `tape/anomalies/dt=2026-07-18.jsonl` were each committed with 3 conflict-marker lines (`=======` / `>>>>>>> 58145d7 (...)` / a dangling `<<<<<<< HEAD`) from a botched VPS-vs-cloud tape merge — invalid JSON sitting undetected in production tape since 2026-07-18 (5+ days, through at least 2 prior audits that didn't grep for it). The surrounding real capture lines were intact and unaffected; only the 6 marker lines themselves were garbage. A conflict marker is never legitimate JSONL and is cheap/unambiguous to detect — this is exactly the class of bug an assertion should catch, not a note. | idle-run policy (c) data-quality deep-dive (`tape-auditor` agent scan), 2026-07-23 research loop; repaired this run (marker lines stripped, all other lines verified unchanged and JSON-valid) | **invariant (GATING)** — new `scripts/invariants.py::_tape_conflict_marker_issues`/`tape_conflict_marker_failure`, wired into `main()`'s tree-scan path (unlike the non-gating tape advisories, this one flips the exit code); `tests/test_invariants.py` (7 cases incl. a HARD acceptance test pinning the real committed tape tree clean). |
| L143 | 2026-07-23 | A cross-family silent-zero/null/constant numeric-field census over all 13 actively-collected tape families (newest committed day each, 4000-line cap) found NO new L96-class collector-mapping defect beyond the already-documented `universe_sweep` `no_ask_size`/`no_bid_size` bug (L96/Q21) — the L96 defect is BOUNDED to universe_sweep, not systemic. Every other always-zero/constant/null flag resolves to legitimate data (crypto `yes_bid`=0 far-OTM wings L26/L31; perp `funding_rate`~0 the Q42-confirmed ±1bp clamp), a config constant (`tick_size`=0.0001, `n_sources`=2, `tolerance_F`=1.0, `n_markets`=6), an honest-accounting nullable (`ticker_parse_error`, `is_provisional`, `expiration_values_disagree`), or a broker_truth-filter consequence (`weather_actuals.*spread`=0, recorded only when the 2 sources agree). Source-tag-enum integrity is clean across all priced families ({real_ask,broker_truth,synthetic} only, 0 MISSING; `orderbook_depth` uses the plural `price_source_tags` map; anomalies/analysis records legitimately carry none). METHODOLOGY CAVEAT: a line-cap census falsely flagged `settlement_ledger.expiration_value` as always-null because that field is populated ONLY for scalar-underlying markets (KXTEMPNYCH temp 72.0/73.0, CPI print values) and the cap truncated before those rows — a market-type-conditional field census must scan full files or stratify by result-type before calling a field always-null (the auditor-side mirror of L96's consumer-side "verify signal non-zero before trusting"). | idle-run policy (c) data-quality deep-dive, 2026-07-23 research loop (research-lead inline read-only census over committed tape; distinct dimension from L142's conflict-marker scan) | **ledger-only** — negative result + methodology caveat; the legit-vs-defect call is per-field interpretive, not statically assertable. Reproducer in scratchpad; recommend hardening into `scripts/tape_field_zero_census.py` + offline tests in a future collector-engineer pass if a standing check is wanted. |
| L141 | 2026-07-23 | **Closes L138's residue** (the ~30 production `datetime.fromisoformat` call sites outside `core/timeutil.py`). Added a non-gating `scripts/invariants.py --full` advisory (`_raw_datetime_fromisoformat_sites`/`raw_datetime_fromisoformat_warning`) that lists every production `.py` line calling `datetime.fromisoformat(` directly instead of `core.timeutil.parse_iso_utc` — the wrapper that normalizes a short (1-2 digit) fractional second or bare `Z` before parsing (the `...04.7Z` shape that crashes Python 3.9's `datetime.fromisoformat`). Scoped to the real hazard only (`\bdatetime\.fromisoformat\s*\(`), NOT `date.fromisoformat` (date-only, no fractional/tz hazard — this module uses it itself, correctly unflagged); `core/timeutil.py` (sanctioned home) and `tests/` (fixtures) exempt; comment lines skipped. 36 occurrences across the tree on today's committed code. Offline-safe (any exception → `[]`) and stderr-only, so it can never flip the gate — same posture as L20/L25/L75/L110. Deliberately does NOT migrate the 36 sites — per-site migration is behavior-sensitive (tz-aware vs naive comparisons) and stays a separate future pass; surfacing the backlog so a NEW raw call can't slip in silently is the enforceable half. | idle-run policy (a), 2026-07-23 research loop (design validated by `research-lead`, applied + gated by the calling session); `scripts/invariants.py`; supersedes L138's `UNENFORCED` residue clause | **invariant (non-gating advisory)** — `_raw_datetime_fromisoformat_sites`/`raw_datetime_fromisoformat_warning` in `scripts/invariants.py::main()` (`--full`/default scan only, never `--pre-edit-hook`/`--db`); `tests/test_invariants.py::test_raw_datetime_fromisoformat_*` (7 cases: finds-real-sites, exempts-timeutil+tests, skips-comments, does-not-flag-date, warning-none-when-empty, message-content, never-gates-exit-code). |
| L147 | 2026-07-24 | **The lessons ledger itself was not protected against ID collisions — two different lesson IDs were each independently assigned to two unrelated rows.** `L130` was shared by a "Polymarket US public API — docs lie, probe first" lesson AND a "temporal-derivative depth feature doesn't escape S22's complementarity trap" (S46 kill) lesson; `L131` was shared by an "Authenticated != order-capable / ws_depth `inv_order_endpoints_confined` collision" lesson AND a "`two_sided_share` is not a maker-capturable proxy / FILL WALL" (S47 kill) lesson — both collisions dated 2026-07-22, evidence of two runs/agents picking a next-ID without checking the ledger's current max, undetected through at least 2 subsequent days of idle-run data-quality audits (L142/L143's censuses scanned tape, not this ledger). Citation-frequency check (grep across `kb/00-LOG.md`, `LOOP-QUEUE.md`, `findings/*.md`) found the "temporal-derivative"/S46 and "FILL WALL"/S47 meanings are the actively-cited ones for L130/L131 respectively (repeatedly referenced in Q21 rounds and idea-gen preps, and correctly still resolve after this row's renumber — spot-checked `kb/00-LOG.md:105,866,963`, `LOOP-QUEUE.md:946,963,1769,1780`, `findings/2026-07-22/-23/-24-*.md`); the "Polymarket US public API" and "ws_depth collision" rows were renumbered instead (L130 -> L146, L131 -> L145; narrative text unchanged, ID column only) and `scripts/invariants.py::inv_order_endpoints_confined`'s L131 self-references (docstring, inline comment, error-message string) + one comment in `tests/test_invariants.py` were repointed to L145. **Verifier-caught correction (this row, same pass):** an earlier draft of this row overstated that the two renumbered rows "were cited only inside this ledger and `scripts/invariants.py`" — false: `kb/00-LOG.md:820,940,942` (append-only, dated 2026-07-22) also cite `L131`/`L130` under their OLD meanings (the ws_depth collision and the Polymarket probe respectively) and were not, and per the file's append-only discipline cannot be, edited to point at L145/L146 — a reader following those three historical citations still lands on the now-renumbered rows' new topics (temporal-derivative/FILL WALL) instead. This is flagged, not silently left inconsistent: see this run's dated `kb/00-LOG.md` entry for the explicit L130->L146/L131->L145 reconciliation table a reader can consult when an old log entry's L130/L131 citation doesn't match the ledger row it now points to. | this run (idle-run policy c-adjacent: ledger-integrity deep-dive, 2026-07-24 research loop; verifier-reviewed, REFUTED-then-fixed on the citation-completeness claim — see kb/00-LOG.md); `kb/lessons/00-lessons.md` (L145/L146 ID columns only); `scripts/invariants.py::inv_order_endpoints_confined` (L131->L145 references); `tests/test_invariants.py` | **invariant (non-gating advisory)** — new `scripts/invariants.py::_duplicate_lesson_id_issues`/`duplicate_lesson_id_warning`, parses `kb/lessons/00-lessons.md`'s `\| L<n> \|` table-row ID column only (prose mentions of an ID inside a row's own narrative text are not counted, so this cannot false-positive on a lesson that merely cites a sibling ID), wired into `main()`'s `--full` advisory path (stderr only, never flips the exit code — same posture as L74/L109/L138/L141/L144). `tests/test_invariants.py`: `test_duplicate_lesson_id_real_tree_is_clean` (HARD — the real ledger now reports zero duplicates), `::test_duplicate_lesson_id_issues_finds_real_duplicate`, `::test_duplicate_lesson_id_issues_ignores_prose_mentions_outside_id_column`, `::test_duplicate_lesson_id_issues_missing_file_is_safe`, `::test_duplicate_lesson_id_warning_none_when_empty`, `::test_duplicate_lesson_id_warning_message_content`, `::test_duplicate_lesson_id_warning_never_gates_exit_code`. |
| L148 | 2026-07-24 | **Generalizes L142** (the conflict-marker-only committed-tape gate) into a full JSON-per-line validity gate — BUT scope matters more than leniency. Extending L142's check to "every line of every committed tape `.jsonl` must parse as JSON" is correct, yet a raw `rglob` filesystem walk also scans UNTRACKED/in-flight files, so a collector mid-append (a torn final line, no trailing newline) would false-fail a GATING invariant and wedge the autonomous loop on data that was never committed. The fix is SCOPE, not torn-line tolerance: restrict the scan to git-TRACKED tape only (`git ls-files -- tape`, filtered to `.jsonl`), with an offline-safe empty-set fallback (any git failure → skip, never wedge). A committed torn/truncated line IS real corruption and must STILL be caught — leniency there would defeat the purpose. The new gate explicitly does NOT double-report L142's own conflict-marker lines (those keep L142's dedicated diagnostic). Verifier-CONFIRMED: independently re-scanned the real committed tape (212 `.jsonl` files, ~1.06M non-empty lines) → 0 malformed; genuinely gating; correct `*.jsonl` scope; no double-report. | idle-run policy (a), 2026-07-24 research loop; parent L142; verifier CONFIRMED core logic + REFUTED-then-fixed the raw-walk scope bug same commit; `scripts/invariants.py` (`_tape_invalid_jsonl_issues`/`tape_invalid_jsonl_failure`/`_git_tracked_jsonl`); `tests/test_invariants.py` | **invariant (GATING)** + test — `scripts/invariants.py::_tape_invalid_jsonl_issues`/`tape_invalid_jsonl_failure` wired into `main()`'s GATING tree-scan path alongside L142's conflict-marker gate (flips the exit code), scoped to `git ls-files -- tape` `.jsonl` only, offline-safe empty-set fallback. Pinned by ~10 tests in `tests/test_invariants.py` incl. both-direction regressions (an UNTRACKED torn final line is NOT flagged; a TRACKED malformed line STILL is) + a HARD real-tree test asserting the current committed tape parses clean (212 files, ~1.06M lines, 0 malformed). |
| L149 | 2026-07-24 | **`hourly_pass` unit tests must stub EVERY hour-gated collector leg, not just the always-run ones.** A leg gated on `ts.hour == N` (`settlement_ledger`/hour 10, `weather_actuals`/hour 12, `forecast_collector`/hour 11, `universe_sweep`/hours {0,6,12,18}) makes a REAL settlement/network call whenever the suite happens to run at that UTC hour if the test leaves it unstubbed — silently doing live I/O, and flaking `assert rc==0` once per UTC day at that hour. Caught this run: `tests/test_hourly_pass.py::test_main_wires_sports_limit_and_crypto_symbols` and `::test_main_returns_nonzero_on_incomplete_pass` never stubbed `hp.settlement_ledger`, so at UTC hour 10 (`SETTLEMENT_LEDGER_UTC_HOUR=10`) `hourly_pass.main()` made a real settlement_ledger network call that errored in-sandbox (the first test's assertion flaked, the second did live I/O silently). The same root also produced this run's stray live-capture tape side-effect (a pytest run wrote two real hour-9 captures to `tape/anomalies`/`tape/econ_prints`, deliberately left untracked/excluded from the commit). Stubbing all hour-gated legs makes both tests wall-clock-independent. | this run (idle-run policy a, 2026-07-24 research loop; verifier-REFUTED-then-fixed a pre-existing wall-clock flake); `tests/test_hourly_pass.py` (both fixed tests) | **test** — both `tests/test_hourly_pass.py::test_main_wires_sports_limit_and_crypto_symbols` and `::test_main_returns_nonzero_on_incomplete_pass` now stub every hour-gated leg (`settlement_ledger`/weather_actuals/forecast_collector/universe_sweep), so they are wall-clock-independent (no live I/O at any UTC hour). Not statically assertable (a scanner cannot know which stub a test must inject for the current wall-clock hour); enforced by the fixed tests. |
| L144 | 2026-07-24 | **Closes L123's STRUCTURAL residue** (supersedes its enforcement column for the "someone added a single-hour committed leg and forgot to register it for monitoring" half; lesson content unchanged: a once-per-UTC-day `if ts.hour == N` collector leg silently FREEZES — no error, no catch-up — whenever the scheduler never lands on hour N). L124 built the TRAILING-freeze DETECTION half (registered `settlement_ledger` in `scripts/tape_gap_monitor.py`'s runtime STALE check) and L126 fixed `weather_actuals` by hand — but the same class bit TWICE because nothing statically caught an unregistered single-hour committed leg, and `settlement_ledger` itself was STILL missing from `scripts/invariants.py::DAILY_CADENCE_FAMILIES` (its `tape/settlement_ledger/` froze at the `dt=2026-07-17` build day — the direct data-adequacy blocker on Q36). This run closes the STRUCTURAL registration-completeness half: (1) added `settlement_ledger` to `DAILY_CADENCE_FAMILIES` (tied to `SETTLEMENT_LEDGER_UTC_HOUR=10`); (2) added a deterministic, offline, NON-gating meta-guard (`_unregistered_single_hour_leg_issues`/`unregistered_single_hour_leg_warning`) that parses `collection/hourly_pass.py`, finds every single-hour committed leg (`ts.hour == *_UTC_HOUR`, EXCLUDING the plural `*_UTC_HOURS` `universe_sweep` set-gate), resolves the committed tape family/families it writes via `SINGLE_HOUR_LEG_FAMILIES`, and advises if any is NOT in `DAILY_CADENCE_FAMILIES` and NOT documented-exempt (`FORECAST_COLLECTOR_UTC_HOUR -> gitignored data/forecast_tape/`) — an UNRECOGNIZED new `*_UTC_HOUR` is SURFACED, not silently passed, so the NEXT unregistered leg trips CI instead of freezing invisibly. Honest scope: the WALL-CLOCK trailing-staleness half stays L124's `tape_gap_monitor.py` job, deliberately NOT duplicated here (a wall-clock check would be flaky/non-deterministic in an offline gate). Interior-skip note: `_daily_family_gap_issues` skips a family with <2 dt files, so registering `settlement_ledger` created no synthetic interior gap — but its REAL 07-18..07-21 freeze (now 2 committed dt files: 07-17 build + a fresh 07-22 VPS pass, contra the finding's 1-file assumption) is correctly surfaced by the existing L74 advisory (8->12 missing days), a true positive, NOT the meta-guard firing (the meta-guard is clean on the real tree). | this run (idle-run policy a, 2026-07-24 research loop); supersedes L123's structural residue; extends L124/L126; `findings/2026-07-21-settlement-ledger-frozen-hour10-deadzone.md`; `scripts/invariants.py` (`DAILY_CADENCE_FAMILIES` += `settlement_ledger`, `SINGLE_HOUR_LEG_FAMILIES`/`SINGLE_HOUR_LEG_EXEMPT` + the meta-guard); `tests/test_invariants.py` (+10 tests) | **invariant/test** — `scripts/invariants.py::_unregistered_single_hour_leg_issues`/`unregistered_single_hour_leg_warning` wired into `main()`'s `--full` advisory path (non-gating stderr, never flips exit code — same posture as L74/L109/L138/L141); `settlement_ledger` now in `DAILY_CADENCE_FAMILIES`. Pinned by `tests/test_invariants.py::test_daily_cadence_families_includes_settlement_ledger` (membership), `::test_unregistered_single_hour_leg_real_tree_is_clean` (HARD — the real repo tree produces no advisory now that it's registered), `::test_unregistered_single_hour_leg_fires_on_unregistered_known_family` + `::test_unregistered_single_hour_leg_fires_on_unrecognized_new_leg` (prove it CATCHES the next-variant bug), plus plural-`*_UTC_HOURS`-ignored / forecast-exempt / warning-message-content / never-gates-exit-code / missing-file-safe cases. The trailing-freeze DETECTION half remains L124's `tape_gap_monitor.py` registration (not this row's claim). |
| L150 | 2026-07-24 | **The Python-3.9 `datetime.fromisoformat` hazard (L136/L138) is LIVE in committed tape at scale — not theoretical.** A full-file census (`scripts/tape_timestamp_parseability_audit.py`, read-only) found **38.27%** of committed ISO timestamp values (638,730 / 1,669,146 over the canonical `dt=` tape; **653,208 / 1,725,070 = 37.87%** incl. probe-cache `.jsonl`, two independent agents — tape-auditor + verifier — reproduced this to the EXACT integer) would crash a strict 3.9 `fromisoformat` consumer. Dominant cause is a bare trailing `Z` (~99.6% of hazards; 3.9 needs `+00:00`); plus the literal L136 short fraction `.7Z`/`.71Z` in `sports_history.occurrence_datetime` (~36); plus a **NEW shape L136 never named — 9-digit NANOSECOND fractions** in `crypto_hourly.exchange_time` (exactly **1,409**). **0 genuinely-corrupt values**, and `core.timeutil.parse_iso_utc` parses **100%** including the nanosecond shape (it truncates the fraction to 6), so the wrapper is the COMPLETE fix and the L138/L141 raw-`fromisoformat` backlog (36 code sites) is an URGENT live landmine on any 3.9 runtime, not a latent one. Any tape reader must go through `parse_iso_utc`; never raw `fromisoformat`; never assume <=6 fractional digits. (Sub-reason label nuance, flagged not patched: 4-5 digit fractions fold into the `short_frac` bucket — the hazard flag is correct, the label is imprecise.) | idle-run policy (c), 2026-07-24 research loop; `findings/2026-07-24-tape-timestamp-parseability-audit.md`; two-agent (tape-auditor + verifier exact-integer agreement); instantiates L136 and **closes L138's explicitly-open "the fix could not be proven against the literal 3.9 failure here" question**; extends L141's code-site advisory with the tape-DATA evidence | **test** — new re-runnable census `scripts/tape_timestamp_parseability_audit.py` (+12 offline tests `tests/test_tape_timestamp_parseability_audit.py`: classifier per shape incl. the 3.11-accepts-but-hazardous `.71Z` case, the load-bearing `parse_iso_utc`-parses-every-shape check, end-to-end URGENT/BENIGN). NOT wired into `scripts/invariants.py`'s gate (a full-tape ~1M-line scan is too slow for the pre-commit path — standalone diagnostic, same posture as `tape_gap_monitor.py`). The actual MIGRATION of the 36 raw call sites stays a separate behaviour-sensitive pass (tz-aware vs naive per site, L138/L141 scope) — this row's enforceable half is the standing reproducer + the confirmed-complete wrapper fix + the surfaced nanosecond shape. |
| L151 | 2026-07-24 | **A runtime-version-conditional hazard is structurally invisible to a test suite that only ever runs the permissive version — the enforcement has to be a static ratchet, not a test.** `pyproject.toml` declares `requires-python = ">=3.9"`, but CI and every sandbox run Python 3.11.15, where all three hazardous ISO shapes (`...04.7Z` short fraction, bare `Z`, 9-digit nanosecond fraction) parse FINE under `datetime.fromisoformat`. So the 1,612-test suite is green no matter how many raw `datetime.fromisoformat(` call sites exist, and can never catch a NEW one — exactly how the debt grew to 33 files / 39 sites while L136/L138/L141 were open (L141's advisory could see them but, being non-gating, could not stop the next one). The generalizable rule: when a hazard is conditional on a runtime/config the test environment does not exercise, a passing suite is not evidence — convert the hazard to a SOURCE-level assert, and where the existing debt is too behaviour-sensitive to migrate in one pass, ratchet it (pin today's count per file; pins may only shrink) so the debt is frozen instead of growing while the migration waits. | this run (idle-run policy (a)); L136 (the 3.9 rejection), L150 (`scripts/tape_timestamp_parseability_audit.py`: 38.27% of committed ISO values, 638,730/1,669,146, would crash a strict 3.9 consumer); `scripts/invariants.py`; measured 33 files / 39 sites on the 2026-07-24 tree | **invariant (GATING) + test** — `scripts/invariants.py::inv_no_raw_datetime_fromisoformat` (registered in `STATIC_INVARIANTS` as `no_raw_datetime_fromisoformat`; ratchet allowlist `LEGACY_RAW_FROMISOFORMAT_SITES`; sanctioned site `SANCTIONED["iso_parse"] = core/timeutil.py`, which calls the stdlib parser legitimately AFTER zero-padding the fractional field). Fires on any non-allowlisted file with >=1 raw call and on any allowlisted file exceeding its pin; passes silently below a pin so migration is always allowed. `date.fromisoformat(` is deliberately NOT matched (a `YYYY-MM-DD` day token has no fraction and no `Z`). Pinned by 10 tests in `tests/test_invariants.py`, load-bearing one = `::test_acceptance_raw_iso_ratchet_real_tree_is_clean`. The residual runtime-pin option (require 3.11+) stays open and would make the ratchet redundant — it is the cheaper terminal fix if Ryan ever drops the 3.9 floor. |
| L152 | 2026-07-24 | **Stale `UNENFORCED` markers in THIS ledger are themselves a bug class: they mis-route the idle-run work queue.** Idle-run policy (a) says "pick an `UNENFORCED` lesson and enforce it", and the STATUS column is the only index into that queue — so a row whose enforcement was actually built by a later run, but whose status was never updated, silently advertises already-done work. It cost real cycles this run: the lead selected L109 (GC dispatch for orphaned directory-shaped `dt=` days), then found `scripts/invariants.py::_tape_dir_shape_orphan_classification` / `tape_dir_shape_orphan_warning` already implementing it, tests and all. Verified-stale and status-corrected this run (lesson text untouched, per the L25 precedent of 2026-07-21): **L74** (built: `_daily_family_gap_issues` / `daily_family_gap_warning` + 5 tests incl. the real-tape acceptance `test_acceptance_l126_weather_actuals_real_gap_detected`), **L109** (built: the orphan classifier + 5 tests incl. `test_orphan_classification_matches_real_committed_tree`), **L123** (candidate (a) built in two halves — L124's `tape_gap_monitor.py` STALE registration and L144's `_unregistered_single_hour_leg_issues` meta-guard, each with tests; candidate (b), widening the exact-hour gates, genuinely remains unbuilt and is recorded as such). The rule: the run that BUILDS an enforcement owns updating the originating row's status in the same pass — and a distiller picking an `UNENFORCED` row must grep the tree for an implementation BEFORE scoping work. | this run (idle-run policy (a) mis-route on L109); L25's 2026-07-21 status correction (same class, same fix); `scripts/invariants.py`, `tests/test_invariants.py`, `tests/test_tape_gap_monitor.py` | **invariant (non-gating advisory) — the weaker proxy this row proposed IS now built (2026-07-25, idle-run policy (a); lesson text unchanged — this row's own rule: "the run that BUILDS an enforcement owns updating the originating row's status in the same pass").** The full claim ("is this row's enforcement actually absent?") remains genuinely undecidable per the reasoning below, unchanged — but `scripts/invariants.py::_stale_unenforced_candidate_issues`/`stale_unenforced_candidate_warning` now implements the named high-precision proxy: it parses every lesson row, restricts to rows whose ENFORCEMENT column still starts with the bold `**UNENFORCED**` marker, extracts backtick `function_name()` tokens containing an underscore from THAT column only (never lesson-narrative text — L105's row cites the pre-existing `_segment_bounds()` as background context, not a candidate, and would false-positive if lesson text were in scope; regression-tested), and flags any name already `def`-ined somewhere in the tree. Bare script-PATH matching was deliberately excluded (almost every candidate names an already-existing file as the site to extend, so it would carry no signal). Wired into `main()`'s `--full` advisory path (stderr only, never flips the exit code). Pinned by 10 tests in `tests/test_invariants.py`, including a HARD real-tree acceptance test (currently 0 issues among the 19 genuinely-open `UNENFORCED` rows) and a fixture regression test reproducing the L105 near-miss shape. The original text below is retained as written history: "Is this row's claimed-missing enforcement actually absent?" is not statically decidable: an enforcement can live in an invariant, a unit test, a monitor, or LOOP-QUEUE prose, under any name, so no regex maps a row to its implementation. A weaker but assertable proxy exists and is NOT built here (out of this milestone's scope): an advisory that flags any `UNENFORCED` row whose text names a concrete `scripts/`-path or `function_name()` that now EXISTS in the tree — a high-precision subset of the stale cases, including L74/L109 above. Filed as the next candidate for this row rather than guessed at now. |
| L154 | 2026-07-25 | **A float-size contract that is only DOCUMENTED gets violated by the very lane that most depends on it: the paper fill model.** L47's rule ("never coerce a ladder level's size to int without an explicit, justified rounding rule") was written into `collection/normalize.py::normalize_snapshot`'s docstring at the shared normalizer — and yet `execution/fill_models.py::_taker_depth`, the code path that produces every `taker_depth` paper fill, carried a bare `take = min(remaining, int(size))` with no stated rounding rule, no L47 citation, and no test pinning the intent. The violation is REAL but numerically BENIGN and I am not inflating it: `int(size)` on a non-negative float IS a floor, which is the conservative direction (it under-states liftable depth, never manufactures it), the real tape carries no negative or NaN sizes, and a full replay of real committed ladders through the old and new code produced byte-identical fills — **zero paper P&L moved**. The residual exposure was sub-contract per level (max 0.999 contracts of unlifted depth) plus the latent crash/negative-depth path on a malformed size. The generalizable point is about ENFORCEMENT PLACEMENT, not about this one truncation: a docstring at the producer (`collection/`) does not bind a consumer in another lane (`execution/`), and a rule with no single callable home gets re-implemented inline at every consumer — the same re-derive-per-site pattern L33/L35/L36/L49 closed for the bootstrap, magnitude-gate, floor-precheck and close-time helpers. Real-tape census supporting L47's premise, re-derived independently this run over all 17 files of `tape/orderbook_depth/dt=*.jsonl` (`yes_bids`+`no_bids` levels; these are DESCRIPTIVE level COUNTS, not prices — the ladders come from records tagged `price_source_tags={"asks":"real_ask","bids":"real_bid"}`): **14,756,132 levels · 747,412 fractional (5.07%) · 5,832 with 0 < size < 1 · 0 zero-size · 0 negative · max 13,209,380.2**. An audit of every int-coercion site across `execution/`, `core/`, `collection/`, `scripts/`, `analysis/`, `validation/` found this to be the ONLY one — the queue-aware fill-sims (S14/S19/Q24/Q27/Q30) already keep queue-ahead depth as a float (`queue_ahead_at`, `bid_size_at_or_above`, `core.depth.capturable_depth` all return `float` and compare with a `- 1e-9` tolerance), so **no published verdict carries an optimistic queue-truncation bias**. (Wording precision, verifier-flagged 2026-07-25: Q37 was originally listed among the queue-aware fill-sims here and should not have been — `scripts/q37_weather_summer_makerno_probe.py:438` computes `queue_ahead` as reported CONTEXT only; its actual fill rule at :441-447 is TOUCH-based. Q37 is float-clean either way, so the audit conclusion is unchanged; only the label was imprecise.) | this run (2026-07-25 idle-run enforcement pass, policy (a)); escalates L47; `execution/fill_models.py::_taker_depth`; `core/depth.py::whole_contracts_available`; `collection/normalize.py::normalize_snapshot`; census over the `tape/orderbook_depth` family (17 files, dt=2026-07-07..2026-07-24) | **helper + test** — fixed by pointing `_taker_depth` at the new `core.depth.whole_contracts_available` (one documented floor rule, one home) and pinned by `tests/test_ladder_size_float_contract.py`, whose real-tape replay test (`test_paper_pnl_unchanged_replaying_real_tape_ladders`) is the standing proof that the swap moved no paper P&L and would fail if a future edit changed the rounding. The recurrence guard is the L47 advisory in `scripts/invariants.py` (`_ladder_size_coercion_issues`), which reports 0 issues on the clean tree — but that is a PARTIAL guard whose coverage is only the shape set its constructed-negative corpus pins (see L47's enforcement column for the fires-on set and the deliberate blind-spot set, and L155 for why 0-on-clean-tree is evidence of precision, not recall). Notably the exact shape `_taker_depth` would most plausibly regress into via a copy from `analysis/observatory/features.py:160` (`int(no_bids[0][1])`) IS covered, but e.g. a renamed intermediate or a multi-line call is not; the hard pin against a P&L-moving regression is the real-tape replay test, not the advisory. The broader placement lesson ("a producer-side docstring does not bind a consumer in another lane; give a rule one callable home") is **protocol** — not statically assertable in general, but now instantiated for this rule. |
| L155 | 2026-07-25 | **A lexical advisory reporting 0 issues on a clean tree is evidence of PRECISION only, never of RECALL.** "0 hits" answers "does this rule fire on correct code?" and says nothing about "would it fire on the bug?" — recall can only be established by a CONSTRUCTED-NEGATIVE corpus: a set of realistic violation shapes asserted to FIRE, checked in alongside the rule. It follows that an enforcement's ledger row must state the **tested** shape set, not the intent; otherwise the kb records coverage the code does not have, and a future run reads the row, believes the class is guarded, and stops looking. Instance from this run: L47's new advisory (`scripts/invariants.py::_ladder_size_coercion_issues`) reported 0 sites on the clean tree and its L47 row claimed it "flags any NEW `int()`/`round()`/`floor()`/`ceil()` on a size-ish name". A verifier probe of **15 realistic shapes refuted that in 10 of the 13 genuine-violation cases** — including `int(no_bids[0][1])`, the double-subscript form `analysis/observatory/features.py:160-161` ALREADY writes for `touch_queue` (i.e. the single likeliest reintroduction path was invisible), plus `int(float(size))`, `math.trunc(size)`, bare `int(depth)`, `int(row[1])`, renamed intermediates, size-ish prefixes, multi-line calls, `size // 1` and `'%d' % size`. This **generalizes L151** ("when a hazard is conditional on a runtime the test environment does not exercise, a passing suite is not evidence") from runtime-conditional hazards to LEXICAL-PROXY coverage claims: L151's suite was green because the hazardous input was never presented; this advisory was green because the hazardous SHAPE was never presented. Same failure mode, different axis — absence of a signal from an instrument nobody probed is not evidence of absence. Corollary, applied here: where widening a lexical rule to catch a shape would false-positive on legitimate code (`depth` is an already-integer level COUNT in the orderbook_depth schema; `row`/`pair`/`entry` are ordinary iteration names), the honest terminal state is a DOCUMENTED, regression-tested recall hole, not a guessed-at wider regex. | this run (2026-07-25 idle-run enforcement pass, verifier attack on the L47 milestone); the verifier's 15-shape adversarial probe (13 genuine-violation shapes, 10 missed pre-fix, 3 widened post-fix, 7 recorded as deliberate holes); L47 and L154 enforcement columns corrected in the same pass; generalizes L151 | **test (a constructed-negative corpus, the enforcement this row prescribes) + protocol** — `tests/test_invariants.py::test_ladder_size_coercion_fires_on_tested_shape_set` (15 parametrized shapes asserted to FIRE) and `::test_ladder_size_coercion_known_blind_spots_are_misses` (12 parametrized shapes asserted to MISS, so the documented hole is itself regression-tested and a future widening must delete an entry ON PURPOSE). The matcher was widened for the three shapes where precision was safe (multi-subscript `no_bids[0][1]`/`ladder[i][1]`, nested cast `int(float(size))`, `math.trunc`) while holding 0 issues on the real tree; the rest are recorded as holes in code comments, in `ladder_size_coercion_warning`'s message text, and in L47's enforcement column. The GENERAL rule ("every new lexical/static advisory ships with a fires-on corpus, and its ledger row states the tested shape set") is **protocol**, not itself statically assertable — no regex can tell whether an arbitrary advisory's test file constitutes an adequate adversarial corpus; that is a reviewer/verifier judgment, which is exactly what caught this one. Addressed to any future run adding a rule to `scripts/invariants.py`. |
| L156 | 2026-07-25 | **A detector with no scheduled caller is not enforcement — it is documentation that happens to be executable.** `scripts/tape_gap_monitor.py` is a *correct* detector: it names `collector_diagnosis="vps_dead"` on six families (`sports_pairs`, `crypto_hourly`, `orderbook_depth`, `weather_books`, `polymarket_macro_pairs`, `perp_tape`; 10 families alerting in total) and would have named it at ANY moment across the 61.7h window in which the VPS `:23` leg was silent and nobody noticed. It named it to no one. The precise, non-inflated framing (an earlier draft said "the detector worked for 61h and paged nobody", which the verifier REFUTED): a whole-repo grep finds the monitor has **no scheduled caller at all** — no cron, no workflow, no `collection/` call site — and its only escalation path is a documented no-op, `[tape_gap_monitor] alerts present but no NTFY_TOPIC_URL / --ntfy-url supplied — ntfy POST skipped`. So the failure is not "the alarm rang into an empty room"; it is "the alarm was never wired to a clock." The generalizable rule: a monitoring capability only counts as enforcement once it is attached to **the one command every run is REQUIRED to run and to read** — in this repo that is `scripts/invariants.py --full` (LOOP-QUEUE step 4), not a standalone script whose invocation depends on somebody remembering. Corollary for severity-vs-gating: attaching a check to the gate command does NOT require making it gating; a dead VPS cron is severe AND physically un-fixable from a cloud sandbox, so gating on it would convert one silent failure into an indefinite loop halt. Non-gating-but-always-printed is the honest middle. | `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md` (idle-run policy (c) escalated to policy (a); verifier CONFIRMED post-correction, and imposed this row's framing); the outage itself (last VPS capture `2026-07-22T17:29:49.498223+00:00`, silence 61.7h at `2026-07-25T07:12Z`); cites L117/L118/L129 | **invariant (advisory, non-gating) + test** — enforced by THIS run: a dead-collector-leg advisory now lives in `scripts/invariants.py`, computed from committed tape only (no network, no git, no commit-author strings), importing the leg minute-signatures and family config from `scripts/tape_gap_monitor.py` rather than duplicating them. Named thresholds `DEAD_LEG_SILENCE_HOURS=24.0`, `DEAD_LEG_ALIVE_HOURS=6.0`, `DEAD_LEG_LOOKBACK_DAYS=10`; it fires ONLY on the "one of two staggered collectors died" signature (a leg silent >=24h while another produced within 6h), reports AMBIGUOUS and never guesses a name when both scheduled legs are silent (L118/L120 attribution discipline), and prints to stderr **without ever touching the exit code**. Pinned by `tests/test_dead_collector_leg_advisory.py`. Live output on current tape names the vps leg, its last-seen timestamp, the 61.4h silence, and that cloud/other are alive. |
| L157 | 2026-07-25 | **A "RECOVERED" declaration is worthless without a stated DWELL window — a point observation cannot distinguish a fixed collector from one that will die again tomorrow.** L129 / `findings/2026-07-22-vps-collector-recovered-post-pr151.md` declared the VPS `:23` leg recovered on the strength of a single fresh pass. The recovery dwelled **18.8h** measured from L129's declared recovery moment (`2026-07-21T22:41Z`), or **18.1h** measured from the first `:23`-cadence pass (`2026-07-21T23:23:01Z`) — **always state which anchor you are quoting.** (An earlier draft of this run said "17h"; that silently dropped the `2026-07-21T23:23:01Z` pass, which is the very pass L129 cited as its recovery evidence. Do not use 17h.) Then it died again, and stayed dead 61.7h unnoticed. **Rule: no collector-recovery finding may be filed without >=24 consecutive hours of the expected signature bucket observed post-restart, and the finding MUST state the dwell it actually observed.** A recovery claim with no dwell number is a hypothesis wearing a verdict's clothes — and it is worse than no claim, because it retires the queue's attention on the outage. | `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md`; supersedes the confidence (not the observations) of L129 / `findings/2026-07-22-vps-collector-recovered-post-pr151.md` | **test + non-gating advisory (BUILT 2026-07-25, idle-run policy (a); lesson text unchanged, stale `UNENFORCED` marker corrected by the kb-distiller convention)** — `scripts/invariants.py::_recovery_dwell_issues`/`recovery_dwell_warning`, wired into `main()`'s `--full` stderr-only path (never appended to the gating `failures` list; every failure mode — detector exception, non-str formatter return, `BaseException`/`SystemExit` from the formatter — is pinned to leave the exit code untouched). Headline-scoped lexical proxy per L155's own discipline: recovery-class membership reads the filename slug + first `# ` H1 only (a body-wide match would be a ~16x precision disaster on the 2026-07-25 tree — 16/83 findings mention "recover" in the body, only 1 in its headline), and the advisory states its TESTED coverage rather than its intent. `tests/test_recovery_dwell_advisory.py` (48 tests): a constructed-positive corpus (must fire), a constructed-negative corpus incl. the L162-style narrow supersession escape hatch, a constructed-blind-spot corpus of genuine misses asserted as misses (repair-verb headlines, H2-only claims, outage-duration-read-as-dwell — measured recall 18/22 on the full adversarial corpus, honestly documented rather than silently widened), and a HARD acceptance suite over the real `findings/` tree. Verified live against the real tree: correctly flags the known-defective `findings/2026-07-22-vps-collector-recovered-post-pr151.md` (no stated dwell) and stays silent on the 07-21 ("still dead on day 3") and 07-25 ("SECOND death") findings, whose headlines are not recovery claims. See `findings/2026-07-25-l157-recovery-dwell-advisory.md`. |
| L158 | 2026-07-25 | **Commit-author archaeology is UNSOUND on a squash-merge repo — collector-liveness must be derived from tape content, never from authorship.** `git log HEAD --author=vps-collector` returns **ZERO** commits, despite ~330 such commits genuinely existing on side refs; `main` squash-merges, so the author identity is erased on the mainline, and the newest VPS commit is unreachable from any ref in a fresh clone. Any liveness/attribution claim built on `git log --author`, commit messages, or committer timestamps will therefore read a live collector as dead (or, on a differently-shaped ref set, a dead one as live) purely as an artifact of merge policy. The sound method is per-line `captured_at` **minute-of-hour bucketing** (vps = minutes 20-29, cloud = minutes 50-59) restricted to `kind=="hourly-dual"` families, per `scripts/tape_gap_monitor.py`'s `COLLECTOR_MINUTE_BUCKETS` — tape content is the durable artifact; git topology is not. Same durability principle applies to citing WHERE a tape line came from: cite its `captured_at` (and the UTC-hour gate it implies), never "the commit that added it" — that answer varies by ref set. | `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md`; verifier fetched all 487 remote heads to establish the zero-reachable-commit fact; extends L118's minute-bucket calibration | **invariant (enforced by construction) + test** — the dead-collector-leg advisory shipped this run in `scripts/invariants.py` derives liveness **exclusively** from committed-tape `captured_at` minute buckets: it makes no network call, runs no git command, and reads no commit-author string. The unsound method is therefore not merely discouraged but absent from the enforcement path, and `tests/test_dead_collector_leg_advisory.py` exercises it over real tape. |
| L159 | 2026-07-25 | **The 09-UTC econ slot is a PERMANENT false `:2x` liveness signal — a raw minute-bucket count will report a dead VPS leg as alive once a day, forever.** `econ_prints`/`anomalies` land at **09:20-09:29** (an earlier draft of this row narrowed the span to 09:25-09:29; the true observed span inside the bucket is 09:20-09:29 — e.g. `econ_prints` dt=2026-07-19 at 09:20 and 09:21, dt=2026-07-21 at 09:23/09:24/09:28/09:29), squarely inside the VPS `:20-29` bucket, so any raw `:20-29` line count forges a live-VPS reading. This is not hypothetical drift: the artifact was present through the ENTIRE first outage (07-19T09, 07-20T09, 07-21T09 — i.e. on every single day the leg was confirmed dead) and again on 07-23T09 during the second. The fix is to restrict liveness counts to `kind=="hourly-dual"` families, which are the only ones actually written by both staggered legs. Generalizes past the econ case: a minute-of-hour signature is only an identity for legs that share the same schedule shape; mixing a differently-scheduled family into the count destroys the signature. | `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md`; refines L118 (`collector_bucket()`) and L128; the 4 contaminating slot-days named above are re-derived from committed tape | **invariant (enforced by construction) + test** — the advisory shipped this run restricts its liveness computation to `kind=="hourly-dual"` families (config imported from `scripts/tape_gap_monitor.py`, not re-declared), so the artifact cannot reach its verdict; pinned by `tests/test_dead_collector_leg_advisory.py`. **Standing caveat, recorded honestly:** `scripts/tape_gap_monitor.py`'s own RAW per-bucket counts still carry the artifact — its `collector_diagnosis` is unaffected, but a human reading its raw `:20-29` numbers can still be fooled, and that half is **not** fixed by this run. |
| L160 | 2026-07-25 | **Tree-hash equality is the cheap, authoritative stranded-tape containment test; `git merge-base --is-ancestor` must NEVER be used for it.** `git rev-parse <branch>:tape` vs `git rev-parse HEAD:tape` settles "is any tape line stranded on this branch?" in ONE command with no per-file work (this run: `origin/tape/hourly-202607250406Z:tape` == `HEAD:tape` == `23b44416b723b76fca0b130feba53f719d2b5676` → zero missing lines, sweep complete). Per-file `comm` is only needed AFTER a hash mismatch, to localize WHICH lines differ. The anti-pattern is specific and costly: because `main` **squash-merges**, a tape branch is *never* an ancestor of `HEAD` even when its content is fully contained — so `git merge-base --is-ancestor` produces a false "stranded" signal **every single time**, sending step-0b sweeps chasing tape that is already merged. (Same root cause as L158: squash-merge destroys topology-based reasoning; content-based reasoning survives it.) | step-0b stranded-tape sweep by a separate `tape-auditor`, 2026-07-25 run; `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md` (§3 + "Reproduce"); relates to L158 | **tool (enforced) + test** — built this run as `scripts/tape_branch_sweep.py`, with a correction this row's own framing needed: on an APPEND-ONLY tree a tree-hash MISMATCH does NOT by itself mean "not contained" — `tape/` grows every hour, so any branch older than the single most-recently-diverged one will mismatch HEAD's tree even when every line it carries is fully present in HEAD's now-larger files (this row's own worked example only held because it checked the newest branch immediately). A mismatch now falls back to a per-file LINE-SET check (`per_file_containment`), scoped via `diff_changed_files`/`--diff-filter=AM` between the two tree objects rather than a full tree walk — a branch's `tape/` snapshot is a full historical snapshot (13,644 files measured live), of which a typical hourly-pass branch touches a handful; enumerating the whole tree per branch made an early draft of this tool time out on 20 branches, the diff-scoped version swept the real 192-branch backlog in ~30s. No call to `git merge-base --is-ancestor` anywhere in the module — pinned by a source-text test (`tests/test_tape_branch_sweep.py::TestNeverUsesMergeBaseIsAncestor`, code-minus-docstring scan via `ast`). 31 offline tests, real temporary git repos (not mocked subprocess), including a fixture pinned to the exact "old snapshot, hash-mismatched, still contained" case. **Live sweep of all 192 real `tape/*` branches (2026-07-25):** 12 fully verified contained, 167 no-problem-found-but-unverified (bulk-family files, e.g. `orderbook_depth`/`universe_sweep`/`sports_pairs`, size-guard-skipped at 2MB rather than read in full — flagged, never silently trusted), 13 flagged "missing" that on inspection are ALL benign (8 are wording differences in the prose doc `tape/cloud-env-check.md`, not append-only tape; 5 are pre-existing git conflict-marker artifacts, L142's own bug class, already correctly excluded from HEAD) — **zero genuine stranded tape found across the entire historical backlog.** See `findings/2026-07-25-tape-branch-sweep-tool-and-backlog-audit.md`. |
| L161 | 2026-07-25 | **Malformed tape-branch names are a RECURRING defect, not a one-off — and a naive lexical "newest branch" sweep silently mis-triages them, which is a data-loss risk in step-0b.** **44 of 192** remote `tape/*` branches fail the canonical name regex `^tape/hourly-[0-9]{8}T[0-9]{4}Z$`, measured 2026-07-25 ~08:5xZ by the exact command this row ships (an earlier draft of this row said "roughly 13-20 of 192" with no command and no definition of "malformed" — unfalsifiable, and re-derived to 44-45 of 192-193 minutes later; an independent verifier measured 45 of 193 in the same hour, i.e. **the count DRIFTS as branches accumulate**, which is precisely why a row asserting a count over refs must inline its command rather than freeze a number): `git ls-remote --heads origin 'refs/heads/tape/*' | awk '{print $2}' | sed 's|refs/heads/||' > /tmp/br.txt` then `wc -l < /tmp/br.txt` (192 total), `grep -cE '^tape/hourly-[0-9]{8}T[0-9]{4}Z$' /tmp/br.txt` (148 well-formed), `grep -vcE '^tape/hourly-[0-9]{8}T[0-9]{4}Z$' /tmp/br.txt` (44 failures). The dominant malformed shape is ~22 branches of the form `hourly-YYYYMMDDHHMMZ` — the `T` separator missing — including this run's own newest branch `tape/hourly-202607250406Z`. **The substance is unchanged and still verified:** `tape/hourly-Z` and `tape/hourly-` (no timestamp at all) both exist. Because `Z` and the empty suffix sort AFTER every dated `tape/hourly-*` branch lexically, a sweep that picks "the newest-looking name" picks one of these degenerate branches, concludes it has already swept past everything, and leaves real unswept tape behind — silently. **Rule: any branch failing the name regex must be triaged by COMMIT DATE, never by name order**, and the count of name-regex failures should be reported by the sweep rather than skipped quietly. | step-0b stranded-tape sweep by a separate `tape-auditor`, 2026-07-25 run; `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md` (§3) | **tool (enforced) + test** — the same `scripts/tape_branch_sweep.py` built for L160 implements this: `is_malformed_branch_name` classifies every branch against the canonical regex, and for malformed names `triage_branch` resolves `commit_date` via `git log -1 --format=%cI` (never name order); `format_report`'s malformed section is sorted by that date. The malformed-branch count is always printed, never skipped quietly. Live run (2026-07-25) found **44 of 192** malformed — matching this row's own measurement exactly. Pinned by `tests/test_tape_branch_sweep.py::TestNameValidation` (7 cases incl. the exact `hourly-Z`/`hourly-`/missing-`T` shapes this row names) and `TestFetchAndTriage` (a malformed-but-contained fixture branch asserts `commit_date is not None`). See `findings/2026-07-25-tape-branch-sweep-tool-and-backlog-audit.md`. |
| L162 | 2026-07-25 | **A gate result quoted mid-edit is stale by the time the run lands — and any lesson asserting a count must inline the command that produced it.** This run's kb entry, finding, and run-log line all recorded `pytest 1743 collected` and "14 tests" for `tests/test_dead_collector_leg_advisory.py`, while the final tree had **1750 collected / 0 failures** and **21 tests**: the numbers were taken before the last test file was finished, then never re-taken. **Rule (first half): the gate line in any finding/log/run-log entry must be re-taken AFTER the final code change, or written as a floor ("&ge;N collected, 0 failures") — never as a mid-edit snapshot presented as final.** **Rule (second half): any lesson row asserting a count over refs/files/branches must inline the exact command that produced it, with the measurement time.** L161 shipped "roughly 13-20 of 192" malformed tape branches with no command and no definition of "malformed"; it re-derived to 44 of 192 (and an independent verifier got 45 of 193 minutes later, because tape branches accumulate continuously). A count with no command is unfalsifiable *and* silently drifts; a count with a command is reproducible even when the number has moved. | this correction round itself (round-2 independent verifier review of `findings/2026-07-25-vps-collector-second-death-and-cloud-slot-attrition.md`, 2026-07-25); corrects the gate lines in that finding + `kb/00-LOG.md` + the `2026-07-25T~08:1xZ` run-log line, and the census in L161 | **UNENFORCED** — candidates: (a) a check that a finding's/log entry's quoted gate numbers match a freshly-run gate (assertable in principle — parse `\*\*(\d+) collected` out of the newest `kb/00-LOG.md` entry and compare against a live `pytest --collect-only` count — but it is a slow, network-free-yet-full-suite check and would false-fire on entries deliberately quoting a historical figure, so it needs a superseded-mention escape hatch before it can ship); (b) the cheaper and probably correct form, a protocol/checklist rule in LOOP-QUEUE.md step 4 ("re-take the gate line after the last edit, or write it as a floor") plus a kb-distiller rule that count-bearing lesson rows ship their command. Not built this round: the round's scope was explicitly the five text corrections and this row. Open work, not a terminal state. |
| L163 | 2026-07-25 | Formal disposition of L162 (supersedes its enforcement column; lesson content unchanged: a gate result quoted mid-edit is stale by the time a run lands, and any lesson asserting a count over refs/files/branches must inline the command that produced it). Idle-run policy order (a): re-derived the open `**UNENFORCED**` set by whole-word grep, tracing every hit forward for a later `supersedes`/`Formal disposition of` row (the L108/L112/L116/L121/L122/L124 discipline) — every row through L161 is closed, and **L47**/**L52** (this morning's earlier idle runs, PR #194/#198) are also closed, leaving **L162 as the sole open row**. Built its own candidate (b), the "cheaper and probably correct form" its text named: (1) `LOOP-QUEUE.md` step 4 gained an inline dated rule — any pytest/invariants count quoted in a finding, `kb/00-LOG.md` entry, or "Log of runs" line must be taken AFTER the commit's last code change, never a mid-edit snapshot, with a floor (`>=N collected, 0 failed`) as the fallback when an exact re-count isn't practical; (2) `.claude/agents/kb-distiller.md`'s Ledger step gained the row's second half verbatim — a count-bearing lesson row must inline the exact command + measurement time. Candidate (a) (a scanner diffing a quoted count against a live re-run) was left explicitly deferred, per L162's own text, on the missing superseded-mention escape hatch. | this run; supersedes L162; `LOOP-QUEUE.md` step 4; `.claude/agents/kb-distiller.md`; `findings/2026-07-25-l162-fresh-gate-line-protocol-rule.md`; re-derived open UNENFORCED set = {} after this row | **protocol, encoded** — `LOOP-QUEUE.md`'s own run-protocol step 4 and `.claude/agents/kb-distiller.md`'s Ledger step now state the fresh-gate-line and command-provenance rules directly, the same enforcement tier as L106/L107/L112/L116/L156's `.claude/agents/edge-prober.md` house-style closures. Not statically assertable this round (candidate (a)'s scanner remains future work); the discipline is now written where every subsequent run's own bookkeeping step reads it, rather than left in a lessons-ledger row nobody re-reads mid-commit. **The lessons ledger's UNENFORCED backlog is empty again as of this row.** |
| L164 | 2026-07-25 | **Chunking a one-shot burst-capture window to bound sandbox-death data loss introduces its own new failure mode: a chunk seam (the commit+push+verify pause between two invocations) can land directly on top of the single most decisive instant in the window.** Built (this run) a chunked-commit mitigation for Q19's FOMC burst leg with a UNIFORM 20-minute/14-tick chunk plan; an independent `verifier` caught that this plan's first seam falls at ~17:59:30Z-18:01:00Z, straddling the FOMC statement's 18:00:00Z release exactly. L57 (`findings/2026-07-14-s17-burst-cpi-q19.md`) already established that an entire burst's signal can live in ONE release-instant capture (removing the June-CPI print's single 12:30:13Z capture collapsed rho from 0.902/0.777 to 0.196/0.037) — a seam-induced gap at the FOMC release risks losing exactly the tick the whole chunking exercise exists to protect, the opposite of its intent. Fixed for this instance with a hand-verified non-uniform first chunk (`[16, 14, 14, 14, 14, 12]` instead of uniform `[14]*6`, sized so 18:00:00Z falls safely inside chunk 1). **Rule: no chunk boundary may fall within one capture interval of a one-shot's decisive release instant** — a uniform chunking tool cannot know this on its own; it must be checked by hand (or by a future tool that accepts a protected-instant parameter) for every event with such a moment. | `findings/2026-07-25-q19-fomc-burst-preflight.md`; `ops/burst_capture_chunked.md`; verifier review of PR #200, 2026-07-25 | **UNENFORCED** — candidate: teach `scripts/burst_chunk_plan.py` an optional `--protect ISO_INSTANT` argument that adjusts the first chunk's tick count so no boundary falls within one interval of it (rather than leaving every future one-shot to hand-verify, as this run did for FOMC). Not built this round — the FOMC-specific instance is fixed by hand in `ops/burst_capture_chunked.md`; generalizing to an automatic check is a separate, non-trivial milestone (must handle events with more than one protected instant, e.g. an FOMC presser Q&A window as well as the statement itself). |
| L165 | 2026-07-25 | **An unsourced count in a "documented facts, not new claims" section is synthetic by CLAUDE.md's trust-default rule, even when the number turns out to be independently true.** This run's Q19 FOMC pre-flight finding stated "captured 144 snapshots" under a heading explicitly labeled "facts ... not new claims" and cited `kb/00-LOG.md`'s 2026-07-14 entry as the source — but that entry does not contain the number (it says the opposite in kind: "produced NO tape anywhere," no snapshot count at all). An independent `verifier` caught the mismatch by grepping the whole repo for "144" in a burst-related context and finding only the finding itself. The number itself was real — it lives in the LIVE `kalshi-burst-fomc-0729`/`-wcfinal-0719` trigger prompts' own "MANDATORY PUSH VERIFICATION" text (read via `list_triggers`, not a repo file) — but it was cited to the wrong artifact, which is indistinguishable from fabrication to anyone re-checking the finding against the repo alone. **Rule: every count in a "facts" section needs LINE-LEVEL provenance to the artifact that actually contains it** — "I recall reading this earlier in the session" is not a citation; if the source is a live tool call result rather than a committed file, say so explicitly rather than pointing at the nearest plausible-looking committed document. | `findings/2026-07-25-q19-fomc-burst-preflight.md`'s correction section; verifier review of PR #200, 2026-07-25 | **UNENFORCED** — candidate: no static scanner can verify a citation is accurate (that requires re-reading the cited artifact and comparing content, which is exactly what the verifier did here) — this is a per-finding methodology discipline, likely terminal as **protocol** (an explicit reminder in `.claude/agents/verifier.md` and/or `.claude/agents/edge-prober.md`'s house style to grep-verify every quoted number's cited source before it ships, not just its truth), same posture as L27/L28/L69. Not built this round. |
| L166 | 2026-07-26 | Formal disposition of L164 (supersedes its enforcement column; lesson content unchanged: a chunk seam can land on a one-shot burst's single decisive release instant, and a uniform chunker cannot know that on its own). Idle-run policy order (a): re-derived the open `**UNENFORCED**` set by whole-word grep over `kb/lessons/00-lessons.md` — L163 had already closed everything through L163 itself, leaving **L164 and L165 as the only two open rows** (both born in the same 2026-07-25 correction round, after L163's "backlog empty" snapshot). Built L164's own named candidate: `scripts/burst_chunk_plan.py` gained `first_chunk_ticks_protecting_instant()` + `chunk_max_ticks_sequence_protecting()` + a CLI `--protect ISO_INSTANT` flag (default margin one `--interval`, override via `--margin-seconds`) that grows chunk 1 only so its last tick lands strictly more than the margin past the protected instant, then chunks the remainder normally. Fed the FOMC's own real numbers back through it (`--start 2026-07-29T17:40:00Z --until 2026-07-29T19:45:00Z --interval 90 --chunk-minutes 20 --protect 2026-07-29T18:00:00Z`) and it reproduces the hand-verified `[16, 14, 14, 14, 14, 12]` recipe EXACTLY — the tool now generates what a human had to hand-check. Scope kept honest, matching L164's own candidate wording: ONE protected instant, must fall after window start by more than the margin (raises otherwise — no boundary before t=0 to move); an instant needing the whole window collapses to one chunk (correct, documented, not silently wrong); a second decisive moment in the same window (e.g. an FOMC presser Q&A) still needs a hand check, as L164 itself flagged as future work. `ops/burst_capture_chunked.md`'s FOMC recipe now shows the reproducing command alongside the literal sequence. | `findings/2026-07-25-q19-fomc-burst-preflight.md`; `scripts/burst_chunk_plan.py`; `tests/test_burst_chunk_plan.py`; `ops/burst_capture_chunked.md` | **tool (enforced) + test** — 14 new offline tests in `tests/test_burst_chunk_plan.py` (margin honored, never shrinks below the normal chunk size, never grows past the total window, rejects an instant too close to window start or at/past window end, exact FOMC-recipe regression pin on both the low-level ticks function and the CLI, and a same-as-before-when-`--protect`-omitted regression guard). `pytest -q`: full-suite count taken after this commit's last edit (see run digest); `invariants --full`: exit 0, unchanged advisory set. Two-agent rule N/A (tool build/lesson-conversion, no verdict/registry/CI). **The lessons ledger's open-UNENFORCED set is {L165} after this row** (L165 disposed separately, see L167). |
| L167 | 2026-07-26 | Formal disposition of L165 (supersedes its enforcement column; lesson content unchanged: an unsourced count in a "facts, not new claims" section is synthetic by CLAUDE.md's trust-default rule even when independently true, and every such count needs line-level provenance to the artifact that actually contains it). L165's own text judged this "likely terminal as **protocol**" — no static scanner can verify a citation is accurate without re-reading the cited artifact and comparing content, which is exactly what a `verifier` pass already does by charter. Added an explicit house-style reminder to both agents whose output carries citations: `.claude/agents/edge-prober.md` gained a bullet ("grep-verify the cited artifact actually contains it... if the real source is a live tool-call result rather than a committed file, say so explicitly") and `.claude/agents/verifier.md` gained a numbered attack step ("Attack citations, not just numbers" — a true number cited to the wrong source is still a REFUTED-class defect). Same disposition shape as L106/L107/L112/L116/L156/L163's agent-house-style closures — the discipline lives where the producing/checking agent's own instructions are read, not only in a lessons-ledger row nobody re-reads mid-write. | `findings/2026-07-25-q19-fomc-burst-preflight.md`'s correction section; `.claude/agents/edge-prober.md`; `.claude/agents/verifier.md` | **protocol, encoded** — both agent files now state the citation-grep-verify rule directly. Not statically assertable (per L165's own text) — a scanner that diffs a quoted claim against its cited artifact's actual content is future work, if ever built. **The lessons ledger's UNENFORCED backlog is empty again as of this row.** |
| L168 | 2026-07-26 | **A 200-OK fetch is not a captured book.** `tape/orderbook_depth/`'s crypto (`KXBTC`/`KXETH`) records go completely HOLLOW (`yes_bids=[]`, `no_bids=[]`, `depth=0`, yet still tagged `real_ask`/`real_bid`) whenever the fetch reaches a ticker at or after its own close — 15,175 of 339,947 committed lines through `dt=2026-07-25` (4.46%), rising to 100% on `dt=2026-07-23`/`dt=2026-07-25` — and `completeness_ok` (computed in `collection/orderbook_depth.py`, never persisted to the tape line) cannot see it, because an empty book IS a valid HTTP 200 response. Two compounding causes, both distinct from ordinary pass-duration overrun: (1) a pass's ticker *discovery* step can run before the hour rolls, so the fetch targets an already-closed contract from the start (789 records, verified: capture `20260723T005605Z` stamped `00:56:05Z` fetched 488 records for markets closing `01:00:00Z`, i.e. *before* its own stamped time by the naive reading — see L169's sibling finding); (2) `captured_at` is a PASS-START timestamp replicated onto every record in a multi-minute capture (1 distinct value per `capture_id`, 425/425 captures verified), so a late-pass fetch's true fetch time can trail its recorded timestamp by the whole pass duration — proven on the same capture, whose 488 "00:56:05Z" records were provably fetched >=235s later, at/after their ticker's 01:00:00Z close. For any deadline-bearing market family, completeness must be measured relative to the ticker's own deadline, not merely to whether the HTTP call succeeded. | `findings/2026-07-26-orderbook-depth-hollow-crypto-ladders.md`; two-agent (tape-auditor find + independent `verifier` re-derivation, both against real tape); `scripts/orderbook_depth_hollow_ladder_audit.py` | **test (detection) + UNENFORCED (repair)** — `scripts/orderbook_depth_hollow_ladder_audit.py` (+ `tests/test_orderbook_depth_hollow_ladder_audit.py`, 2 HARD real-tape acceptance tests) is the read-only reproducer, and a non-gating `scripts/invariants.py::hollow_crypto_ladder_warning` advisory (+ `tests/test_hollow_crypto_ladder_advisory.py`) now surfaces any recent day >=50% crypto-hollow. The REPAIR (per-record fetch timestamp; deadline-first ticker ordering so crypto is fetched before non-expiring families; `run()` counting a post-close fetch as a drop) is a `collection/orderbook_depth.py` change — Ryan-gated collector lane, not built this run. |
| L169 | 2026-07-26 | **When a suspected failure mechanism is itself a function of minute-of-hour, a minute-of-hour collector-leg attribution is collinear with the mechanism and cannot be tested against it.** The first-pass `tape-auditor` finding attributed L168's hollow-ladder defect to "the cloud leg degrading" (a hand-rolled minute-bucket split gave cloud 52,477/28.9% hollow vs VPS 64,274/0.0%) — an independent `verifier` REFUTED the causal claim two ways: (a) using the project's OWN `scripts/tape_gap_monitor.py::collector_bucket` instead of the hand-rolled rule changes the cloud figure to 54,055/28.0% (the hand-rolled version had silently dropped 1,578 clean off-slot records from its own denominator, inflating the rate); (b) the discriminating case is an off-schedule cloud-hour capture at minute :41 (`20260721T224112Z`, ~18.8min runway) that is 488/488 CLEAN — a cloud-leg capture with enough runway is fine, so "cloud" was never the operative variable, "runway to close" was (see L168). Rule: any collector-leg split MUST (i) use the repo's own `collector_bucket`, never a re-hand-rolled minute rule, and (ii) be checked against an off-schedule/off-slot capture in the SAME leg as a discriminator before a leg-identity causal claim ships — a same-schedule-only split can never distinguish "this leg is worse" from "this leg's slot has less of whatever the real variable is." | `findings/2026-07-26-orderbook-depth-hollow-crypto-ladders.md`; verifier review of the tape-auditor's first-pass claim, 2026-07-26 | **test (partial) + protocol** — `scripts/orderbook_depth_hollow_ladder_audit.py::audit_leg_crosstab` imports `tape_gap_monitor.collector_bucket` rather than re-deriving a minute rule (pinned by `test_leg_crosstab_uses_tape_gap_monitor_collector_bucket`), and its docstring/the finding's own write-up state the leg-crosstab table is DESCRIPTIVE ONLY, not causal. No static scanner can catch a future hand-rolled minute-split elsewhere in the tree (a lexical proxy would need to know what counts as "a leg-causal claim") — the discipline is methodological, same terminal shape as L27/L28/L69/L165. |
| L170 | 2026-07-26 | **A collector's "idempotent across staggered collectors" claim is only as global as the tape it dedups against — and this repo's tape is routinely NOT merged when a second collector runs.** `collection/hyperliquid_funding.py::_committed_time_ms` dedups a pass's "already archived" set against the LOCAL working tree only. With two collectors (VPS + cloud) racing on unmerged `tape/hourly-*`/`tape/burst-*` fallback branches (LOOP-QUEUE.md step 0b's standing state — 189 such branches on `origin` at audit time), each can independently see "nothing new" and both archive the same print. Confirmed on real committed tape: `('BTC', 1784725200052)` and `('ETH', 1784725200052)` each appear twice, written by commits `d845dfb`/`2235aa3` that landed out of capture order (`git log -S` trace). Blast radius today is nil — the only current consumer, `collect_hl_hourly`, already re-dedups on `(coin, hour_index)` before use — but the module's own docstring overclaimed unconditional global idempotency, which would mislead a future consumer that aggregates raw rows (e.g. a naive `n_prints` count) into trusting a guarantee the collector doesn't actually provide. Same failure class as any "per-print/per-line dedup against committed tape" claim in this repo's other incremental collectors — worth a grep-check the next time one is audited. | `findings/2026-07-26-hyperliquid-funding-tape-audit.md` (tape-auditor subagent + main-context fix, 2026-07-26 idle-run policy (c)) | **protocol, encoded + test** — `collection/hyperliquid_funding.py`'s docstring now states the branch-local caveat explicitly instead of the prior overclaim; `tests/test_q42_crossvenue_funding_join.py::test_collect_hl_dedups_cross_branch_duplicate_records` reproduces the exact real-tape scenario (same `(coin, time_ms)` print in two separate records) and pins that `collect_hl_hourly` handles it correctly today. The underlying tape's 2 duplicate lines are NOT rewritten (append-only) — this closes the code/doc gap, not the historical data point, which is harmless per the above. |
| L171 | 2026-07-26 | **A collector whose record carries a retrospective list (not one point-in-time observation) cannot be audited for coverage by `dt=` day-file presence — that only measures WHEN a pass ran, not what history it captured.** `tape/hyperliquid_funding/` has no committed file for `dt=2026-07-18`…`dt=2026-07-21` (the L127 VPS-freeze window), which reads as a 4-day gap by file-count — but the union of every record's `prints[].time_ms` across the whole family is 1282 distinct hourly prints per coin, 2026-06-03T00:00Z→2026-07-26T09:00Z, 0 non-1h steps: the post-freeze catch-up pass backfilled the entire window in one paginated call. The correct coverage unit for this shape is the union of the embedded historical timestamp field, not the day-file list. Same shape would apply to any future collector whose payload is "everything since X," not "what I see right now" (the `mode=backfill` records already committed elsewhere in this repo are the general case). | `findings/2026-07-26-hyperliquid-funding-tape-audit.md` (tape-auditor subagent, 2026-07-26 idle-run policy (c)) | **test** — see L172 (formal disposition, this same day). |
| L172 | 2026-07-26 | Formal disposition of L171 (supersedes its enforcement column; lesson content unchanged: a retrospective-list family's coverage cannot be read from `dt=` day-file presence, only from the union of its embedded per-observation timestamps). Idle-run policy (a): re-derived the open `UNENFORCED` set by whole-word grep tracing every historical row to a later `supersedes`/`Formal disposition of` closure (the L108/L112/L116/L163/L166/L167 discipline) — **L171 is the sole open row.** Built L171's own named candidate: `scripts/tape_gap_monitor.py` gains `RETROSPECTIVE_LIST_FAMILIES` (a small explicit map, mirroring `EXPECTED_COLLECTOR_BUCKETS`'s shape — record's embedded-list key, per-item timestamp key, grid step-size; seeded with `hyperliquid_funding`: `prints`/`time_ms`/3600s) and `retrospective_coverage(tape_root, family)` (union of every embedded observation timestamp across ALL committed files for a registered family → `n_observations`/`span_start`/`span_end`/`n_missing_steps`; `None` for an unregistered family; malformed/missing embedded timestamps skipped, never fabricated, same discipline as `extract_completeness`). `evaluate_family()` gained an optional `tape_root` parameter (default `None`, so every existing call site is unaffected) that attaches the reading to a registered family's health record as `retrospective_coverage`; `build_report()` now threads `tape_root` through automatically. Does NOT touch the STALE/UNDER-CAPTURE alert path — `hyperliquid_funding`'s forward-cadence freeze detection (L127/L128) was already correct; this adds a genuinely new, correct signal for *historical* completeness that file presence structurally cannot represent for this record shape. Live real-tape result: 1,289 distinct hourly `time_ms` values (up from L171's own 1,282 as more tape landed), **0 missing steps**, while the same real tape's `dt=2026-07-18`..`dt=2026-07-21` files are confirmed still genuinely absent (the HARD acceptance test fails loudly, not silently, if that premise ever stops holding). | `findings/2026-07-26-l171-retrospective-list-coverage.md`; `scripts/tape_gap_monitor.py`; `tests/test_tape_gap_monitor.py` | **test, enforced** — 7 new tests in `tests/test_tape_gap_monitor.py`: unregistered-family/no-tape/malformed-items edge cases, a fixture reproducing the EXACT L171 day-file-hole-but-complete-embedded-coverage shape (and independently confirming the naive day-file read WOULD have seen a gap, proving the function isn't a no-op relative to it), a genuine-gap fixture (`n_missing_steps == 2`), 3-way `evaluate_family` wiring coverage, and 1 HARD real-tape acceptance test. **The lessons ledger's `UNENFORCED` backlog is empty again as of this row.** |
| L173 | 2026-07-26 | **A duration-gated "stale window"/persistence statistic with no PRE-EVENT BASELINE measures the venue pair's permanent overround, not the event's lag.** On the 2026-07-14 CPI burst the absolute rule (`|gap| - fee > 0` sustained) flagged **12 of 15** units persistent, of which **7** had `|gap| - fee > 0` on **100% of their PRE-release captures** and whose 4502s "stale windows" were simply the full post-release span of the tape — i.e. a standing cross-venue level difference re-labelled as an event-driven dislocation. Baselining each unit against its own pre-release gap cut it to **4 of 15**. Extends L76 from "count vs wall-clock" to "absolute vs baselined": a persistence measure must be differenced against the same unit's own quiet-period value, or it cannot distinguish lag from level. | `scripts/q48_s55_fomc_lag_probe.py` (Q48/S55 idle-run policy (b) prep, 2026-07-26; producer + 2 independent `verifier` rounds); counts re-derivable via `python3 scripts/q48_s55_fomc_lag_probe.py --release-ts 2026-07-14T12:30:00Z` (measured 2026-07-26 on committed `tape/polymarket_macro_pairs/`, 9000 records) | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_stale_window_is_baselined_against_the_units_own_pre_release_gap` (a unit whose gap exceeds fee identically before and after the release must NOT count as persistent) and `::test_stale_window_baseline_unmeasurable_is_none_never_false` (no pre-release captures → `None`, never a silent `False`). |
| L174 | 2026-07-26 | **A refusal floor keyed to an OPERATOR-SUPPLIED instant is ADVISORY, not structural.** `MIN_BURSTS_FOR_CI=3` was documented as unreachable "by construction" on today's tape; the independent `verifier` reached **`n_bursts=7`** with a strictly-positive **`ci95=[+0.00372, +0.00915]`** using a single `--release-ts` string of fabricated instants against the same committed tape — because nothing validated that a supplied instant was a real scheduled event, and a 2-pass sub-300s *recurring-cadence coincidence* counted as a "burst". Rule: a refusal is structural only when the count it gates is derived from something the caller cannot choose; if the caller supplies the keying instants, the gate is a courtesy and the output must say so on its face. | `scripts/q48_s55_fomc_lag_probe.py` verifier round 1 (Q48/S55 prep, 2026-07-26); reproduce with `python3 scripts/q48_s55_fomc_lag_probe.py --release-ts <comma-separated fabricated instants>` (measured 2026-07-26) | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_fabricated_release_list_reaches_the_ci_path_but_is_flagged_not_burst_cadence` (the CI path IS reachable with fabricated instants and every such result carries the not-burst-cadence flag) and `::test_cadence_qualification_separates_a_real_burst_from_a_recurring_pair`. |
| L175 | 2026-07-26 | **A hardcoded provenance tag in a human summary line is CLAUDE.md's untagged→synthetic hole reopened at the copy-paste surface.** The probe's one-liner printed the literal string `[real_ask both legs]` even when run over a tape with **zero** observed `price_source_tag` values — true today (9000/9000 records `real_ask`, measured 2026-07-26) but a claim the code could not have known. Every provenance claim on a report surface must RENDER from the data-derived tag set actually observed in the rows being summarised, never from a literal. | `scripts/q48_s55_fomc_lag_probe.py` verifier round 1 (Q48/S55 prep, 2026-07-26); tag census re-derivable via `python3 scripts/q48_s55_fomc_lag_probe.py` (measured 2026-07-26) | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_one_liner_provenance_is_rendered_from_the_observed_tags` (a fixture carrying a `midpoint` row must change the rendered provenance string). |
| L176 | 2026-07-26 | **A strict `> 0` edge gate with no tick floor books float dust as trades.** In the 07-14 CPI dry-run **9 of 24** "fired trades" carried edge **8.67e-18** — pure floating-point residue from the normalization arithmetic — and those values entered the bootstrap's unit values as if they were real fills. Any fire/no-fire comparison on a price-derived quantity needs an explicit sub-tick floor (below one venue tick = not a trade, counted separately as a diagnostic), not `> 0`. Sibling of Hard Rule #3's normalization discipline: normalizing creates non-representable quantities that a naive comparison then dignifies. | `scripts/q48_s55_fomc_lag_probe.py` verifier round 1 (Q48/S55 prep, 2026-07-26); reproduce with `python3 scripts/q48_s55_fomc_lag_probe.py --release-ts 2026-07-14T12:30:00Z` (measured 2026-07-26) | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_subtick_candidate_is_counted_not_fired` and `::test_subtick_candidates_are_reported_by_run_probe` (sub-tick edges are reported as a separate count and never enter the fired-trade population). |
| L177 | 2026-07-26 | **L165 RECURRENCE (extends it from "cite the artifact" to "re-measure at every hop").** The figure "the recurring `polymarket_macro_pairs` leg runs only ~2 captures/day" propagated unverified across FOUR artifacts — `findings/2026-07-26-q21-idea-gen-round.md` → `LOOP-QUEUE.md` Q48 → `kb/strategies/00-index.md` S55 → the new probe's docstring — and was contradicted by the same sentence's own parenthetical. The tape says **29.1 passes/day** over 20.6 days (median inter-pass gap 1,856.3s ≈ 31 min) and **9.4/day** since 2026-07-19 (median gap 10,778s ≈ 3.0h) — wrong by 5-15x, though the S9-cadence-wall CONCLUSION survives (a 3h cadence still cannot see a seconds-to-minutes reprice). An idea-stage number inherited into a queue item and then into code is a CITATION CHAIN, and each hop must re-measure from the tape, not re-copy the prose. Same run also corrected the conflation of the SIGNED front-meeting gap (mean +0.553¢ / median +0.600¢, n=2400) with mean\|gap\| (+0.743¢). | `scripts/q48_s55_fomc_lag_probe.py` docstring + Q48 Status correction (Q48/S55 prep, 2026-07-26, verifier-confirmed); cadence re-derivable via `python3 scripts/q48_s55_fomc_lag_probe.py` (measured 2026-07-26 over committed `tape/polymarket_macro_pairs/`, 9000 records / 600 passes) | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_docstring_states_the_corrected_cadence_and_retracts_the_false_claims` (pins the corrected figures + the explicit retraction in the module docstring, so the false number cannot be re-copied back in). |
| L178 | 2026-07-26 | **Correction to L174 (its lesson stands; one of the two guards it credits does NOT).** L174's row credits `clears_tick_magnitude = False` as one of the gates that caught the verifier's fabricated-`--release-ts` attack. That is no longer true after the producer's `MIN_ENTRY_EDGE` fix (see L179/L181): removing the float-dust legs that had been dragging the attack's CI under one tick leaves the SAME attack shape printing a CI comfortably ABOVE a tick with **`clears_tick_magnitude = True`**. Re-derived on the committed tape 2026-07-26 with a deterministic reconstruction of the attack (midpoint of each of the 7 non-cadence-qualified sub-300s windows): `verdict = "BOOTSTRAPPED (n_bursts=7)"`, `ci95 = [+1.125¢, +1.833¢]`, `clears_tick_magnitude = True`, fired=9, sub-tick dropped=15 — the verifier's own independently-chosen instants gave `ci95 = [+1.091¢, +1.714¢]`, same conclusion. **The only surviving guards are `bootstrap_verdict_admissible.admissible = False` (`below_min_units`, `no_opposing_unit`), the per-window `cadence_qualified` flag (`n_covering_burst_windows_cadence_qualified = 0`) and `bootstrap_cadence_warning`.** The probe's own docstring already says this correctly; this row makes the ledger match it. | Supersedes the ENFORCEMENT-EVIDENCE clause of **L174** (L174's row text left intact per append-only). Verifier round 2, Q48/S55 prep, 2026-07-26. Exact command (measured 2026-07-26, 9000 records / 600 passes on committed `tape/polymarket_macro_pairs/`): `python3 -c "import sys;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;r=P.load_family_records(P.DEFAULT_TAPE_DIR)[0];t,_=P.pass_times(r);m=[w['start']+(w['end']-w['start'])/2 for w in P.detect_burst_windows(t) if not w['cadence_qualified']];rep=P.run_probe(release_ts=','.join(x.isoformat().replace('+00:00','Z') for x in m));print(rep['n_bursts'],rep['bootstrap']['ci95'],rep['clears_tick_magnitude'],rep['bootstrap_verdict_admissible'],rep['n_covering_burst_windows_cadence_qualified'])"` | **test** — unchanged from L174 (`tests/test_q48_s55_fomc_lag_probe.py::test_fabricated_release_list_reaches_the_ci_path_but_is_flagged_not_burst_cadence` asserts the surviving guards, NOT the magnitude gate) plus `::test_docstring_states_the_corrected_cadence_and_retracts_the_false_claims`, which pins the `"fabricated instants"` retraction text in the module docstring. |
| L179 | 2026-07-26 | **Correction to L176 (its lesson stands; its count was wrong).** L176 says "9 of 24 fired trades at edge 8.67e-18". Precise truth from the FINAL code on the 2026-07-14 CPI dry-run: **11** sub-tick candidates were dropped (24 candidates → **13** fired), of which **9 are float dust** (5 × `8.673617379884035e-18`, 4 × `1.734723475976807e-17`) and **2 are genuinely sub-tick but REAL** (`+0.001` and `+0.002`, both `2026-07\|hike_25`, `BUY_NO`). The smallest surviving edge is exactly `0.01` (`0.010000000000000026`); **no candidate ≥ 1 tick was lost.** `n_subtick_candidates_dropped` counts all 11, dust and real alike — that is deliberate: the gate is a TICK floor, not a dust filter, and conflating the two would re-open the same "is it economically real?" question L27 exists to answer. Per-unit split: `2026-07\|hike_25` 3, `2026-07\|no_change` 4, `2026-09\|no_change` 4. | Supersedes the COUNT in **L176** (row text left intact per append-only). Verifier round 2, Q48/S55 prep, 2026-07-26. Exact command (measured 2026-07-26): `python3 -c "import sys;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;rep=P.run_probe(release_ts='2026-07-14T12:30:00Z');print(rep['n_fired_trades'],rep['n_subtick_candidates_dropped']);[print(k,u['n_subtick_candidates_dropped'],u['n_fired_trades'],[round(e,5) for e in u['fired_edges']]) for k,u in rep['bursts'][0]['units'].items()]"` → `13 11` | **test** — unchanged from L176 (`tests/test_q48_s55_fomc_lag_probe.py::test_subtick_candidate_is_counted_not_fired`, `::test_subtick_candidates_are_reported_by_run_probe`). The count correction is bookkeeping; the enforcement was already correct. |
| L180 | 2026-07-26 | **A BASELINE needs its own n-adequacy guard — differencing against a pre-event period does not remove vacuity if the pre-period can be n=1.** With `n_pre = 1` the pre-release fraction-of-`>fee`-captures is confined to `{0, 1}`, so a single momentarily-tight pre-capture flips a PERMANENT standing cross-venue overround back into a "release-caused dislocation" — exactly the failure L173's baseline was built to prevent. Demonstrated by the verifier and re-derived here: identical permanent 6¢ overround, identical post-release rows, `n_pre=1` → `has_persistent_stale_window = True`; `n_pre=23` → `False`. **SECOND RESIDUAL, same family:** the stale-window comparison `excess_max >= STALE_WINDOW_MIN_EXCESS_DOLLARS` (`scripts/q48_s55_fomc_lag_probe.py::analyze_unit`) is a bare `>=` with NO epsilon — `2026-10\|cut_25` clears it at `excess_max = 0.010000000000000009` only because its `pre_max = -5.204170427930421e-18`; one ulp the other way and the headline is **3** persistent units, not 4. Same float-dust class that `MIN_ENTRY_EDGE` was added to fix on the entry side (L176/L179), left unfixed on the exit side. **This affects NO number reported today** — all 15 units of the 2026-07-14 CPI dry-run have `n_pre = 23` — **but it is LIVE for the 2026-07-29 FOMC firing**, where a thin pre-release baseline is entirely possible and nothing in the report flags one. | Verifier round 2, Q48/S55 prep, 2026-07-26 (`scripts/q48_s55_fomc_lag_probe.py` was frozen for the verification rounds — see L182 — so the repair is deliberately NOT in this diff). n_pre demo command (measured 2026-07-26): `python3 -c "import sys;from datetime import datetime,timedelta,timezone;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;R=datetime(2026,7,14,12,30,tzinfo=timezone.utc);o=lambda t,k,p:{'captured_at':t,'kalshi_yes_price':k,'kalshi_no_price':1-k,'poly_ask_signal':p,'poly_bid_signal':p-0.01,'raw_gap':k-p,'normalized_gap':k-p};post=[o(R+timedelta(seconds=60*i),0.50,0.44) for i in range(1,25)];pre1=[o(R-timedelta(seconds=60),0.50,0.50)];pre23=[o(R-timedelta(seconds=60*(23-i)),0.50,0.44) for i in range(22)]+pre1;print(P.analyze_unit(pre1,post,R)['has_persistent_stale_window'],P.analyze_unit(pre23,post,R)['has_persistent_stale_window'])"` → `True False`. Epsilon residual command: `python3 -c "import sys;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;rep=P.run_probe(release_ts='2026-07-14T12:30:00Z');[print(k,u['n_pre_captures'],repr(u['excess_max_abs_gap_net_fee']),repr(u['pre_release_max_abs_gap_net_fee']),u['has_persistent_stale_window']) for k,u in rep['bursts'][0]['units'].items()]"` | **CLOSED (2026-07-27, kalshi-edge-hunter Unit-3 probe-prep — 2 days ahead of the firing).** Both residuals fixed in `scripts/q48_s55_fomc_lag_probe.py` and pinned by 4 new tests in `tests/test_q48_s55_fomc_lag_probe.py`: (1) `MIN_PRE_CAPTURES_FOR_BASELINE = 2` + a per-unit `thin_baseline` flag AND report-header `n_units_thin_baseline`, with thin-baseline units treated like the existing UNMEASURABLE baseline (`has_persistent_stale_window = None`, excluded from `n_units_with_persistent_stale_window`, biasing toward the KILL); (2) `EXCESS_EPSILON = 1e-9` subtracted in the `excess_max >= STALE_WINDOW_MIN_EXCESS_DOLLARS` comparison, mirroring `MIN_ENTRY_EDGE` on the entry side. Tests: `test_thin_pre_release_baseline_is_none_not_true_L180` (the n_pre=1→None / n_pre=23→False demo), `test_thin_baseline_is_counted_and_excluded_from_persistent_L180`, `test_excess_epsilon_admits_a_genuine_one_tick_widening_lost_to_float_dust_L180` (excess 0.00999999999999995 admitted), `test_excess_epsilon_still_rejects_a_clear_subtick_widening_L180`. Enforcement is the unit test written WITH the fix (not a static invariant — this is a numerical-adequacy property of one probe's statistic). Verified no number moved today: the 2026-07-14 CPI dry-run still shows 4 baselined-persistent / 0 thin units (all n_pre=23), exactly as predicted. Was: **UNENFORCED** — a LIVE residual defect. |
| L181 | 2026-07-26 | **Tightening one gate can LOOSEN another — re-derive BOTH after any change to the population.** Adding `MIN_ENTRY_EDGE` (a correct fix, L176/L179) removed the float-dust legs that had been holding the fabricated-release attack's bootstrap CI below one tick, so `clears_tick_magnitude` flipped **False → True** and the adversarial case got *stronger* on that gate while getting weaker on the one that was actually fixed. The general rule: **L27's magnitude gate filters ECONOMIC INSIGNIFICANCE, never POPULATION LEGITIMACY** — a fabricated population of large edges sails through it. Whenever two gates are cited as JOINTLY guarding an output, any change to the population that feeds them invalidates the joint claim, and the adversarial fixtures must be re-run, not assumed. | Verifier round 2, Q48/S55 prep, 2026-07-26; the concrete instance is L178's row (with its exact re-derivation command). Generalizes the L27/L34 magnitude-gate discipline from "is this edge big enough?" to "big enough AND drawn from a population the caller could not choose". | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_fabricated_release_list_reaches_the_ci_path_but_is_flagged_not_burst_cadence` (asserts the legitimacy guards independently of the magnitude gate, so the two can never again be conflated) + `::test_docstring_states_the_corrected_cadence_and_retracts_the_false_claims`, which pins the docstring's explicit "the tick-magnitude gate is therefore NOT a reliable guard against a fabricated instant list" retraction. |
| L182 | 2026-07-26 | **Do not edit an artifact while a verification run against it is in flight — a PHANTOM defect is the result.** The producer edited `scripts/q48_s55_fomc_lag_probe.py` at 22:33:49Z while a verification run was mid-execution; `inspect.getsource` re-read the changed file with line numbers captured before the edit and returned the WRONG function body, producing a spurious failure of `test_one_liner_provenance_is_rendered_from_the_observed_tags`. Nothing was wrong with the code or the test — a less careful verifier would have reported it as a real defect, and a producer "fixing" it would have damaged working code. Source-introspecting tests (`inspect.getsource`, docstring pins) are the specific class that fails this way, because they read the file at assert time rather than at import time. | Verifier round 2, Q48/S55 prep, 2026-07-26 (phantom failure observed and correctly diagnosed by the verifier, not reported as a defect). | **protocol** — encoded as a queue rule in `LOOP-QUEUE.md`'s Q48 Status line for this run and generalizable to every producer→verifier handoff: **once a verifier round is dispatched, the producer FREEZES the artifact until that round returns**; a re-run against a moving file is not a verification. Not assertable statically (there is no way for a static check to know a subprocess is mid-run), so `protocol` is the honest terminal state — an agent-coordination rule, not a code property. |
| L183 | 2026-07-26 | **A cross-venue "steady-state gap" number is MEANINGLESS without its tenor cut.** Q48/S55's ≈0.6¢ is the **FRONT-meeting** figure (2026-07, mean\|gap\| **+0.743¢**, n=2400). Pooled across the 3 listed `KXFEDDECISION` meetings the same statistic is **+2.031¢** (n=7199) — **2.7×** larger — because far-dated illiquid buckets carry a much wider standing cross-venue overround: 2026-09 **+1.935¢**, 2026-10 **+3.413¢** vs front **+0.743¢**. Quoting the pooled number as "the Kalshi-vs-Polymarket gap" would treble the apparent opportunity using buckets the 07-29 statement does not even decide. **Any log entry, queue item, registry row or docstring quoting a cross-venue gap must name the TENOR CUT that produced it.** Sibling of L177's citation-chain rule: there, each hop must re-measure; here, each hop must carry the population definition along with the number. | Verifier round 2, Q48/S55 prep, 2026-07-26. Exact command (measured 2026-07-26, non-burst observations only, committed `tape/polymarket_macro_pairs/`): `python3 -c "import sys,statistics as st,collections;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;r=P.load_family_records(P.DEFAULT_TAPE_DIR)[0];t,_=P.pass_times(r);o,_=P.build_observations(r,t,P.ladder_sums(r));b=set(c for w in P.detect_burst_windows(t) for c in w['capture_ids']);ns=[x for x in o if x['capture_id'] not in b];m=collections.defaultdict(list);[m[x['meeting']].append(abs(x['raw_gap'])) for x in ns];print({k:round(st.mean(v),6) for k,v in sorted(m.items())},'pooled',round(st.mean(abs(x['raw_gap']) for x in ns),6),len(ns))"` → `{'2026-07': 0.00743, '2026-09': 0.019353, '2026-10': 0.034135} pooled 0.020306 7199` | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_docstring_states_the_corrected_cadence_and_retracts_the_false_claims` (the module docstring carries the front-vs-pooled split and the explicit "NOT a valid headline" label on the pooled figure) and the report's own `baseline` block, which emits `front_meeting_only` and `all_meetings` as SEPARATE named cuts — there is no un-cut gap number for a reader to copy. |
| L184 | 2026-07-26 | **On a complete two-venue bracket ladder the pooled MEAN normalized gap is EXACTLY ZERO by construction — never quote it.** Both ladders normalize over the SAME bucket set (Hard Rule #3), so the per-pass normalized gaps sum to zero identically; the observed `front_meeting_only.normalized_gap.mean = -1.1338e-17` (and `all_meetings` `-8.5127e-18`) is float dust on an exact zero, verified at `max \|sum\| = 3.2959746043559335e-16` across all **1,439** complete (≥2-bucket) non-burst (meeting, capture) groups. Same family as **L51**'s complementary hit rates: the mean carries no information about agreement. Only `mean_abs`/`median_abs` and the **per-bucket sign** do — and those are NOT degenerate: front-meeting per-bucket normalized means span **−1.488¢** (`no_change`) to **+0.766¢** (`cut_50plus`) (`hike_25` −0.528¢, `hike_50plus` +0.502¢, `cut_25` +0.748¢), so the per-observation disagreement signal the probe's direction gate consumes is real; **only the pooled mean is degenerate.** `baseline.*.normalized_gap.mean` must never be quoted as evidence of cross-venue agreement. | Verifier round 2, Q48/S55 prep, 2026-07-26. Exact command (measured 2026-07-26): `python3 -c "import sys,statistics as st,collections;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;r=P.load_family_records(P.DEFAULT_TAPE_DIR)[0];t,_=P.pass_times(r);o,_=P.build_observations(r,t,P.ladder_sums(r));b=set(c for w in P.detect_burst_windows(t) for c in w['capture_ids']);ns=[x for x in o if x['capture_id'] not in b];g=collections.defaultdict(list);[g[(x['meeting'],x['capture_id'])].append(x['normalized_gap']) for x in ns if x['normalized_gap'] is not None];c=[v for v in g.values() if len(v)>=2];print(len(c),max(abs(sum(v)) for v in c));d=collections.defaultdict(list);[d[x['bucket']].append(x['normalized_gap']) for x in ns if x['meeting']=='2026-07' and x['normalized_gap'] is not None];print({k:round(st.mean(v),6) for k,v in sorted(d.items(),key=lambda kv:st.mean(kv[1]))})"` → `1439 3.2959746043559335e-16` / `{'no_change': -0.014876, 'hike_25': -0.005278, 'hike_50plus': 0.005022, 'cut_25': 0.007475, 'cut_50plus': 0.007657}`. The same command's floor-pin decomposition (L178-block, corrects the 2026-07-26 log entry): `python3 -c "import sys,statistics as st;sys.path.insert(0,'scripts');import q48_s55_fomc_lag_probe as P;r=P.load_family_records(P.DEFAULT_TAPE_DIR)[0];t,_=P.pass_times(r);o,_=P.build_observations(r,t,P.ladder_sums(r));b=set(c for w in P.detect_burst_windows(t) for c in w['capture_ids']);f=[x for x in o if x['capture_id'] not in b and x['meeting']=='2026-07'];p=[x for x in f if abs(x['kalshi_yes_price']-0.01)<1e-9];n=[x for x in f if abs(x['kalshi_yes_price']-0.01)>=1e-9];print(len(f),len(p),sum(abs(x['raw_gap']) for x in p)/sum(abs(x['raw_gap']) for x in f),st.mean(abs(x['raw_gap']) for x in p),st.mean(abs(x['raw_gap']) for x in n),st.mean(x['poly_ask_signal'] for x in p),sum(1 for x in p if x['raw_gap']>=0)/len(p))"` → `2400 1248 0.43334641915765215 0.006191506410256411 0.008770833333333332 0.003810096153846154 0.999198717948718` | **test** — `tests/test_q48_s55_fomc_lag_probe.py::test_normalized_gap_mechanical_identity_is_flagged` (asserts `"MECHANICAL IDENTITY"` in the report's `normalized_gap_note` AND that the pooled mean is `< 1e-12` on a complete ladder) pins the report's `normalized_gap_note`, which states the mechanical identity in the artifact itself (`"the pooled MEAN normalized gap is ~0 by construction, not by agreement (same class of trap as L51's complementary hit rates)"`), so the degenerate statistic cannot be read off the report without its own disclaimer attached. |
| L185 | 2026-07-27 | **A capped, newest-first-paginated harvest with no time-window parameter has a coverage ceiling of `cap / event_rate`, independent of how often the leg runs — and it does not improve as the ledger accumulates more days.** `collection/settlement_ledger.py`'s daily pull (`MAX_SETTLED_MARKETS=5000`, no `min_close_ts`/`max_close_ts`) reaches back only ~1.3-3.8 hours of `close_time` per pass (independently re-derived: 800 rows/1.26h, 4,200/3.83h, 5,000/3.25h) against a platform settling ~1,100-1,540 markets/hour — a ceiling of ~13.5% of any day's true settlement population, non-randomly biased to the few hours before the 10:00 UTC cron fire, and NOT fixed by the two committed live-day files having zero `close_time` overlap (each day just restarts the newest-first cursor from "now"). Consequence traced end-to-end: the family's one documented join-rate result (Q21/S52's 605/10,605 = 5.7%, `findings/2026-07-25-q21-idea-gen-round.md`) turns out to be **entirely the 605 migrated legacy rows — zero of the 10,000 live-harvested rows are joinable** — so "collect more days" will not raise that rate; the cap/window shape is the binding constraint, not sample size. Generalizes beyond this one collector: any future capped-pagination collector must reconcile its cap against the observed per-hour event rate for what it's enumerating, not choose the cap as a bare memory guardrail. | `findings/2026-07-27-settlement-ledger-tape-audit.md` (`tape-auditor` subagent + main-context independent re-derivation of the capture-window/join-rate numbers, 2026-07-27 idle-run policy (c)) | **CLOSED (2026-07-27, research loop, idle-run policy (a)).** The candidate check below was built exactly as described. `scripts/tape_gap_monitor.py`: a `CAPPED_PAGINATION_FAMILIES` config (one entry today — `settlement_ledger`: `time_key="close_time"`, `cadence_hours=24.0`, `cap=5000`, `min_rows_for_span=50`, `span_ratio_alert=0.5`; a second capped-pagination collector is one dict entry, nothing is settlement_ledger-specific) + `capped_pagination_span_coverage(tape_root, family)`, which groups every committed line by `capture_id` and reports per-capture `span_hours`, `span_ratio` (= `coverage_ceiling_fraction`) and `rows_per_hour` — L185's `cap / event_rate` arithmetic made computable — plus a `_parse_event_time` bare-`Z` helper (see L187). Attached to the family health record under `"capped_pagination_span"` in `evaluate_family`. `scripts/invariants.py`: `_capped_pagination_span_issues()` + pure `capped_pagination_span_warning()`, wired into `main()` as a **stderr-only, `except BaseException`-wrapped, NON-GATING advisory** (same posture as `hollow_crypto_ladder_warning` / `dead_collector_leg_warning`), importing the config and check from tape_gap_monitor via the existing `_load_tape_gap_monitor` path-import rather than re-declaring them. **Non-gating is deliberate:** a cap/window mismatch is a collector DESIGN property that no cloud run can repair mid-loop and that is true of every pass ever committed, so gating would halt the loop indefinitely (the L156 reasoning). Enforcing tests (+18 across the two files), both acceptance tests anchored to the REAL committed tape rather than fixtures: `tests/test_tape_gap_monitor.py::test_acceptance_12_l185_settlement_ledger_real_tape_span_vs_cadence` and `tests/test_invariants.py::test_acceptance_l185_settlement_ledger_real_tape_advisory_fires`; non-gating posture pinned by `tests/test_invariants.py::test_capped_pagination_span_warning_never_gates_exit_code_L185` and `::test_capped_pagination_span_advisory_raise_cannot_flip_exit_code_L185`; the untouched alert path pinned by `tests/test_tape_gap_monitor.py::test_capped_pagination_span_never_touches_the_alert_path_L185`. **No existing number moved:** the STALE/UNDER-CAPTURE `alert`/`alert_reason` path is byte-identical with and without the new computation (test-pinned key-by-key), `python3.11 scripts/invariants.py --full` still exits 0 with `invariants: all green` on stdout (the advisory goes to stderr), and the pytest failure set is unchanged — baseline on clean `origin/main` (ac8a758) 2069 collected / 2 failed, after this diff 2087 collected / 2085 passed / 2 failed, the same two pre-existing `tests/test_q42_funding_estimate_path_inference.py` real-tape-drift failures, **0 introduced**. Re-runnable (measured 2026-07-27 from the repo root with `python3.11`): `python3 -c "import sys;sys.path.insert(0,'scripts');from pathlib import Path;import tape_gap_monitor as T;c=T.capped_pagination_span_coverage(Path('tape'),'settlement_ledger');print(c['n_captures'],c['n_captures_narrow']);[print(x['capture_id'],x['n_rows'],x['span_hours'],x['span_ratio'],x['narrow']) for x in c['captures']]"` → `4 3` / `20260717T122238Z 605 193.605 8.066875 False` / `20260717T122243Z 800 1.2558 0.052326 True` / `20260717T122302Z 4200 3.8281 0.159502 True` / `20260722T103141Z 5000 3.2514 0.135475 True`. Tier: **test** — enforcement is the unit test written WITH the fix, not a static invariant (the correct cap/window tradeoff remains a per-endpoint design call for Ryan; this run built the DETECTOR, not the fix, and `collection/settlement_ledger.py` is unmodified). Was: **UNENFORCED** — a coverage-adequacy judgment for capped-pagination collectors, not statically assertable (the correct cap/window tradeoff is per-endpoint and per-event-rate, a design call like L6/L28/L32's population choices); candidate: a `tape_gap_monitor.py`-style per-`capture_id` `close_time`-span-vs-cadence check that flags a family whose captured window is much narrower than its firing interval, mirroring L172's `retrospective_coverage` shape but for "span per pass" instead of "union of embedded timestamps." Not yet built. |
| L186 | 2026-07-27 | **A coverage detector needs a NEGATIVE CONTROL drawn from the SAME family, not just a positive hit.** `settlement_ledger`'s own 605-row `migrated:q26_settlement_cache` legacy backfill (span **193.605h**, ratio **8.066875**) sits in the same committed tape as the three narrow live captures (ratios **0.052326 / 0.159502 / 0.135475**) and is correctly **NOT** flagged. That single-family asymmetry is what demonstrates the check measures **span**, not row count (the un-flagged capture has the FEWEST rows, 605 vs 800/4,200/5,000), not file age, and not family membership — a detector validated only against examples that all flag is indistinguishable from `return True`. Sibling of this repo's existing "verified against real committed tape, not just self-report" discipline (L154/L172), but one level stricter: the real tape must contain **BOTH** classes, and the detector must be asserted to split them. | L185 close-out build, 2026-07-27 research loop (idle-run policy (a)); numbers from `findings/2026-07-27-settlement-ledger-tape-audit.md` re-derived by the check. Exact command (measured 2026-07-27, `python3.11` from repo root): `python3 -c "import sys;sys.path.insert(0,'scripts');from pathlib import Path;import tape_gap_monitor as T;c=T.capped_pagination_span_coverage(Path('tape'),'settlement_ledger');print(c['n_captures'],c['n_captures_narrow']);[print(x['capture_id'],x['n_rows'],x['span_hours'],x['span_ratio'],x['narrow']) for x in c['captures']]"` → `4 3` / `20260717T122238Z 605 193.605 8.066875 False` / `20260717T122243Z 800 1.2558 0.052326 True` / `20260717T122302Z 4200 3.8281 0.159502 True` / `20260722T103141Z 5000 3.2514 0.135475 True` | **test** — `tests/test_tape_gap_monitor.py::test_acceptance_12_l185_settlement_ledger_real_tape_span_vs_cadence` asserts the legacy capture is `narrow is False` (with `span_hours > 24*7` and `span_ratio > 1.0`) in the SAME assertion block that asserts the three live captures are `narrow is True` with `span_ratio < 0.2`, so the split cannot be satisfied by a constant; `tests/test_invariants.py::test_acceptance_l185_settlement_ledger_real_tape_advisory_fires` asserts `flagged == {"20260717T122243Z","20260717T122302Z","20260722T103141Z"}` and `"20260717T122238Z" not in msg`. Backed on the synthetic side by the deliberate pair `tests/test_tape_gap_monitor.py::test_capped_pagination_wide_span_is_not_flagged_L185` / `::test_capped_pagination_narrow_span_vs_cadence_is_flagged_L185`. The GENERAL rule (every future detector must ship a same-population negative control) is a methodology judgment and is not statically assertable — these tests pin the one instance. |
| L187 | 2026-07-27 | **A bare-`Z` timestamp is a SILENT per-family blind spot for any shared ISO helper of the defensive `return None` kind.** `scripts/tape_gap_monitor.py::_parse_iso` returns `None` (not a crash) for `2026-07-22T10:30:00Z` on Python < 3.11, and `settlement_ledger`'s `close_time` is written in exactly that bare-`Z` form — so a new consumer reusing the helper as-is would have found "no parseable event times" for the **ENTIRE** family and printed a confident zero rather than failing loudly. Caught during the L185 build; fixed with a `_parse_event_time` string-normalization wrapper (`Z` → `+00:00`, then the existing `_parse_iso`), which adds **NO** new `datetime.fromisoformat` call site, so `inv_no_raw_datetime_fromisoformat`'s 1-site allowance for that file is unchanged. **Sibling of L136/L138, which are about the CRASH; this is about the SILENT SKIP** — the defensive helper converts a parse failure into a *wrong count* instead of an exception, and a wrong count reads as evidence. Rule: any new field consumed through a repo ISO helper must be spot-checked against the real committed bytes for the `Z` / short-fraction shapes before its counts are believed. | L185 close-out build, 2026-07-27 research loop (idle-run policy (a)); `scripts/tape_gap_monitor.py::_parse_event_time` docstring carries the same statement. Committed-bytes check (measured 2026-07-27, `python3.11` from repo root): `python3 -c "import json,glob;v=[json.loads(l)['close_time'] for f in glob.glob('tape/settlement_ledger/**/*.jsonl',recursive=True) for l in open(f)];print(len(v),all(x.endswith('Z') for x in v),sum('+' in x for x in v),v[0])"` → `10605 True 0 2026-07-07T12:29:58Z` — all 10,605 committed `close_time` values are bare-`Z`, zero carry an explicit offset. | **test** — `tests/test_tape_gap_monitor.py::test_acceptance_12_l185_settlement_ledger_real_tape_span_vs_cadence` cannot pass if the bare-`Z` shape is silently skipped: it asserts `n_captures_judged == 4`, `n_captures_not_judged == 0` and the exact per-capture row counts (605/800/4,200/5,000) on the REAL committed tape, all of which collapse to zero-judged under the un-wrapped helper. The fixture builder `_sl_capture` in `tests/test_tape_gap_monitor.py` emits `strftime("%Y-%m-%dT%H:%M:%SZ")` bare-`Z` timestamps deliberately, so the synthetic block would fail the same way. Honestly: the **GENERAL** rule (every future helper reuse spot-checked against real bytes) is **not** statically assertable — a static check cannot know which field a future consumer will read — so these tests pin the one instance, and the rule itself lives as ledger guidance. |
| L188 | 2026-07-27 | **Formal disposition of L22, L27, L28, L32, L39, L45, L51, L59, L64, L65, L66, L68, L69, L76, L86, L105, L117, L119, L162, L164, L165 (supersedes each row's enforcement column; every lesson's content is unchanged and no existing row was edited).** Idle-run policy (a): an independent `verifier` parsed the whole ledger and classified every row whose enforcement column starts `**UNENFORCED**` — the file's own stated standing work queue (header line 9). Result: **185 rows parsed, 21 marked `**UNENFORCED**`, and ALL 21 are already-BUILT stale markers with file:line evidence; 0 genuinely open, 0 terminal-as-protocol-unbuilt.** Terminal tiers, with evidence: **L22** test (`core/source_tag.py:17-27` documents the deliberate decision NOT to add `real_bid`; `tests/test_invariants.py::test_db_real_bid_tag_is_caught_as_invalid_enum` — its "kb-distiller to decide" is DECIDED); **L27** test (`core/bootstrap.py:122 clears_tick_magnitude()`; `tests/test_bootstrap.py:75-97`; `.claude/agents/edge-prober.md:34`); **L28** test (`core/bootstrap.py:138 floor_pinned_fraction()`; `tests/test_bootstrap.py:102-120`; `edge-prober.md:38`); **L32** test (`core/bootstrap.py:155 bracket_by_movement()`, returns `frac_frozen` :177 AND both cuts; `edge-prober.md:47`; adopter `scripts/q37_weather_summer_makerno_probe.py:36`); **L39** test (`core/bootstrap.py:241 decompose_edge_by_leg_volume()` + `core/income_legs.py:23,37`, 12 tests; `edge-prober.md:99`); **L45** test (`core/timeutil.py:105 parse_crypto_hour_token_close_utc()`; `tests/test_timeutil.py:19-66`); **L51** test (`core/bootstrap.py:281 disagreement_subset_calibration()`; `tests/test_bootstrap.py:451-507`; `edge-prober.md:211`); **L59** test (`core/reversal.py:30 direction_precheck()`; `tests/test_reversal.py`, 11 tests) — **RESIDUAL recorded honestly: it has ZERO callers outside its own test and no `L59` mention in any `.claude/agents/*.md`, so a future momentum probe can still hand-roll a frequency-only classification without tripping anything**; **L64** test (`core/timeutil.py:125/147/160`; `tests/test_timeutil.py:74-82`; `edge-prober.md:139`); **L65** protocol (`edge-prober.md:148-160`, verbatim idea-stage kill incl. the 0.024h / empty-book numbers); **L66** protocol (`edge-prober.md:161-168`); **L68** protocol (`edge-prober.md:169-176`); **L69** protocol (`edge-prober.md:109-121`); **L76** test (`core/bootstrap.py:187 collapse_duration_gated_runs()`; `tests/test_bootstrap.py:171-207`; adopter `scripts/q43_perp_binary_consistency_probe.py:73`); **L86** test (`core/bootstrap.py:344 catastrophic_leg_drop_stress_check()`; `tests/test_bootstrap.py:304-366`); **L105** protocol (`edge-prober.md:188-196`); **L117** test/invariant (`scripts/tape_gap_monitor.py:448 collector_bucket()` + `:460 diagnose_collector()`; `tests/test_tape_gap_monitor.py:280-290`; surfaced non-gating at `scripts/invariants.py:2286`; already superseded in substance by L118); **L119** test (`core/pricing.py:186 book_notional_at_touch()` with `LOW_TOUCH_NOTIONAL_WARN_DOLLARS=50.0`; `tests/test_pricing_book_notional.py`, 8 tests); **L162** protocol (candidate (b), which the row itself calls the cheaper and probably correct form, is built at both named sites — `LOOP-QUEUE.md:72-77` step-4 fresh-gate-line rule and `.claude/agents/kb-distiller.md:26-30`; candidate (a) deliberately NOT built, since it would require running the full suite inside `invariants.py`, a regression of that file's offline/fast contract); **L164** test (`scripts/burst_chunk_plan.py:115/149/216` `--protect`; `tests/test_burst_chunk_plan.py:235-241` pins the hand-verified FOMC recipe `[16,14,14,14,14,12]`; `ops/burst_capture_chunked.md:89-91` documents the same invocation; CLI re-run reproduces it byte-identically); **L165** protocol (`.claude/agents/verifier.md:36-41` and `edge-prober.md:227-231`). | `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (2026-07-27 idle run, policy (a); producer + independent `verifier` round, evidence re-run per row). Row census command (measured 2026-07-27, repo root): `python3 -c "import sys;sys.path.insert(0,'scripts');import invariants as I;r=I._parse_lesson_rows();u=[x for x in r if x[2].startswith('**UNENFORCED**')];print(len(r),len(u),[x[0] for x in u])"` → `185 21 [...]`. Same disposition shape as **L116**/**L118**/**L124**/**L163**/**L172**, but for the whole queue at once rather than one row. | **disposition (machine-readable)** — DISPOSES: L22, L27, L28, L32, L39, L45, L51, L59, L64, L65, L66, L68, L69, L76, L86, L105, L117, L119, L162, L164, L165. Each ID's own terminal tier is stated in the lesson column above (14 `test`, 6 `protocol`, 1 `test/invariant`); this row moves no code, it records the audit and permanently suppresses those rows in `scripts/invariants.py::_stale_unenforced_candidate_issues` via the canonical marker whose grammar is documented and test-pinned at `scripts/invariants.py::_lesson_disposed_ids`. **The one part NOT statically assertable:** that each cited artifact really enforces the lesson it is cited for — that is a read-and-compare judgment (L165's own terminal finding), performed here by a `verifier` and re-checkable only by re-reading the cited file:line. **The ledger's `**UNENFORCED**` work queue is empty as of this row**, and — unlike every prior "backlog empty" claim — the emptiness is now machine-visible rather than prose. L59's zero-caller residual is recorded, not closed. |
| L189 | 2026-07-27 | **A detector that reports 0 issues must PUBLISH its recall, not merely have one.** `scripts/invariants.py::_stale_unenforced_candidate_issues()` — built as L152's own follow-up, with the single job of catching lesson rows still marked `**UNENFORCED**` whose enforcement was already built — extracted **0 candidate tokens from 21 of 21 already-built stale rows and printed nothing at all**. Its silence was byte-identical to the silence of a genuinely clean queue, so the ledger's standing work queue could be **100% false** (185 rows parsed, 21 `**UNENFORCED**`, 21/21 stale, 0 genuinely open) while its purpose-built detector reported green. This is the STRONGER form of **L155** (a low count is precision evidence, not recall): knowing the recall internally is not enough — the figure must be emitted **in the warning text**, so a reader of the output can tell "found nothing" from "cannot see most of the population". The rebuilt advisory now prints, every run, that extraction **reaches 7 of 21 open rows, 8 hits (`func=0, path_symbol=1` L76, `script_flag=1` L164, `agent_charter=5` L28/L32/L51/L105/L165)**, names the disposed count, and states in-line that "a 0-issue report is a COVERAGE limit of this detector, NOT evidence of a clean queue". | `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (2026-07-27 idle run, policy (a)); strengthens **L155**, and is the recurrence of **L152**'s own failure mode one level up (L152 was "a stale marker mis-routes a run"; this is "the stale-marker detector cannot see stale markers"). Recall measured against a frozen fixture of the 21 rows, `tests/fixtures/lessons_unenforced_21_2026-07-27.md` (byte-identical copies), measured 2026-07-27: `python3 scripts/invariants.py --full` prints the figure on stderr; the 185/21/0 census command is on L188's row. | **test** — `tests/test_stale_unenforced_advisory.py` (49 tests) pins the recall figure itself (7-of-21, and the per-matcher split), the exact blind-spot ID set as a named constant, and the presence of the coverage-limit sentence in the emitted warning — so the number cannot silently drift and the disclaimer cannot be dropped. Advisory stays NON-GATING (a stale marker is bookkeeping, not a data-integrity violation — the L156 posture). Honestly NOT assertable: that the PUBLISHED recall is the true recall against the population of all future lesson prose. It is measured against one frozen 21-row corpus; a static check cannot know the shape of a row nobody has written yet, so the general rule ("every detector publishes recall") lives as ledger guidance plus this one pinned instance. |
| L190 | 2026-07-27 | **A queue that only a machine can close needs a MACHINE-READABLE close marker — enforcement status flipped in prose is unparseable — and a precision requirement has a measurable recall cost that must be published alongside it.** Twenty-one ledger rows were closed in substance by later runs, but each closure lived only as prose ("built by a later run", "supersedes L…"), which no scanner can distinguish from a mention; consequently the rows stayed on the queue forever and the detector had no way to stop re-reporting a row a human had already adjudicated. The fix is ONE canonical marker in the ENFORCEMENT column only — case-sensitive literal `DISPOSES:`, then `L<digits>` tokens separated by commas and/or spaces, terminating at the first non-separator token (a `.` ends the list); a prose mention of an ID NEVER suppresses. The second half of the lesson is the cost: making the `agent_charter` matcher precise (it only fires when the named charter file actually cites the row's own ID) means it MISSES rows whose charter does not self-cite — **measured: 22 of the 164 non-`UNENFORCED` rows name a charter that does not cite their own ID**. That number is published with the matcher, because an unpublished precision/recall tradeoff reads to the next agent as a bug rather than a decision. | `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (2026-07-27 idle run, policy (a)). Marker grammar documented at `scripts/invariants.py::_lesson_disposed_ids` (docstring) and first used by **L188**. Precision-cost figure measured 2026-07-27 over `kb/lessons/00-lessons.md` (164 non-`UNENFORCED` rows at measurement time; the count is corpus-relative and will move as the ledger grows — per **L161/L162** it is quoted with its command and its measurement time, not as a standing fact): `python3 scripts/invariants.py --full` (advisory text) and the matcher tests below. | **invariant (advisory) + test** — the marker is parsed by `scripts/invariants.py::_lesson_disposed_ids` and consumed by `_stale_unenforced_candidate_issues()`; grammar and the "prose mention never suppresses" rule are pinned case-by-case in `tests/test_stale_unenforced_advisory.py`, as is the published precision cost. Enforcement-column-only scoping is deliberate: a `DISPOSES:` written in the lesson-text column must NOT suppress, or a quoted example would silently close rows. NOT statically assertable: whether a disposition is CORRECT — the marker records that a human/verifier adjudicated the row, it cannot check the adjudication. That is the same honest terminal state as L165's citation rule. |
| L191 | 2026-07-27 | **A tape-pinned acceptance test must name the exact `dt=` day-slice its number was measured over.** Four `@_real_tape` pins in `tests/test_q42_funding_estimate_path_inference.py` globbed `tape/perp_tape/dt=*.jsonl` — an open-ended glob over a **live, still-growing** collector family — so three routine days of capture (07-25/26/27) red-lined `main`'s binding gate with **zero code change anywhere**: frozen slice `dt=2026-07-17..24` vs today's full glob gives records 1667→1803, n_estimate_groups 299→377, n_joined_windows 286→364, n_discriminating **42→58**, LOO n_tickers_dropped 7→8, n_funding_times_dropped 18→23, n_drops **67→89**, p_hard_gap(11) **0.2057→0.3655**, p_hard_gap(14) 0.1042→0.2390. At 20,000 draws the binomial se at p≈0.2 is ≈0.0029, so 0.2057→0.3655 is **≈55 se** — a population change, not Monte-Carlo noise. Three corollaries, each load-bearing: (1) **two of the four pins were still GREEN purely by luck** (`..._no_leave_one_out_drop_restores_a_hard_gap`, `..._leave_one_out_max_gap_is_still_negative` — extreme-order statistics that happened not to move), so **a green tape pin over an open glob is NOT evidence that the population is stable**; (2) the growth-vs-mutation diagnosis must be settled by **blob-comparing the old day-files** (`git rev-parse <rev>:<path>` at the pinning commit vs HEAD), never by mtime — here all 8 files `dt=2026-07-17..24` and the probe script are byte-identical between `cebe691` and HEAD, and `ac8a758`'s 1,798 recovered stranded lines touched ONLY `dt=2026-07-27.jsonl`, which **FALSIFIES** the "recovery mutated old day-files" hypothesis for `perp_tape`; (3) that diagnosis must come BEFORE any re-pin, because **re-pinning a number that moved only because the glob widened destroys the original claim** rather than updating it. Also: a finding's *Reproduce* command must name the frozen slice, not `dt=*` — `findings/2026-07-24-q42-funding-estimate-path-inference.md`'s *Artifacts* footer named the slice correctly while its *Reproduce* section said `dt=*`, so the published command had stopped reproducing the published numbers (corrected in place this run). Sibling of **L140**, which froze the `now` of a real-tape alert test; this freezes the **POPULATION** of a real-tape statistic test. | `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (2026-07-27 idle run, Unit 2). Baseline before repair (measured 2026-07-27 on `main` HEAD `ad6b95e`): `python -m pytest -o addopts="" -q` → **2087 collected, 2085 passed, 2 FAILED** (`test_tape_leave_one_out_67_drops_decomposes_as_7_18_42`, `test_tape_random_same_size_subsets_reproduce_the_dense_cuts_hard_gap`), recorded as "known pre-existing real-tape-drift failures" by PR #215. Blob check (measured 2026-07-27): `for d in 17 18 19 20 21 22 23 24; do git rev-parse cebe691:tape/perp_tape/dt=2026-07-$d.jsonl; git rev-parse HEAD:tape/perp_tape/dt=2026-07-$d.jsonl; done` → identical pairs; same for `scripts/q42_funding_estimate_path_inference.py`. After repair: `python -m pytest -o addopts="" -q` → **2136 passed, 0 failed**, EXIT 0. | **test** — **no pin was moved and no tolerance was widened**; the population was made explicit as `_FROZEN_TAPE_SLICE_DAYS` in `tests/test_q42_funding_estimate_path_inference.py`, plus a ratchet test `::test_frozen_tape_slice_is_intact_and_its_population_is_unchanged` that fires if a future stranded-line recovery ever union-appends into one of those 8 closed day-files. A tree-wide audit found these four were the ONLY pinned-statistic-over-open-ended-live-glob tests; the other real-tape tests are already frozen (`tests/test_dead_collector_leg_advisory.py::_SLICE_MAX_DAY`, `tests/test_orderbook_depth_hollow_ladder_audit.py::_FROZEN_MAX_DAY`) or assert properties/ratios rather than statistics, so there is nothing else to repoint today. Honestly NOT (yet) statically assertable: a scanner that flags "a `dt=*` glob inside a test that pins a numeric statistic" would have to tell a pinned STATISTIC from a pinned PROPERTY — the audit above shows both shapes exist and only the former is a defect — so the general rule stays ledger + protocol guidance and the four instances are pinned by the frozen constant. **Q42's own verdict is UNCHANGED by this row: still H1 UNDECIDABLE, no CI, no registry change** — this is test hygiene, not evidence. |
| L192 | 2026-07-27 | **An acceptance test that asserts a LIVE-DOCUMENT advisory FIRES on the real tree binds the gate to that document's current content — and the advisory exists to CHANGE that content.** Writing **L188** (which formally disposes of all 21 `**UNENFORCED**` rows) drove `_stale_unenforced_scan`'s `n_open_unenforced` to **0**, so `stale_unenforced_candidate_warning()` correctly returned `None` (its documented contract: a zero-issue scan that reached every open row is silent) — and that turned two real-tree tests RED: `tests/test_stale_unenforced_advisory.py::test_advisory_is_non_gating_on_the_real_tree` (asserts `"open UNENFORCED row(s)" in captured.err`, commented "the recall statement always prints") and `tests/test_invariants.py::test_stale_unenforced_candidate_warning_never_gates_exit_code` (asserts `"UNENFORCED lesson row(s)" in captured.err`). **Nothing was wrong with the code and nothing was wrong with the ledger:** the tests bind a NON-GATING advisory's *firing* to the mutable content of `kb/lessons/00-lessons.md`, so the exact act the advisory exists to provoke — disposing of every stale row — is what breaks them. **The sharpest part: appending THIS row turned them green again**, because L192 is itself an open `**UNENFORCED**` row that names no machine-checkable artifact, which restores the coverage-only note (`Extraction reached 0 of 1 open UNENFORCED row(s) (21 formally disposed via `DISPOSES:`)`) and with it both asserted substrings. **So the suite is green only while the queue is non-empty** — a repo that ever achieves a genuinely empty, fully-disposed lessons queue goes RED, and the greenness observed today is a coincidence of this row's own existence, not a property anyone verified. Same family as **L191**: there a statistic was pinned to a live-GROWING tape population; here a warning's presence is pinned to a live-EDITED document. The property those two tests genuinely own — *the advisory can never touch the exit code* — is independent of whether it fires. **Proposed minimal fix (NOT applied this run — see enforcement): assert `rc == 0` and `"invariants: all green" in out` unconditionally, and assert the recall sentence only when the advisory is present** (`if "UNENFORCED" in err: assert "does NOT affect the exit code" in err`), moving the "a fired advisory always carries its recall sentence" claim onto the frozen, immutable 21-row fixture (`tests/fixtures/lessons_unenforced_21_2026-07-27.md`) instead of onto the live ledger. | This run's own gate, 2026-07-27 idle run (`findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`, Unit 1). Exact reproduction, measured 2026-07-27 from the repo root. (i) With L188 appended and L192 NOT yet written: `python3 -m pytest -o addopts="" -q tests/test_stale_unenforced_advisory.py tests/test_invariants.py` → `2 failed, 237 passed`; full suite `python3 -m pytest -o addopts="" -q` → **2134 passed, 2 failed** (only those two, **0 other regressions**; the same tree was 2136/0 immediately before L188 was written). (ii) With L192 appended: the same two node-ids → `2 passed in 89.99s`. Queue state re-derivable at any time with `python3 -c "import sys;sys.path.insert(0,'scripts');import invariants as I;r=I._parse_lesson_rows();d=I._lesson_disposed_ids(r);u=[x[0] for x in r if x[2].startswith('**UNENFORCED**')];print(len(r),len(u),[x for x in u if x not in d])"` → `190 22 ['L192']` (was `189 21 []` at step (i)). | **UNENFORCED** — deliberately not fixed by this row's author: `scripts/invariants.py` and `tests/` were **FROZEN for a concurrent `verifier` round** at the time (the **L182** rule — a re-run against a moving file is not a verification), and the kb-distiller lane does not rewrite another agent's in-flight artifact. The two-line repair is stated verbatim in the lesson column so no rediscovery is needed; it is **owed to the research-lead** and this row must be superseded when it lands. The gate is currently GREEN, but for the accidental reason recorded above — **do not read today's green as evidence this is fixed.** Not statically assertable in general (a scanner cannot tell an acceptance test that legitimately pins a real-tree signal from one that pins a mutable document's current state); the assertable half is the narrow shape "an advisory test whose assertion is that the warning TEXT appears, over an artifact under `kb/`", which is a candidate for a future `scripts/invariants.py` rule and is not built. |
| L193 | 2026-07-27 | **CORRECTION of L188 (append-only supersession, the L124/L172/L178/L179 precedent — L188's own text is untouched and its per-row evidence STANDS): L188's 21-row audit is sound, but its closing sentence "the ledger's UNENFORCED work queue is empty as of this row" is FALSIFIED.** An independent `verifier` re-measured the same ledger and found the queue was never empty: **190 rows, 23 carrying an UNENFORCED marker, 21 disposed by L188, leaving exactly 2 genuinely OPEN — `L145` and `L192`.** `L145` (the `collection/ws_depth.py` vs `inv_order_endpoints_confined` policy collision, already flagged to parent/Ryan and deliberately untouched) was never audited into the 21 and was invisible to the census for TWO independent reasons, each sufficient on its own: the row misparsed (L194) and its marker spelling did not match the literal tested for (L195). Retracted with it: the derived claim, in **L192**'s own row and in this run's finding, that L192 was "the queue's ONE genuinely-open row" — it was one of two. What survives untouched: the 21 dispositions, their file:line evidence, their terminal tiers, and L59's zero-caller residual. The rule this row carries forward: **a whole-queue "it is empty now" claim is a much stronger claim than the per-row audit that produced it, and must be re-derived by a second party against the LIVE artifact before it is written down** — the audit and the closure are separate claims with separate evidence. | This run's `verifier` round (REFUTED), 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (corrected in place). Live census, measured 2026-07-27 from the repo root AFTER the parser/marker remediation: `python3 -c "import sys;sys.path.insert(0,'scripts');import invariants as I;r=I._parse_lesson_rows();d=I._lesson_disposed_ids(r);u=[x[0] for x in r if x[2].lstrip().startswith('**UNENFORCED')];print(len(r),len(u),[x for x in u if x not in d])"` → `190 23 ['L145', 'L192']`. Same run's advisory line: `Extraction reached 0 of 2 open UNENFORCED row(s) (21 formally disposed via DISPOSES:)`. Counts quoted with command + time per **L161**/**L162**; this row's own append moves them. | **test** — `tests/test_stale_unenforced_advisory.py::test_real_tree_current_state_is_pinned` deliberately pins the ARITHMETIC on the live tree (`n_disposed + n_open_unenforced == n_unenforced`, `flagged_ids ⊆ open_unenforced_ids`, `n_rows >= 190` as an append-only floor) rather than a live count, because another agent's legitimate ledger edit must not red-line the gate (that is **L192**'s lesson, applied). The exact numbers live on the frozen fixture `tests/fixtures/lessons_unenforced_21_2026-07-27.md`, which nobody rewrites. NOT statically assertable: that a disposition is CORRECT (L165/L188's own terminal state), and whether `L145`'s policy collision should be resolved by exempting `collection/ws_depth.py` or by moving the code — that is Ryan's call and stays open. |
| L194 | 2026-07-27 | **A markdown ledger row is NOT `str.split` on a bare pipe character — and `cols[-2]`, the obvious fix, is also wrong.** `scripts/invariants.py::_parse_lesson_rows` split rows on a naive bare-pipe split, so any row whose prose contains a pipe shifted its enforcement column: **14 of 190 rows misparsed — L25, L37, L62, L89, L109, L145, L147, L161, L173, L177, L179, L180, L183, L184.** The trap that makes this expensive: the seductive repair "just take the second-to-last cell" **passes a spot-check on 13 of those 14** and fails only on **L147**, whose embedded pipe sits INSIDE the enforcement cell itself — so a fix validated on a handful of examples looks total and is not (**L186**'s negative-control rule, recurring in a parser). Only a delimiter-aware splitter is right for 190/190: a backslash-escaped pipe is a literal, and a pipe inside a backtick code span is a literal. The general form: **when a format has escaping and quoting rules, a regex over the delimiter is a guess; the parse must honour the format, and the acceptance test must be the WHOLE corpus, not a sample.** Sibling of **L161** (a count over a growing corpus needs its command); this is the layer below — a count whose EXTRACTOR is wrong is wrong at every measurement time. | This run's `verifier` round (REFUTED the census that L188 rested on), 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Reproduction (measured 2026-07-27, repo root): `python3 -m pytest -o addopts="" -q tests/test_stale_unenforced_advisory.py::test_naive_pipe_split_really_does_break_those_rows` — the counterfactual test that asserts, for each of the 14 IDs, that the OLD naive rule disagrees with the correct column AND that it agrees on every other row (so the pin cannot decay into a tautology). The 14 IDs are a named constant, `MISPARSED_14_ENFORCEMENT_PREFIX`. | **test** — the delimiter-aware splitter is pinned by `tests/test_stale_unenforced_advisory.py::test_split_lesson_row_treats_escaped_pipe_as_literal`, the 14-row prefix pin, and the naive-split counterfactual above; `tests/test_invariants.py` collects 193 and the advisory file 65 (both measured 2026-07-27, `python3 -m pytest -o addopts="" -q --collect-only <file>`). NOT closed, and recorded as such in **L200**: `scripts/gen_problems_dashboard.py::cells` holds a SECOND, independent copy of this same parsing job with a DIFFERENT bug. |
| L195 | 2026-07-27 | **A status-marker detector must match the marker's SHAPE, not one literal spelling.** Even once the columns were parsed correctly, the membership test `startswith("**UNENFORCED**")` still missed **L145**, whose real enforcement cell reads `**UNENFORCED — UNRESOLVED COLLISION, flagged to parent/Ryan (NOT touched this docs-only pass).**` — the qualifier and the em dash sit INSIDE the bold span, so the closing `**` never lands where the literal expects it. The row was maximally visible to a human (it shouts UNRESOLVED) and completely invisible to the scanner. The rule: **a status token in prose is a prefix with optional qualifiers, never an exact string** — match `**UNENFORCED` and let the rest of the span be free text, or normalise the cell before testing. Corollary for every future status vocabulary in this repo: the detector and the writing convention must be introduced together, and the detector must be run against the existing corpus at the moment it is written (**L198**). | This run's `verifier` round, 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Measured 2026-07-27, repo root: the strict-literal test `x[2].startswith('**UNENFORCED**')` yields 22 rows and omits `L145`; the shape test `x[2].lstrip().startswith('**UNENFORCED')` yields 23 and includes it (`python3 -c "import sys;sys.path.insert(0,'scripts');import invariants as I;r=I._parse_lesson_rows();print(len([x for x in r if x[2].startswith('**UNENFORCED**')]), len([x for x in r if x[2].lstrip().startswith('**UNENFORCED')]))"`). | **test** — the shape-matching membership rule is exercised by `tests/test_stale_unenforced_advisory.py` (the L145-shaped marker case) and consumed by `scripts/invariants.py::_stale_unenforced_candidate_issues`. NOT statically assertable in general: no check can enumerate the qualifier spellings a future author will invent — the assertable half is that the CURRENT vocabulary is matched by shape and that the live-tree arithmetic in `test_real_tree_current_state_is_pinned` balances, which is what would surface the next missed row. |
| L196 | 2026-07-27 | **Two independent invisibilities can hide the SAME row twice — so verify a count's own extraction path BEFORE quoting the count as evidence that a queue is empty.** `L145` was missed by the column parser (**L194**) and, independently, by the marker literal (**L195**). Either defect alone would have hidden it; fixing only one would have left the audit still wrong while looking repaired, and the fix would have shipped with a green spot-check. This is the shape that turns a measurement into a false conclusion: the audit re-read 21 rows very carefully and never asked whether 21 was the right denominator. The rule: **an emptiness claim is a claim about the rows you did NOT see, so it is exactly the claim your extractor cannot support** — before writing "the queue is empty", re-derive the population by a second, independent path (a raw grep of the marker text, a hand count, another agent) and reconcile the two numbers. A single-path census can only ever prove a lower bound on what is open. | This run's `verifier` round, 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Independent cross-check available at any time (measured 2026-07-27, repo root): `grep -c "UNENFORCED" kb/lessons/00-lessons.md` returns a deliberately OVER-broad count (it also matches prose such as "the UNENFORCED backlog is empty" in L163/L166/L167), which is the point — a second path that errs in the opposite direction bounds the parsed figure from above; the parsed shape-matched figure is 23 with 2 open (command on **L193**). | **test** — partially: `tests/test_stale_unenforced_advisory.py::test_real_tree_current_state_is_pinned` asserts the balance `n_disposed + n_open_unenforced == n_unenforced` on the live tree, which is the machine-checkable half (a row that the extractor cannot classify at all still cannot silently vanish from the arithmetic). Honestly NOT assertable: the discipline of re-deriving a population by a second independent path before publishing an emptiness claim is a methodology rule for the agent, not a property of the tree — it lives here and in the finding, and this row is its citation. |
| L197 | 2026-07-27 | **A detector's emitted EVIDENCE must claim only what the matcher actually showed — a NAME match is not an enforcement match.** The stale-marker advisory's original wording implied that a hit meant the lesson's enforcement was already built. The worked counterexample is **L76**: the detector's hit was the RIGHT verdict cited to the WRONG artifact. It matched `tests/test_probe_ladder_coherence.py::test_runs_single_deep_snapshot_fails_duration_gate`, whose NAME contains "duration gate" but whose BODY asserts a snapshot COUNT (`MIN_SNAPS=2`) — precisely the mechanism L76's own text says is NOT a duration gate. L76 genuinely IS stale, but for an unrelated reason: **L93** later built `core.bootstrap.collapse_duration_gated_runs`. A reader who trusted the emitted evidence would have flipped the row's status on a string coincidence. This is **L165** (a citation is not a verification; only reading the cited artifact is) **recurring INSIDE the fix for L152** — the tool built to stop stale statuses was itself about to certify one on a name. Rule: every automated evidence string states its own epistemic ceiling in the text it emits ("the cell NAMES an artifact that EXISTS in the tree — a name match, never proof the enforcement is built"), and a hit is a prompt to READ, never a licence to flip. | This run's `verifier` round (REFUTED the L76 evidence line), 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Emitted-wording and hazard docstring at `scripts/invariants.py` (the matcher evidence formatter and the `_stale_unenforced_candidate_issues` docstring, which carries L76 as the named worked example). | **test** — the "name match only -- not proof it enforces `<id>`" clause is pinned per-matcher in `tests/test_stale_unenforced_advisory.py` (including a test asserting L76 is documented in the docstring as the name-coincidence hazard), so the disclaimer cannot be dropped and the wording cannot silently strengthen. NOT statically assertable: whether a named artifact truly enforces its lesson — that is the read-and-compare judgment **L165** already recorded as an honest terminal state. This row is the second recorded instance of that class, which is itself the argument for keeping the ceiling in the emitted text rather than in an agent's head. |
| L198 | 2026-07-27 | **A supersession grammar that matches ZERO existing rows is a claim about the future, not a measurement — report coverage of the convention already in use before adding a new one.** The `DISPOSES:` marker (**L190**) was introduced with `n_disposed = 0`, which was read as "no row has ever been closed". It was nothing of the kind: it was the arithmetic consequence of a brand-new grammar matching a corpus written before the grammar existed. The ledger already HAD a supersession convention, in prose, and **L107 enumerates the closure map** (L22→L24, L27/L28→L33+L34, L32→L35, L39→L73/L98, L45→L49, L51→L103, L59→L72/L94, L64→L101, L65→L104, L68→L106, L76→L93, L86→L99, plus L66→L116, L69→L112, L105→L107, L117→L118, L119→L121, L162→L163, L164→L166, L165→L167); **L163/L166/L167 each already state in prose that the backlog is empty**. So the "zero closures" reading inverted the truth. Rule: when you add a machine-readable form of a convention that already exists in prose, the FIRST number you publish is its coverage of the existing corpus (how many prose closures the new grammar can see, and how many it cannot), not its own hit count — otherwise the new grammar's emptiness gets read as the domain's emptiness. Same class as **L155**/**L189** (a low count is precision evidence, not recall) applied to a WRITING convention rather than a detector. | This run's `verifier` round, 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Prose closure map at `kb/lessons/00-lessons.md` **L107**; prose "backlog empty" statements at **L163**, **L166**, **L167**. Live disposed count, measured 2026-07-27 (repo root): `python3 -c "import sys;sys.path.insert(0,'scripts');import invariants as I;print(len(I._lesson_disposed_ids(I._parse_lesson_rows())))"` → `21`, all from **L188** — i.e. still zero rows predating the grammar. | **protocol + test** — the grammar itself is pinned (`scripts/invariants.py::_lesson_disposed_ids` + `tests/test_stale_unenforced_advisory.py`), and the advisory now prints the disposed count alongside the open count so the two are never confused. The rule ABOVE is protocol: it binds the author of the next machine-readable convention in this repo, and no static check can tell "a new grammar with no matches" from "a domain with nothing to match" — that distinction requires knowing the pre-existing prose convention, which is exactly the human judgment being demanded. Recorded UNENFORCEABLE in that specific sense, deliberately, not for want of trying. |
| L199 | 2026-07-27 | **CORRECTION of the counts published by L189 and by this run's own finding / log / LOOP-QUEUE line (append-only; L189's text is NOT edited).** Three numbers written during this run were false at the moment they were written. (1) **The gate line.** `2136 passed, 0 failed` was quoted as the post-edit gate; the suite was in fact `2 failed, 2134 passed` at that instant. The TRUE final gate, taken after the LAST edit of the run: `python -m pytest -o addopts="" -q` → **2155 passed in 1264.26s, 0 failed**; `python scripts/invariants.py --full` → exit 0, `invariants: all green`; `git status --porcelain -- tape/ paper/` empty. (2) **The test count.** `tests/test_stale_unenforced_advisory.py` was published as **49 tests** in L189's enforcement column, in the finding and in the log; it was **48** at that moment and is **65** now (48 + 17 remediation tests); `tests/test_invariants.py` now collects **193**; whole suite **2155**. (3) **The recall figure.** "reaches 7 of 21" is valid ONLY against the frozen fixture `tests/fixtures/lessons_unenforced_21_2026-07-27.md`, measured 2026-07-27 — it is **fixture-measured**, and must never be presented as a property of the live ledger, whose reading today is `0 flagged, extraction reached 0 of 2 open UNENFORCED rows, 21 formally disposed`. The lesson, and why this is not mere bookkeeping: **a gate line is a measurement with a timestamp, so quoting one taken before the last edit is publishing a stale observation as a current fact** — the same class as **L162**'s fresh-gate-line rule, here violated by the very run that cited it, and the reason **L162** exists. Every count in this row carries its command and its measurement time per **L161**/**L162**. | This run's `verifier` round (REFUTED all three), 2026-07-27, plus the remediation pass that followed; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md` (corrected in place), `kb/00-LOG.md` (this run's entry, corrected), `LOOP-QUEUE.md` (this run's Log-of-runs line, corrected). Test-count commands, measured 2026-07-27 from the repo root: `python3 -m pytest -o addopts="" -q --collect-only tests/test_stale_unenforced_advisory.py` → `65 tests collected`; `... tests/test_invariants.py tests/test_stale_unenforced_advisory.py` → `258 tests collected` (hence 193 for `test_invariants.py`). Baseline for the run, on `main` HEAD `ad6b95e`: `2087 collected / 2085 passed / 2 failed`. | **protocol** — the fresh-gate-line rule is already written at `LOOP-QUEUE.md` step 4 and `.claude/agents/kb-distiller.md` (built as **L162** candidate (b)); this row is the evidence that the rule is not yet self-enforcing, and the counterexample it needed. Deliberately NOT converted to an invariant: **L162** candidate (a) — having `scripts/invariants.py` run the suite to check the published number — was rejected then and is rejected now, because it would regress that file's offline/fast contract and would itself be a full-suite run quoting its own result. The honest terminal state is protocol plus this correction row. No number in L189, L191 or L192 is edited; where they disagree with this row, **this row is the current one**. |
| L200 | 2026-07-27 | **Three open items this run FLAGGED and deliberately did not close, recorded so they are not rediscovered.** (1) **A second, independently-buggy copy of the lessons-row parser.** `scripts/gen_problems_dashboard.py::cells` re-implements the job fixed in **L194** with a different bug: it honours a backslash-escaped pipe but NOT a pipe inside a backtick code span, so it still mis-buckets **3 of 190 rows — L89, L161, L173** — in the HTML dashboard. Two copies of one parsing job with two different bugs is the defect; the fix is one shared parser, not a second patch. (The same file also hardcodes `REPO = find_repo("/Users/ryan.gillon/Active/01-projects/kalshi.headless")`, so it raises `SystemExit` on any checkout not at that absolute path — it cannot run in a cloud sandbox at all, which is why the divergence went unnoticed and why the reproduction below inlines its regex rather than importing it.) (2) **The L138 residual.** `tests/test_invariants.py::test_raw_datetime_fromisoformat_warning_never_gates_exit_code` still asserts advisory TEXT against the real tree. It was left in place, not silently kept: its dependency is the repo's OWN source snapshot rather than a live ledger or growing tape, so it is not the **L192**/**L191** hazard shape — but it is the same family and is on notice. (3) **A documentation gap for Ryan:** the `DISPOSES:` grammar is documented in `scripts/invariants.py` and pinned by tests, but is NOT documented in `.claude/agents/kb-distiller.md`, so the agent expected to USE the marker is not told it exists. No file under `.claude/` was touched by this pass, by instruction — this is recorded for Ryan to action. Also disclosed here rather than buried: `tests/test_invariants.py` had one HARD real-tree assertion **narrowed** during the remediation (`assert _stale_unenforced_candidate_issues() == []` → a per-matcher assertion → structural arithmetic). It is honestly commented in the file and it is the correct response to **L192**, but it IS a relaxation of an existing assertion and is named as one. | This run's `verifier` round + remediation, 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. Dashboard-divergence reproduction, measured 2026-07-27 from the repo root (inlines the dashboard's own `SPLIT` regex so it runs without importing the unrunnable module): `python3 -c "import sys,re;sys.path.insert(0,'scripts');import invariants as I;S=re.compile(r'(?<!\\\\)'+re.escape(chr(124)));c=lambda l:[x.strip() for x in S.split(l.strip())][1:-1];rows={r[0]:r[2].strip() for r in I._parse_lesson_rows()};ls=[(I._LESSON_ID_ROW_RE.match(l).group(1),l) for l in open('kb/lessons/00-lessons.md') if I._LESSON_ID_ROW_RE.match(l)];print([i for i,l in ls if c(l)[4]!=rows[i]])"` → `['L89', 'L161', 'L173']` (3 of 190 rows at that time; the figure is corpus-relative and moves as the ledger grows, per **L161**). | **Item (1): test** (2026-07-28, idle-run policy (a)) — `scripts/gen_problems_dashboard.py::cells` now delegates to `invariants._split_lesson_row` (one parser, not two) instead of re-deriving the pipe-split rule, and `REPO = find_repo(__file__)` replaced the hardcoded Mac path (the very thing that let the divergence go unnoticed — the script can now actually run in a cloud sandbox). Pinned by `tests/test_gen_problems_dashboard.py`: a frozen-fixture regression test over the exact L89/L161/L173 row text named above (immune to ledger growth), a live-tree structural cross-parser-agreement check (dashboard vs `invariants._parse_lesson_rows`, 0 mismatches across 215 rows — not pinned to a count, so it stays valid as the ledger grows), and a portability test. Live run: `python3 scripts/gen_problems_dashboard.py` now exits 0 from this checkout and reports 0 divergent rows. Items (2) and (3) are **still UNENFORCED / open** — (2) is a judgment call about an existing green test, owed to the research-lead; (3) is a `.claude/agents/kb-distiller.md` documentation gap, Ryan-owned. This row remains the queue entry for (2)/(3); supersede fully when they land. |
| L201 | 2026-07-27 | **A regression test may pin a LIVE append-only document's STRUCTURE; it may never pin that document's ENUMERATION.** "Rows X..Z have property P and no other row does" is an assertion about PROSE — it makes the document's author a hostage of the gate. This run's own fix for **L152** shipped exactly that shape: a test asserting, against the LIVE `kb/lessons/00-lessons.md`, that a named set of rows contains pipe characters and that no other row does. It went red twice in one day and forced a kb-distiller to **REWORD ITS OWN LESSON TEXT** to get the binding gate green — the gate editing the knowledge it exists to protect. The repair: every content-enumerating assertion was moved onto a frozen fixture, and the live-tree coverage was replaced by a purely STRUCTURAL contract (nothing dropped, split lossless, every enforcement cell starts at the head of the 5th cell and runs to end-of-row, `n_rows >= 190`), which is true of ANY prose a future row may contain. Structure is an assertion about CODE and survives arbitrary appends; enumeration is an assertion about WORDS and cannot. This is the operational form of **L192**/**L200**, and it fired inside this run's own fix for **L152**. | This run's final code fix, 2026-07-27; `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`; frozen fixture `tests/fixtures/lessons_pipe_split_2026-07-27.md` (49 rows, `grep -c "^\| L" tests/fixtures/lessons_pipe_split_2026-07-27.md` → `49`, measured 2026-07-27 19:18Z); live ledger `grep -c "^\| L" kb/lessons/00-lessons.md` → `198` rows at the same instant. Operationalises **L192** and item (2) of **L200**. | **test** — `tests/test_stale_unenforced_advisory.py::test_live_ledger_is_wholly_parsed_and_never_truncated` is the live-tree coverage and is content-independent by construction; `::test_frozen_pipe_fixture_satisfies_the_same_structural_contract` re-runs the identical contract over the frozen 49-row snapshot (whose 14 pipe-carrying rows pass it), which is what shows the live check is a real constraint and not a vacuous one that only ever sees well-behaved rows. All enumerating assertions now read the fixture only (`::test_the_fourteen_misparsed_rows_now_yield_their_enforcement_column`, `::test_naive_pipe_split_really_does_break_those_rows`). File collects `69` tests: `python -m pytest -o addopts="" --collect-only -q tests/test_stale_unenforced_advisory.py | tail -3` → `69 tests collected`, measured 2026-07-27 19:18Z. |
| L202 | 2026-07-27 | **When freezing a counterfactual, freeze the NEGATIVE population too.** A snapshot containing only the rows that BREAK the naive rule proves "the naive rule breaks these" while silently dropping the anti-tautology half of the claim — "and it agrees with the correct parser everywhere else". A fixture of only-failures cannot detect an over-eager replacement that misparses rows the old code got right, so the differential test degenerates into a restatement of its own selection. `tests/fixtures/lessons_pipe_split_2026-07-27.md` was therefore built as all **14** misparsing rows PLUS a **35**-row representative sample of correctly-parsing rows: every immediate neighbour of a misparsing row (adjacency is where an off-by-one in a line-oriented parser shows up), the **L188** `DISPOSES:` row, a 1-in-20 spaced sample across the ledger, and the first and last rows. 49 rows total, copied byte-identically. | This run's final code fix, 2026-07-27; `tests/fixtures/lessons_pipe_split_2026-07-27.md`; extends **L194** (which found the 14) and the fixture practice established this run by `tests/fixtures/lessons_unenforced_21_2026-07-27.md`. Row count command and time as in **L201**: `grep -c "^\| L" tests/fixtures/lessons_pipe_split_2026-07-27.md` → `49`, 2026-07-27 19:18Z (14 misparsing + 35 agreeing). | **test** — `tests/test_stale_unenforced_advisory.py::test_frozen_pipe_fixture_is_the_snapshot_it_claims_to_be` pins the snapshot's byte-identity and composition; `::test_naive_pipe_split_really_does_break_those_rows` is the negative half and `::test_the_fourteen_misparsed_rows_now_yield_their_enforcement_column` the positive half, both scoped to the fixture. The fixture is deliberately **NEVER** identity-checked against the live ledger: such a check would re-couple the gate to live prose and recreate exactly the **L201** defect one level up. |
| L203 | 2026-07-27 | **Do not assert a check STRICTER than the parser it guards.** The structural ledger contract was first written with `len(fields) == 7` (the canonical column count). That would have gone red on a row containing a BARE unescaped pipe — a shape `scripts/invariants.py::_parse_lesson_rows` handles correctly BY DESIGN, by re-joining any residual field beyond the 5th column. A guard stricter than its subject re-creates the very defect it was written to prevent, one abstraction down: the gate would again dictate what a lesson row may SAY, while the code under test was already fine. Pin the parser's OWN admission threshold instead — here `len(fields) >= 6`, the exact condition `_parse_lesson_rows` uses to accept a row — so anything the parser accepts, the guard accepts. | This run's final code fix, 2026-07-27; `scripts/invariants.py::_parse_lesson_rows` (admission threshold `if len(cols) < 6: continue`); sibling of **L201**; the bare-pipe case is **L194**'s. | **test** — the rejected `== 7` and the adopted `>= 6` are both documented in the helper's docstring in `tests/test_stale_unenforced_advisory.py` (`_assert_ledger_is_wholly_parsed_and_never_truncated`), and the bare-pipe shape that would have reddened `== 7` is pinned by `::test_an_unknown_extra_pipe_never_truncates_the_marker`. |
| L204 | 2026-07-27 | **Parameterise a live-tree assertion body on its PATH, so "would a future append red this?" becomes an executable proof instead of a reasoning claim.** A live-tree test written inline can only be reasoned about; the same body factored into a helper taking a path can be (a) re-run over a scratch copy with hypothetical future rows appended — turning the safety argument into a test — and (b) re-run over the frozen fixture, which is what demonstrates the live check is not vacuous. Proven here by mutation: five rows covering every pipe shape (backslash-escaped, code-span, escaped INSIDE the enforcement cell, bare unescaped, and a `DISPOSES:`-carrying mixed row) were appended to a scratch copy of the ledger; the new structural check stays green on all five, whereas the enumerating check it replaced fails on all five. | This run's final code fix, 2026-07-27; helper `_assert_ledger_is_wholly_parsed_and_never_truncated(path)` in `tests/test_stale_unenforced_advisory.py`; the mutation run was executed against a scratch copy, not committed tape or `kb/`. Companion to **L201**. | **test** — the same helper is invoked by `::test_live_ledger_is_wholly_parsed_and_never_truncated` (live tree) and `::test_frozen_pipe_fixture_satisfies_the_same_structural_contract` (frozen fixture); the five pipe shapes are individually pinned as `tmp_path` tests (`::test_split_lesson_row_treats_escaped_pipe_as_literal`, `::test_split_lesson_row_treats_code_span_pipe_as_literal`, `::test_enforcement_cell_with_an_embedded_pipe_keeps_its_marker`, `::test_an_unknown_extra_pipe_never_truncates_the_marker`, `::test_disposes_needs_a_left_word_boundary`). The five-row scratch mutation itself is a one-shot proof, not a committed test — that residue is named, not hidden. |
| L205 | 2026-07-27 | **Grep `kb/`, `findings/` and `LOOP-QUEUE.md` for `::test_name` citations BEFORE renaming or deleting a test.** Ledger rows cite tests by node id as their enforcement evidence, and the agent that owns test files is forbidden to edit `kb/` — so a rename made in the test lane creates a dangling citation that the renaming agent cannot repair, and the kb lane cannot even see. Four node ids in `tests/test_stale_unenforced_advisory.py` are load-bearing in ledger row text as of this run. A citation that no longer resolves silently downgrades a `test`-tier row to an unverifiable prose claim, which is the failure mode this whole ledger exists to prevent. | This run's final code fix, 2026-07-27; observed while retiring the enumeration test of **L201**; lane split is per `CLAUDE.md` and `.claude/agents/kb-distiller.md`. Citation census command (repo root, measured 2026-07-27 19:18Z): `grep -rno "::test_[a-z0-9_]*" kb/ findings/ LOOP-QUEUE.md`. | **invariant (non-gating advisory) + test — BUILT 2026-07-28** (idle-run policy (a); this row's own status updated in the same pass per the **L152** rule, lesson text unchanged). The candidate named below was built exactly as described: `scripts/invariants.py::_cited_test_node_issues` / `::dangling_test_citation_warning`, wired into `main()`'s `--full` stanza list under an `except BaseException` guard (the L156 DEFECT-1 posture), extracts every `::test_...` token in `TEST_CITATION_DOC_GLOBS` (`kb/**/*.md`, `findings/**/*.md`, `LOOP-QUEUE.md`) and resolves it against `def`/`async def` test names under `tests/` — path-qualified citations against THAT file (so a MOVE is caught, not just a rename), bare `::test_x` continuations against the union, elided family citations (`::test_parse_iso_utc_*` / `_...` / trailing `_`) by prefix, with `CITATION_PLACEHOLDER_NAMES` and `CITATION_MIN_PREFIX_LEN` skipping metasyntactic/vacuous tokens. **NON-GATING on purpose**: repairing a hit means editing `kb/`, which the code lane may not do, so a gate here would be one the on-call lane cannot clear. Pinned by 21 cases in `tests/test_invariants.py` (`test_cited_test_node_*_L205`, `test_dangling_test_citation_*_L205`), all constructed negatives per **L155** plus one deliberately STRUCTURAL live-tree case (`::test_dangling_citation_real_tree_resolution_is_structural_L205`) that pins no hit count, since `kb/` and `findings/` grow every run (**L191**/**L192**). Resolution is by NAME only and can never verify the cited test still asserts what the row claims (**L165**). **Live result on the 2026-07-28 tree** — `python3 scripts/invariants.py --full` at 2026-07-28, 4 hits / 2 distinct node ids, both real renames and both still OPEN (repairing them is a kb-text judgment, deliberately not made in this code pass): node id `test_acceptance_8_l127_hyperliquid_funding_join_stale` in `tests/test_tape_gap_monitor.py` (cited by **L128**; the tree now has `..._forward_refreshed_catches_freeze_via_stale`) and node id `test_acceptance_exactly_one_real_finding_is_recovery_class` in `tests/test_recovery_dwell_advisory.py` (cited by `kb/00-LOG.md` twice + `findings/2026-07-26-issue-205-recovery-dwell-acceptance-pin-fix.md`; the tree now has `test_acceptance_recovery_class_findings_are_a_small_headline_scoped_subset`). Both broken ids are quoted HERE without their `::` separator on purpose, so this row's report of them is not itself counted as two more dangling citations. |
| L206 | 2026-07-27 | **CORRECTION of the suite count published by L199 and by this run's own finding / log / LOOP-QUEUE line (append-only; no existing row is edited).** A final code fix landed AFTER the kb pass that published `2155 passed`, adding tests. The AUTHORITATIVE final gate for the 2026-07-27 idle run is **`2159 passed, 0 failed`**, not 2155; and `tests/test_stale_unenforced_advisory.py` now collects **69** tests, not the 65 recorded mid-run. **L199** is the only row among L188-L200 carrying either stale figure (it states `2155` twice and `65` once); where it disagrees with this row, **this row is the current one**. `tests/test_invariants.py` is unchanged at **193**. The baseline for the run on `main` HEAD `ad6b95e` — `2087 collected / 2085 passed / 2 failed` — is unchanged and remains correct. The lesson is **L199**'s own, recurring one abstraction later: a gate line is a MEASUREMENT WITH A TIMESTAMP, and a run that edits code after its kb pass has published a number must re-measure and re-reconcile every artifact, not just the ledger. Per **L162**, a count with no command is unfalsifiable; per **L161**, it drifts as the repo grows. | This run's final reconciliation pass, 2026-07-27. Commands, run from the repo root: final gate `python -m pytest -o addopts="" -q` → `2159 passed in 1152.67s (0:19:12)`, 0 failed; `python scripts/invariants.py --full` → exit 0, `invariants: all green`; `git status --porcelain -- tape/ paper/` → empty. Collected counts measured 2026-07-27 19:18Z: `python -m pytest -o addopts="" --collect-only -q tests/test_stale_unenforced_advisory.py | tail -3` → `69 tests collected`; `python -m pytest -o addopts="" --collect-only -q tests/test_invariants.py | tail -3` → `193 tests collected`. Corrected in place (not ledger rows, which are append-only): `kb/00-LOG.md` this run's entry, `LOOP-QUEUE.md` this run's Log-of-runs line, `findings/2026-07-27-stale-unenforced-queue-audit-and-tape-pinned-gate-repair.md`. | **protocol** — the fresh-gate-line rule is already written at `LOOP-QUEUE.md` step 4 and `.claude/agents/kb-distiller.md`; this row is the SECOND counterexample in one day and the evidence that the rule is still not self-enforcing. Deliberately NOT converted to an invariant for the reason **L162** candidate (a) was rejected and **L199** re-rejected it: having `scripts/invariants.py` run the suite to verify a published number would regress that file's offline/fast contract and would itself be a full-suite run quoting its own result. Honest terminal state = protocol plus this correction row. |
| L207 | 2026-07-27 | **Residual, recorded and NOT fixed: one hard real-tree assertion is still coupled to the live tree — to its SYMBOLS rather than to prose.** `tests/test_invariants.py::test_stale_unenforced_candidate_real_tree_func_matcher_is_clean` asserts `by_matcher["func"] == 0` over the live repository, so a future genuinely-open lesson row whose enforcement column backticks a function-with-parens name that already exists somewhere in the tree would turn the binding gate RED — and the author would again be pushed to reword a lesson to get green. This is a DIFFERENT coupling from **L201**'s (it binds to the tree's symbol table, not to the document's words) and is materially narrower — it fires only on a specific token shape in a specific column of a specific row class — but it is the same family and is on notice alongside **L200** item (2). It was verified green against all five mutation rows of **L204**, so it is not currently a latent red. | This run's final reconciliation pass, 2026-07-27; `tests/test_invariants.py:748` (the assertion, already honestly commented as NARROWED per **L200**); shape family of **L192**/**L201**. | **UNENFORCED** — not closed. The candidate repair is the **L201** move applied again: assert the func matcher's cleanliness over a frozen fixture and keep only a structural/absence-of-effect assertion on the live tree. Not done here because `tests/` is outside this pass's write lane and was frozen at the final gate; and because narrowing it further trades a real (if brittle) live-tree guarantee for portability, which is a judgment call owed to the research-lead rather than one this lane should make unilaterally. |
| L208 | 2026-07-27 | **A per-window density statistic computed only over windows that produced ≥1 observation is a survivorship statistic, not a coverage statistic.** `scripts/q42_funding_estimate_path_inference.py`'s `min/median/max_samples_per_window` is built from the set of *observed* funding windows, so a window with ZERO capture passes can never enter it — of 33 8h funding windows in `tape/perp_tape/` (07-17→07-27), 4 have zero passes and are structurally invisible to this statistic (12/33 = 36% path-inadequate once 1-sample windows are counted too). The honest denominator for any window-bucketed tape metric is the expected window grid (derived from the collector's own cadence), never the observed-windows set. | `findings/2026-07-27-perp-tape-audit.md` (PERP-F1, tape-auditor + lead spot-check) | **UNENFORCED** — a coverage-adequacy judgment for any window-bucketed density statistic; candidate: a generic "expected window grid vs observed window grid" gap check, mirroring L172/L185's shape but keyed to a collector's own cadence instead of a fixed interval. |
| L209 | 2026-07-27 | **Faithful numeric coercion is not validation — a venue's int64-max no-quote sentinel survives every null check and every `> 0` guard as a plain (enormous) float.** `tape/perp_tape/dt=2026-07-23.jsonl` carries `ask = 922337203685477.6` (`9223372036854775807 / 1e4`) on 5 non-BTC/ETH contracts in one pass; `collection/perp_tape.py`'s `_f()` coerces it with no magnitude guard, and `scripts/q43_perp_binary_consistency_probe.py`'s guard (`bid <= 0.0 or ask <= 0.0`) only screens zero/negative placeholders, not sentinel magnitudes — a BTC/ETH occurrence would silently wreck the lead-lag correlation (`mid ≈ 4.6e14`) instead of being dropped. Sentinel-magnitude bounds belong next to null/zero checks whenever a venue's no-quote convention is a huge number, not a null. | `findings/2026-07-27-perp-tape-audit.md` (PERP-F2, tape-auditor; sentinel presence + guard code independently re-confirmed by the lead via direct grep/read) | **UNENFORCED** — not yet a live risk (BTC/ETH untouched so far) but the guard gap is real and reproducible; candidate: an upper-bound sanity check (e.g. `ask < 1e6`) alongside the existing zero/negative guard in `q43_perp_binary_consistency_probe.py`. |
| L210 | 2026-07-27 | **A second-granularity `capture_id` is not a unique key when a one-shot backfill can land in the same wall-clock second as a scheduled pass.** `tape/perp_tape/`'s `20260717T010032Z` maps to two distinct `captured_at` values (a `--backfill-funding` one-shot and a regular pass sharing that second) — this is the previously-unreconciled discrepancy between `findings/2026-07-23-q43-capture-density-advisory.md` (31 passes on 07-17) and the 07-23 edge-hunter LOOP-QUEUE entry (30 passes): both were right, counting by different keys. Join/dedupe on `(capture_id, record_type, mode)` or on `captured_at` directly, never on a bare `capture_id`. | `findings/2026-07-27-perp-tape-audit.md` (PERP-F3, tape-auditor; collision independently re-confirmed by the lead) | **UNENFORCED** — cosmetic today (one day, off-by-one) but the join-key hazard is general; candidate: a `tape_gap_monitor`-style duplicate-`capture_id`-with-differing-`mode` check. |
| L211 | 2026-07-27 | **Formal disposition of L209** (supersedes L209's enforcement column; L209's content is unchanged and not edited). Idle-run policy (a): built the upper-bound sanity check L209 named as its own candidate — `scripts/q43_perp_binary_consistency_probe.py`'s `NO_QUOTE_SENTINEL_FLOOR = 1e6` constant, checked alongside the existing `bid <= 0.0 or ask <= 0.0` guard in `load_perp_bbo()` (`if bid >= NO_QUOTE_SENTINEL_FLOOR or ask >= NO_QUOTE_SENTINEL_FLOOR: continue`). Two new tests reproduce the exact committed sentinel value (`922337203685477.6`) on a joinable BTC contract in one pass alongside a clean pass in the same file and assert only the sentinel pass is dropped, plus a headroom pin (`NO_QUOTE_SENTINEL_FLOOR` strictly between `1e5` and the real venue sentinel `9223372036854775807/1e4`) so neither bound can silently drift into the gap. | `scripts/q43_perp_binary_consistency_probe.py` (constant + guard); `tests/test_q43_perp_binary_consistency_probe.py::test_load_perp_bbo_drops_int64_max_no_quote_sentinel`, `::test_no_quote_sentinel_floor_below_the_real_venue_sentinel` (2026-07-27 research-loop run, idle-run policy (a)). | **disposition (machine-readable)** — DISPOSES: L209. Terminal tier: **test** (the two node ids above). This row moves no code; the fix lives at the cited file:line and is enforced by the cited tests. Q43 itself is still GATED (perp_tape density inadequate, per L208) — this closes only the sentinel-guard gap PERP-F2 flagged, not the probe's live-run gate. |
| L212 | 2026-07-28 | **A collector that computes `completeness_ok` and does not persist it makes its own gaps unfalsifiable — and a zero-line pass is indistinguishable from a non-run.** `collection/polymarket_pairs.py::run_fed_decision` (`:634-657`) builds a full honesty summary (`completeness_ok`/`unmatched_kalshi`/`unmatched_polymarket`/`ambiguous_kalshi`/`book_errors`/`polymarket_discovery_error`) then writes ONLY the tape lines and returns the summary in-process — `collection/hourly_pass.py` folds it into an in-memory total (`:463-470`) and drops it, so nothing under `tape/` records it. Consequence: a pass where Polymarket discovery raises produces `lines == []` → zero bytes written → permanently indistinguishable from "the collector never fired." `tape/polymarket_macro_pairs/` is the only actively-collected family this audit has found where a `completeness_ok` false-rate cannot be recomputed from committed tape (every sibling audit — perp_tape, settlement_ledger, hyperliquid_funding — could quote one). | `findings/2026-07-28-polymarket-macro-pairs-tape-audit.md` (D1, tape-auditor + lead independent re-derivation) | **UNENFORCED** — candidate: an invariants-layer rule that any collector computing a `completeness_ok`-shaped summary must persist it somewhere under `tape/` (e.g. a `record_type:"capture_summary"` line or a sibling `summaries/dt=*.jsonl`), mirroring the L185/L208 "what a check can see vs what actually happened" family. Not statically fully assertable (detecting "computes but doesn't persist" needs dataflow, not lexical matching) so likely terminal as **protocol** once a probe/collector precedent encodes the persist-it discipline. |
| L213 | 2026-07-28 | **A recurring collector's own historical cadence at a specific wall-clock slot is a check-able predictor of whether a one-shot burst trigger's fallback will work — and for `polymarket_macro_pairs` at the FOMC release slot, it says the fallback won't.** Over the 10 days preceding this audit (07-18→07-27), the recurring 3-hourly `kalshi-collector` leg landed **zero** captures in the 17:40-18:30Z window that a 2026-07-29T18:00Z FOMC statement needs; since 07-23 the nearest slot (18Z) fired on only 2/5 days, both ~55min late. Cross-checked against the live trigger API (`trig_01L9RysFtWUUjj3BgQmNKw7g`, `kalshi-burst-fomc-0729`): it still runs the single-commit-at-end recipe (`ops/burst_capture_chunked.md`'s seam-safe chunked alternative, built and verified 2026-07-26, was never applied to this trigger) — the same design that lost 2 of this project's 4 prior one-shot bursts (WC-semi1, WC-final) entirely. Before relying on any time-gated burst trigger as the sole data source for an event, audit the recurring leg's own cadence AT that event's specific wall-clock slot (not just its daily average) to know whether a burst failure is silently recoverable or a total loss. | `findings/2026-07-28-polymarket-macro-pairs-tape-audit.md` (D3 + live-risk section 1-2, tape-auditor + lead's own `list_triggers` cross-check) | **UNENFORCED, time-critical — flagged for Ryan, not fixable from a cloud run.** Trigger prompt edits are Ryan's own account state and out of scope for an autonomous rewrite (a tool-output-driven prompt change is not a user request); candidate for Ryan: confirm/apply the chunked recipe to `trig_01L9RysFtWUUjj3BgQmNKw7g` before 2026-07-29T17:40Z, and/or run a manual bracketing capture around 17:55Z/18:05Z as cheap insurance regardless. Also candidate: a `tape_gap_monitor`-style slot-specific cadence check ("what's this family's capture rate AT hour H, not just per-day") for any future burst-fallback risk assessment. |
| L214 | 2026-07-28 | **A cross-venue pairing can be confirmed correct at match time and be structurally unauditable at read time if only the matched IDs are persisted, not the matched terms.** `collection/polymarket_pairs.py::match_fed_pairs` (`:528-551`) confirms Kalshi's `*_50plus` bucket (derived from title magnitude `>=25`bps, `:394`) against Polymarket's own `*_50plus` bucket (derived from `groupItemTitle` wording `>=50`bps, `:469`) — genuinely different settlement terms for a 26-49bp move — but the tape persists only `ticker`/`event_id`/`market_id`, never the title/question text or a resolution-basis tag, so this asymmetry (and any future one shaped like it) is invisible to every downstream consumer and every future audit; it can only be found by re-reading collector source, as this audit did. | `findings/2026-07-28-polymarket-macro-pairs-tape-audit.md` (D2, tape-auditor + lead source read) | **UNENFORCED** — low live-risk today (non-25bp-multiple Fed moves are near-zero probability) but the underlying gap generalizes to any cross-venue matcher; candidate: persist the matched title/question text plus a `resolution_basis` tag (`kalshi_rulebook`/`uma_oracle`) per leg at write time, not just the winning IDs — same family as L212 (a check computed at collection time that dies before it reaches tape). |
| L215 | 2026-07-28 | **Formal disposition of L212** (supersedes L212's enforcement column; L212's content is unchanged and not edited). Idle-run policy (a): `collection/polymarket_pairs.py::run_fed_decision` now always appends one `family:"capture_summary"` line to the per-day tape file (even on a zero-match or fully-failed-discovery pass, which previously wrote nothing), carrying `completeness_ok`/`unmatched_kalshi`/`unmatched_polymarket`/`ambiguous_kalshi`/`n_book_errors`/`polymarket_discovery_error` — the exact fields the 2026-07-28 audit found unrecoverable from committed tape. Given its own `schema_version` (`polymarket_macro_pairs_summary.v1`, distinct from the pair-record schema) so every existing reader gating on schema_version/family (`q31_cross_venue_arb_probe._iter_records`, `q48_s55_fomc_lag_probe.load_family_records`) skips it like a foreign record — verified against both readers' actual filter logic, not assumed. 4 new tests in `tests/test_polymarket_pairs.py` (2 updated to reflect the now-always-written summary line, 2 new: zero-match pass persists a `capture_summary`, and the persisted line's fields match the in-process `summary` dict exactly plus the schema-exclusion check). | `collection/polymarket_pairs.py::run_fed_decision`; `tests/test_polymarket_pairs.py` (2026-07-28 research-loop run, idle-run policy (a)) | **disposition (machine-readable)** — DISPOSES: L212. Terminal tier: **test** (the 4 test node ids above, in `tests/test_polymarket_pairs.py`). This row moves no other code; the fix lives at the cited file and is enforced by the cited tests. L212's own note that this is "likely terminal as protocol" is superseded — persisting the summary record is directly testable, not dataflow-only. |
| L216 | 2026-07-28 | **`scripts/tape_branch_sweep.py`'s size guard makes stranded-line checks on `orderbook_depth`/`universe_sweep`/`sports_pairs` structurally invisible — and three consecutive prior runs (PRs #217/#219/#220) reported `tape/hourly-20260727T1303Z` as "already recovered"/"nothing new to sweep" without noticing the guard meant those three families were never actually checked on it.** Direct capture_id-level comparison against `HEAD` found the branch genuinely carried a full 20,000-line `universe_sweep` capture (`20260727T130302Z`), a 1,001-line `orderbook_depth` capture (`20260727T125540Z`), and a 302-line `weather_books` capture (`20260727T130038Z`) — none present anywhere in `HEAD`'s tape tree. The tool's own report format correctly distinguishes "fully contained + verified" from "no problem found but NOT FULLY VERIFIED (size-guard skip)," but prose summaries collapsed that distinction into a clean bill of health. A size-guard skip is a "not checked" signal, never a "checked and clean" one. | `findings/2026-07-28-stranded-tape-recovery-hourly20260727T1303Z-bulk-family-blindspot.md` (this run's direct capture_id comparison, independent of the sweep tool) | **UNENFORCED** — candidate: either a cheaper per-family check for the three size-guard-exempt bulk families (e.g. capture_id-set comparison via a streaming/line-count parse instead of a full line-set diff, avoiding the need to load the whole file) or a standing rule that any branch touching a bulk family gets the manual capture_id check this run performed, never trusted to the default guard alone. Not done here — this run's milestone is the recovery itself; the sweep-tool fix is a design task for a future milestone. |
| L217 | 2026-07-28 | **Formal disposition of L216** (supersedes L216's enforcement column; L216's content is unchanged and not edited). Idle-run policy (a): built candidate (a), a cheaper per-family check. `scripts/tape_branch_sweep.py::per_file_containment` gains `BULK_CAPTURE_ID_FAMILIES` + `capture_ids_in_blob()`: an oversized file under one of these families is checked at capture_id-set granularity (every distinct `capture_id` the branch carries must already be in HEAD's version of the same file) instead of skipped outright — a structurally-sound proxy since one capture atomically mints all of its lines under one `capture_id` (this run's own L216 recovery table is the direct evidence: one row per capture, not per line). An oversized bulk file yielding ZERO capture_ids (malformed content, or a schema genuinely lacking the field) still falls back to the honest size-guard skip — no signal is never read as a clean bill of health, verified by the two PRE-EXISTING `sports_pairs`-fixture size-guard tests continuing to pass unmodified (their content has no `capture_id` field). Surfaced as its own `BranchTriage.capture_id_checked_files`/`capture_id_only` and report bucket, never folded into the line-level "fully verified" claim. **Scope note: `weather_books` added as a FOURTH bulk family**, beyond the three L216 named — re-running the fix against the real `tape/hourly-20260727T1303Z` branch found its `weather_books/dt=2026-07-27.jsonl` (2,303,754 bytes) also exceeds the 2MB guard and carries `capture_id`, and it is the SAME family whose 302-line capture L216's own recovery table already listed as stranded; the blind spot always covered it, L216's prose just didn't name it. **Live re-verification:** `triage_branch()` on `tape/hourly-20260727T1303Z` against current `HEAD` now returns `contained=True`, `capture_id_only=True`, 0 missing capture_ids on both its bulk-family files — an independent confirmation the earlier recovery was complete, not a second unverified skip. 12 new tests (`tests/test_tape_branch_sweep.py::TestCaptureIdsInBlob`, `::TestBulkCaptureIdCheck`, plus a `format_report` case), all against a real temporary git repo per this module's existing discipline. | `scripts/tape_branch_sweep.py` (`BULK_CAPTURE_ID_FAMILIES`, `capture_ids_in_blob`, `per_file_containment`, `BranchTriage.capture_id_checked_files`/`capture_id_only`, `format_report`); `tests/test_tape_branch_sweep.py` (2026-07-28 research-loop run, idle-run policy (a)); addendum to `findings/2026-07-28-stranded-tape-recovery-hourly20260727T1303Z-bulk-family-blindspot.md` | **disposition (machine-readable)** — DISPOSES: L216. Terminal tier: **test** (the cited test classes above). This row moves no other code; the fix lives at the cited file:function and is enforced by the cited tests. Residual, NOT closed here: the coarser capture_id check does not prove byte-identical content within a matched capture_id — left as a documented, deliberate trade (surfaced via `capture_id_only`), not a gap to silently fix later. |
| L218 | 2026-07-29 | **Formal disposition of L210** (supersedes L210's enforcement column; L210's content is unchanged and not edited) — and a correction to its own severity estimate. L210 recorded the colliding `capture_id` as "cosmetic today (one day, off-by-one)" in `perp_tape` alone. A full census of committed tape (1,140,735 lines, all families) finds it is **not confined to perp_tape and not off-by-one**: **7 collided item groups across 3 families** — `perp_tape` `20260717T010032Z` (a `--backfill-funding` one-shot and a scheduled pass, differing in `mode`/`n_prints`/`start_ts`), `econ_prints` `20260716T092842Z` (**all 5** series `KXCPI`/`KXCPICORE`/`KXCPIYOY`/`KXGDP`/`KXPAYROLLS` written twice, 414ms apart), and `anomalies` `20260714T091958Z` (two byte-identical summary rows 84ms apart, so a consumer summing `n_anomalies` double-counts 324). The repo already contains a live consumer of the flawed key: `scripts/tape_gap_monitor.py::aggregate_family` counts distinct passes by `capture_id`, so its UNDER-CAPTURE ratio undercounts passes by exactly the collision count. **The load-bearing design finding is what NOT to check.** The obvious rule — one `capture_id` must map to one `captured_at` — is WRONG and would fire a false positive on `hf_burst`, whose single committed capture_id legitimately spans 10 strikes over 1.3s because one ladder round stamps each row as it is fetched. The correct discriminator is whether the **same logical item repeats** under one id: a pass walking a ladder visits each item once, two colliding passes revisit every item they share. Burst families are additionally exempted **structurally**, on the presence of a within-pass sequence field (`capture_seq`/`capture_mono_ns`/`round_index`), never on a family name-list, so a future burst collector inherits the exemption with no edit to the detector. | Census + detector this run over committed `tape/` (read-only, no network); L210's original `perp_tape` observation. | **disposition (machine-readable)** — DISPOSES: L210. Terminal tier: **advisory + test**. Detector `scripts/tape_gap_monitor.py::duplicate_capture_id_collisions` (+ the `_collision_candidate_families` regex prefilter, whose union-of-all-matches makes it provably unable to under-nominate a family, cutting the gate cost from ~16s to ~5s), wired non-gating into `scripts/invariants.py::duplicate_capture_id_warning`. Non-gating because these are historical properties of already-committed append-only tape that no run can retroactively repair. Tests: `tests/test_tape_gap_monitor.py::test_l210_two_invocations_in_one_second_are_flagged`, `::test_l210_ladder_walk_within_one_pass_is_not_flagged`, `::test_l210_within_pass_sequence_field_structurally_exempts`, `::test_l210_prefilter_never_under_nominates_relative_to_json_parse`, `::test_acceptance_13_l210_real_tape_capture_id_collisions_are_reported`; `tests/test_invariants.py::test_duplicate_capture_id_warning_message_content_L210` (2026-07-29 research-loop run, idle-run policy (a)). |
| L219 | 2026-07-29 | **Formal disposition of L207** (supersedes L207's enforcement column; L207's content is unchanged and not edited). Idle-run policy (a): applied the exact "L201 move" L207 itself specified — `tests/test_invariants.py::test_stale_unenforced_candidate_real_tree_func_matcher_is_clean` no longer hard-asserts `by_matcher["func"] == 0` against the LIVE tree (a future genuinely-open lesson row that happens to backtick a `function_name()` candidate already present somewhere in the tree would have turned this file's own gate red for a reason unrelated to the code under test — the same L192 hazard shape, one matcher over); it now asserts only that the report computes and the matcher key is present with a well-typed, non-negative count (STRUCTURE, survives arbitrary future appends). The ENUMERATION half — "today's open rows name no colliding `func()` candidate" — moved onto the existing frozen fixture `tests/fixtures/lessons_unenforced_21_2026-07-27.md` (already used by `tests/test_stale_unenforced_advisory.py` for the sibling recall checks; no new fixture manufactured) via a new test, `test_stale_unenforced_candidate_frozen_fixture_func_matcher_is_clean`, which can never go red from a future lesson append. **Separately noted, not touched:** `kb/lessons/00-lessons.md` L192 itself — its "deliberately not fixed by this row's author" narrative is now STALE; the exact 2-line repair it specified is already live in `tests/test_invariants.py`/`tests/test_stale_unenforced_advisory.py` as of the SAME originating commit (`bc50296`, 2026-07-27, PR #216) that wrote L192's row — confirmed by `git log -p -1 bc50296 -- tests/test_invariants.py tests/test_stale_unenforced_advisory.py`, which shows the described split (live-tree absence-of-effect assertion + frozen-fixture text assertion) landing in that same commit. L192 is left open here rather than disposed unilaterally: its own row asserts the fix is "owed to the research-lead," and confirming that a fix already in the tree fully discharges that specific row's language is a judgment call better made by a second party, not asserted in the same pass that found the discrepancy. | `tests/test_invariants.py` (the narrowed live-tree test + the new frozen-fixture test); this run's own inspection of `bc50296`'s diff (2026-07-29 research-loop run, idle-run policy (a)). | **disposition (machine-readable)** — DISPOSES: L207. Terminal tier: **test**. `tests/test_invariants.py::test_stale_unenforced_candidate_real_tree_func_matcher_is_clean` (narrowed) + `::test_stale_unenforced_candidate_frozen_fixture_func_matcher_is_clean` (new). This row moves no other code; the fix lives at the cited file and is enforced by the cited tests. L192 is flagged stale in the lesson column above but deliberately NOT disposed by this row — it names a different artifact (the advisory-text split, not the func-matcher split) and its own closure is left for a second party per the note above. |
| L220 | 2026-07-29 | **Formal disposition of L192** (supersedes L192's enforcement column; L192's content is unchanged and not edited). L192's own row named a precise 2-line fix and stated it was "owed to the research-lead"; **L219** (the immediately preceding row, same day) then found via `git log -p bc50296` that the fix was already live in the SAME originating commit that wrote L192, but deliberately did NOT dispose L192 itself — "confirming that a fix already in the tree fully discharges that specific row's language is a judgment call better made by a second party." This run is that second party. Independently re-verified from scratch (not trusting L219's account): `git show bc50296 -- tests/test_invariants.py tests/test_stale_unenforced_advisory.py` shows the diff performs EXACTLY the split L192's own "Proposed minimal fix" cell specified — `test_stale_unenforced_candidate_warning_never_gates_exit_code` narrowed to assert only `rc == 0` / `"invariants: all green" in out` (the text-presence assertions removed), with the recall-sentence TEXT assertion moved onto a NEW test, `test_stale_unenforced_advisory_text_on_the_frozen_fixture`, which reads the frozen fixture (`tests/fixtures/lessons_unenforced_21_2026-07-27.md`) instead of the live ledger; the sibling file `tests/test_stale_unenforced_advisory.py` carries the identical split (`test_advisory_is_non_gating_on_the_real_tree` / `test_advisory_text_is_pinned_on_the_frozen_fixture`). Confirmed all four node ids exist in the CURRENT tree (not just the historical diff) and pass: `python3 -m pytest -o addopts="" -q tests/test_invariants.py tests/test_stale_unenforced_advisory.py -k stale_unenforced` → `12 passed`. L192 is CLOSED — its fix has been live and green since 2026-07-27, three runs before this one noticed. | `git show bc50296 -- tests/test_invariants.py tests/test_stale_unenforced_advisory.py` (independently re-run this run, 2026-07-29, repo root); `python3 -m pytest -o addopts="" -q tests/test_invariants.py tests/test_stale_unenforced_advisory.py -k stale_unenforced` → `12 passed` (2026-07-29 research-loop run). | **disposition (machine-readable)** — DISPOSES: L192. Terminal tier: **test**. `tests/test_invariants.py::test_stale_unenforced_candidate_warning_never_gates_exit_code`, `::test_stale_unenforced_advisory_text_on_the_frozen_fixture`; `tests/test_stale_unenforced_advisory.py::test_advisory_is_non_gating_on_the_real_tree`, `::test_advisory_text_is_pinned_on_the_frozen_fixture`. This row moves no code — the fix has been live since commit `bc50296` (2026-07-27, PR #216); it only formalizes the second-party confirmation L219 deferred. |
| L221 | 2026-07-29 | **An hour-equality collector gate (`if ts.hour == N`) is a rate gate, not an idempotence gate.** `econ_prints`'s `ECON_PRINTS_UTC_HOUR` (`collection/hourly_pass.py:500`) admits UNBOUNDED passes inside its hour and ZERO outside it — the same line of code produces both failure modes at once: 54.4% byte-redundant re-capture of a monthly-cadence payload when multiple invocations land inside hour 09 (L222), and 5 fully-lost calendar days when a pass never reaches hour 09 or dies before its leg (07-24/07-25/07-27, confirmed absent from every stranded-tape branch too — genuinely lost, not unswept). The honest fix shape is a once-per-day *key* ("has `dt=<today>` already got a pass for this leg?"), never an hour predicate — the predicate cannot express either "already done today" or "still owed today." | `findings/2026-07-29-econ-prints-tape-audit.md` (D2/D3, tape-auditor + lead independent re-derivation of the hour-09 pass-count contradiction) | **UNENFORCED** — candidate: a `hourly_pass`-level once-per-day dedup key per leg (has today's `dt=` file already received a pass from this leg?), replacing the bare hour-equality gate; would also close L74/L123/L126/L144's sibling instances of the same shape in other daily-cadence legs. |
| L222 | 2026-07-29 | **A tape family whose observed firing pattern contradicts its only in-repo caller cannot be root-caused without invocation provenance on the record itself.** On `tape/econ_prints/dt=2026-07-23.jsonl`, 18 passes (90 lines) land in hour 09 while `sports_pairs`/`crypto_hourly`/`orderbook_depth` — legs #1/#2/#6 of the SAME `hourly_pass.run()` invocation econ_prints is leg #10 of — recorded ZERO captures in hours 09 or 10 that day (independently re-derived by direct per-line `captured_at` hour census over the real committed files, not the subagent's report alone); the same shape repeats on 07-16 and 07-20. Consecutive pass-start gaps as low as 0.153s (07-14, independently reproduced) prove at least two concurrent invocations, given `validation/v3_market.py`'s ~1.8s-per-pass rate-limit floor — but roughly 65% of the family's 344 passes have no identifiable caller among `hourly_pass`/`burst_capture`/tests. `perp_tape`'s equivalent question WAS answerable in one line because its records carry a `mode` field (L210/L218); `econ_prints` records carry no comparable field, so the tape cannot answer its own provenance question. | `findings/2026-07-29-econ-prints-tape-audit.md` (D1, tape-auditor; hour-09 pass-count contradiction and the 0.153s gap both independently re-derived by the lead from raw tape before publication) | **UNENFORCED** — candidate: every collector record carries a `capture_source` (module path + entrypoint, e.g. `hourly_pass.econ_leg` / `burst_capture` / `manual`) alongside `capture_id`, and a tape-quality check asserts each family's realized pass count is explicable by its registered callers' known firing windows. Generalizes past this one family — cheap, and retires the whole "who wrote this line" question class. |
| L223 | 2026-07-29 | **A `no_settled_events`/`not_built`-shaped status that counts toward `pass_complete` converts a regression into silence.** `econ_prints`'s `gdp` leg reported one real settlement (`KXGDP-26APR30`, 2026-07-05) then `no_settled_events` on all 340 subsequent lines (07-06→07-28) while `pass_complete: true` held throughout (`collection/econ_prints.py:317-319` counts it as non-error) — a 23-day silent regression that reads identically to "this series never had a settlement" in every persisted field. The honest-status vocabulary needs a THIRD state, not two: never-had-this vs used-to-have-this-and-lost-it vs currently-has-it — the discriminator (prior tape for the same key) is already sitting in the committed history, so detecting the second state needs no new collection, only a check that reads backward. | `findings/2026-07-29-econ-prints-tape-audit.md` (D5, tape-auditor) | **UNENFORCED** — candidate: a per-key regression check (any series/event key whose most recent non-`not_built` status is a real value, followed by N consecutive `no_settled_events`/equivalent-null passes, flags) — same family as L212's "compute vs persist" gap, one step further: here the field IS persisted, but its two failure meanings collapse to one string. |
| L224 | 2026-07-29 | **A venue's `broker_truth` scalar is a string until proven otherwise, and its format can change between two events of the same series.** `econ_prints`'s `expiration_value` field — its only `broker_truth`-tagged value — reads `'0.2'` for `KXCPICORE-26MAY` and `'0%'` for `KXCPICORE-26JUN`; `KXPAYROLLS-26JUN` carries a thousands separator (`'57,000'`). `float(expiration_value)` raises on 3 of the 8 committed settled prints. Value/`n_markets` agreement across every repeated capture of the same key is 100% (0 drift) — the SOURCE tag is correct, the field's TYPE discipline is what's missing, and a future S12 join is the consumer that would discover this the hard way. | `findings/2026-07-29-econ-prints-tape-audit.md` (D4, tape-auditor) | **UNENFORCED** — candidate: normalize `expiration_value` at capture time into a numeric field (strip `%`/`,`, coerce to float) while persisting the raw string alongside it, never overwriting — same provenance-preserving shape CLAUDE.md's Hard Rule #4 already requires for price tags, applied one field over. |
