"""Shared contracts imported by validation/ and analysis/.

core/ is the single home for transaction-cost modeling (execution), time handling
(timeutil), data contracts (schema), and report I/O (io). Nothing venue-specific
or strategy-specific lives here.
"""
